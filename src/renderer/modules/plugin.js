/**
 * Plugin marketplace panel — stub that exports initPlugin for backward compat.
 * Actual rendering is in app.js to avoid circular imports.
 */

import { $ } from "./dom.js";

export function initPlugin() {
  // Plugin panel interactions are handled in app.js
  // This function exists for backward compatibility
}
