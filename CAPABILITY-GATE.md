# Capability Gate — 防止"变笨"硬门槛

This is a HARD GATE for every change in this platform. A change that cannot pass it
does not ship. Its purpose: never let the product get *dumber* than it is today.

## The one rule

> **No change ships if it can make the product worse than today. Every feature's
> FAILURE MODE must degrade to today's behavior — fail open / fall back to the
> strong default — never to a worse state.**

Why this is the right test: a feature can only make the product dumber if, *when it
goes wrong*, the result is worse than not having the feature. If its failure mode
equals today's behavior, the worst case is "no improvement," which is acceptable.
So for every change, ask: **when this breaks, is it worse than today, or the same?**
Only "the same (or better)" passes.

## Gate checklist (every capability-affecting change must pass — a "no" blocks merge)

Applies to anything that changes how the agent answers, routes, executes, learns, or
remembers.

1. **Failure degrades to baseline.** When this errors / its input is missing /
   malformed / oversized / forged → does it fall back to today's behavior? Ship a
   test that proves the fallback. No silent break, no worse-than-today.
2. **No silent capability downgrade.** If it routes to a weaker model, fewer tools,
   or less context, there must be a guaranteed fallback to the strong default, and
   reasoning / critical work must never be downgraded.
3. **Bound non-progress, not effort.** Any stop / limit / timeout fires only on
   CONFIRMED no-progress (byte-identical repeat, unchanged output) — never on
   legitimate long or evolving work.
4. **Graceful, never silent-kill.** On a limit or error: recover partial output,
   nudge the model, or ask the user. Never silently abort or hand off to ad-hoc
   improvisation.
5. **No improvisation fallthrough.** When a structured path (learned flow, parsed
   intent, learned API contract) fails, surface it or ask — never silently let the
   model wing it with ad-hoc scripts/plans.
6. **Persona / context / memory invariants hold.** The workbench persona is not
   overridden by the engine's coding-CLI baseline; the per-turn system prompt stays
   within budget; long-session compaction still works.
7. **Closed-loop guard.** Add an automated test that FAILS if this capability
   regresses, and register it below.

## Known 变笨 vectors → their guard (the registry)

Every recurring "got dumber" cause must stay guarded. When you touch one of these,
the guard test must still pass; when you add a new capability, add a row.

| Vector (how it got dumber) | Guard (test / mechanism) |
|---|---|
| Coding-CLI persona overrides the workbench persona (default.txt) | `test-opencode-config-builder.mjs` — basePrompt suppresses the coding baseline for build/plan |
| Legacy engine guide mirror reappears and splits the source of truth, or guide migration drops user-authored content | `test-skill-catalog.mjs` + `test-data-migration.mjs` — AGENT.md is canonical; guide generation must not create `CLAUDE.md`; startup migrates/removes stale legacy files while preserving distinct content; later guide refreshes only replace the Lily-managed block |
| Per-turn system prompt bloat | `test-context-budget-manager.mjs` — token-budget estimation + compaction trigger; `test-agent-guide-i18n.mjs` — static `AGENT_GUIDE_MAX_BYTES` assertion for generated AGENT guide base text, all local skill-index entries, and provider contract injection |
| Native compaction 500 → long-session memory lost | `test-opencode-config-builder.mjs` — compaction/title pinned to the resolvable main model; `test-context-budget-manager.mjs`; `test-opencode-sdk-session.mjs` — SDK summarize uses top-level `sessionID`, not generated-client route placeholders |
| Subagent infinite nesting (套娃) | `test-subagent-telemetry.mjs` — `nestedTaskBreaches` detector; isolation prompt aligned to depth-1 cap |
| Subagents fall back to a generic coding baseline and skip Lily's evidence discipline | `test-opencode-config-builder.mjs` + `test-skill-catalog.mjs` — shared subagent config injects Lily Subagent Rules; subagents must return evidence-backed summaries and must not spawn nested Task subagents |
| Runaway / doom loops | `test-loop-detector.mjs` — RESULT-AWARE (only byte-identical no-progress), graceful nudge, fail-open; loop hints must not tell the model to downgrade just because work is slow |
| Reasoning/thinking BYOK models false-rejected or mis-graded by the compatibility probe (so a capable custom model is refused or silently downgraded) | `test-model-compatibility-probe.mjs` + `test-model-probe-thinking-toolchoice.mjs` — the probe tolerates reasoning models: recognizes `reasoning_content` (not only `reasoning`), gives the liveness/tools probe 512 output tokens (16 starved reasoning models into empty content → false `MODEL_NO_CONTENT`), and falls back to `tool_choice:"auto"` when a FORCED tool_choice is rejected (e.g. deepseek thinking-mode 400) instead of concluding `MODEL_TOOL_CALLS_UNAVAILABLE`. Fail-safe: only converts false-rejects into a real test, never accepts a model that genuinely can't answer/tool-call. Validated live: deepseek-v4-pro probes clean (grade=standard) and scores 5/5 on `eval:model` |
| Workspace knowledge index cites a chunk whose source file was deleted or changed — "confidently wrong about removed content", worse than having no index | `test-workspace-index-freshness.mjs` — INVARIANT: index is a pointer/cache, the filesystem is truth. Auto-index stamps persist mtime+size+SHA-256; `queryIndex` verifies every hit against the live source, drops and evicts missing or fingerprint-mismatched chunks, and reports `staleSourcePaths` for re-index. Same-size/same-mtime edits are covered by the hash. FAIL-OPEN: URL/synthetic sources, legacy unstamped indexes, and ambiguous fs errors are kept so retrieval cannot destroy unprovably stale data. Kill switch `LILY_WORKSPACE_INDEX_VERIFY=0` |
| A user removes content from a generated/attached file, then a follow-up restores it from the staged attachment or historical `write/edit` input | `test-live-file-history-guard.mjs` + `test-opencode-message-parts.mjs` + `test-document-translator.mjs` + `test-workspace-index-freshness.mjs` — readable `sourcePath` outranks the immutable staged copy across model parts and document/vision preflight; `live-file-history-guard.js` removes only stale historical mutation bodies before model calls and blocks modification of an existing changed file until a successful live read records its current fingerprint. Post-write fingerprints preserve normal multi-step work; external edits re-arm the gate. Missing live files keep historical content for recovery; unchanged historical writes remain byte-identical; all hooks fail open and `LILY_LIVE_FILE_GUARD=0` restores baseline [gate: live-file-authority] |
| Dragged workspace/app package detection blocks ordinary attachments, or shared scheduled tasks run unexpectedly after import | `test-workspace-package-drop.mjs` + `test-workspace-package-inspector.mjs` + `test-workspace-automation-sharing.mjs` + `test-workspace-share.mjs` — package identity comes from a bounded main-process manifest read, not the extension; unrecognized/corrupt/future/oversized packages and inspection errors fail open to the existing chat attachment path. Recognized packages require an explicit import/attach/cancel decision. Scheduled tasks are opt-in on export and import, carry definitions only, receive new IDs/scopes, and always enter paused with no run history or `nextRunAt`. Old packs without the hidden automation entry still import unchanged. |
| Scheduled work binds to whichever chat is active later, crosses accounts/projects, duplicates after pause/restart, replays every missed interval, disappears when its bound chat is busy, or loses normal file-analysis capability | `test-scheduled-task-isolation.mjs` + `test-scheduled-task-bridge.mjs` + `test-scheduled-tasks.mjs` + `test-turn-orchestrator.mjs` — task owner/project/origin are immutable host-derived scope; each occurrence is a durable message admission to that exact conversation, so it inherits conversation context/permission mode and the normal document/vision/dependency preflight, queues behind an active turn in the same conversation, and runs concurrently with other conversations; permission metadata is uniformly `inherit`, including persisted migration of legacy databases and shared templates; exact run IDs, SQLite occurrence keys, and leases prevent cross-completion or duplicates; one SQLite transaction reclaims never-started queues under a fresh lease and terminalizes possibly side-effecting runs, so restart re-admits the former exactly once but never replays the latter; `run_once_on_launch` advances past current time instead of replaying every missed occurrence; unattended permission questions auto-deny instead of hanging; account changes remove only the foreign owner's volatile queue projection while retaining the exact durable run/admission for one-time recovery when that owner returns, and interrupt only a foreign run that actually started with the same turn/attempt/owner claim; corrupt legacy JSON and owner-unavailable migrations are retained instead of overwritten. |
| Unknown files, folders, or archives are rejected at the attachment boundary, uploaded raw, traversed without bounds, or bulk-extracted unsafely | `test-file-staging-manager.mjs` + `test-clipboard-file-paste.mjs` + `test-archive-intelligence.mjs` + `test-workspace-index-source-bounds.mjs` + `test-file-intelligence-core.mjs` + `test-file-intelligence-mcp.mjs` + `test-opencode-message-parts.mjs` + `test-attachment-metadata.mjs` — every path-backed regular file and directory is admitted; known small files retain staging while large/unknown/archive/directory inputs remain live path references; pathless buffers keep the old allow-list/size cap. Directory traversal is depth/file/entry bounded and symlink-safe. Bundled 7-Zip lists archives without extraction and reads only one exact safe bounded text entry; encrypted, traversal, binary, oversized, corrupt, or unavailable-tool cases fail explicitly. Message parts preserve exact paths and never upload directories/archives/unknown binaries raw. |
| Auto-indexing the workspace ingests stale content, wipes the index on each update, or indexes files outside the workspace | `test-workspace-auto-index.mjs` — auto-ingest is OPT-IN (`LILY_WORKSPACE_AUTOINDEX=1`), driven by a turn's real `record.fileChanges`: created/edited files are re-chunked into a stable per-workspace auto-index via `upsertSourceChunks` (replaces ONLY that source's chunks — never wipes the index), deleted files are evicted, and out-of-workspace paths are skipped; bounded (≤ maxFiles), deterministic (no model call), fail-open + non-blocking (`setImmediate`, per-file try/catch) so it can't slow or break a turn. The slice-1 freshness guard is the backstop if a change is ever missed |
| Background workspace reconcile stalls the UI, misses external edits, or re-indexes unchanged files every scan | `test-workspace-reconcile.mjs` — reconcile is ASYNC + THROTTLED by contract (yields to the event loop every `batchSize` files, bounded by `maxFiles`, fired via `setImmediate` off the turn path, debounced ≤ once/workspace/`LILY_WORKSPACE_RECONCILE_MS`) so it can't affect UX; per-source `source_stamps` (mtime+size) SKIP unchanged files, (re)index new/edited external changes, and evict stamps whose files are gone; opt-in `LILY_WORKSPACE_AUTOINDEX=1`, fail-open. Complements slice-2 turn-driven ingest (catches edits made outside the agent) |
| Proactive workspace retrieval dilutes ordinary-turn context, injects stale/deleted content, or makes strong models dumber | `test-workspace-autoinject.mjs` + `test-turn-orchestrator.mjs` — OPT-IN (`LILY_WORKSPACE_AUTOINJECT=1`) and injected ONLY on substantive `grounded`/`coverage` turns (never casual/fast chat → zero dilution on normal turns, like ①); `retrieveWorkspaceContext` returns a BOUNDED (`maxChars`) block, freshness-verified (deleted files never cited), explicitly labeled "RETRIEVAL, not proof — open the file to confirm" so it stays a pointer the model must ground, and fail-open (any error → inject nothing → baseline) |
| Memory reinforcement entrenches a once-popular memory, lets usage frequency surface an irrelevant memory, or drops/dumbs recall | `test-memory-reinforcement.mjs` — OPT-IN (`LILY_MEMORY_REINFORCE=1`); the boost is BOUNDED (≤ `MAX_BOOST`=8, vs relevance*25 in the ranker) so it only reorders already-relevant items and a relevant unboosted item ALWAYS beats an irrelevant max-boosted one (proven by test); recency-weighted so stale-but-once-popular items decay (no rich-get-richer); usage is recorded only for memories actually SELECTED for injection; store is per-project, LRU-capped, fail-open; NOTHING is ever deleted — reinforcement only re-weights. Default off → ranking byte-identical to today |
| Cross-session memory dropped on compaction | `test-compaction-memory-export.mjs` / `test-compaction-memory-plugin.mjs` — inject memory, fail open |
| Real-embedding memory recall degrades or slows recall when the embedding endpoint is unset, keyless, or failing | `test-memory-vector-index.mjs` — semantic recall ships DEFAULT-ON in code (`embeddingEnabled` defaults true; only explicit `LILY_MEMORY_EMBEDDING=0`/`false`/`off` disables — a source-controlled default since the bundled settings file is gitignored/regenerated) but is KEY-GATED and fail-open, so it can only improve on the lexical baseline: `resolveEmbeddingConfig` returns null (→ lexical hash, zero egress, zero added latency) whenever the flag is `0` (kill switch) OR no API key resolves — so keyless / BYOK-only users NEVER fire a doomed 401; base/key/model default to DashScope compatible-mode + the shared `DASHSCOPE_API_KEY` (override via `LILY_EMBEDDING_BASE_URL`/`_API_KEY`/`_MODEL`), resolvable through remote-config so the managed gateway can flip it without a code change; `makeEmbeddingCaller` returns null on incomplete config; `semanticRelevanceMap` returns null on any error/wrong-shape response; `buildContextMemoryAsync` fingerprint is identical to the sync lexical `buildContextMemory` when disabled/keyless/failed; vectors cached per provider+model so recall re-embeds only the query. Live-recall gate `scripts/validate-embedding-recall.mjs` PASSED 3/3 vs lexical 1/3 (DashScope text-embedding-v3) before default-on |
| Memory-usage chip mis-renders, blanks a turn header, or affects the answer | `test-memory-usage-summary.mjs` + `test-turn-memory-chip.mjs` + `test-turn-live-turn-adapter.mjs` + `test-turn-article-frame.mjs` — the "🧠 记忆 N" chip is READ-ONLY over the per-turn contextMemory trace, so it cannot change any model's reasoning; `summarizeMemoryUsage` returns null on bad/absent input (fail-open → chip hidden) and is kill-switched at source by `LILY_MEMORY_USAGE=0`; `renderTurnMemoryChip` only draws on sealed turns that actually used memory, is idempotent (reused, not duplicated), removes stale chips, and its call site is wrapped so a render error can't blank the status/header; `memoryUsage` rides in `record.meta` (no schema migration) and round-trips on reopen via `liveTurnFromRecord` |
| Autonomous solution-learning spams the approval queue or pollutes memory with wrong/turn-specific "lessons" | `test-auto-memory-proposals.mjs` — solution distillation is OPT-IN (`LILY_MEMORY_LEARN_SOLUTIONS=1`; off → zero behavior change) and HIGH-PRECISION: only `turn.completed` turns that overcame ≥1 tool error with a substantive (≥20 char) problem qualify, so trivial one-shot wins never propose; explicit user intent (记住 / correction) always wins over autonomous distillation; every proposal still flows through the existing HUMAN-APPROVAL queue (`promoteMemoryProposalsFromRecord` → user approve/dismiss → `appendLearnedConvention`), so nothing unverified is auto-injected into active memory; deterministic (no model call, never blocks the turn); deduped + capped (≤50) + fail-open (promote wrapped in turn-archive) |
| Improvisation fallthrough (parse/learned-flow fails → model writes a script) | scheduled-task: recognized intent shows the card / interval floored, never improvises; web-system: no learned flow → "needs re-learn", not ad-hoc |
| Model mirrors internal execution scaffolding back as its reply: on operational/resumed/continued turns it dumps an "Objective / Work State / Completed / Active(none) / Blocked / Next Move / Relevant Files" status report (plus a blanket evidence disclaimer) instead of answering | `test-task-contract.mjs` + `test-session-followup-context.mjs` + `test-session-bootstrap.mjs` — `PLATFORM_BASELINE_RULES` (every active task contract), the short-follow-up layer, and the resume notice all carry a positive reply-shape rule: the reply IS the deliverable in prose; objective/plan/work-state/next-step are internal tracking, never rendered back; continuations build on prior work without re-sending the previous conclusion. Lily asserts the known `record.terminal` status instead of asking the model to infer it. Evidence-grounding discipline is preserved; only genuinely unsupported claims are flagged inline, no blanket disclaimer on grounded answers. Fail-open: wording only, worst case = today. Code-level backstop (2026-07-22, `test-status-scaffold.mjs`): `src/main/status-scaffold.js` detects the rigid header structure (≥4 known headers incl. ≥2 anchors, first line a header) and STRIPS the scaffold prefix on all three paths — the streaming gate (`turn-runtime-event-router.js`, holds the head until proven clean; a confirmed scaffold never fails open), the terminal finalizer (before the evidence gate/persist; a purely-scaffold reply is replaced by a plain note, salvage keeps the real reply), and history load (`opencode-conversation-source.js`, rewrites prefix dumps instead of dropping the whole message). Red line: an ambiguous boundary keeps the original verbatim |
| Shallow learning → hollow, unusable capabilities | `test-web-system-har-contracts.mjs` — query-POST classified read + request fields → params; SPA render-wait before snapshot |
| Web learning depends on the model remembering every professional step | `test-web-system-learning-orchestrator.mjs` + `test-web-system-learning-skill.mjs` — one default `learn_web_system.cjs` orchestrator plans contract discovery, bootstrap HAR, JS intelligence, source-seeded expanded scan, HAR merge, auth recipe, and finalizer; normal learning forbids ad-hoc here-doc/browser scripts |
| Learned web skills still pop browser pages during normal use | `test-web-system-api-execution.mjs` + `test-web-system-learning-skill.mjs` — all-API execution stays HTTP/browser-free; API fallback is skipped unless `--allow-browser-fallback` is explicit; browser primary plans return `BROWSER_EXECUTION_DISABLED` unless `--allow-browser` is explicit |
| SPA learning misses APIs hidden in page JavaScript bundles | `test-web-system-frontend-source-intelligence.mjs` — analyzes large same-domain JS asset sets, fetches missing protected JS bodies with storageState, extracts endpoint+method hints, persists them as non-executable `apiHints`, and auto-generates frontend source from `scan.harPath` in the finalizer |
| Active API-coverage probing mutates data, bypasses permissions, or over-claims completeness on a contract-less system | `test-web-system-probe-coverage.mjs` — `probe_api_coverage.cjs` verifies frontend-hinted endpoints READ-ONLY (only GET/HEAD ever sent; POST/PUT/PATCH/DELETE hints recorded unverified, NEVER executed); 401/403 respected + recorded as `access:"forbidden"`, never retried/bypassed; templated detail endpoints filled only with an id HARVESTED from a verified collection (no guessing); domain allowlist enforced; bounded probe count + rate-limit + timeout; authed session cookies only (no UA/webdriver spoofing); merges WITHOUT overriding authoritative/HAR contracts; session-gated (skipped without one) + fail-open; reports honest verified/gated/stale delta and still records remaining gaps in `health.json` (never claims complete) |
| Web automation breaks on session expiry / pagination / CSRF / writes | `test-web-system-{auth-refresh,pagination,csrf-rotation,idempotency,param-binding}.mjs` — all opt-in, capped, fail-safe |
| Optional runtime installation corrupts or weakens the base execution environment | `test-runtime-pack-installer.mjs` + `test-runtime-packs.mjs` + `test-runtime-pack-settings-ui.mjs` + `test-runtime-pack-main-progress-ui.mjs` + `test-runtime-release-policy.mjs` — failed installs leave no target dir; missing artifact dirs are not treated as installed; bundled read-only packs work without userData install state; Python packs enter PYTHONPATH only when marked `pythonPath`; native/browser packs enter PATH/env through catalog metadata; settings UI cannot uninstall bundled packs; main chat stays background-silent for normal install progress while failures remain visible; release scripts require platform runtime + LibreOffice instead of shipping a package that downloads runtime on first use |
| Background Office rendering opens a Windows terminal or blocks on an unavailable network printer | `test-runtime-health.mjs` + `test-render-document-profile-uri.mjs` — Windows LibreOffice probes and all bundled Office adapters prefer `soffice.exe` over the console-only `soffice.com`, use no-window subprocess flags, preserve headless/invisible conversion, and disable synchronous printer detection. Conversion failures remain explicit and keep the prior document-delivery fallback instead of silently dropping rendering. |
| UI-first workflow makes natural-language tasks feel blocked or less intelligent | `test-capability-broker.mjs` + `test-chat-native-dependency-flow.mjs` + `test-skill-catalog.mjs` — capability context is chat-native, fail-open, bounded, and only injected for operational turns; the composer must not import runtime-pack preflight/install code; main/subagent guides prefer capability contracts over telling the user to click UI first |
| Live rendering queues several forced scroll-to-bottom callbacks, so the user's first upward wheel gestures appear ignored and older messages require repeated scrolling | `test-scroll-geometry.mjs` + `test-scroll-autofollow.cjs` + `test-message-live-render-model.mjs` — each panel coalesces delayed auto-follow, navigation intent versions invalidate stale callbacks, an upward wheel detaches immediately, and the explicit Latest action still re-enables follow. Pure geometry plus a real Electron scroll panel guard both behavior and DOM timing. [gate: conversation-scroll-control] |
| Static AGENT guide grows without a runtime cap, so a large skill catalog or provider contract can crowd out user messages before the task even starts | `test-agent-guide-i18n.mjs` — `buildAgentGuideContent()` must enforce `AGENT_GUIDE_MAX_BYTES` at runtime, keep base/provider guidance ahead of the skill index, truncate oversized synthetic skill catalogs with an explicit notice, and never rely only on today's bundled catalog size |
| Mobile Command runtime emits ad-hoc error codes that the phone, OpenAPI, or docs cannot interpret | `test-mobile-command-error-contract.mjs` — runtime Mobile Command `MC-ERR-*` codes must exist in both `mobile-command-error-recovery-catalog.md` and `mobile-command.openapi.yaml`; catalog/OpenAPI enums must stay identical |
| Mobile Command kill switches or evidence-gated routes fail open and leave remote authority available | `test-mobile-command-kill-switch.mjs` + `test-mobile-server-final-shape.mjs` — configured-off capabilities return `MC-ERR-CONFIG-FEATURE-DISABLED` with `chat_only` fallback at route boundaries; unaccepted push/diagnostics/artifact-content/live/voice/native routes are registered but typed-disabled, not 404 or accidental success |
| Mobile Command docs claim exact machine-readable contracts but schema references drift or disappear | `test-mobile-command-schema-references.mjs` — API matrix refs must resolve against OpenAPI, WebSocket/DataChannel event schema, and native bridge schema; matrix HTTP operations must exist in OpenAPI |
| Mobile Command push/diagnostics contracts quietly grow raw user data, paths, screenshots, clipboard, or unredacted support payloads | `test-mobile-command-privacy-redlines.mjs` — evidence-gated push and diagnostics schemas must stay metadata-only: explicit diagnostics consent, redacted bundle/snapshot, minimal push receipt/open event fields, and no raw content/path/body/text/header fields in those payloads |
| Mobile Command telemetry, customer status, or support diagnostics become free-form and leak provider details, paths, content, stack traces, URLs, or unredacted support data | `test-mobile-command-observability-contracts.mjs` — observability/support JSON schema must compile, accept only bounded telemetry/status/diagnostics fields, enforce resolved/fallback status invariants, require redacted diagnostics manifests, and reject unknown sensitive/free-text fields |
| Capability context bloats ordinary chat and slows or weakens model reasoning | `test-capability-broker.mjs` — ordinary casual text does not inject capability context; files, dependency advisories, grounded/coverage turns, or explicit operational requests do inject bounded context |
| Default compact capability context is truncated before showing representative non-Office or bundled Office capabilities | `test-capability-broker.mjs` — default `compactCapabilityContext` must keep key fail-open routes plus representative app, mail, research, runtime, and bundled Office skills such as `anthropics-xlsx` inside the bounded prompt budget; focused contexts still use task-ranked ordering |
| Task-focused recommendations exist but are invisible in real turns because the capability context injection trigger is narrower than the catalog router | `test-capability-broker.mjs` + `test-turn-orchestrator.mjs` — operational domains covered by the recommendation graph, including mail, stock/source research, app/script building, UI/code/intent evaluation, and Office/media work, must trigger bounded capability context injection; casual chat must still avoid injection, and real engine payload trace must expose the recommended skill ids |
| Focused capability context re-sorts the recommended graph, so the prompt shows a supporting skill before the top task skill | `test-capability-broker.mjs` + `test-turn-orchestrator.mjs` — focused `compactCapabilityContext` must preserve `recommendSkillCapabilityGraph` order; app creation must show `lily-app-builder` before `lily-browser-qa`, stock research must show `lily-stock-research` before source synthesis, and UI quality must show `lily-ui-quality` before browser QA in the actual engine payload |
| Top-tier system-intelligence requests such as "系统更聪明" or "最终形态" are treated as casual chat or generic browser QA, so the agent misses deterministic routing/capability-quality checks | `test-task-contract.mjs` + `test-capability-broker.mjs` + `test-turn-policy.mjs` + `test-turn-orchestrator.mjs` — these requests must activate an evidence-backed `agent_quality` / `architecture_audit` contract, require file search/read evidence, and put `lily-intent-eval` / `lily-skill-quality-gate` ahead of generic browser QA in the real engine payload |
| Active session skills pollute task-focused recommendations even when unrelated to the current request | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — `activeSkillIds` may boost already-relevant skills, but must not turn a zero-relevance skill into a focused recommendation; an active mail skill must not appear in PDF extraction recommendations |
| Capability status only says which tools are visible, hiding why a relevant tool is unavailable and causing the agent to guess or downgrade | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — `lily_capability_status` must keep backward-compatible available tool names, but also expose `toolDetails`/`unavailableTools` with machine-readable reasons such as `SKILL_NOT_ACTIVE`, `MAIL_BRIDGE_UNAVAILABLE`, and `BROWSER_RUNTIME_UNAVAILABLE` |
| Capability status lists unavailable tool names and reasons but omits what the tool actually does, forcing the agent to infer capability from terse names | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — every `lily_capability_status.toolDetails[]` / `unavailableTools[]` item must include the tool description alongside availability and reason |
| Capability status says `SKILL_NOT_ACTIVE` but omits which skill gate is missing, forcing the agent to infer enablement requirements from separate fields | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — tool details must include `missingSkillIds`, with inactive gated tools naming the exact missing skill ids and available tools returning an empty list |
| Capability status says a connector/runtime is unavailable but omits the exact status key/value, so the agent has to guess which host state must change | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — connector-gated tools must include `connectorStatusKey`/`connectorStatusValue` such as `mailConnected:false`, and runtime-gated tools must include `runtimeStatusKey`/`runtimeStatusValue` such as `browserAvailable:false` |
| Platform-only broker status reports `SESSION_REQUIRED` but omits whether a tool actually needs a session, so the agent may confuse missing session with missing skill or connector setup | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — every tool detail must include `requiresSession` and `sessionAvailable`; platform tools remain available with `requiresSession:false` even when no session context exists |
| Tool visibility is mistaken for broker-handler executability, causing the agent to call placeholder handlers that return not-wired/unavailable errors | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — tool details must include `brokerHandlerAvailable` and `brokerHandlerError`; platform broker tools with real handlers report available, while placeholder/external-surface tools disclose their broker handler error code |
| Tool status discloses that the broker handler is not wired but omits the intended execution surface/server, leaving the agent unsure whether to use mail MCP, learned-web-system MCP, browser runtime, or the broker itself | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — tool details must include `executionSurface` such as `tool_broker`, `mail_mcp`, `learned_web_system_mcp`, or `browser_runtime`, plus `mcpServerName` when the execution surface maps to a registered MCP server |
| Execution surface/server routing is hardcoded in a central group switch, so adding a new tool family silently gets the wrong surface or no server name | `test-tool-broker-registry.mjs` — broker tools must declare `executionSurface` / `mcpServerName` in their tool definitions or dynamic tool generation; status may read those fields but must not rely on broad group-name guessing for known tool families |
| Active skills require optional runtime packs, but status only lists installed packs, so the agent cannot tell which pack must be installed to regain full capability | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — `lily_capability_status.runtimePacks` must expose `requiredByActiveSkills` and a deduped `missing` list; installed packs must not be reported missing |
| Task-focused capability recommendations list required runtime packs but not the current missing/install gap, so the agent may try a degraded path before installing the right pack | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — focused `lily_capability_list` calls must expose `runtimePacks.requiredByRecommendedSkills` and deduped `missing` based on current installed packs; default unfocused lists must stay bounded |
| Unfocused capability lists skip runtime-pack probing for budget reasons but return empty arrays that look like a real "nothing installed / nothing missing" result | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — `lily_capability_list.runtimePacks.evaluated` must be `false` for default unfocused calls and `true` for focused query/file calls; `lily_capability_status` is always evaluated |
| Missing runtime packs are exposed only as opaque ids, so the agent has to guess what the pack is or call extra tools before making an install decision | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — capability list/status runtime pack gaps must include `missingDetails` from `PACK_SPECS` with id, category, localized label, description, and size estimate while preserving the compact `missing` id list |
| Capability recommendations identify missing runtime packs but do not say whether the install tool is currently available, so the agent may ask the user or hunt for scripts unnecessarily | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — focused `lily_capability_list.runtimePacks.installToolAvailable` must reflect actual visibility of `runtime_pack_install`, matching `lily_capability_status` |
| Missing runtime pack metadata still leaves the agent to hand-build install tool arguments, risking wrong pack ids or unsafe install behavior | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — every `runtimePacks.missingDetails[]` item must include `installAction: { tool: "runtime_pack_install", args: { packId }, destructive: true, requiresConfirmation: true }` so the next step is machine-readable and visibly confirmable |
| Learned web-system tools are registered in MCP but omitted from capability list/status, so the agent cannot see natural-language system operations and may improvise scripts | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — broker-visible learned web-system tools must appear consistently in `tools/list`, `lily_capability_list.tools`, and `lily_capability_status.toolDetails` when the learned skill is active |
| First-party skills decay into one-off prompt hacks that bypass platform capability contracts | `test-skill-capability-contracts.mjs` + `test-long-task-supervisor-migration.mjs` — operational skills must start from natural language, fail open, route long work through `lily_process_jobs`, expose `job_status`/`job_logs`, avoid UI-first operation, and forbid ad-hoc runtime installs when a Lily pack exists |
| User-imported workspace skills overwrite platform skills or leak into unrelated workspaces | `test-workspace-skill-import.mjs` + `test-skill-catalog.mjs` — standalone workspace skill imports normalize non-learned ids into `learned-*`, reject protected bundled skills and dependency folders, mark imports as workspace-only, bind them to the active project, and fail without mutating existing skills |
| Lily platform catalog skills are mistaken for OpenCode native `skill` commands, so catalog capabilities such as `lily-*` or bundled `anthropics-*` appear "not found" | `test-opencode-config-builder.mjs` + `test-opencode-runtime-reducer.mjs` + `test-turn-orchestrator-steer.mjs` + `test-skill-catalog.mjs` + `test-capability-broker.mjs` + `test-runtime-pack-preflight.mjs` + `test-web-system-learning-intent.mjs` — OpenCode native `skill` is denied in runner permissions; AGENT.md/index/subagent prompts say session catalog entries are platform capability guides, not native skills; dependency/web-learning prompts route through Lily MCP/tools/scripts instead of native `skill <id>`; reducer-level fallback recognizes catalog-backed skill IDs, annotates native `skill <id>` failures with the Lily guide path, and emits a bounded steer recovery so the task does not stop at "not found" |
| Registry-declared specialist Office skills are omitted from task-focused recommendations, causing the agent to use generic tools for PDF forms, structured document drafting, evidence lookup, PPT visual QA, or spreadsheet analysis | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — focused capability recommendations must put `lily-pdf-form` first for explicit PDF form fill/validation, surface `anthropics-doc-coauthoring` for structured document writing, put `lily-document-query` first for document evidence lookup, put `lily-ppt-design-qa` first for PPT layout/overflow review, and put `lily-excel-data-analysis` first for spreadsheet analysis/cleanup/chart work, while still excluding unrelated spreadsheet/PDF/template/DOCX skills; PPT export-to-PDF must not route through PDF extraction |
| Referenced spreadsheets without file objects are mistaken for code repair because words like `fix` and `formula` look like implementation work | `test-capability-broker.mjs` — current/uploaded/attached workbook or spreadsheet formula work must put `lily-excel-data-analysis` first, retain `anthropics-xlsx`, and omit `lily-code-repair` even when no file object is present |
| Runtime-pack installation / enablement is treated as generic Office work, so missing OCR/Docling/large-document capability repair is delayed or improvised | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — focused recommendations for dependency/runtime-pack/OCR/Docling enablement must put `lily-runtime-packs` first while preserving the relevant PDF extraction context; failure must stay fail-open to bundled capabilities |
| Vendored authoring skills drift back to latin-centric or dark-by-default output, so first-pass Word mixes per-machine CJK fallback fonts and first-pass decks ship dark/unstyled | `test-office-authoring-contract.mjs` + `test-office-style-helper.mjs` — overlays + `lily-office-intent` must pin the latin+eastAsia font pair and light-first decks to `resources/runtime-scripts/lily_office_style.py`; helper selftest runs runtime-backed (skips gracefully without the bundle) |
| Cross-built runtime bundle ships build-machine absolute paths (pyvenv.cfg home/executable, bin shims), so the customer-machine venv python dies and every pack health check false-fails after a successful install | `test-runtime-python-fixup.mjs` — `ensureVenvCfgFixed` rewrites stale/placeholder cfg paths on-device (win32 only, mac stays sealed), never touches healthy cfgs, and degrades silently on read-only/partial installs |
| Image OCR is mistaken for scanned-PDF extraction because the text says "scan/OCR", causing PDF tools to be recommended for `.png/.jpg` files | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — image OCR must put OCR/runtime support first and retain image context, but must not recommend `lily-pdf-extraction-router` or `anthropics-pdf`; scanned PDFs still keep the PDF route |
| Referenced image transform without a file object is mistaken for image QA, Office/PDF routing, or creative generation; generic OCR text suppresses PDF context | `test-capability-broker.mjs` — current/uploaded/attached image resize/convert/export/webp work, wording variants like "make smaller", "save as jpeg", and "change to png", plus background removal/cutout requests like "remove background", "remove bg", and "抠图去背景", must put `lily-runtime-packs` first through catalog-declared hints and must not route as Office/PDF or creative generation; textual `OCR` alone must not make PDF runtime enablement lose PDF extraction context |
| Visual creation / prompt-writing requests are reduced to image QA or generic fallback instead of using creative direction and prompt-building skills | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — image/poster/video creation must put `lily-creative-director` first, prompt-writing/enhancement must put `lily-prompt-enhancer` first, and image review must keep `lily-image-qa` first without letting generic "generate" steal spreadsheet chart work |
| Source-backed/current research, stock analysis, skill review, or intent routing tests are mistaken for document evidence lookup, browser QA, coding work, or generic fallback | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — stock/financial research must put `lily-stock-research` first and keep source synthesis support; general source-backed/current research, including latest news, browser rankings, and API price comparisons with citations, must put `lily-research-synthesis` first; skill quality review must put `lily-skill-quality-gate` first; intent routing evaluation must put `lily-intent-eval` first; and `lily-document-query`/browser QA/coding must not steal no-file research just because the user asks for evidence/citations or the topic contains words like browser/API |
| Capability routing overfits a small hardcoded keyword/company-name list, or truncates/catalog-matches hints too literally before multilingual and word-form variants can match, so real variants fail to receive focused context | `test-capability-broker.mjs` — registry-declared `capability.matchHints` must survive contract normalization into the skill graph and drive both focused recommendations and `shouldInjectCapabilityContext`; ticker/portfolio, current research variants such as latest news/API prices/带来源/给出处, runtime/image/media variants such as image resize/background removal/video transcode/image resizing, English UI audit, English SaaS dashboard variants, and engineering variants such as missing test/bug risks must route through catalog-declared hints instead of adding company names or broad verbs to broker code; preserving match hints must not expand the compact prompt context |
| Positive routing hints have no negative counterpart, so a broad word such as `prompt` steals intent-routing evaluation requests | `test-capability-broker.mjs` — registry-declared `capability.avoidHints` must survive contract normalization into the skill graph and suppress matching skills; English skill-route evaluation must put `lily-intent-eval` first and must not include `lily-prompt-enhancer` |
| Broad generic words in capability routing steal specialized artifact review or Office QA tasks | `test-capability-broker.mjs` — image artifact-defect review must route to `lily-image-qa`, not app creation; PPT slide overflow / visual consistency with a deck file must route to `lily-ppt-design-qa`, and generic UI quality must not outrank the presentation-specific QA path |
| Media/runtime enablement is mistaken for creative video generation because generic `video` words trigger creative routing | `test-capability-broker.mjs` — ffmpeg/video processing pack enablement must put `lily-runtime-packs` first and must not include `lily-creative-director`; creative routing requires an explicit creative action/object such as poster, cover, storyboard, prompt, visual concept, or generation/design wording |
| Referenced media without file objects falls through to Office/PDF fallback instead of runtime media processing | `test-capability-broker.mjs` — current/uploaded/attached video/audio compression or transcoding must put `lily-runtime-packs` first and must not include `lily-office-intent` or `lily-pdf-extraction-router` even when no file object is present |
| Mail drafting/search, UI quality review, or runtime error repair fall through to generic Office/browser routing | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — email drafting/search must put `lily-mail-assistant` first, UI visual quality review must put `lily-ui-quality` first while retaining `lily-browser-qa`, and TypeError/build/test/runtime repair must put `lily-code-repair` first without stealing image/document review tasks |
| App creation, component implementation, or engineering discipline review falls through to browser QA, Office, document drafting, or visual creative routing | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — new app/page/tool creation must put `lily-app-builder` first while retaining `lily-coding-core` and browser QA support, component/code implementation must put `lily-coding-core` first, and engineering rules/anti-overdesign/capability-regression/code-review requests, including English overengineering/capability-regression and diff-review/missing-tests/missing test coverage/bug risks variants, must put `lily-engineering-rules` first without being mistaken for proposal/document drafting, generic Office routing, or creative `cover` work just because the text contains `coverage` |
| Script creation, template fill, or media transcode tasks fall through to Office/browser/creative-prompt routing | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — script/batch-file creation must put `lily-app-builder` first with coding support, Word/template placeholder filling must put `lily-template-fill` first while retaining DOCX tooling, and video/audio trim/transcode work must put `lily-runtime-packs` first without being mistaken for visual creative direction |
| Office artifact creation is mistaken for app creation because of broad "create/build" verbs | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — PDF creation/editing must put `anthropics-pdf` first, Word report creation must put `anthropics-doc-coauthoring` first with DOCX tooling, and PowerPoint creation must put `anthropics-pptx` first; `lily-app-builder` must only trigger on explicit app/page/tool/script creation, not Office deliverables |
| Document read/summary is mistaken for evidence lookup, or evidence lookup is weakened into generic file tooling | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — ordinary PDF/DOCX/PPTX/XLSX read-summary tasks must put the corresponding `anthropics-*` file tool first while preserving routers/verification, but explicit query/search/cite/evidence requests over uploaded documents must still put `lily-document-query` first |
| Explicit document delivery verification is mistaken for document drafting, or generic verification steals PPT-specific QA | `test-capability-broker.mjs` — current/uploaded report verification for pagination/table layout/links/rendering must put `lily-document-verify` first and omit `anthropics-doc-coauthoring`; PPT slide overflow/layout QA must still put `lily-ppt-design-qa` first instead of generic document verification |
| Browser QA / localhost smoke tests are mistaken for high-risk web-system learning or generic fallback routing | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — focused recommendations for console errors, frontend smoke tests, localhost, responsive layout, and web QA must put `lily-browser-qa` first and omit `lily-web-system-learning`; explicit learn/automate-a-system requests must still put `lily-web-system-learning` first |
| Short web-system abbreviations match inside unrelated English words and steal document evidence lookup | `test-capability-broker.mjs` — `oa` must be word-boundary matched so text like `uploaded contract` does not trigger web/browser routing; uploaded/attached contract evidence lookup must put `lily-document-query` first and must not include `lily-browser-qa` |
| Non-high-risk first-party skills exist in the catalog but are not installed/enabled by default | `test-skill-catalog.mjs` — non-high-risk Lily skills such as stock research, skill quality, and intent eval must be `defaultEligible`; only high-risk/on-demand skills such as mail and web-system learning may stay default-off |
| Long-running primary work is mistaken for failure, causing the agent to switch to a weaker/secondary path | `test-opencode-agent-session.mjs` + `test-loop-detector.mjs` + `test-skill-catalog.mjs` — active foreground tools extend the no-progress lease and emit observable progress instead of force-ending; loop hints require progress evidence before route changes; AGENT.md states "slow is not failure" and forbids secondary/degraded approaches until explicit failure, user stop, or proven no-progress |
| Pre-turn context compaction hangs and freezes the turn forever at "Preparing to compact conversation context…" (unbounded `await server.summarize` on a stuck/slow model call; try/catch catches errors but not a hang) | `test-opencode-agent-session.mjs` — `compactContext` bounds the summarize call with `runWithTimeout(..., compactionTimeoutMs())` (90s default, ≥15s floor, `LILY_COMPACTION_TIMEOUT_MS`); on timeout it rejects → caught → returns false → the orchestrator's existing fail-open path runs the turn WITHOUT compaction (baseline). Compaction is memory maintenance, never a turn gate |
| A single giant tool result (e.g. read/write of a 6 MB file) balloons the model context — then compaction has to summarize the blob (slow/hangs) and the whole session degrades. ROOT cause behind the compaction hang; the timeout above only bandages it | `test-large-output-guard.mjs` — engine plugin `resources/opencode-plugins/large-output-guard.js` runs in `tool.execute.after` (its `output` is model-facing), caps oversized tool output to head+tail + writes the FULL text to a `.lily-work/tool-output/` sidecar the model can re-read on demand. Budget `LILY_TOOL_OUTPUT_MAX_CHARS` (32k default, ≥8k), kill switch `LILY_TOOL_OUTPUT_GUARD=0`. Fail-open: any error / null output leaves it untouched; small output untouched; non-text parts survive. Registered in `session-runner-pool.js` plugin list |
| Steer ("插话") drops/abort-restarts a message, or hides/merges a visible follow-up after restart, instead of injecting into the running turn | `test-turn-orchestrator-steer.mjs` + `test-session-runtime-store.mjs` + `test-message-store.mjs` + `test-opencode-conversation-source.mjs` + `test-message-render-keys.mjs` — on by default, kill-switch `LILY_ENABLE_STEER=0`; engine-reject / kill-switch both degrade to the queue (today's behavior); accepted steers persist as same-turn user messages with `meta.steer`, survive busy sync, projection recovery, official-history merge, and render-key de-dupe |
| Conversation minimap navigator breaks the chat / desyncs from the transcript | `test-conversation-minimap.mjs` (model) + `conversation-minimap-regression` in `test-renderer-import.cjs` (DOM); DOM-derived + try/catch teardown → any failure removes the rail, chat scrolls as today |
| **DATA LOSS:** session index wiped — root cause was (a) non-atomic write → interrupted write leaves a corrupt file, (b) corrupt read silently treated as "empty", (c) `load()` then auto-creates + `saveImmediate()` overwrites it, (d) `reconcile` prunes all sessions when projects fail to load | `test-session-load-recovery.mjs` + `test-session-save-guard.mjs` — ATOMIC write (tmp→rename) + rolling `.bak`; corrupt read is quarantined & recovered from `.bak` (not emptied); failed load bails (no auto-create/save); empty project set never prunes; `_guardSessionCollapse` refuses overwriting a healthy/corrupt index with a collapsed one |
| Reopened conversations look empty because the UI waits for OpenCode official history before showing the local latest page | `test-opencode-conversation-source.mjs` + `test-renderer-import.cjs` — initial conversation loads use a local-first latest page and request official OpenCode history in the background; older pagination and official refresh remain available |
| Reopening a finished conversation appends a DUPLICATE of the last turn: a rich local assistant turn keeps its answer in `record.assistantText` with empty top-level `content`, so the background official-history refresh (plain text, different key, no inherited turnId) fails every text/key dedup and is appended below | `test-conversation-pagination.mjs` + `test-session-runtime-store.mjs` + `test-opencode-conversation-source.mjs` — assistant dedup compares `content \|\| text \|\| record.assistantText` across all three merge layers (renderer page merge, renderer committed-store, main-side projection merge), so the official copy collapses into the one rich turn instead of duplicating on every reopen |
| Switching conversations blocks the UI while preparing OpenCode runner/session guides on slower Windows filesystems | `test-session-switch-fast.mjs` + `test-turn-orchestrator.mjs` — session switch changes visible state immediately and only warms the runner in the background; sends, rewinds, skill changes, and other execution paths still call the strong `ensureSessionRunner` path before acting |
| **DATA LOSS:** streamed assistant text disappears if the app crashes before `assistant.final` / terminal archive | `test-message-store.mjs` + `test-opencode-conversation-source.mjs` + `test-session-runtime-store.mjs` — `assistant.delta` is projected durably into SQLite; open projected turns are recovered as visible `turn.stalled` assistant messages across reopen; OpenCode official history merges local projections even while the runner is busy, so partial answers are not hidden by an incomplete official page |
| Non-essential renderer module statically imported into the bootstrap chain (app.js→message.js→…) can blank the whole app on load failure | message.js loads the minimap via guarded `import().catch()` (not a static import) → a broken optional module degrades to "no minimap", never "no chat/sessions" |
| **DATA LOSS:** renaming the product moves Electron's name-derived userData folder, orphaning every install's sessions/licenses/model config/memory (this app has already been terminal-chat-claude → ai-super-terminal → Lily Workbench) | `test-main-single-instance-lock.mjs` — asserts `src/main.js` pins `userData` to a fixed `<appData>/lily-workbench` and that the pin runs BEFORE `bindRuntimePaths()`; changing that folder string fails the build. (Legacy pre-pin folders are still swept by scan-based `legacyUserDataRoots()` — `test-data-migration.mjs`.) |
| Media-gen (image/video/speech) distribution must not break old clients/servers or leave a device with no media | `test-client-config-service.mjs` (resolveMediaSelection): no `config.media` → all key-backed providers + server default (today); selection gated by available keys; default falls back; old clients ignore the additive `effectiveConfig.media`; client `serviceSelection` null on old servers → today's behavior. `test-agent-env-media.mjs`: server-delivered media gateway env (`LILY_IMAGE_PROVIDER`, `LILY_VIDEO_PROVIDER`, `LILY_SPEECH_PROVIDER`, DashScope/Ark/Kling/MiniMax/Zhipu proxy URLs and short tokens) survives spawn env conversion so generation skills can actually call the gateway. `test-admin-media-provider-surface.mjs`: admin config surfaces built-in Lily image/video/speech services and profile rules can select Lily + speech without leaking upstream endpoints. `test-media-provider-settings.mjs`: client settings keep configured first-party Lily media visible even when a stale media allow-list only named DashScope. `test-media-provider-contracts.mjs`: server-delivered media request contracts are additive, scoped to actually available providers, can be generated from the upstream OpenAPI schema, and are injected into client runtime env. `test-agent-guide-i18n.mjs`: AGENT.md exposes current media/search providers and all selected-provider contract parameters instead of hardcoded vendor guesses. `test-media-gateway-providers.mjs` + `test-media-generation-skills.mjs`: Lily GPU private/relative asset URLs are rewritten through the gateway, downloaded with the short Lily token, and speech/video generation can execute from server-delivered contract defaults without leaking 127.0.0.1 result URLs to clients. |
| User-selected media providers are bypassed after an error, causing the agent to silently switch to another vendor | `test-agent-guide-i18n.mjs` — AGENT.md exposes image/video/speech/search provider selections, dynamically describes media skills using the selected provider, and explicitly forbids automatic provider fallback after a configured provider error; the agent must report the error and ask the user whether to retry, switch provider, or provide a key. |
| Web access guide advertises a non-executable search/fetch command because cross-skill script placeholders are left unresolved | `test-agent-guide-i18n.mjs` — generated websearch/webfetch inline guide must contain concrete `websearch.cjs` and `webfetch.cjs` paths for every locale and must not leave `{{WEBSEARCH_SCRIPT}}` / `{{WEBFETCH_SCRIPT}}` placeholders in AGENT.md |
| Search env resolution assumes Electron-only globals and crashes in plain Node helpers before the server-delivered IQS env can be applied | `test-search-settings.mjs` — `getSearchSpawnEnv()` must keep the managed `iqs` default and not throw when `LILY_USER_DATA_DIR` is set but `process.resourcesPath` is absent; server-delivered `WEBSEARCH_IQS_API_KEY` / `WEBSEARCH_IQS_API_URL` must still win when present |
| Rankings/latest/prices/news/roles/releases/statistics are treated as memory-answerable general knowledge, unsupported prose streams to the customer before its evidence check, a no-search constraint is bypassed, failed search calls count as evidence, or the model invents links/items/scores after searching | `test-external-fact-grounding.mjs` + `test-answer-evidence-finalizer.mjs` + `test-turn-orchestrator.mjs` — high-risk external facts receive an additive `external_fact` task contract automatically (while local UI/code/release work and creative chat retain their existing routes); their answer prose is buffered until final claim assessment, and a rejected answer is replaced by one concise scope question or an honest evidence gap, so unsupported claims never enter the visible final or archive; required evidence must come from a SUCCESSFUL websearch/webfetch/live API or authoritative supplied document; answer URLs and ranked items must occur in this turn's evidence, while external years/percentages/versions/latency values are grounded by default. Explicit no-search instructions are honored and never auto-retried; ambiguous rankings and completed research attempts also avoid duplicate retries. A well-scoped memory-only fact may request ONE side-effect-free verification retry, guarded by rescue one-shot/cooldown and both retry kill switches. Gate/finalizer errors fail closed to the honest external-fact fallback, while non-external turns retain their prior fail-open behavior. |
| External claims depend on hard-coded domain vocabulary, so an unfamiliar certification, authorization, membership, or status question bypasses the evidence gate; a model semantic plan cannot activate the host gate; a source merely mentioning an entity is mistaken for proof of the requested predicate; or the gate only refuses after repeatedly searching low-authority sources | `test-semantic-external-fact-contract.mjs` + `test-general-claim-verification.mjs` + `test-model-verification-plan.mjs` + `test-public-enterprise-grounding.mjs` + `test-external-evidence-recovery.mjs` + `test-external-fact-grounding.mjs` + `test-answer-evidence-finalizer.mjs` + `test-evidence-risk-tiers.mjs` + `test-evidence-entailment-judge.mjs` + `test-tool-semantics.mjs` + `test-tool-call-rescue.mjs` + `test-opencode-conversation-source.mjs` + `test-platform-intent-eval.mjs` + `test-turn-orchestrator.mjs` — existing deterministic profiles remain as high-precision fast paths, but correctness no longer depends on adding domain terms. The selected model may declare any bounded semantic `claimKinds` plus `externalFact=true`; the host safely activates only a non-operational general turn, preserves every user constraint and host route, and enforces declared `authorityHosts` and `evidenceAnchorGroups` per named/listed entity. Structured list extraction is language- and suffix-independent. A real web/API observation also promotes an initially general turn into a generic external-evidence floor, including failed research attempts, so the final answer cannot fall back to memory after searching. Research first discovers the source owner, changes strategy after low-authority results, verifies whether a requested classification is formal, and delivers a verified result or explicitly partial supported subset. Malformed plans or recovery context retain the strict baseline; local code/document/operation tasks cannot be model-rerouted. A repairable evidence gap gets one changed-strategy retry that inherits only bounded successful read-only external evidence from the immediately completed turn. Its internal prompt is hidden, and a durable supersession marker prevents the first pre-gate engine answer from returning after history reload. Unsupported conclusions are replaced with an honest limit. [gate: external-claim-evidence] |
| Image/PDF reading is mistaken for media generation, `识别` is parsed as the negation `别`, failed extraction counts as evidence, or guessed attachment details stream before verification | `test-content-task-intent.mjs` + `test-task-contract.mjs` + `test-send-preflight.mjs` + `test-evidence-ledger.mjs` + `test-evidence-gate.mjs` + `test-answer-evidence-finalizer.mjs` + `test-task-run-policy.mjs` — one semantic content-intent owner resolves operation/source/output before routing; read/recognize/summarize requests use `content_extraction`, while create/modify/convert retain artifact routes. Native vision, bridge recognition, and document extraction emit explicit complete/available/partial/unavailable source coverage; failed reads never satisfy `source_content`. Source answers are buffered until the gate passes, unsupported claims are replaced with an honest read-failure answer, and proven prior-turn source context may continue as available but never masquerades as a fresh complete extraction. Intent failure leaves existing routing and attachments intact. [gate: attachment-content-grounding] |
| Colloquial software requests such as “写一个牛逼的程序” retain model/tools but miss the coding task contract, so testing, workspace delivery, and truthful completion become optional | `test-program-task-intent.mjs` + `test-task-contract.mjs` + `test-platform-intent-eval.mjs` — a focused programming semantic owner combines action, software target, workspace/code attachments, and quality requirements; only an action+target pair activates `code_change`, so ordinary chat, emails, and “程序员排行” do not become coding tasks. The generic semantic draft carries implement/debug/refactor/optimize/inspect plus answer/artifact/workspace-change output modes; unknown or malformed signals leave the existing classifier and strong model/tool baseline intact. [gate: task-intelligence-baseline] |
| User intent exists only as transient prompt advice, terse continuation loses the prior goal, stale tasks are resurrected across unrelated chat, or `turn.completed` is presented as verified completion without satisfying delivery criteria | `test-intent-contract.mjs` + `test-task-contract.mjs` + `test-turn-orchestrator.mjs` + `test-task-run-policy.mjs` + `test-tool-call-rescue.mjs` + `test-tool-broker-{registry,mcp}.mjs` + `test-session-memory.mjs` + `test-session-runtime-store.mjs` + `test-turn-view-renderer.mjs` + `test-platform-intent-eval.mjs` + `test-capability-readiness.mjs` + `test-turn-dependency-readiness.mjs` — additive Intent Contract v3 keeps a bounded host-owned goal, relation (`new/continue/refine/correct/cancel`), deliverables, success criteria, constraints, and capability needs; exact continuations inherit contract id/objective and increment revision, corrections become explicit amendments, and a later non-task answer clears the inheritance anchor. The same selected model may optionally refine semantics through `lily_intent_contract_commit`, but the host preserves identity/routing and monotonically retains baseline deliverables, criteria, constraints, capabilities, and risk; malformed/absent calls change nothing. This one host-owned idempotent MCP remains replay-safe so it cannot disable evidence recovery; all unknown/external MCP tools remain side-effect guarded. Legacy archives synthesize a compatible contract; malformed history or summary/store read failure falls open to current-turn deterministic classification with the original selected model, tools, context, and user text unchanged. TaskRun keeps legacy `status`/events for old clients while adding persisted/rendered `completionStatus`: required evidence/test gaps are `delivered_unverified`, observed work is distinct, and only verified evidence becomes `verified_complete`. [gate: intent-to-capability-plan] [gate: truthful-completion] |
| Native-vision model toggles are saved but stale config profiles still route images through the weaker bridge path | `test-client-config-service.mjs` — provider metadata `nativeVision:true` and profile `capabilities.vision:true` both expand to preset `capabilities.vision:true`; stale generated `vision:false` cannot suppress the provider's current native-vision capability |
| Background service startup must not weaken OpenCode foreground Bash or leave invisible detached processes | `test-process-jobs-core.mjs` + `test-process-jobs-mcp.mjs` + `test-process-job-protocol.mjs` + `test-mcp-config.mjs` — adds an optional `lily_process_jobs` MCP with PID/log/health/stop; MCP unavailable guidance falls back to normal foreground shell; OpenCode `bash` lifecycle is untouched |
| Long tasks look stuck because the platform cannot show liveness, phase, or output hints | `test-process-job-observability.mjs` + `test-process-jobs-core.mjs` — process jobs expose compatible `status` plus normalized `state`, progress-derived `phase`, `heartbeatAt`, `outputFiles`, `recoverable`, logs, and existing PID/log paths without changing foreground tool behavior |
| Transient engine liveness snapshots are rendered as heavy floating cards, making "still running" UI visually louder than completed tool rows — and stale "still running" lines persist after tools finish, even in saved history | `test-engine-notice-policy.mjs` + `test-turn-renderable-timeline.mjs` + `test-turn-notice-entry.mjs` + `test-turn-process-timeline-model.mjs` + `test-turn-live-clock-patch.mjs` — `longWait`/`toolProgress` keep the lightweight `is-liveness` style but render only WHILE TRUE: `toolProgress` needs a running tool, `longWait` must be the newest timeline entry, and neither reaches the sealed transcript; the heartbeat tick is a surgical patch (status line + `patchLiveToolClocks` text updates on running rows), never a full-article re-render, so elapsed time stays out of the visual signature |
| Foreground upload/download commands look dead or require feature-specific UI — or shell discard sinks such as Windows `NUL` are misrendered and persisted as fake `Download: nul` cards | `test-work-progress-protocol.mjs` + `test-opencode-runtime-reducer.mjs` + `test-turn-process-timeline-model.mjs` + `work-progress-notice-regression` in `test-renderer-import.cjs` — generic progress inference emits optional `workProgress` notices for recognizable transfer output/commands; real discard sinks normalize to no artifact path and fall back to today's tool row without changing command execution; ordinary filenames such as POSIX `null`, real downloads, and structured/live progress remain visible; sealed legacy discard-sink notices are hidden by an exact, reversible read-time compatibility guard |
| Model traffic may silently bypass Lily Gateway or be impossible to audit | `test-model-route-audit.mjs` + `test-opencode-model-config.mjs` — final resolved `LILY_*` env is classified as gateway/direct/invalid with redacted URL/key-kind diagnostics; route audit is attached to OpenCode config diagnostics and runner diagnostics without changing routing |
| Model grading or recovery makes the selected model less capable, repeats side effects, loses the current turn, or lets a lite model stop before verified delivery | `test-capability-grading.mjs` + `test-model-compatibility-probe.mjs` + `test-model-self-heal.mjs` + `test-tool-call-rescue.mjs` + `test-subtask-resilience.mjs` + `test-opencode-agent-session.mjs` + `test-context-budget-manager.mjs` + `test-session-memory.mjs` + `test-mcp-config.mjs` + `test-opencode-message-parts.mjs` + `test-model-eval-policy.mjs` — destructive lite behavior requires confirmed repeated evidence; full/standard remain byte-identical; lite support retains the strong-default executable MCP surface and only adds guidance/verified compatibility recipes; recovery is current-turn anchored and side-effect guarded; retained-context pressure includes history plus the new turn; the lite execution protocol survives measured prompt limits; shared broker context cannot leak a conversation; evals require a baseline and Lily persona |
| OpenCode protocol selection falls back to URL guessing and loses tool calls on OpenAI-compatible providers | `test-opencode-model-config.mjs` + `test-client-config-service.mjs` + `test-model-settings-secret-storage.mjs` + `test-scheduled-tasks.mjs` — service presets explicitly deliver OpenCode provider id/npm/base/protocol; customer custom models/gateways persist protocol; legacy URL heuristics are migration fallback only |
| A valid managed model ID is silently rewritten to another model, so the UI selection and billed upstream model disagree | `test-opencode-model-config.mjs` + `test-model-gateway-openai-adapter.mjs` — protocol normalization may remove only a protocol-invalid legacy suffix; official IDs such as `deepseek-v4-flash` survive admin storage, client OpenCode config, and gateway forwarding unchanged |
| Edge service DNS/routing failure silently makes remote config fail and leaves the model on direct fallback | `test-service-client-edge-fallback.mjs` — UAE edge network failure falls back to the canonical domestic service for bootstrap and signed service calls, pins the runtime service base to the reachable host, and keeps the agent route auditable |
| OpenCode resume id crosses Lily conversations, causing one chat to inherit another chat's history, tools, or skills | `test-resume-binding.mjs` + `test-resume-continuity-guard.mjs` + `test-ensure-session-runner-resume-reset.mjs` — every new resume id is bound to the Lily session/project/workspace/skill-set/version/first-user fingerprint; mismatches clear the resume and start fresh with local-history rehydration, while legacy unbound resumes fail open to a recent-history continuity guard instead of silently trusting the engine cache |
| Recoverable model/file failures poison one Lily conversation so the user must create a new chat to recover | `test-opencode-agent-session.mjs` + `test-turn-orchestrator.mjs` — safe failures replay inside the same turn; unsafe/resumed/attachment-linked visible failures invalidate only the bottom engine session, clear the persisted resume id, terminate that runner view, and keep the Lily chat/history intact so the next message starts clean in the same conversation |
| Screenshot/image turns leak raw base64 to a non-vision model, or model-side 413 is exposed to the user instead of being compacted/replayed | `test-opencode-message-parts.mjs` + `test-opencode-server-manager.mjs` + `test-opencode-agent-session.mjs` — image file parts require explicit native-vision opt-in; non-native image sends stay text/path/vision-result only; `Request Entity Too Large` at dispatch or model-event time isolates a fresh engine session and replays the current request without a sticky visible failure |
| Malformed or empty OpenCode completions are treated as completed answers and then poison summaries or compaction memory | `test-turn-error-classify.mjs` + `test-turn-orchestrator.mjs` + `test-session-memory.mjs` + `test-compaction-memory-export.mjs` + `test-remote-config-gateway-token-expiry.mjs` — leaked `<tool_call>/<function>/<parameter>` fragments and empty assistant completions become retryable protocol failures, are never stored as assistant memory, are never injected back into compaction memory, and expired Lily gateway tokens invalidate remote config so sends refresh instead of running on stale credentials |
| An interrupted historical turn already persisted an empty assistant message, so a strict OpenAI-compatible provider rejects every later request before the model or tools run and Retry can never recover | `test-empty-assistant-history-guard.mjs` — the engine plugin `empty-assistant-history-guard.js` repairs truly provider-empty assistant rows in `experimental.chat.messages.transform` before both ordinary and compaction model calls by adding one bounded neutral text marker; message ids/order and valid text remain byte-identical, tool-only messages remain untouched so call/result pairing is preserved, the repair is idempotent, and failures or `LILY_EMPTY_ASSISTANT_HISTORY_GUARD=0` retain baseline behavior [gate: opencode-history-compatibility] |
| Stale, malformed, or placeholder Lily managed gateway tokens make default models fail with 401 while user BYOK direct models still work | `test-opencode-agent-session.mjs` + `test-turn-error-classify.mjs` + `test-remote-config-gateway-token-expiry.mjs` — exact `MODEL_GATEWAY_TOKEN_INVALID/EXPIRED` and gateway-routed 401 refresh remote config, rebuild the runner env, restart the engine session, and replay once; `$LILY_GATEWAY_TOKEN` placeholders and malformed `lilygw.*` cache values force send-preflight refresh; generic BYOK 401 remains a visible API-key auth error |
| Skill presets eagerly install every heavyweight optional dependency before the user asks for that capability | `test-skill-presets.mjs` + `test-runtime-pack-preflight.mjs` — presets only select skills; task text/files decide required versus enhancing packs, and ordinary PDF work keeps heavy engines optional |
| A runtime pack installs successfully but the current runner/MCP keeps its stale environment, so the model cannot use the new capability | `test-runtime-pack-coordinator.mjs` + `test-runtime-health.mjs` + `test-runner-live-config.mjs` — installs are deduplicated and health-checked before activation; an idle pre-dispatch runner is refreshed only after a verified install |
| Dependency preparation replays the user request or dispatches it twice, duplicating side effects | `test-turn-dependency-readiness.mjs` + `test-turn-orchestrator.mjs` — user input is admitted and committed first, preparation runs before runner creation, then exactly one engine payload is sent; planner failure keeps the baseline path |
| A transient network failure restarts a large dependency download from zero or fills the disk | `test-runtime-pack-download.mjs` — stable `.part` files resume with Range, bounded retry handles transient failures, ignored Range restarts safely, and disk headroom is checked before network transfer |
| A downloaded artifact is marked installed before its imports/binaries/browser can actually run | `test-runtime-health.mjs` + `test-runtime-pack-installer.mjs` — known packs are probed in staging before atomic activation; failed health checks preserve the previous version and never write healthy installed state |
| The catalog/release advertises an optional capability for a platform whose artifact was never verified there | `test-runtime-pack-release-matrix.mjs` + `test-runtime-release-policy.mjs` — build publishers write the lock only after same-target probes; formal release preflight is strict across darwin-arm64, darwin-x64, and win32-x64, and one-click upload also compares production metadata |
| Dependency assistance makes a lite model lose executable surfaces or changes the strong model's base config/context | `test-capability-grading.mjs` + `test-mcp-config.mjs` + `test-turn-dependency-readiness.mjs` — lite retains the complete executable MCP surface and receives additive recipes; standard/full remain on the strong baseline, while readiness adds only bounded task context and fails open to baseline |
| Mobile Command malformed, unsupported-version, or oversized remote frames reach local admission and mutate a Lily session | `test-mobile-protocol-version.mjs` + `test-mobile-agent-bridge.mjs` — unsupported protocol versions, oversized text, and too many attachments are rejected before `admitExternalCommand`; local Lily chat remains the strong default, and remote failure cannot create a queued turn |
| Mobile Command phone can only target the desktop foreground chat, or a selected mobile target steals/changes the desktop active session | `test-mobile-agent-bridge.mjs` + `test-mobile-pair-web.mjs` + `test-mobile-pairing-wiring.mjs` — phone requests a bounded current-project session list, selects a mobile target session, sends `lilySessionId` on commands, and desktop validates the selected session while falling back to the active session without changing desktop UI focus |
| Mobile Command voice entry is advertised as production/native ASR or silently fails on unsupported browsers | `test-mobile-pair-web.mjs` — phone exposes only browser dictation as text-entry convenience through `SpeechRecognition`/`webkitSpeechRecognition`, logs unsupported/permission failures visibly, and keeps production voice/ASR gated |
| A huge pasted/typed message (a data dump, e.g. 100 texts / a big log) is sent verbatim into the model context instead of being persisted and retrieved from — bloating history and the window at the source (the top-tier pattern is ingest→workspace→retrieve, not dump) | `test-large-input-staging.mjs` — `large-input-staging.stageLargeInputText` (wired into `opencode-server-manager.sendPrompt` before the prompt body) stages an oversized message to a content-addressed workspace file (`.lily-work/inbox/`) and replaces the ENGINE text with a compact directive + head/tail preview + path that routes the model to the existing `lily_file_intelligence` (inspect/index/query) for selective retrieval; the FULL input is preserved on disk (nothing lost) and Lily's own message store keeps the user's original text (UI unchanged); only triggers above a high char threshold so normal messages pass through untouched (never dumber), fail-open leaves the text unchanged, kill switch `LILY_LARGE_INPUT_STAGE=0` |
| A data-heavy session grows past the model's context window and deadlocks — too big to run AND too big to self-compact (compaction must send the whole head to the summarizer, which also overflows), so the engine retries build/compaction in a loop that keeps appending and growing; the worst bloat is a `write`/`edit` tool INPUT holding a whole large file, which the engine's `truncateToolOutput` never trims (input, and build-path output, are untouched) | `test-context-window-guard.mjs` — the engine plugin `context-window-guard.js` on `experimental.chat.messages.transform` bounds oversized part CONTENT (tool input/output, text/reasoning) to a per-part cap and, if the whole request still exceeds a token budget, tightens largest-first until it fits — before BOTH the turn and compaction model calls, so compaction can finally shrink and the loop breaks; the budget is MODEL-AWARE (session-runner-pool derives `LILY_CONTEXT_TOKEN_BUDGET` from the active model's real context window via `resolveContextBudget` × 0.85 headroom, scaling 32k→1M) so a big model is never over-trimmed and a small one never overflows; whole messages and tool-call/result PAIRING are never dropped, content that fits the real window passes through byte-identical (never dumber), and it is fail-open with a kill switch (`LILY_CONTEXT_GUARD=0`) and a conservative fallback budget when the window is unknown |
| A non-image attachment (JSON/XML/data file) is sent as a raw file part to a model whose provider only accepts image file parts, so the engine's AI SDK throws `AI_UnsupportedFunctionalityError` pre-gateway and the whole turn/compaction fails — worst on unknown custom/BYOK models, and once such a part is in a session's history every later turn AND compaction stays stuck | `test-opencode-message-parts.mjs` + `test-opencode-server-manager.mjs` + `test-turn-error-classify.mjs` + `test-filepart-text-coercion.mjs` — three fail-open layers: (1) client file-part eligibility is capability-driven (`filePartMimeAllowed` reads the preset's declared `capabilities.filePartMimes`), default DENY for every non-image mime, text-like files inline as fenced text and oversized/unknown ones fall back to a source-path note; (2) the engine plugin `filepart-text-coercion.js` on `experimental.chat.messages.transform` coerces every non-media file part (image/pdf left alone) to inline text just before both the turn and compaction model calls, so ALREADY-poisoned sessions self-heal model-agnostically and nothing is lost; (3) the unsupported-file-part error is classified as `ATTACHMENT_UNSUPPORTED` (non-retryable, actionable) instead of being relabeled a dead model or a network drop. All three default-safe with kill switches (`capabilities.filePartMimes`/`LILY_FILEPART_COERCE=0`) and degrade to baseline text/path, never a raw file part |
| Intent, routing, prompt, tool-safety, runtime, skill installation, or completion behavior changes in separate files and silently drifts; oversized coordinators keep accumulating responsibilities until fixes become risky | `test-capability-gate-registry.mjs` + `test-architecture-boundaries.mjs` + `test-context-budget-manager.mjs` + `test-external-command-runtime.mjs` + `test-opencode-agent-session.mjs` + `test-opencode-session-policies.mjs` + `test-skill-catalog.mjs` + `test-skill-guide-sync.mjs` + `test-subagent-runtime-projection.mjs` + `test-task-run-kernel.mjs` + `test-turn-runtime-event-router.mjs` + `test-platform-intent-eval.mjs` + `run-capability-gate.mjs` - machine-readable capability ownership records each baseline, fail-open route, and guard test; the architecture manifest ratchets every existing source hotspot so it may shrink but not grow, rejects new source files above 500 lines, prevents main/renderer cross-imports, enforces intent-before-readiness, preserves one source of truth for tool semantics and runtime contracts, and keeps mobile admission, context compaction, runtime event projection, terminal finalization, subagent projection, TaskRun state transitions, guarded turn recovery, OpenCode liveness/history/failure policy, and bundled-skill synchronization out of their coordinators. The multi-turn golden set exercises production intent, policy, and capability-planning code, including continuation/refinement/correction, stale-task breaks, rankings, optional PDF enhancements, and explicit runtime needs. [gate: architecture-ratchet] |

| Generated/edited Office or PDF files are called polished or verified after merely being written, an optional Node generator is missing even though the bundled Python route exists, a QA retry replays file-authoring side effects, or CJK output loses its font | `test-document-delivery-gate.mjs` + `test-turn-artifacts.mjs` + `test-tool-call-rescue.mjs` + `test-document-fonts.mjs` + `test-office-authoring-contract.mjs` — document creation requires a real output file; verification additionally requires deterministic reopen, render, and actual page-image inspection (all pages up to 12, first/last plus at least 6 for longer files). Missing QA triggers at most one path-scoped continuation that inherits the existing artifact and never replays the original authoring request. Word/PPT/Excel/PDF scratch authoring stays on the bundled Python stack, direct PDF drawing receives a discovered CJK font path, and any failure preserves the file as `delivered_unverified` instead of making a false quality claim. [gate: office-document-delivery] |

| A model writes the substantive Markdown deliverable, calls TodoWrite only to mark the task complete, then emits a short acknowledgement; sealing the turn treats that acknowledgement as the answer and collapses the real report into process history | `test-turn-terminal-narrative.mjs` - a substantial report separated from the closing text only by TodoWrite bookkeeping is promoted into one Markdown-safe terminal narrative. Evidence-changing tools never promote earlier prose; short progress and malformed timelines fail open to the current behavior. The model output, tool execution, and live stream remain untouched. |

| Publishing one Windows or Mac build for an already-current universal version rewrites the signed static `latest.json` with only that platform, a slow older release overwrites a newer concurrent release after its large upload finishes, or a Windows installer URL contains spaces rejected by Microsoft Store | `test-release-admin.mjs` + `test-release-version-guard.mjs` + `test-release-artifact-naming.mjs` - a single-platform publish can merge only a same-version, signature-verified base manifest; the new artifact replaces its platform while untouched platforms remain. Invalid signatures and mismatched versions fail loud. `release-one-click.mjs` retrieves that base and checks remote version monotonicity both before artifact publication and immediately before moving latest pointers, so an older release can never replace a newer one. Windows NSIS, immutable release, and auto-update artifacts share the space-free `LilyWorkbench-<version>-x64` name while macOS keeps its existing product name. |

| Character card parsing, session binding, world-book activation, or context compilation changes a native turn, crosses conversations, displaces Lily guidance, exposes private card data, or lets imported executable behavior run | `test-character-worlds-store.mjs` + `test-character-card-parser.mjs` + `test-character-binding-isolation.mjs` + `test-character-context-injection.mjs` + `test-character-worlds-capability-gate.mjs` + `test-character-worlds-concurrency-stress.mjs` + `test-character-worlds-policy.mjs` + `test-character-world-book-store.mjs` + `test-character-world-book-import.mjs` + `test-character-world-book-activation.mjs` + `test-character-world-book-compile.mjs` + `test-character-world-book-decorators.mjs` + `test-character-world-book-ipc.mjs` — native conversations send byte-equivalent Lily/OpenCode prompt bodies with no card parsing or extra model request; bindings are local, optional, immutable-revision pinned, conversation/owner-scoped, and snapshotted in the same SQLite transaction as turn admission, so retries, recovery, queue promotion, and steer inherit by trusted source turn id and never re-read the latest binding; compiled character context is a bounded lower-authority suffix of the per-request system field after the protected Lily prefix — never user text, visible history, or file parts — separately budgeted with no guaranteed minimum; every failure mode (disabled policy, missing binding, missing revision, parser/macro/compiler error, over-budget, provider without safe system context) fails open to the byte-identical native turn with exactly one dispatch and unchanged tools, skill IDs, model, permission mode, files, evidence context, subagent surface, user text, and output reserve; embedded world books import bounded and inert, activation is a deterministic pure resolver over the admitted snapshot plus the durable timed checkpoint, activated entries render only inside the same lower-authority envelope, and every world-book failure mode (missing or corrupt book revision, resolver error, over-budget world content) fails open to the byte-identical no-book compiled body of the same bound character with a single dispatch; world-book timed checkpoints persist only after successful turn finalization, never advance on failed turns, and rewind purges them; world-book IPC is read-only with whitelisted summaries (no raw book content or mutation surface crosses the bridge); imported scripts/plugins/regex scripts/unknown macros never execute and unknown card data is preserved inertly; `LILY_CHARACTER_WORLDS=0` and the signed rollout policy restore native behavior without touching stored data [gate: character-worlds-isolation] |

| Unknown tools or shell commands are replayed automatically after a failure, or retry, rescue, evidence, and TaskRun code disagree about side effects and replay safety | `test-tool-semantics.mjs` + `test-tool-call-rescue.mjs` + `test-tool-broker-registry.mjs` — unknown tools and shell commands are never replayed automatically; unknown semantics are treated as external-side-effectful and not replay-safe, so a failed turn is recovered by nudging the model or asking the user, never by silent re-execution [gate: tool-semantics-single-source] |

| Main, persistence, and renderer drift apart on the versioned RuntimeEvent and TaskRun contracts, so a producer writes an event or TaskRun field a consumer silently cannot read | `test-runtime-contract-manifest.mjs` + `test-runtime-event-schema.mjs` + `test-session-runtime-store.mjs` — existing event names and TaskRun fields stay compatible; unsupported versions and malformed typed producer payloads are rejected before persistence or rendering [gate: runtime-contract] |

| Internal prompt layers (rehydration, memory, advisories, capability context) crowd out or silently truncate the original user request | `test-engine-message-layers.mjs` — normal-size prompt layers and the full visible user message remain byte-identical; only oversized internal layers are bounded, and explicit truncation diagnostics are preserved [gate: prompt-envelope] |

## Enforcement

- **AGENTS.md Rule 13** makes this gate binding for every task.
- Each vector above has an automated guard; the full suite (`npm run test:unit`
  + `test:renderer`/`test:runtime`/`test:service`/`test:skills`) must be green.
- Any new capability-affecting feature adds its own (1) failure-mode test proving
  it degrades to baseline and (2) closed-loop guard, and registers it here.
- "Closed-loop verified" means tested. "Looks fine" / "should work" does not pass —
  state honestly what is automated vs what still needs a live/build verification.
