# Capability Gate — 防止"变笨"硬门槛

Structured transport recovery (2026-09-20): `test-runner-failure-contract.mjs`
and `test-transport-closure-restart.mjs` retain native causes, explicit
non-retryability and execution progress through terminal cleanup and SQLite
restart. Interrupted tool work continues through the existing durable owner;
unknown writes are not replayed. Cancellation, questions, permissions, duplicate
claims, specialized context repair and old string-error paths remain intact.
No model/tool/context changes; acceptance-gap auto-repair remains opt-in.
Scope and remaining UI acceptance: `docs/2026-09-20-structured-task-recovery.md`.

Terminal ownership follow-up (2026-09-20): `test-opencode-terminal-ownership.mjs`
guards normal idle settlement across superseded status/history/replay reads,
unknown status, stale supplemental output and health probe cancellation.
History selects the newest non-compaction assistant even when it has no text;
errors remain failures and unfinished tool steps remain incomplete, never an
older narration promoted to success. Existing progress/health guards remain
active during unavailable status. No model, tools or context are reduced.
These are deterministic mechanism tests, not customer-device acceptance.

Native UI follow-up: OpenCode's successful status map omits idle sessions.
The server adapter now distinguishes that valid sparse map from malformed or
failed queries. `test-opencode-bundled-usage.mjs` verifies this contract against
the actual shipped binary, and `test-opencode-server-manager.mjs` guards unknown
and error responses. Unknown transport remains unknown, never successful idle.

Long-task recovery ownership (2026-09-20): `test-long-turn-recovery-ownership.mjs`
requires authoritative idle before treating a completed assistant step as final.
Busy engines retain the progress/health guards rather than the transport grace
deadline. Superseded recovery reads cannot settle or fail a renewed turn.
`test-long-turn-virtual-clock.mjs` checks 48h of simulated progress, stale history
reads across progress/new turns/user questions, genuine stalls and explicit caps.
These are deterministic regressions, not reproduction of the customer's device.

Novice UI source/intent follow-up (2026-09-20): tests
`test-source-resolution-guidance.mjs`, `test-turn-user-context.mjs`,
`test-document-delivery-response.mjs`, `test-turn-orchestrator-steer.mjs`,
`test-agent-autonomy-guidance.mjs` and `test-workbench-empty.mjs` guard input
events, attachment provenance, turn-owned accepted revisions, source-first
planning, same-model judgement and structured replies with reasoning content.
No tools/model/context are removed. Pending input never becomes verified file
delivery; chat-only applicability reruns source/fact checks. Unavailable/invalid
applicability preserves the model answer with an unverified-file note, never
verified delivery or automatic file creation. Valid delivery verdicts retain
literal file validation. Native evidence and remaining scope are tracked in
`docs/2026-09-20-novice-ui-acceptance.md`; this is not all-platform acceptance.

Ten-round follow-up: `test-conversation-load-state.mjs` guards request-owned
loading/error/ready states and cached history; `test-turn-replay-input.mjs`,
`test-large-message-reader.mjs` and `test-task-continuity-integration.mjs` guard
exact-turn accepted revisions during replay after store reopen. Reader failures
are explicit and cannot silently discard a user's later restrictions. No full
history scan, model downgrade, tool removal or context truncation is introduced.
See `docs/2026-09-20-ten-round-native-qa.md` for per-round evidence and boundaries.

Windows Office resolution follow-up: `test-runtime-system-office.mjs` guards
real executable discovery without assuming a shim is usable, keeps Lily Python
ahead of LibreOffice's private Python and real Office ahead of stale bin shims.
Printer enumeration suppression is child-environment scoped, not a Windows
setting. `test-office-authoring-contract.mjs` and `test-agent-guide-headroom.mjs`
guard literal current-build helper paths and unchanged authoring capabilities.

Vision delivery follow-up: the first-party vision CLI emits an ASCII-safe
local-image receipt only after a successful nonempty visual response. Delivery
assessment consumes these receipts after rendering and matches actual page
paths; it retains native image tools and rejects failed, stale, unrelated and
partial-page evidence. `test-vision-inspection-receipt.mjs` exercises the real
CLI against a local transport; `test-document-delivery-gate.mjs` guards coverage.
This is inspection provenance, not a claim that every visual defect was found.

Windows office follow-up: `test-xlsx-recalc.mjs` exercises real LibreOffice
recalculation in deep output paths, formula caches, source preservation and
formula-error rejection. Conversion profiles are temporary and independent of
the delivery directory; failed conversions remain errors. The first-party XLSX
helper never overwrites the original or claims full Excel feature fidelity.
Other formats retain their existing routes. Native UI conversion/rendering was
retested; installed skill refresh and all-capability acceptance remain separate.

Large-history responsiveness (2026-09-20): `test-large-message-reader.mjs`
checks queued worker reads, exact display parity, complete answers/tools,
untouched archived diagnostics, WAL visibility, dispatch/close failures and
indexed recovery without full-history reads. `test-message-enrichment-worker.mjs`
checks off-main derivation/compression, conditional writes preserving concurrent
edits/deletions, unchanged evidence/hot columns/FTS, cursor resume and corrupt-row
isolation. Recovery still selects the newest unsuperseded assistant of the exact
session/turn; no model, context, tool or retry capability is removed. Reader
failures surface; maintenance failures leave stored evidence and read-time
artifact derivation available, never synchronous full-history retries.
Production enrichment uses a read-only worker and the existing main-process
writer for conditional compressed commits. A foreground read/write-transaction
regression guards against the independent-writer SQLITE_BUSY snapshot race
observed in the 2GiB desktop test.
Evidence and remaining gaps: `docs/2026-09-20-large-history-responsiveness.md`.
This is not acceptance of every startup path or the customer's exact 66s stall.

Windows native follow-up (2026-09-20): `test-media-input-encoding.mjs` guards
UTF-8/BOM file transport for all three media CLIs while retaining stdin and
exercises Windows PowerShell character loss. `test-windows-office-cli.mjs`
guards plugin-factory exports and a narrow existing-sibling CLI substitution
for literal Windows LibreOffice informational commands; printing, conversion,
unknown paths and other platforms retain baseline behavior. `test-runtime-health.mjs`
and `test-render-document-profile-uri.mjs` guard child-scoped printer discovery
isolation. Recovery projection and renderer tests retain host outcome-unknown
explanations without replacing partial work. Native evidence and explicit gaps:
`docs/2026-09-20-windows-native-acceptance.md`. These are not all-capability,
installed-release, arbitrary long-task or all-provider acceptance.

智能体管理 (2026-09-14): `test-agent-definition.mjs`, `test-agent-repository.mjs`, `test-agent-activation.mjs`, `test-agent-draft-tool.mjs`, `test-agent-knowledge-preparation.mjs`, `test-agent-guidance.mjs`, `test-official-agent-catalog.mjs`, `test-agent-routing.mjs`, `test-agent-workspace-portability.mjs`, `test-agent-distribution.mjs`, `test-agent-library-model.mjs`, and `test-agent-library-ui.cjs` guard the 智能体 bundle: one validated definition model with field-level coded rejections; owner-scoped immutable revisions + CAS session bindings + append-only events in messages.db; activation through the EXISTING setters (character binding, session skills, permission mode, model selection, paused automation import) where every dimension fails open independently with a named receipt reason, cold dimensions (model, tools.disallow) degrade beyond the serve fork budget (`LILY_AGENT_SERVE_FORK_LIMIT`, default 3), and deactivation restores the exact pre-activation snapshot; the inert `lily_agent_draft` broker tool (platform-scoped, metadata-only, never binds); knowledge preparation where the official legal ROLE rule is preserved verbatim and a bound agent's `knowledge.packs` join it (unknown pack ids block by name, never guessed); the bounded hot-path guidance section; and an official catalog whose every role/skill/pack reference is verified against the shipped catalogs. `LILY_AGENTS=0` hides the library, refuses activation and makes every agent-derived policy read inactive while stored bindings stay readable. Unbound sessions are byte-for-byte today's behaviour. Natural-language creation routes 智能体 intents to the draft tool (engineering uses of "agent" do not); `.lilyspace` packs carry an `agents.json` section (definitions only, local role cards embedded on opt-in, imports deduped and re-validated); distributed agents from the server registry are validated, narrowed by the signed `config.agents` targeting, installed as `distributed` revisions and a targeted default binds to new conversations fail-open. The renderer reuses the character library dialog (agents tab default, roles second), the session banner popover and composer starters over the narrow `assistantClient.agents` facade with CAS retries and honest degradation notices; the role features stay intact. Loop closure (2026-09-15, user report "只看到表相"): `test-agent-binding-continuity.mjs` — every binding change (activate / deactivate / role chosen under an agent) leaves an idempotent durable in-conversation record listing what the agent brings (renderer card via `meta.agentBinding`); a deliberate skill change (agent activation or manual toggle) re-pins the engine resume binding so the next spawn RESUMES with context instead of starting fresh (`LILY_KEEP_ENGINE_ON_SKILL_CHANGE=0` restores the reset); an explicit role choice releases the bound agent first, restores its snapshot and reports `agentDeactivated` to the renderer; each assistant record carries `meta.agent` naming the agent that answered. Renderer: the picker lists every agent with capability chips and an explainer, the role list notes when an agent owns the role, and messages show the binding card and "由智能体「X」回答". These are deterministic mechanism checks, not installed-client or real-model acceptance. [gate: agent-management]

OpenCode upgrade continuity (2026-09-14): `test-resume-binding.mjs`, `test-ensure-session-runner-resume-reset.mjs`, `test-resume-continuity-guard.mjs`, `test-opencode-version-alignment.mjs`, and opt-in `test-opencode-upgrade-native.mjs` guard the explicit 1.18.29 to 1.18.30 forward transition, strict ownership and unknown-version rejection. Native acceptance requires `LILY_TEST_OLD_OPENCODE_BIN`; default skip is not cross-version verification. No arbitrary semver compatibility or historical metadata rewrite is permitted. Continuity comparison (2026-09-14 field case): the engine stores Lily's LAYERED text, so `test-resume-continuity-guard.mjs` now pins that the guard compares the user's own words (user_original_request unwrapped) with a script-aware containment floor (CJK ≥2 chars, Latin ≥6) — a chat of short Chinese turns ("继续"/"帮我优化") must not be reset as a history mismatch, while unrelated histories still are. 2026-09-23 补：对话的连续性不能被"不是换模型的事"夺走。`test-engine-config-continuity.mjs` 钉住引擎配置的两个指纹各司其职——完整指纹认出"必须换一个 serve"（引擎只在启动时读一次配置），脱敏后的路由指纹认出"模型和到达方式没变"；于是每小时一次的网关令牌轮换走 recycle（换进程、留 resume），真正换模型才走 restart（丢 resume 才正确）；密钥规则只有一份，日志打印与重启判定共用，绝不再各写一套；拿不到路由指纹时一律退回旧行为。同一测试还钉住压缩绝不把运行中的引擎不认识的 provider 发出去——配置在引擎繁忙时变更会延后生效，期间的引用要么换成该引擎声明过的默认模型、要么整个省略让引擎用会话自己的模型，压缩永远能继续（否则长会话压不动，上下文只增不减直到死锁）。`test-turn-continuity-policy.mjs` 钉住两条：纯上游故障（网关超时/过载）在本回合没跑过不可重放工具时保留 resume，跑过副作用工具或有待决权限/提问时仍按原规则丢弃，调用方不提供证据则严格如旧；以及任务目标是散文不是标识符——多行目标原样保留、换行统一、其余控制字符照拒、长度照限，而标识符字段仍禁止任何控制字符。 [gate: opencode-upgrade-continuity]

Database self-recovery (2026-09-13): worker preflight gates startup before data
migration/session loading. Native hot-journal rollback retains original evidence;
existing corrupt history is never silently reset. Verified rolling snapshots,
explicit candidate confirmation, no-clobber crash-resumable replacement and
retained original journals fence recovery. Historical copies disclose unknown
data cutoff instead of displaying preparation time as snapshot recency. Old
queued admissions are durably outcome-unknown; new post-recovery work remains
eligible even if receipt acknowledgement fails. Independent localized repair UI
and actual version remain available when ordinary initialization fails. Tests:
`test-database-recovery.mjs`, `test-database-recovery-service.mjs`,
`test-database-recovery-flow.mjs`, `test-database-recovery-startup.mjs`,
`test-restored-task-fence.mjs`, `test-database-recovery-ui.cjs`, and
`test-database-recovery-native-boot.cjs`. Real main-process UI uses a synthetic
temporary profile and scripted confirmation; this is not native Windows,
physical power-loss, signed-installer or customer-file acceptance.

Database self-recovery guards: `test-database-recovery.mjs`, `test-database-recovery-service.mjs`, `test-database-recovery-flow.mjs`, `test-database-recovery-startup.mjs`, `test-restored-task-fence.mjs`, `test-database-recovery-ui.cjs`, and `test-database-recovery-native-boot.cjs`. [gate: database-self-recovery]

Upstream refresh rejection (2026-09-13): `test-upstream-auth-failure.mjs` guards
explicit upstream token-refresh 401 nested in retryable 502 errors. Native message
metadata, session errors and fresh session-owned build diagnostics retain the
non-retryable auth cause, surface it immediately and request a session-scoped abort
before settlement. Auxiliary/stale/foreign diagnostics cannot stop a foreground
turn; ordinary transient failures retain recovery. Abort rejection and delayed
abort, duplicate idle/error tails and gateway-key versus upstream-auth separation
are covered with controlled transport. This does not restore upstream credentials
or claim production gateway recovery. Guard: `test-upstream-auth-failure.mjs`. [gate: upstream-model-auth-failure]

Config rule target identity (2026-09-22): a rule scoped to 「某个授权」 matched the license's internal id while the admin form took free text, so an operator who typed the license key customers are given saved a rule that listed and never fired — every device kept the global model and nothing said so. `test-config-profile-license-target.mjs` holds the seam that fixed it: the admin write paths resolve what was typed (a license key hashes to its license) to the id delivery matches, refuse a target that matches nothing with the reason, and never write raw input into the table; delivery keeps comparing ids and does no lookups. Migration 054 heals rules already written with a key, only where the hash matches exactly one license and the value is not already a valid id. [gate: config-profile-target-identity]

Config delivery explainability (2026-09-22): merge order was (priority, updated_at) with no notion of scope, so "more specific wins" was a habit maintained by hand-picked numbers — a global rule with a larger priority silently beat a license rule — and the admin preview ran its own copy of the matcher, already drifted (it knew nothing about user- or organization-scoped rules). `test-config-scope-precedence.mjs` holds the single selection: specificity orders the merge (global → organization → group → license → user → device), priority orders rules inside one scope, an unknown scope never matches and never outranks a device rule, and neither delivery nor preview may keep a private matcher or order in SQL. `test-config-delivery-provenance.mjs` holds the receipt: the merge records which rule established each field, and each pipeline stage is DIFFED so a stage that drops or rewrites a field reports it without knowing it must — silence is not inheritable by a stage added later. The receipt rides beside the config (never inside the signed payload) and is bounded. [gate: config-delivery-explainability]

Admin console honesty (2026-09-22): 81 admin reads went through a helper that swallowed every error and returned an empty fallback, so on 30 of those sites an API outage rendered identically to a console with nothing configured — and the console had almost no gates behind 49 pages. `test-admin-console-honesty.mjs` deletes that helper (a source scan across web/ forbids its return), routes every page read through a request-scoped ledger (React `cache`, so one operator's outage never appears on another's page), and requires AdminShell to render the ledger — endpoint and reason, role=alert — with translated copy in all three locales. A failed read still returns its fallback: half a console beats a stack trace. The same gate covers release artifacts: creating a release now HEADs the download URL and refuses only a definite 404/410, because a host that guards or cannot answer HEAD must never block a release. It renders AdminShell for real — Next's own swc bindings, a vm sandbox and renderToStaticMarkup, the same door the enterprise flow gate uses — and asserts the banner, the endpoint, the reason and role=alert reach the page in all three locales while a healthy request shows none of it. [gate: admin-console-honesty]

Media provider manageability (2026-09-22): the config rule form offered six media providers from a list hardcoded in the browser bundle, delivery decided availability from a second hand-written list, and four of the six needed a credential stored under an id an operator could not guess from the chip (火山方舟 → `volcengine-media`). Production had none of those rows, so ticking them saved and was silently dropped, and nothing in the console said where to configure them. `test-media-provider-manageability.mjs` holds the one catalog (`media-provider-catalog.js`): every provider declares what it can generate and where its credential lives, delivery derives availability from it, an admin endpoint reports each provider's status and source, the form renders that answer — an unusable provider is shown struck through and cannot be ticked, with the credential id to create, in three locales — and the providers page offers those ids instead of expecting them to be guessed.  Retired 2026-09-23: the self-hosted GPU provider and the bespoke integration behind it — its own endpoint env vars, its own gateway routes and asset proxy, its own contract module and a read-only "built-in" provider row, roughly 600 lines no other provider needed. Its endpoints no longer exist, and an integration that needs a private path through five files is not one anyone can repeat. The gate now asserts the rule directly: one generic /llm/media/:provider route, no provider-specific endpoint env, no contract module, no built-in rows, no bespoke skill adapter — a provider is a catalog entry, a credential row and an adapter.[gate: media-provider-manageability]

Admin destructive actions and chrome (2026-09-23): deleting a device group or a model provider submitted on the FIRST click, while deleting a config rule right beside them asked — two behaviours behind the same red link teach an operator the wrong lesson. `test-admin-destructive-and-chrome.mjs` forbids wiring any delete/remove/rollback/revoke/merge action into a bare form: they all go through `danger-form.js`, which has no path that skips the question, names the target in it, and is translated in three locales. The same gate holds the chrome: the navigation rail is sticky with its own scroll (it used to scroll away with the content), and a narrow window no longer hides it outright — hiding it left every page but the current one unreachable, so it becomes a short scrollable strip instead. [gate: admin-destructive-and-chrome]

Admin consistency (2026-09-23): the console did the same thing three ways. An empty list rendered whatever `empty` a page handed the shared table, including nothing; four panels each defined their own form Field, so help text appeared on one page and not another and a field-level error appeared nowhere; row actions were laid out by hand per table; and required/url fields were checked only by the server, so a typo cost a round trip. `test-admin-consistency.mjs` holds all four: the table supplies a translated empty state when a caller does not, AdminEmpty carries no language of its own, exactly one Field exists (with help, required marker and error slots), row actions go through one wrapper, and the provider form declares the same minimum, shape and url-ness the server enforces — with no form allowed to disable browser validation. [gate: admin-consistency]

Admin lists and analytics (2026-09-23): every admin list was a bare `.limit(300)`, so past 300 rows the console showed a truncated list and said nothing. `test-admin-lists-and-analytics.mjs` holds one keyset pager (`server/src/services/admin-pagination.js`: opaque cursor, exact total, one row more to know there is a next) that every list answers through, including joined lists whose cursor must read the row's own key — `devices.last_seen_at` comes back as `last_seen_at`, and reading the qualified name made every next page empty. Usage answers the shape of the spend (top devices, share, distribution), not only its sum. [gate: admin-lists-and-analytics]

Admin pages render (2026-09-23): the contacts page shipped reading `cursor`, `params` and `rows` it never declared, and the config-rule form shipped calling a `splitCsv` left behind in another file — both threw on every visit while `next build` stayed green, because the build never runs a function body. `test-admin-pages-render.mjs` compiles every page under `web/app/admin` and everything it imports with Next's own swc, prerenders each through `react-dom/static` (which awaits async server components) with the API answering its declared fallback, and fails on any page that throws. It also renders the list cells that carry meaning with rows: used/seats and expiry countdown on licenses, drift from the release a device's platform-arch is offered, which release row each platform is offered now, a contact inbox that opens on waiting requests and can mark one handled, users that hide three always-zero billing figures until an order exists — and forbids a toggle labelled with a state word ("已禁用" on the button that disables). Diagnostics keeps its table inside a horizontal scroller (the card clips, so a wide table was cut off), ranks kinds by the devices they reach, and leaves traces (up to 40 KB each) off the list and on the record's own page. [gate: admin-pages-render]

No undeclared identifiers (2026-09-23): a name read but never declared throws only when its line runs, so it passes `next build`, `node --check` and every import. It reached production three times in one day — contacts read `cursor`/`params`/`rows`, the config-rule form called a `splitCsv` left in another file, and `GET /api/admin/config-profiles` read `request` in a handler written `async () =>`, answering 500 to every operator. `test-no-undeclared-identifiers.mjs` parses 1,400+ files (server, server scripts, desktop main and shared, web app/components/lib, desktop renderer) with the Babel bundled in Next and fails on any program-level global that is neither declared, imported, nor a runtime global; browser globals are allowed only where a browser runs the code. It proves on a probe that it catches the handler case and refuses `window` on the server. [gate: no-undeclared-identifiers]

Shared workspace receive interaction (2026-09-14): `test-remote-task-workflow-ui.cjs` exercises actual Electron project/session selection, saved-binding reuse, locale preservation and navigation fences. Since 2026-09-22 it settles on the fixture going QUIET (in-flight api calls) instead of a fixed tick count, and assertions about state that follows async work wait for that state: one IPC round trip on a loaded machine outlasts any tick count, which produced a suite-only failure no standalone run could reproduce. `REMOTE_TASK_UI_TEST_LATENCY_MS=40` reproduces that machine — the old fixed-tick version fails under it, this one passes. `test-remote-task-dual-client-ui.cjs` runs both legacy mode and `LILY_TEST_SHARED_WORKSPACE=1` through real IPC/SQLite/ZIP/files and native clicks, including chosen-project isolated Open and delivery/application/rollback. Account/transport/object/project registration and external editing remain fixtures. `test-remote-task-workspace-binding.mjs` covers native-chooser cancellation and revocation before local registration. No production, full-source dual-client or physical-device acceptance is implied.

Session-owned task execution directory (2026-09-14): `test-session-workspace.mjs`, `test-remote-task-session.mjs`, `test-remote-task-workspace-binding.mjs` and `test-ensure-session-runner-resume-reset.mjs` guard isolated task sessions under an existing project, persisted root identity and no fallback into private project cwd after task-root loss/replacement. Turn preflight, engine/resume binding, document context, session skills, learned skills, runtime checkpoints and session-scoped file writes use the resolver. `test-turn-orchestrator.mjs`, `test-filetree-security.mjs` and `test-session-skill-tree.mjs` retain ordinary behavior. This is cwd selection and file-handler fencing, not an OS sandbox or live model acceptance.

Shared workspace session binding (2026-09-14): `test-remote-task-workspace-binding.mjs` guards identity-only bind commands, encrypted account/device-local mapping, cross-task SQLite reopen reuse, immutable target and authorization-before-session-resolution. `test-remote-task-session.mjs` exercises the real session index and rejects foreign/deleted project targets; binding a running session does not change it or navigate. This is the main-process binding substrate, not receive UI, isolated execution directory wiring or complete shared task cards.

Shared workspace negotiation (2026-09-14): `test-remote-task-policy.mjs`, `test-collaboration-policy.mjs` and `test-remote-task-workspace-identity.mjs` guard exact protocol version one, signed-config gates, encrypted source identity reuse after SQLite reopen, conversation isolation and legacy request omission. Downgrading the service cannot silently remove workspace identity from an existing intent. This connects task creation only; existing-project receive/session binding and conversation cards are not yet acceptance-complete.

Shared workspace identity (2026-09-14): `test-remote-task-contract.mjs`, `test-remote-task-routes.mjs`, and `server/scripts/collaboration-tasks-integration.mjs` guard optional stable workspace identity, immutable owner/conversation binding, matching encrypted and relational task identity, concurrent creation/replay and object-failure rollback. Migration 047 is additive and precedes workspace-bearing requests. Existing task-party/object ACL remains authoritative; knowing a workspace ID grants no task/history/object access. PostgreSQL tests use isolated schemas and seeded verified objects, not signed HTTP or real bucket acceptance. Desktop generation/binding and conversation cards remain separate work.

Remote task preparation continuity (2026-09-14): `test-remote-task-preparation.mjs` guards a durable encrypted identity before bundle I/O, readable pending state during preparation, pre-ready send refusal, same-identity retry and SQLite reopen. `test-remote-task-project-source.mjs` retains source/account/revocation fences; `test-remote-task-workflow-ui.cjs` covers the actual Electron retry affordance. This is a preparation foundation, not completed persistent conversation cards, shared-workspace binding or live two-device acceptance.

Task continuity audit repair (2026-09-12): `test-task-continuity-integration.mjs`
guards real SQLite supervisor-to-wake progress admission, unchanged-receipt
refusal, explicit user retry versus automatic recovery budgets, durable abandoned
wake notification retry and renderer projection without replacing a newer turn.
These deterministic integration checks do not prove installed-client, real-model
or customer-data acceptance. Automatic 8-round/24-hour caps remain unchanged.

Conversation render ordering (2026-09-12): `test-conversation-render-order.cjs`
executes production message scheduling/cache entry points with actual Electron
DOM and controlled frame scheduling. It guards interleaved history/live arrivals,
late insertion, mounted-only keys, order-aware cache validation, retained article
identity, old-generation cancellation, window eviction, detached reading, per-panel
jobs, completion callbacks, duplicate keys and live-to-sealed answer boundaries.
Article content rendering is a fixture in this test; the existing renderer-import
suite covers full renderer integration. This is source-client regression evidence,
not installed-release acceptance. Guard: `test-conversation-render-order.cjs`. Link integrity (2026-09-15): a bare URL followed by full-width punctuation (`http://127.0.0.1:5173，88 个工具`) linked the comma and the digits with it — an address that does not even parse, so the click went nowhere. `test-markdown-link-trim.mjs` cuts a BARE autolink at the first ideographic/full-width punctuation character and restores the remainder as text, while an explicit [label](url), a local-file link, ASCII punctuation and a genuine CJK path are left untouched. [gate: conversation-render-order]

Session database safety (2026-09-12): `test-session-db-safety.mjs` uses real
SQLite WAL commits after process termination, failed snapshot publication/retry,
destination collisions, interrupted source/destination compaction, invalid/empty
backups, orphan journals and exclusive locks. First migration publishes a verified
standalone snapshot without replacing existing evidence; failed migration retains
the source. Recovery precedes migration and message-store creation. Diagnostics
separate access/lock/I/O failures from confirmed corruption and never advise deletion.
Existing `test-message-db-compaction.mjs`, `test-data-migration.mjs`,
`test-message-store.mjs`, `test-session-load-recovery.mjs` and
`test-support-diagnostics.mjs` remain companion guards. Node and Electron Node-mode
checks do not establish native Windows, physical power-loss or customer-database
recovery acceptance. Existing damaged primary databases are never auto-replaced.

Session database safety guards: `test-session-db-safety.mjs`, `test-message-db-compaction.mjs`, `test-data-migration.mjs`, `test-message-store.mjs`, `test-session-load-recovery.mjs`, and `test-support-diagnostics.mjs`. 2026-09-23 补：诊断产物必须可取回，且占盘有上限。`test-diagnostic-log-bounds.mjs` 钉住主进程日志真正落盘并带**日期**（此前只写 console，打包后用户关掉应用就再也拿不到；时间戳只有时分秒，跨天的文件无从判读），且**同时接管 console**——绝大多数调用点绕过 logger，只接 logger 会漏掉大半输出还显得已经修好；终端行为保持原样，文件是叠加的。同时钉住共享轮转沿：无论写入多少，磁盘占用恒不超过 单文件上限×(代数+1)，代数用尽即丢最旧一份，不保留历史时也仍是一个有界文件而非无界文件，轮转保留近期历史而不是一截了之；看门狗那份此前无轮转无上限、全仓无人读回（真实机器实测 86 天 47 MB、176711 行）现已纳入同一条规则。并钉住写日志永不成为故障源：磁盘满、目录被删、路径不可写、值在格式化时抛异常，一律降级为"这行没写下"，绝不向正在打日志的调用方抛错。 2026-09-23 补：能力静默降级必须留下原因。`test-swallowed-failure.mjs` 钉住"降级开放但不沉默"——平台设计上允许某项能力跑不起来时让回合照常完成，但此前是靠空 catch 抹掉原因，于是"悄悄变笨"与"本来就不需要"在外部完全无法区分（现场 2026-09-23：目标覆盖度审计整场每轮失败，超时/端点不可达/请求被拒/JSON 损坏归为同一个结果）。记录必须带：能力名（按用户理解的说法而非模块路径）、真实原因（含错误码）、可归因的会话或回合。红线在反方向——**绝不能变成新的日志洪水**：同站点同原因去重、超过上限只留一行并声明其余被抑制、但计数完整保留；不断变化的原因不得无限增长内存；记录降级这件事本身绝不能抛错。并且明确**不是**"每个 catch 都要打日志"：处理预期情况的 catch（输入非法、会话过期、文件不存在、可能不是 JSON）不在此列，给它们加日志正是本条要防的噪音。累计降级同时进入支持报告，让"这一轮为什么变笨"从散落告警变成可查事实。 [gate: session-database-safety]

Long-task boundary repair (2026-09-11): `test-task-long-progress.mjs` exercises
3,200 successful receipts, bounded recent-fingerprint storage, repeated/failed
observations, long cycles, unchanged hard continuation caps, productive total
exhaustion handoff and no-progress recovery refusal. `test-turn-orchestrator.mjs`
preserves the stop reason and unfinished count in terminal events. The existing
single-parent recovery limit remains intact; this is not a durable batch scheduler.
`test-gateway-stream-completion.mjs` retains partial streamed text but refuses
normal completion without protocol evidence, and covers malformed/error streams,
fragmented text, final unterminated lines and token usage. These are deterministic
source tests, not installed-client, real-proxy or customer-task acceptance.

Native UI recovery correction (2026-09-08): `test-turn-orchestrator.mjs` now guards stable recovery admission IDs and recovery provenance through real vision/document preflight. `test-task-acceptance-recovery.mjs` distinguishes grounded missing requirements from unknown template audit evidence: unknown remains unverified, but cannot invent extra work and recursively re-enter a completed task. Declared missing outputs, grounded missing requirements and productive native handoffs retain recovery. `test-opencode-conversation-source.mjs` preserves committed host interruption across native abort-tail coalescing; `test-session-runtime-store.mjs` fences late starts of terminated turns. Real source-client Mac UI rechecks verified one completion for one multi-step request, explicit stop/continue, session switching and normal restart history; see `docs/superpowers/plans/2026-09-08-native-task-ui-acceptance.md`. This is not installed-release or arbitrary complex-task acceptance.

Native task continuity and recovery (2026-09-08): `test-task-request-continuity.mjs` preserves the full original request through owner-scoped durable input references and SQLite close/reopen; absent or foreign sources remain unknown. `test-task-source-resolution.mjs` covers explicit/admitted/restored source identity; `test-task-execution-classification.mjs` retains document/media mutation semantics through TaskCore without treating read-only questions as artifact work. `test-task-acceptance-recovery.mjs` exercises the real completion wrapper's unknown-source path without tools or extra model calls. `test-parent-closure-lease-resume.mjs` covers real SQLite restart before lease expiry, fenced timer recovery and stable admission identity. `test-turn-loop-guard.mjs` resumes exact-prose observation after known closing fences while preserving code and uncertainty fail-open behavior. These extend the task-completion-integrity gate below and are deterministic host integration tests, not full-app lifecycle, real-model success-rate or installed-client acceptance.

Packaged repetition helper (2026-09-08): `test-job-observation-packaging.mjs` reproduces Electron's external `extraResources` layout and verifies both host consumers load the same job-observation implementation. Missing or invalid helpers retain baseline behavior instead of crashing startup. Release acceptance must additionally execute the packaged modules: source-mode tests alone cannot prove ASAR resource resolution.

Confirmed repetition guard (2026-09-08): `test-turn-loop-guard.mjs`, `test-job-observation.mjs`, `test-job-observation-packaging.mjs`, `test-loop-detector.mjs`, `test-task-execution-progress.mjs`, `test-process-job-turn-guard.mjs`, and `test-process-job-generation.mjs` guard fragmented repetitive text, unchanged process-job observations, actual-progress and foreground/user-wait protection, code/quote/oversized fail-open behavior, bounded abort, retained partial output and diagnostic archival without automatic parent re-entry. Job timestamps do not refill progress budgets; valid terminal receipts retire historical running blockers without asserting task acceptance. Generation fencing prevents old process callbacks and delayed observations from overwriting replacement jobs. Unknown tools/data retain baseline and `LILY_TURN_LOOP_GUARD=0` restores the unguarded path. Mechanism tests are not customer-log, real-model, or installed-client acceptance. [gate: confirmed-turn-repetition]

Production rollout update (2026-09-08): the user explicitly approved ordinary-account task/workspace collaboration. The production global default-off override is now disabled through the audited admin API; account/conversation/device authorization and emergency switches remain intact. The historical default-off/ordinary-rollout-pending statements below describe earlier acceptance stages, not current production policy. See `docs/releases/2026-09-08-0.1.168-mac-server.md` for verified release status and remaining installed-client/physical-device limitations.

Windows workspace handoff (2026-09-08): `test-workspace-collaboration-entry.cjs` verifies immediate focus after the chooser closes without depending on animation frames in a hidden/background window. Controlled delayed frames additionally prove queued panel/back focus cannot steal the recipient input's focus; the panel shell respects focus already inside its content. Existing navigation/account ownership checks still fence the callback. Windows Electron profile locks are reported by the test cleanup helper without changing the behavioral exit code; unexpected cleanup failures remain failures.

Workspace collaboration source guard (2026-09-08): `test-remote-task-project-source.mjs` exercises strict optional project identity, main-only directory resolution, real frozen ZIP/files, encrypted local binding, missing/deleted project refusal without chooser fallback, and account/conversation/scope rechecks across directory and freeze awaits. Omitted project identity preserves the native chooser path. Preparation never uploads, sends, or starts an engine; safe renderer views omit absolute source paths. This guard does not claim renderer or live two-client acceptance.

Workspace collaboration entry/UI guard (2026-09-08): `test-workspace-collaboration-entry.cjs` loads the actual project menu/center/CSS and guards authorized conversation/friend/Team selection, explicit identity-only preparation, disabled/login/service explanations, fresh policy retry, canonical friend resolution, immutable unknown Team retries versus terminal rejection, canceled and availability/revocation-event-fenced asynchronous work, preserved drafts and live locale focus, and compact light/dark/narrow/detached geometry. `test-remote-task-dual-client-ui.cjs` drives the actual workspace menu and both task surfaces through create/unknown-receipt retry/accept/receive/deliver/approve/preview/apply/rollback, with production IPC validation, SQLite, ZIPs and source-file assertions. Directory, transport/object storage, project registration/session focus and external editing are explicit fixtures. These guards are not production, full-app session registration, installed-client or physical two-machine acceptance; see `docs/remote-task-workspace-ui-acceptance.md`.

Workspace collaboration live evidence (2026-09-08): the same acceptance record separately documents native UI operation of two dedicated authenticated full-source clients against the real service/private object transport, from project menu through task creation, acceptance, actual independent project/session registration, delivery, approval, explicit deletion-consented application and rollback. Synthetic source and immutable baseline were checked before application; replace/add/delete and exact source restoration were checked after UI operations. External synthetic-file edits are not AI/editor validation. This is one Mac with isolated test identities, not a released signed installer, ordinary-account rollout, or physical two-machine acceptance. Full-source UI exposed project-tree refresh and generic transfer-tray leaks, subsequently fixed and rechecked after restart, including legacy cached task transfers. `test-remote-task-focus-ui.cjs` guards actual idle project/session registration and preload-driven tree/header updates; `test-remote-task-cached-transfer-ui.cjs` guards authorized cached-object ownership retrofit and real IPC-driven tray refresh without object network requests, broad workspace filtering or repeat change events.

IM visible-read wiring (2026-09-08): `test-collaboration-visible-read.cjs` exercises the actual Electron center and CSS with controlled preload responses and focus conditions: list-only/hidden/search views do not acknowledge, visible message sequences do, both unread indicators follow authoritative list refresh, scrolling only observes intersecting rows, failed admission retries and teardown stops observation. The existing `test-collaboration-read-{ack,checkpoint,recovery}.mjs` and `test-collaboration-activity-projection.mjs` retain durable command, account/revocation and exact-count recovery coverage. The renderer never clears aggregate counts optimistically. These are isolated source tests, not installed-client or production two-device acceptance.

SQLite WAL swap guard (2026-09-08): `test-message-db-compaction.mjs` uses real WAL-only committed rows and a WAL created during compaction. A main-file replacement must be refused when the source WAL exists, both before copying and before swapping. Refusal preserves the original database and WAL; close/reopen and integrity checks retain the committed rows. A WAL belongs to its source page layout and must never be replayed onto a `VACUUM INTO` copy. This is prevention, not automatic recovery or permission to delete a customer's WAL.

Live verification follow-up (2026-09-07): `test-task-verification-shell.mjs` executes actual passing/failing POSIX shell programs to distinguish the inner test status from a successful echo. Literal fake status, comments swallowing the suffix, previous fake output, missing/truncated output and unfinished tools cannot verify. Other compound programs remain observations; nothing is replayed. `test-task-summary-localization.mjs` guards all three locale dictionaries and unknown/prototype-key fallback labels. Base/per-turn guidance requests independent final verification after deliverable edits. Renderer reload was checked on the real client; existing stored records are not retroactively upgraded.

Task completion integrity (2026-09-07): `test-task-completion-integrity.mjs` rejects label-only, failed, stale and incomplete acceptance evidence and preserves unresolved native todos in both task and durable lifecycle state. `test-task-completion-multistage.mjs` uses actual temporary files and process exits; file existence is only an observation, not semantic quality. `test-task-continuation-handoff.mjs`, `test-parent-task-closure-persistence.mjs` and `test-opencode-agent-session.mjs` protect one progress-qualified durable handoff after the shared budget, restart identity, duplicate suppression and user-input boundaries. `test-task-verification-shell.mjs` and `test-task-summary-localization.mjs` cover real shell receipt safety and three-locale status presentation. `test-task-original-acceptance.mjs` covers immutable requirements and typed file identity through the real MCP contract entry. `test-task-objective-coverage.mjs` rejects invented requirements/evidence and failed receipts and retains long original instructions; since 2026-09-22 it also holds the audit's own liveness: every end-of-turn audit resolves its connection through `resolveAuditConnection`, preferring the model the turn actually ran on and degrading to the active preset rather than losing the audit, and every "unknown" that means the audit could not run is logged and reported once per cause (`objective_coverage_unavailable`) — the benign reason list is closed, so a reason invented later is loud by default, while a judge that ruled and found a requirement unproven stays quiet. A structural scan asserts the audit reads only turn-state fields that something sets. `test-task-acceptance-recovery.mjs` exercises finalizer acceptance, both asynchronous stale-turn windows and a real SQLite close/reopen/recovery dispatch. `test-task-execution-progress.mjs` checks native reducer receipts and unchanged hard continuation caps. Unknown checks never block answer delivery. These are deterministic mechanism checks, not real-model complex-task success-rate acceptance. Added continuity/recovery guards: `test-task-request-continuity.mjs`, `test-task-execution-classification.mjs`, `test-task-source-resolution.mjs`, `test-parent-closure-lease-resume.mjs`, `test-parent-closure-live-failure.mjs`; see native task continuity evidence above. Long-task boundary guards: `test-task-long-progress.mjs`, `test-gateway-stream-completion.mjs`. Durable shared-budget guards: `test-task-continuation-budget-store.mjs`, `test-task-continuation-dispatch.mjs`, `test-task-continuity-integration.mjs`, `test-long-task-session-wakeup.mjs` enforce finite cross-round continuation, original task identity and cancellation. Closed-loop repairs (2026-09-15, from the 2026-09-14 toolhub field audit): `test-first-response-watchdog.mjs` — a model that returns ZERO bytes ends the turn after 90s (`LILY_OPENCODE_FIRST_RESPONSE_TIMEOUT_MS`, 0 disables) as retryable `MODEL_NO_RESPONSE` (Chinese copy, classified before the broad connection matcher, two silent fresh-engine rescue attempts) and marks the model silent in the picker; pending cards and active tools pause the fuse. `test-model-recovery-watch.mjs` — a model-silent failure is `ELIGIBLE_MODEL_SILENT` for parent closure but waits for a successful readiness ping (bounded ~15 min, `LILY_MODEL_RECOVERY_WATCH=0` disables) before the single continuation is dispatched; cut-off work with real execution behind it (stalled/handoff, ≥3 successful tool receipts) may now continue WHATEVER the request was classified as — including a turn with no task contract at all, because a bare follow-up such as "可以 继续做" produces none and a field case refused a turn that had run 120 successful tool calls (2026-09-16); a single tool call is still not a cut-off long task and a clean end is never re-entered (`test-task-execution-classification.mjs`). `test-step-budget-exhaustion.mjs` — 2026-09-16 there is NO primary step cap by default: the 160-step "runaway backstop" added on 2026-06-26 behaved as a wall (the engine's MAX_STEPS prompt disables tools and forces a summary), so a long task stopped in the same place every round. `agent.steps` is left unset for build/plan so the engine keeps its own Infinity, subagent fan-out keeps a cap, and `LILY_OPENCODE_MAX_STEPS` re-arms it — whereupon exhausting it is a named stalled stop with a `budget_exhausted` handoff that CONTINUES automatically whatever the task type (a full budget of tool calls is execution evidence), never a silent completion (`LILY_STEP_BUDGET_GUARD=0` restores baseline). The automatic-continuation round ceiling is likewise off by default (`LILY_CONTINUATION_MAX_ROUNDS` re-arms it): a chain is bounded by EVIDENCE — at least three new execution receipts per round and a 24h deadline — not by a count. `test-session-delete-cleanup.mjs` — deleting a conversation fences continuations and stops/cancels its durable process jobs (other sessions untouched, failures reported). `test-unfinished-tasks.mjs` — the unfinished list is the user's CURRENT unfinished work only: the latest turn of a conversation (a later message supersedes), never a completed-but-unverified answer, never a user-interrupted turn, within 48 h; resumable in the original conversation via `sourceTurnId`; the old row is annotated, never rewritten (field check 2026-09-15: 20 rows → 1). `test-task-center-unfinished.cjs` (Electron) — the task-center dock stays reachable for unfinished tasks without live items, resume goes through the facade with BUSY/ok toasts, non-resumable rows are disabled, the busy-send dialog recommends steer first, and the silent-model badge renders in zh/en/ar. Deep re-audit repairs (2026-09-15, P0–P2): `test-compaction-continuity-plugin.mjs` — after an in-turn auto-compaction the engine's compaction user message carried no `system`; the compaction-continuity plugin re-attaches Lily's per-turn guidance (memory, then the on-disk handoff) plus a bounded task anchor (original request, acceptance criteria, live todo list) once the summary exists, restores pruned `question`/`todowrite` outputs on copies, and feeds the anchor to the summarizer; Lily's bare continuation nudges now carry guidance too (`LILY_COMPACTION_CONTINUITY=0` disables). `test-parent-closure-notice.mjs` — `turn.parent_closure_recovery` / `turn.model_recovery` are legal runtime events that survive the source terminal and a restart (before: dropped in-process, thrown after restart so the restart lane never dispatched); a refused or budget-stopped continuation of a cut-off task leaves an idempotent durable assistant record the renderer commits; acceptance-gap re-dispatch of COMPLETED turns is opt-in (`LILY_ACCEPTANCE_GAP_CONTINUATION=1`; it produced 26 of 29 historical continuations in 3-minute loops); the parent-closure lane requires ≥3 new receipts per follow-up round (`LILY_CONTINUATION_MIN_PROGRESS`) while the process-job wake lane keeps its baseline; the step-budget copy only promises a continuation the gate agreed to. `test-parent-closure-work-state.mjs` — continuation prompts and the manual 继续 follow-up carry files written/edited, commands run, the todo list with statuses and the last output, persisted with the recovery row for the restart lane. Not claimed: real-model acceptance of these lanes. 压缩不得冒充回答 (2026-09-15)：`test-compaction-summary-not-answer.mjs` 钉住摘要消息（`summary:true` / `agent:compaction`）既不进聊天历史也不作为本轮输出，历史回收宁可判定「本轮无输出」也不采用它，已流式产出的真实回答不会被后到的摘要覆盖，减速器记录压缩窗口；`test-first-response-watchdog.mjs` 钉住首响应看门狗在压缩期间延长而非判定模型失联，压缩结束后重新获得完整窗口，真正沉默的模型仍按时结束；`test-status-scaffold.mjs` 钉住反引号/引号包裹的文件行属于脚手架正文，流式阶段只信任终结段边界且只按完整行判定。失败退化：任一判定失手都回到既有行为——终结器仍剥离脚手架且交付内容永不为零，看门狗读不到状态时按原逻辑计时。 Recovery ownership: `test-long-turn-recovery-ownership.mjs` and `test-long-turn-virtual-clock.mjs` guard live-engine status and stale asynchronous completion across progress and user waits. `test-opencode-terminal-ownership.mjs` guards normal completion ownership. 2026-09-23 补：压缩预算必须按模型**真实**上下文来量。`test-model-context-window.mjs` 钉住窗口不再只有"运营方手工配置"一个来源（真实机器实测 595 次决策中 573 次落到写死的 120000）——端点自己在模型清单里报的窗口不再被丢弃（实测中转返回 context_window: 272000），按 context_window/max_model_len/context_length 等各种拼写识别、按端点+模型记忆、荒谬值（单位混淆的过小值、解析事故的过大值）一律拒收、后续清单不报该字段也绝不抹掉已学到的值；优先级为 运营配置 > 端点观测 > 兜底默认，观测永不越过配置。这条错在**危险方向**：把 32K 模型当成 120K，触发阈值会定到模型根本吃不下的位置，压力永不触发、会话永不压缩，最后以"预算说不可能"的上下文溢出告终。同时钉住后台压缩不再用默认参数屏蔽更好的来源（此前 contextWindowTokens 与 tokenPressureThreshold 的形参默认值会盖过模型自报窗口和精确用量应得的更紧阈值），以及预算解析之后的每条决策都报告它所依据的预算——此前最常见的两个出口把算好的预算整个丢掉，导致仅凭回合数发起的压缩无从判断是否必要。 [gate: task-completion-integrity]

IM page unification (2026-09-07): `test-collaboration-page-polish.cjs` loads the actual renderer and center with controlled APIs. It guards channel-first/collapsed enterprise navigation, modal contact lookup/request/rejection, closed-dialog late lookup rejection, actionable request empty states, explicit group management, member-add focus, readable non-overlapping narrow drawer rows, visible close controls and standalone Back navigation. `test-collaboration-group-drawer.cjs` now asserts the approved stable member-row layout instead of the old hover-management avatar grid. Independent windows switch to one pane below the existing 760px threshold; messaging, permissions and durable commands are unchanged. These checks do not claim packaged-client or production two-machine acceptance.

IM creation dialog (2026-09-07): `test-collaboration-create-dialog.cjs` uses actual Electron DOM/CSS to guard native modal creation, 15 selected people, light/dark/narrow bounds, visible submit controls, background-refresh draft and keyboard focus preservation, and canceled group/channel late-result fencing. Closing a dialog dismisses navigation, not a durable server operation. `test-collaboration-social-ui.cjs` retains permission/revocation and pending-command recovery coverage. These are controlled local-source tests, not packaged Mac or two-device acceptance.

IM detail navigation (2026-09-07): `test-collaboration-detail-navigation.cjs` exercises real Electron detail/drawer ownership: explicit request opening, in-place refresh, Back then directory/search refresh, section departure, unrelated detail preservation, delayed group-detail replies after close, and replacement by a newer owner. `test-collaboration-online-presence-center.cjs` also checks Back/section/locale refresh through the real center. Rendering must not open a closed request view; surface closure invalidates its owner's pending navigation, independently of durable operations. These are local source UI tests, not packaged Mac acceptance.

IM enterprise navigation (2026-09-07): `test-collaboration-online-presence-center.cjs` reproduces background-sync navigation theft, late history completion after changing sections, Back followed by refresh, and direct conversations mixed into enterprise channels. Section/Back navigation invalidates pending opens; background history has no authority to reopen the chat pane. Explicit chat navigation, drafts, authorized history refresh and channel access remain available. This is real Electron with controlled service responses, not packaged-client acceptance.

IM online presence (2026-09-07): `test-collaboration-online-status.mjs`, `test-collaboration-online-presence-{ipc,service,socket}.mjs` guard account/directory/socket fences, clock-skew-safe leases, hung-poll expiry, failure-to-unknown, bounded per-window declarations, signed tickets and the independent presence lane. `test-collaboration-online-presence-{ui,center}.cjs` exercise actual Electron friend/Team/direct-header consistency, typing priority, first-frame logout, stable composer/member nodes, unknown-page navigation and three locales. `test-collaboration-online-presence-native.cjs` uses Electron's production native WebSocket constructor with a local signed ticket HTTP fixture and the real gateway: heartbeat (accelerated test clock), hints, fresh-ticket reconnect and close. Presence never persists in directory snapshots or blocks durable messaging. Native WebSocket does not inherit Electron HTTP's system-proxy integration; failure retains HTTP sync and unknown status. These are local-source tests, not installed Windows or production two-device acceptance.

Remote task live acceptance (2026-09-07): `docs/remote-task-live-acceptance.md` records real production/private-object lifecycle and same-host full-source clients separately from installed/physical devices. `test-live-electron-device-isolation.mjs` prevents local acceptance profiles from sharing the hardware-derived device ID or fingerprint; only the optional macOS acceptance host virtualizes its hardware probe, never production licensing. `test-account-menu-login-refresh.mjs` guards authoritative login/logout footer updates and late-response fencing. `test-account-login-config-concurrency.mjs`, `test-remote-config-account-fence.mjs`, and `test-password-login-config-refresh.mjs` prevent anonymous/previous-account config from defeating account-scoped rollout. Ordinary-user rollout remains off pending release acceptance.

Remote task foundation: `test-remote-task-contract.mjs`, `test-remote-task-service.mjs`, `test-remote-task-routes.mjs` guard role/revision/delivery binding, encrypted task projections, private event recipients, receipt rollback and disabled-feature fallback. `server/scripts/collaboration-tasks-integration.mjs` verifies real PostgreSQL task/object migration, concurrent receipt replay, CAS and task-only object ACL with seeded verified uploads. Task feature remains default-off pending installed two-client/private-bucket acceptance. Existing message/object authorization and encryption tests remain required.

Remote task desktop/UI guard: `test-remote-task-desktop.mjs`, `test-remote-task-ipc.mjs`, `test-remote-task-policy.mjs` verify encrypted SQLite intent recovery, stable command/device identity, conversation-to-task binding, scoped cleanup, strict renderer vocabulary and default-off signed rollout. `test-remote-task-ui.cjs` uses real Electron with controlled API results: role/version-bound decisions, unknown-result recovery, navigation/locale failure fencing, preserved feedback focus and light/dark/narrow layouts. Reads do not block ordinary IM navigation. Task acceptance never implies local application or starts an engine. Creation/material import, delivery-file access, local application and real two-client acceptance remain unreleased; a task-management surface is not end-to-end collaboration completion.

Remote task file workflow guard (2026-09-07): `test-remote-task-workflow.mjs`, `test-remote-task-records.mjs`, `test-remote-task-bundle.mjs`, `test-remote-task-application.mjs`, `test-remote-task-recovery.mjs`, `test-remote-task-transfer.mjs`, `test-remote-task-session.mjs` exercise real account-isolated SQLite, immutable ZIP/hash binding, editable independent copies, safe file application, personal encrypted crash recovery after Team revocation, idle session registration and actual encryption/decryption through fixture object HTTP. `test-remote-task-workflow-ui.cjs` validates renderer commands through production IPC projections. Task control files are not imported as agent authority; acceptance does not execute scripts, install dependencies or apply files. Read-only recovery discovery must not emit a change hint/reload loop. Independent owned-file rollback remains available if task rollout is disabled, and cannot authorize remote downloads. These tests are not a claim of real private-bucket/two-installed-client or Windows power-loss acceptance.

IM cached navigation guard: `test-collaboration-fast-open.mjs` verifies account-scoped local-preview eligibility, revoked scopes/conversations and pending hydration rejection, obsolete queued HTTP skipping and recovery after failure. `test-collaboration-social-navigation.cjs` verifies real renderer preview before a delayed network response and preserved navigation/draft ownership. `test-collaboration-ipc.mjs`, `test-collaboration-client-service.mjs` and `test-collaboration-scope-revocation.mjs` retain the preload/service/account boundaries. Local preview never hydrates or acknowledges history; authoritative network writes remain on the existing durable lane. This is controlled delayed-network validation, not a production latency measurement.

This is a HARD GATE for every change in this platform. A change that cannot pass it
does not ship. Its purpose: never let the product get *dumber* than it is today.

## The one rule

> **No change ships if it can make the product worse than today. Every feature's
> FAILURE MODE must degrade to today's behavior — fail open / fall back to the
> strong default — never to a worse state.**

Why this is the right test: a feature can only make the product dumber if, *when it
goes wrong*, the result is worse than not having the feature. If its failure mode
equals today's behavior, the worst case is "no improvement," which is acceptable.
So for every change, ask: **when this breaks, is it worse than today, or the same?**
Only "the same (or better)" passes.

## Gate checklist (every capability-affecting change must pass — a "no" blocks merge)

Applies to anything that changes how the agent answers, routes, executes, learns, or
remembers.

1. **Failure degrades to baseline.** When this errors / its input is missing /
   malformed / oversized / forged → does it fall back to today's behavior? Ship a
   test that proves the fallback. No silent break, no worse-than-today.
2. **No silent capability downgrade.** If it routes to a weaker model, fewer tools,
   or less context, there must be a guaranteed fallback to the strong default, and
   reasoning / critical work must never be downgraded.
3. **Bound non-progress, not effort.** Any stop / limit / timeout fires only on
   CONFIRMED no-progress (byte-identical repeat, unchanged output) — never on
   legitimate long or evolving work.
4. **Graceful, never silent-kill.** On a limit or error: recover partial output,
   nudge the model, or ask the user. Never silently abort or hand off to ad-hoc
   improvisation.
5. **No improvisation fallthrough.** When a structured path (learned flow, parsed
   intent, learned API contract) fails, surface it or ask — never silently let the
   model wing it with ad-hoc scripts/plans.
6. **Persona / context / memory invariants hold.** The workbench persona is not
   overridden by the engine's coding-CLI baseline; the per-turn system prompt stays
   within budget; long-session compaction still works.
7. **Closed-loop guard.** Add an automated test that FAILS if this capability
   regresses, and register it below.

## Known 变笨 vectors → their guard (the registry)

Every recurring "got dumber" cause must stay guarded. When you touch one of these,
the guard test must still pass; when you add a new capability, add a row.

Mention-candidate companion guard: `test-collaboration-mention-candidates.mjs` keeps authorized candidates separate from management members, preserves legacy unknown, rejects malformed/oversized projections and exercises the narrow IPC boundary. Only candidate hints are cached; management details remain fresh. Account, authorization and directory changes fence cached and delayed responses, while ordinary draft writes do not starve typing. `server/scripts/collaboration-conversations-http-integration.mjs` covers real signed public/private/direct/group recipient sets, missing profiles, the 1000/1001 boundary, disabled membership and desktop IPC cache reads; keyboard interaction is exercised by the explicit-reminder guards below. Full two-client acceptance remains separate.

| Vector (how it got dumber) | Guard (test / mechanism) |
|---|---|
| Skill declarations reach preparation only for explicit task selections without turning every enabled or recommended skill into heavy downloads. | `test-skill-runtime-declarations.mjs` + `test-selected-skill-readiness.mjs` — The hardcoded skill map and existing task readiness remain the baseline. [gate: skill-runtime-declarations] |
| Workspace guides stay inside the workspace, obey explicit conversation selection and never displace platform context. | `test-workspace-local-skills.mjs` — Workspace skill directories were not discovered; platform guide and learned sections must remain intact. [gate: workspace-local-skills] |
| Same-workspace conversations cannot regain a disabled skill through a shared native registry or stale local guide. | `test-native-skill-registry-isolation.mjs` — Native skill paths stay empty; explicit conversation skills are authoritative. [gate: native-skill-registry-isolation] |
| Long descriptions retain bounded routing exclusions without removing any previously discoverable skill. | `test-skill-index-negative-routing.mjs` + `test-skill-description-quality.mjs` — The original 180-character description index is preserved if expansion cannot fit. [gate: skill-index-negative-routing] |
| Real missing dependencies get actionable managed recovery without misclassifying input files, missing cwd, printed examples or inherited catalog keys. | `test-runtime-dependency-hint.mjs` — Failed bash output is preserved and the model can continue using existing tools. [gate: runtime-dependency-hint] |
| A started background pack job is not reported ready; terminal failures remain observable and retries use freshly resolved managed environment. | `test-runtime-pack-rescue-tools.mjs` — Existing background installer and artifact integrity/health checks remain authoritative. [gate: runtime-pack-rescue-tools] |
| Advisory candidate matching cannot masquerade as model selection, successful reads or completed tasks. | `test-skill-usage-metrics.mjs` — Legacy audits contain path attempts but no reliable outcome; those observations stay unknown. [gate: skill-usage-metrics] |
| Discovery evals require actual successful guide-read tool events rather than text merely naming a skill. | `test-guide-eval-evidence.mjs` — Answer-only discovery cannot prove the guide was used. [gate: guide-eval-read-evidence] |
| A typed name or untrusted markup fabricates a reminder identity, a delayed candidate leaks across conversations, or an old send clears newer reminder tags | `test-collaboration-mentions-ui.cjs` + `test-collaboration-mention-navigation.cjs` exercise real renderer keyboard, candidate, full-draft intent and lifecycle behavior using controlled preload responses; `test-collaboration-mentions-locales.mjs` guards safe three-language text and logical styling. Explicit stable IDs, not body parsing, determine reminders. Unknown or failed candidate reads preserve existing IDs; selection is limited to the current authorized candidate projection. Sixteen native-button default-activation cases with annotated trusted IME/229 keydowns prevent accidental selection/removal; normal Enter/Space remain usable. This is not full OS IME acceptance. `server/scripts/collaboration-unread-integration.mjs` verifies signed candidate IDs, plain @ text versus explicit reminders, same-key replay and the production bootstrap/authorized-hydration/SQLite path, including reminder identities persisted before ACK. Real two-client acceptance remains separate. |
| Reply UI rebuilds immutable quotes from edited source text, clears newer same-body draft metadata, or a delayed/detached action crosses conversation or account boundaries | `test-collaboration-reply-ui.cjs` + `test-collaboration-reply-navigation.cjs` execute actual Electron DOM with controlled preload responses; `test-collaboration-reply-locales.mjs` guards the three-language surface. Quotes are read-model display data, while drafts and sends retain only reply IDs and explicit mention IDs. Real PostgreSQL barriers in `server/scripts/collaboration-reply-snapshot-integration.mjs` verify both orders of capture versus edit/revoke, without substituting sequential calls for concurrency. These do not replace real two-user/client or object-bucket acceptance. |
| A contradictory creation receipt confirms delivery or releases a conversation barrier, while strict validation strands valid legacy commands | `test-collaboration-create-receipt.mjs` + `test-collaboration-receipt-view.mjs` — production receipt validation binds type, conversation, IDs, creation revision and matching event sequences; malformed evidence never authorizes replay or cancellation. Explicit unknown retains original-key recovery. Server views use the verified immutable event lineage, derive legacy creation revision one without consulting current edited/revoked projections, and reject conflicting stored metadata. Signed PostgreSQL-to-desktop restart coverage lives in `server/scripts/collaboration-receipt-integration.mjs`. |
| Coding-CLI persona overrides the workbench persona (default.txt) | `test-opencode-config-builder.mjs` — basePrompt suppresses the coding baseline for build/plan |
| Legacy engine guide mirror reappears and splits the source of truth, or guide migration drops user-authored content | `test-skill-catalog.mjs` + `test-data-migration.mjs` — AGENT.md is canonical; guide generation must not create `CLAUDE.md`; startup migrates/removes stale legacy files while preserving distinct content; later guide refreshes only replace the Lily-managed block |
| Per-turn system prompt bloat | `test-context-budget-manager.mjs` — token-budget estimation + compaction trigger; `test-agent-guide-i18n.mjs` — static `AGENT_GUIDE_MAX_BYTES` assertion for generated AGENT guide base text, all local skill-index entries, and provider contract injection |
| Native compaction 500 → long-session memory lost | `test-opencode-config-builder.mjs` — compaction/title pinned to the resolvable main model; `test-context-budget-manager.mjs`; `test-opencode-sdk-session.mjs` — SDK summarize uses top-level `sessionID`, not generated-client route placeholders |
| Subagent infinite nesting (套娃) | `test-subagent-telemetry.mjs` — `nestedTaskBreaches` detector; isolation prompt aligned to depth-1 cap |
| Subagents fall back to a generic coding baseline and skip Lily's evidence discipline | `test-opencode-config-builder.mjs` + `test-skill-catalog.mjs` — shared subagent config injects Lily Subagent Rules; subagents must return evidence-backed summaries and must not spawn nested Task subagents |
| Persisting a runtime event bounds its SIZE but erases its MEANING: the compaction depth cap replaced objects with the literal `"[object]"`, so every stored question event kept `options: ["[object]","[object]","[object]"]` and replay lost the choices the user was actually offered | `test-runtime-event-compaction-index.mjs` — at the depth cap `compactValue` keeps the object's SCALAR LEAVES (bounded: ≤8 keys, strings ≤300 chars, thumbnail/base64 keys still stripped) instead of erasing it, so labels and descriptions survive. The cap still caps: nesting BELOW a kept leaf is dropped, not expanded, and an object with nothing legible to keep still yields the old `"[object]"` marker [gate: persisted-event-fidelity] |
| Time the turn spent BLOCKED ON THE USER is reported as the assistant's own latency, and the progress heartbeat claims to be working while it is actually waiting — a question card answered after 8m27s rendered as "已完成 · 507.8s" while the panel repeated "question 正在运行 · 已运行 8m 29s · 最近活动 8m 23s 前", which reads as a hang | `turn-user-wait.js` + `test-turn-user-wait.mjs` + `test-turn-timeline.mjs` — the turn state machine already knows (`phase = "awaiting_user"` + the pending permission/question maps), so wait time is ATTRIBUTED, not guessed: one open interval per turn (concurrent cards share it, so no double count), credited to each suspended tool as `waitMs` bounded by the overlap with its own lifetime, in BOTH card-resolved-first and tool-done-first event orders. The renderer subtracts `waitMs` and labels it (`timeline.toolAwaitingYou`, three locales) instead of billing it as compute; the liveness heartbeat emits a distinct `awaitingUser` notice naming the true state, sharing one replace slot with `toolProgress` so they swap rather than stack. Covers the permission-card case too (2026-07-22: an `rm -rf` card showed 1200s of "tool latency"). FAIL-SAFE: a missed hook, absent/negative/oversized `waitMs`, or a missing `translate` all degrade to today's plain elapsed time, and wait time is never shown as compute [gate: user-wait-attribution] |
| The engine retries a transient provider failure and the user sees only unexplained silence: `opencode serve` logs `message="stream error" … error.error="AI_APICallError: Server Overloaded"` to stderr, the host wrote it to the log file ONLY, and a 4s/7s backoff read as a hang (the classification in `agent-runner.js` only fires once the error becomes a terminal failure, so retries that succeed narrated nothing) | `opencode-serve-diagnostics.js` + `test-serve-diagnostics.mjs` — the serve's logfmt stderr is parsed and re-published on a dedicated `diagnostic` channel (a diagnostic is not turn content, so it never travels with turn events), routed by `diagnosticBelongsToSession` on an EXACT session-id match because one shared serve hosts many sessions, and narrated by the liveness heartbeat as `engineRetry` naming the model service, the upstream reason and the attempt count. It shares the heartbeat replace slot so real progress clears it instead of leaving a stale "retrying" line. NARROW BY DESIGN: only the transient family (overload/rate-limit/quota/5xx/timeout/socket) is softened into "retrying" — a genuine config error (bad key, unknown model) still flows to normal terminal-failure classification. FAIL-SAFE: an unparseable or chunk-split line, a non-ERROR level, or a missing session id yields nothing and stays log-only, exactly as before [gate: engine-retry-visibility] |
| The ask path pads every turn with payload-free prompt layers and misroutes ordinary questions into an extra model call: an empty context layer shipped as its own preamble (~250 chars/turn of noise, and `extracted_attachments` told the model "here is platform-extracted attachment content, treat it as evidence" on turns with NO attachment), while bare `schedule`/`daily`/`weekly`/`提醒我` matched scheduled-task intent so questions like "schedule 这个词在代码里怎么用的" paid a blocking `parseDraftSmart()` model call before any answer | `test-engine-message-layers.mjs` + `test-scheduled-tasks.mjs` — a CONTEXT layer with no payload is not emitted at all (a 6-char question went from a 759-char envelope to 187), while every payload-bearing layer still ships unchanged; `user_original_request` is exempt on purpose because it anchors later merges (`userOriginalLayerIndex`) and is what `extractUserOriginalRequest` reads back, so an attachment-only message with no typed text still carries it. Scheduled-task intent now needs either unambiguous creation phrasing (`定时任务`, `创建定时…`) or a weak keyword TOGETHER WITH a clock time / explicit interval / weekday — text a schedule could actually be built from. Both directions are pinned: real scheduled-task phrasings still match, and the four reproducible false positives no longer do. FAIL-SAFE: a miss routes the message down the ordinary agent path instead of the scheduler [gate: ask-path-hygiene] |
| Image questions pay one serial model round trip PER image and the answering model is never told it did not actually see the picture: the bridge looped images one at a time (measured 30s average, 71s worst, 24s of it before the turn even started) and its descriptions were handed over as if first-hand, so follow-ups about undescribed detail were confidently invented | `src/main/vision-bridge-runner.js` + `test-vision-translator.mjs` + `test-send-preflight.mjs` — per-image bridge calls run concurrently under a cap (default 3, `VISION_CONCURRENCY`, hard max 6, because the same gateway also answers "Server Overloaded" under load), results are keyed BY INDEX so the evidence order the model sees is identical to the serial version, and one image failing is isolated rather than fatal. The extracted layer states plainly that the answering model did NOT see the images and must say so rather than guess about omitted detail, and the progress notice names the real reason (the active model cannot read images), the cost, and the way out. Images are still stripped from the outbound files because a non-vision model rejects image parts outright (`AI_UnsupportedFunctionalityError` pre-gateway); the originals remain in the turn record via `displayFiles`. FAIL-SAFE: a blank recognition counts as failed, a bad concurrency value clamps to one working lane, and a bridge failure keeps the original image for a native-vision retry [gate: image-question-path] |
| A user who PINNED a text-only model is silently forced through the vision bridge on every image question — auto selection already prefers a vision-capable model for image turns (keeping the reasoning baseline first), but a manual selection returns early before any of that, so those users got ~30s of transcription with no explanation and no way out | `vision-model-advice.js` + `test-vision-model-advice.mjs` + `test-send-preflight.mjs` — the bridge notice now names the pinned model that cannot read images, the cost, and the vision-capable models the user ACTUALLY HAS (capped, active model excluded, "等 N 个" for the rest). CRITICAL INVARIANT, pinned by the test: this path ADVISES, it never switches. A manual pick is an explicit user choice and a vision-capable model can be weaker at reasoning, so silently rerouting would both override the user and risk a capability downgrade; the module exports no switch/select/apply surface and must never call `setModelSelectionPreference`/`writeStoredSelection`. FAIL-OPEN: no catalog, no alternatives, already on the only vision model, a malformed option, or any lookup error drops the clause entirely rather than promising a model the user does not have [gate: pinned-model-vision-advice] |
| The IM has no live signals — no "typing", no read receipt, no reactions — and a future attempt re-implements the transport because the gap looks like a server gap. It was NOT: the realtime gateway already validated and fanned out `typing`/`presence` frames, `conversation.read` already carried `recipientUserIds`, and the desktop already held the websocket. Only the client ends were missing, and a reaction genuinely needed a new server event | `test-collaboration-typing.mjs` + `test-collaboration-peer-reads.mjs` + `test-collaboration-reactions.mjs` — typing is a HINT: a self-expiring registry with NO timers (pruned on read, so a crashed peer stops showing as typing on its own), bounded per conversation and per user against a hostile peer, a far-future `expiresAt` clamped to the 30s cap, and a missed frame degrading to "nobody is typing". Read receipts are a claim about OTHER people, so the double tick means read by EVERY known peer (the slowest holds the watermark, a group never claims read-by-all from its fastest member), the watermark is monotonic against replayed sync pages, and UNKNOWN membership yields no tick rather than an optimistic one. Reactions are one row per (message, user, emoji) on both sides so a toggle is an insert/delete two devices cannot clobber, they carry NO `expectedRevision` and never bump a message revision (or every reaction would read as an edit to reply snapshots and edit/revoke conflict detection), the commit evidence is the event id plus the echoed (message, emoji, direction) rather than a borrowed message projection, and the chip tie-break is by CODE POINT so the order is identical in every locale. Emoji are bounded but never allowlisted, so new emoji need no deploy on either side [gate: im-live-signals] |
| The IM panel measures as "cheap" and nobody can point at why. It was measurable all along: an audit that rendered the real panel in BOTH themes found metadata at 9.5px (timestamps, delivery ticks), six element classes under 11px, 38 targets under 24px including reaction chips at 39x22 — and four literal `#fff` colours that only happen to pass contrast because both themes ship the same accent | `test-collaboration-legibility.mjs` — reads DECLARED values out of the stylesheet rather than rendering, so the floor holds for panel states no fixture builds. Readable text >=11px and interactive `min-height`/`min-inline-size` >=24px; numeric-only badges are exempt by NAME with a stated reason (a count in a pill is glanceable at 10px), and an exemption whose selector has disappeared FAILS, so the list cannot rot into a bypass. Colours must be tokens: the literal-`#fff` case is why — `--accent-on` now names the one contrast colour, so changing `--accent` cannot strand white text on a light fill. Mutation-checked against all four regressions it claims to catch [gate: im-legibility-floor] |
| A message attachment rendered as N identical buttons labelled "Download" — no name, no size, no thumbnail — and the cost of fixing it was mis-scoped twice before being measured: the transfers table was said to lack filename/MIME (it has them, added after v1), and the metadata was going to be put in the sender's encrypted payload (which would never reach a recipient, because `hydrateAuthorizedHistory` persists the SERVER's authorized plaintext view). The server already stored `original_name`/`mime_type` from object init; only the history projection omitted them | `test-collaboration-attachment-metadata.mjs` + `test-collaboration-timeline.cjs` — `attachmentIds` addresses and AUTHORIZES a download, `attachments` only DESCRIBES, and the whole gate is that the second can never affect the first: the join is a LEFT join (an inner join silently shortens the id list when an object row is gone), metadata is re-derived at the IPC boundary against the ids actually projected rather than trusted from the record, an id the authoritative list omits cannot be introduced, a path-bearing filename is DROPPED rather than sanitized (it would become a save-dialog default), and a revoked message withholds metadata on both sides — pinned by handing each layer a revoked message WITH ids, because the ordinary callers blank the list first and that path proves nothing. Thumbnails are attempted for image types only and only from a FINISHED download, so the sender and the not-yet-downloaded case keep the drawn glyph and the download action. Icons are inline SVG, not emoji: U+1F5BB rendered as a thin bar and coverage is worse on Windows [gate: im-attachment-metadata] |
| Contacts and Teams were rebuilt after being rendered and measured, and the numbers are the record: a contact occupied ~220px because `.collaboration-social-row` had NO `display` — its own children were already written for a flex row (`flex: 1` content, `flex: 0 0 auto` actions with an opacity transition), so the intent was always a row and only the parent was missing; every contact carried three always-visible text buttons, two destructive, at the same weight as "open chat"; the view had grown its OWN search box, placed below the list it filtered, because the panel's search box was hidden outside the inbox; and `team:t_design` was printed twice per team. Two existing tests had to be INVERTED because they asserted the raw scope id was shown | `test-collaboration-address-book.mjs` + `test-collaboration-social-ui.cjs` + `test-collaboration-social-navigation.cjs` — rows are 56px with the row body as the primary action, destructive actions are icon-only and hover/focus-revealed (visible under `hover: none`, where there is no hover to reveal them with), one search box in the header is retargeted at whichever list is on screen, pending requests sit behind a counted "new friends" entry, and the roster moved off the team row into the header's detail view because listing every member inline buries the channels people came to open. A–Z sectioning uses Chromium's ICU pinyin collation rather than a bundled table, so characters no hand table would cover still section correctly; it sorts by (letter, then name) because a raw pinyin sort strands every Latin name after every Han name. Mutation-checked against all six regressions, including the two sectioning ones [gate: im-address-book] |
| The three destinations sat in a header `<details>` popover behind a hamburger — three places you switch between constantly, with the current one invisible until the menu was open — and every conversation's avatar was the first character of its TITLE, so a workspace full of "设计…" and "周会…" had identical tiles. The group roster needed for a composed tile turned out to be ALREADY LOCAL (`conversation_members`, with `listConversationMembers` already in use), so no server change was involved; only the list projection omitted it | `test-collaboration-rail-and-mosaic.mjs` + `scripts/test-collaboration-shell-surface.mjs` — the rail reuses the popover's exact tab ids and `aria-controls`, so navigation is untouched, and it is icon-only with `data-i18n-aria-label`/`data-i18n-title` because `applyDomI18n` sets textContent and `data-i18n` would DELETE the `<svg>`. The rail's CSS uses the compound selector `.collaboration-nav.collaboration-rail` on purpose: `.collaboration-nav` sets `display: grid` and is declared later in the sheet, and a single-class rule lost on source order — which rendered the three icons side by side over the list. The roster is a drawing input only: capped at 9 (more cannot be told apart in a 40px tile), skipped row by row when malformed, returning empty rather than throwing when the read fails, re-validated and re-bounded at the IPC boundary, and falling back to the single-initial tile below two resolved names so it can only improve on the old tile. A direct chat is excluded by kind. Mosaic cells sit at 9px, which is a named exemption in the legibility gate because the tile is `aria-hidden` and the name is on the row. Mutation-checked against all six regressions [gate: im-rail-and-group-tiles] |
| Creating a group was measured against what chat clients do and failed on three counts, two of them functional: the member control was a `<select multiple>` at 361x96 with no search, no avatars and no indication of who was chosen (modifier-click to pick more than one); the group NAME was the first field and REQUIRED, so a group had to be named before anyone was chosen; and a group could be created with NOBODY in it. Checking the server first showed both of the last two were purely client choices — it accepts an empty title (`(raw.title || "").trim()`, capped at 200) and imposes no minimum member count | `test-collaboration-member-picker.mjs` + `scripts/test-collaboration-social-ui.cjs` — members first, name last and optional, submit reading "Done (N)" and disabled until someone is picked. The rule is enforced in the submit HANDLER as well as on the button, because a disabled button is bypassed by `requestSubmit()`. Selection lives in a Set of ids rather than in the DOM, which is what makes it survive typing in the search box (a naive rebuild drops whoever is filtered out) and what lets a roster refresh drop only people who are no longer selectable; ticking a box repaints the strip but NOT the list, or the row moves out from under the pointer mid-click. A blank name derives from the members' names WITHOUT `t()`: it returns the key when no locale has loaded, so the group would have been named "collaboration.social.groupTitleMore" — the marker is "+N", which reads the same in every locale, and the result is cut to the server's cap. The pick row restates `display: flex` because `.collaboration-social-form label` sets `display: grid` and, being class+element, outranked the row rule and made it 77px. The channel form and the add-member form now use the same picker: a channel has no minimum (it may start with only its creator) and a PUBLIC channel HIDES the picker, because its membership is the whole team and a greyed list of names implies the choice still matters; adding a member is single-select, since `conversation.member` takes one target per call and a partly applied batch has no story here. Unfinished channel forms survive a background sync — and repairing that surfaced a regression from the nav-rail commit: the team header button had gained `data-team-id`, so a bare `[data-team-id]` query matched two nodes per team, the field-less button won the drafts Map, and every sync silently wiped the typed channel title. Both queries are now scoped to `section.collaboration-team[data-team-id]`. The picker's own selection is snapshotted separately from the text fields, and no picker input carries a `name`, so a name-matched restore can never write a draft value onto a checkbox. `test-collaboration-center-lifecycle.mjs` was ALSO dead: its hand-rolled DOM stub predated the panel shell's window listeners, so it threw before reaching any assertion. With the stub completed it reaches its assertions, and the one that then failed was a stale expectation rather than a defect — the composer is deliberately gated on the policy being KNOWN, so the notification path (taken before any panel open) leaves it inert; the test now loads the policy first, as a real session does [gate: im-group-creation] |
| Adding a contact sent the request BLIND: you typed an exact Lily ID, submitted, and learned from a generic "operation failed" whether that person even existed. The server had a rate-limited `lookupLilyId` all along — it simply had no HTTP route, so the client could not reach it. The server also accepts a greeting with the request (`message`, <=500 chars) that the client never sent, and your own Lily ID, the only handle by which anyone can add you, was plain text with no way to copy it. Phone-number lookup is NOT part of this and must not be added casually: migration 032 states the rule — "Contacts discover a Lily ID; phone/email/login credentials never become collaboration profile fields or search keys" — and phone numbers exist in this system only as wallet/login credentials | `test-collaboration-add-contact.mjs` — the lookup is a READ, not a command: `deviceBody` rather than `commandBody`, no clientCommandId, and it does not run through `ui.run`, so it leaves no outbox row, receipt or retry path behind. The load-bearing constraint is that a lookup may report AVAILABLE or NOT and NEVER why: the server answers identically for "no such id", "that is you", "discoverability is hidden" and "either side blocked the other", because distinguishing them would let anyone probe which Lily IDs exist — so the failure copy is checked in all three locales for words that would name a cause. A malformed id is refused locally so it cannot spend a rate-limit slot; an empty or partial profile is unavailable rather than a half-rendered person; a switched account aborts instead of answering; a superseded lookup cannot paint over a newer one; and editing the id invalidates the confirmation so the request can never be addressed to someone other than the person shown. Only userId/lilyId/displayName cross the IPC boundary — never `discoverability`. Mutation-checked against all seven regressions [gate: im-add-contact] |
| A conversation containing the single word "你好" showed a HORIZONTAL SCROLLBAR. `.collaboration-message-actions` — the hover-revealed reply/react overlay — is absolutely positioned and was anchored to the OUTER edge of its row (`is-outgoing` set `inset-inline-start: 0`, and an own row sits at the right), so it ran 52px past the thread; an absolutely positioned box still contributes scrollable overflow. Removing it took scrollWidth from 511 to exactly clientWidth 459, which is what proved it was the sole cause | `test-collaboration-thread-overflow.cjs` — measured in a real 460px column with the real stylesheets, because this defect exists only in CSS geometry and a bare DOM cannot see it. Anchoring is now to the side of the row that HAS room (incoming grows right, outgoing grows left) and BOTH directions are checked, because anchoring either way is wrong for one of them. Fixing the anchor was not enough: with reply + edit + revoke + five quick reactions the overlay measured 607px inside a 459px thread, so no anchoring could contain it — `flex-wrap` is the load-bearing fix, since an absolutely positioned box shrink-to-fits inside its containing row once its content may wrap. A `max-inline-size` capped to the panel width was tried and REMOVED: a mutation showed it changes nothing, because the row already bounds the width. Also pinned: a 300-character unbroken URL wraps rather than widening the thread [gate: im-thread-no-sideways-scroll] |
| "I can't tell what's what" — and a screenshot of the running app showed why: ONE scrolling column held the panel title ("协作中心"), the list's own title ("团队"), a create action, a "个人" section of groups, a "团队" section with its empty state, AND the member roster of a conversation somebody had tapped. Two titles for one place, and browse stacked on top of detail. The panel already had the right shape — opening a conversation swaps the home view for one with its own header and a back button — but rosters and pending requests appended into the list they came from instead | `test-collaboration-one-screen.mjs` — one title, in the panel header, following the active destination (the rail is icon-only, so if the header stopped naming it nothing would); the list's second heading is gone and its absence is asserted. A detail view sits BESIDE the list column, never within it, and the guard checks the nesting rather than just its existence, because being inside is the whole defect. Opening a detail hides the list, so only one of the two is ever on screen, and changing destination closes it — a roster from one list cannot be left over on another. The detail surface is optional and each view falls back to the inline container it always used, which is how these modules render standalone in the DOM tests. Mutation-checked against all six regressions Two follow-ups came straight from a screenshot of the running app: the contacts screen showed TWO columns and its first row read "未知用户 / 暂不可用". The second column was the detail view — its `hidden` attribute WAS set, but `.collaboration-detail { display: flex }` outranks the UA's `[hidden] { display: none }`, so it still took 192px; the stylesheet already carried that guard for the other views and the new one was added without joining it. And "unknown user" is `identityName`'s fallback, fair for someone else and nonsense for yourself, appearing because a fresh account has no profile yet — there is now no identity row until there is an identity. `scripts/test-collaboration-hidden-views.cjs` MEASURES the rendered box of every toggleable view, shown and then hidden, rather than grepping for a `[hidden]` rule, because the box is the property that matters and a rule that does not apply cannot satisfy it [gate: im-one-screen-one-thing] |
| The collaboration stylesheet grew through a dozen passes, each appending a new block for a selector it had already styled. 82 declarations could never take effect, and a reader could not tell which value was live — which is not cosmetic: BOTH CSS bugs in this session came from that layering (the nav rail's three icons rendering side by side because `.collaboration-nav`'s `display: grid` is declared later, and a member-pick row measuring 77px because `.collaboration-social-form label` outranks a single-class row rule) | `test-collaboration-css-no-dead-rules.mjs` — dead means the SAME selector text declares the SAME property again at TOP LEVEL later, with neither `!important`: identical selector text is an identical match set, so the later declaration always wins and the earlier one provably cannot render. @media rules are excluded on both sides because one of those does not unconditionally override anything, and shorthands are compared as written rather than expanded. The removal was verified by a rendered FINGERPRINT — the computed style of every element in the panel across eight states and both themes — which came back byte-identical; a first attempt with a hand-rolled selector scanner mis-grouped a multi-line selector, deleted a live rule, and the fingerprint caught it, so the second attempt took its selector identity from Chromium's own CSSOM and validated the text block boundaries 1:1 against it. The duplicate-selector count is ratcheted at its measured post-cleanup value (28) rather than a round number with slack in it [gate: im-css-no-dead-rules] |
| "Is the IM window still too small — can't we replicate WeChat?" The answer was a cap, not a tuning value: `MAX_WIDTH = 560` in the panel shell, so the desktop-chat shape (rail, list and thread all on screen) was not reachable by dragging AT ALL, and a conversation always replaced the list. WeChat's layout assumes list and thread side by side, which needs roughly 760px | `test-collaboration-two-pane.cjs` + `scripts/test-collaboration-hidden-views.cjs` — at or above 760px the panel lays out list beside thread, keeps the list visible so the selected row holds its context, and hides the back button because there is nothing to go back from; below it, one column and a conversation that replaces the list, unchanged. Two panes are DOCKED-only, since an overlay panel covers the workbench and splitting it there leaves both halves narrow. The width ceiling reserves space for the workbench, and that reserve is checked in a window narrow enough for it to BIND — an earlier version of the check passed with the reserve deleted, because at 1800px the absolute cap was the smaller of the two. Crossing the threshold either way re-lays out an open conversation. The list keeps a fixed 380px so widening grows the thread rather than stretching the rows. Fixing this also exposed that hiding the back button did nothing: almost every component here sets its own `display`, which outranks the UA's `[hidden] { display: none }` — per-view guards had already been added twice and missed a third case, so the panel now carries one blanket `[hidden]` rule and the guard measures RUNTIME-hidden elements too, which a static scan cannot see [gate: im-two-pane-when-wide] |
| The panel could dock and resize but not be pulled out, so a chat could never sit on a second monitor or beside the workbench as its own window. Checking feasibility FIRST showed the data layer was already multi-window-ready: `collaboration:subscribe` keys its subscriptions by `sender` and hands each one its own unsubscribe, so no service change was needed | `test-collaboration-detached-window.cjs` — the window loads the SAME `index.html` with `?view=collaboration`, never a second copy of the panel's markup, because two copies drift and every fix would have to be made twice. Reusing the entry means the workbench markup is in the document, so the guard asserts NOTHING but the panel is laid out (`strays` empty) — and, separately, that the workbench was not BUILT: the boot function is shared, and it starts pollers, model lists, mail accounts and session UI that a second window would duplicate for nothing, so the standalone path runs only i18n, messaging UI and the panel. One window at a time: opening again focuses the existing one. A remembered position is reused only when part of the window is still reachable on a display that exists, and that predicate is EXPORTED and tested directly, because macOS clamps an off-screen window by itself — a mutation that disabled the check passed through window creation and is caught by the direct test Window first paint (2026-09-15 黑屏 report): `test-window-appearance.mjs` — every window's pre-paint `backgroundColor` is the ACTIVE theme's `--bg-body` (both were pinned to a dark navy while the app defaults to light, so creation, reload, a slow boot or a failed load showed a large dark rectangle that reads as a crash), no window is created already visible, the reveal happens on first paint / load / failure / a bounded timer so a window can never stay invisible, a failed main-frame load is reported instead of leaving a silent rectangle, and the renderer mirrors an explicit theme choice to the main process so the NEXT window and the next launch paint the right colour. A window that still cannot paint — load failed, renderer gone, or nothing drawn within the watchdog — replaces the blank rectangle with a self-contained, escaped, localized page naming the cause (`window-blank-guard.js`), and a failing fallback never loops. [gate: im-detached-window] |
| Runaway / doom loops | `test-loop-detector.mjs` — RESULT-AWARE (only byte-identical no-progress), graceful nudge, fail-open; loop hints must not tell the model to downgrade just because work is slow |
| A finished long task is reported as a failure: the unfinished-todo gate pushes a model that is BLOCKED (parked items awaiting a user decision) back into the same turn until it gives up, then labels a complete delivery `stalled` and buries it under "本轮没有形成完整最终回答" plus every mid-turn tool error | `test-opencode-agent-session.mjs` + `test-opencode-session-policies.mjs` + `test-turn-error-classify.mjs` + `test-turn-orchestrator.mjs` — the nudge budget is refilled ONLY when the unfinished todo set actually SHRINKS (re-planning/renaming the list is not progress, which is how one field turn burned 7 nudges / 13 minutes on 2 blocked items), with an absolute per-turn ceiling on top; a turn that produced real output settles NORMALLY with one honest `本轮还有 N 项待办没有标记完成` line and `unfinishedTodoCount`, and only an answerless turn keeps the `stalled` terminal; the incomplete-turn summary never prints "本轮没有形成完整最终回答" above real text, drops the successful-tool dump when an answer exists, and omits failures the turn RECOVERED from (a later same-tool success). Kill switch `LILY_DISABLE_TODO_COMPLETION_GATE=1` still restores the ungated baseline [gate: turn-completion-honesty] |
| Completion gates hand off to each other and re-enter a finished turn for round after round: each of the three (required-tool ≤2, unfinished-todo, deliverable ≤1) is individually bounded but nothing bounded their SUM, so a long turn could be pushed back into the model up to nine times | `turn-continuation-budget.js` + `test-opencode-agent-session.mjs` + `test-opencode-session-policies.mjs` — ONE shared per-turn re-entry budget (default 4) that every gate must claim from, recording which gate spent it. It is a backstop, not the primary bound: each gate keeps its own no-progress logic, and exhausting the shared budget takes that gate's existing GRACEFUL path — the todo gate settles with the answer intact, the deliverable gate (advisory) simply does not fire, and the required-tool gate still emits its honest persistence failure rather than degrading to a silent success. Malformed overrides fail open to the default, a missing state object never crashes a turn, and `LILY_TURN_CONTINUATION_BUDGET=0` restores the previous per-gate-only behaviour [gate: turn-continuation-budget] |
| Reasoning/thinking BYOK models false-rejected or mis-graded by the compatibility probe (so a capable custom model is refused or silently downgraded) | `test-model-compatibility-probe.mjs` + `test-model-probe-thinking-toolchoice.mjs` — the probe tolerates reasoning models: recognizes `reasoning_content` (not only `reasoning`), gives the liveness/tools probe 512 output tokens (16 starved reasoning models into empty content → false `MODEL_NO_CONTENT`), and falls back to `tool_choice:"auto"` when a FORCED tool_choice is rejected (e.g. deepseek thinking-mode 400) instead of concluding `MODEL_TOOL_CALLS_UNAVAILABLE`. Fail-safe: only converts false-rejects into a real test, never accepts a model that genuinely can't answer/tool-call. Validated live: deepseek-v4-pro probes clean (grade=standard) and scores 5/5 on `eval:model` |
| Workspace knowledge index cites a chunk whose source file was deleted or changed — "confidently wrong about removed content", worse than having no index | `test-workspace-index-freshness.mjs` — INVARIANT: index is a pointer/cache, the filesystem is truth. Auto-index stamps persist mtime+size+SHA-256; `queryIndex` verifies every hit against the live source, drops and evicts missing or fingerprint-mismatched chunks, and reports `staleSourcePaths` for re-index. Same-size/same-mtime edits are covered by the hash. FAIL-OPEN: URL/synthetic sources, legacy unstamped indexes, and ambiguous fs errors are kept so retrieval cannot destroy unprovably stale data. Kill switch `LILY_WORKSPACE_INDEX_VERIFY=0` |
| A user removes content from a generated/attached file, then a follow-up restores it from the staged attachment or historical `write/edit` input | `test-live-file-history-guard.mjs` + `test-opencode-message-parts.mjs` + `test-document-translator.mjs` + `test-workspace-index-freshness.mjs` — readable `sourcePath` outranks the immutable staged copy across model parts and document/vision preflight; `live-file-history-guard.js` removes only stale historical mutation bodies before model calls and blocks modification of an existing changed file until a successful live read records its current fingerprint. Post-write fingerprints preserve normal multi-step work; external edits re-arm the gate. Missing live files keep historical content for recovery; unchanged historical writes remain byte-identical; all hooks fail open and `LILY_LIVE_FILE_GUARD=0` restores baseline. 2026-09-14 field case (6 writes landed the placeholder ON DISK): the engine passes LIVE message objects, so the guard now sanitizes on a COPY of the message, never touches a tool call that has not completed, words the marker as "this call succeeded; body omitted", and `tool.execute.before` refuses a body that IS the marker (`LILY_LIVE_FILE_MARKER_REJECTED`) **2026-09-19 现场再收一处**:对本轮刚完整读过的自建脚本 edit,反复返回 `LILY_LIVE_FILE_READ_REQUIRED`,模型无路可走、只能绕道用 Python 脚本改写文件——**正是本条守卫反对的那种无校验写入**。根因不是路径形态(已归一化),而是守卫用一张**手维护的两项名单**判断「你读过了」,而平台本身有集中分类表:名单既太窄(`notebookread` 是真正的只读文件工具却不在内),又含有本引擎不存在的 `read_file`。判据改为镜像 `evidenceKind === 'file_read'`,收进 `lib/file-read-tools.cjs` 一处定义,并由 `test-live-file-history-guard.mjs` 断言两者一致——上游新增读取工具不会在这里被静默拒绝;刻意不纳入 `bash`(同一工具也能写)与抽取类(返回派生投影,不是 edit 所针对的原文)。同时拒绝信息点名「用 `read` 工具读」并说明哪些方式不算数——**不可满足的守卫比没有守卫更糟**。 [gate: live-file-authority] |
| Dragged workspace/app package detection blocks ordinary attachments, or shared scheduled tasks run unexpectedly after import | `test-workspace-package-drop.mjs` + `test-workspace-package-inspector.mjs` + `test-workspace-automation-sharing.mjs` + `test-workspace-share.mjs` — package identity comes from a bounded main-process manifest read, not the extension; unrecognized/corrupt/future/oversized packages and inspection errors fail open to the existing chat attachment path. Recognized packages require an explicit import/attach/cancel decision. Scheduled tasks are opt-in on export and import, carry definitions only, receive new IDs/scopes, and always enter paused with no run history or `nextRunAt`. Old packs without the hidden automation entry still import unchanged. |
| Scheduled work binds to whichever chat is active later, crosses accounts/projects, duplicates after pause/restart, replays every missed interval, disappears when its bound chat is busy, loses normal file-analysis capability, or traps a false-positive natural-language interpretation in the scheduler | `test-scheduled-task-isolation.mjs` + `test-scheduled-task-bridge.mjs` + `test-scheduled-tasks.mjs` + `test-scheduled-task-rejection.mjs` + `test-turn-orchestrator.mjs` — task owner/project/origin are immutable host-derived scope; each occurrence is a durable message admission to that exact conversation, so it inherits conversation context/permission mode and the normal document/vision/dependency preflight, queues behind an active turn in the same conversation, and runs concurrently with other conversations; permission metadata is uniformly `inherit`, including persisted migration of legacy databases and shared templates; exact run IDs, SQLite occurrence keys, and leases prevent cross-completion or duplicates; one SQLite transaction reclaims never-started queues under a fresh lease and terminalizes possibly side-effecting runs, so restart re-admits the former exactly once but never replays the latter; `run_once_on_launch` advances past current time instead of replaying every missed occurrence; unattended permission questions auto-deny instead of hanging; account changes remove only the foreign owner's volatile queue projection while retaining the exact durable run/admission for one-time recovery when that owner returns, and interrupt only a foreign run that actually started with the same turn/attempt/owner claim; a pending schedule interpretation can be rejected exactly once and resumes the exact original text through the same session's ordinary Agent path without creating a task; corrupt legacy JSON and owner-unavailable migrations are retained instead of overwritten. |
| Unknown files, folders, or archives are rejected at the attachment boundary, uploaded raw, traversed without bounds, or bulk-extracted unsafely | `test-file-staging-manager.mjs` + `test-clipboard-file-paste.mjs` + `test-archive-intelligence.mjs` + `test-workspace-index-source-bounds.mjs` + `test-file-intelligence-core.mjs` + `test-file-intelligence-mcp.mjs` + `test-opencode-message-parts.mjs` + `test-attachment-metadata.mjs` — every path-backed regular file and directory is admitted; known small files retain staging while large/unknown/archive/directory inputs remain live path references; pathless buffers keep the old allow-list/size cap. Directory traversal is depth/file/entry bounded and symlink-safe. Bundled 7-Zip lists archives without extraction and reads only one exact safe bounded text entry; encrypted, traversal, binary, oversized, corrupt, or unavailable-tool cases fail explicitly. Message parts preserve exact paths and never upload directories/archives/unknown binaries raw. |
| Auto-indexing the workspace ingests stale content, wipes the index on each update, or indexes files outside the workspace | `test-workspace-auto-index.mjs` — auto-ingest is OPT-IN (`LILY_WORKSPACE_AUTOINDEX=1`), driven by a turn's real `record.fileChanges`: created/edited files are re-chunked into a stable per-workspace auto-index via `upsertSourceChunks` (replaces ONLY that source's chunks — never wipes the index), deleted files are evicted, and out-of-workspace paths are skipped; bounded (≤ maxFiles), deterministic (no model call), fail-open + non-blocking (`setImmediate`, per-file try/catch) so it can't slow or break a turn. The slice-1 freshness guard is the backstop if a change is ever missed |
| Background workspace reconcile stalls the UI, misses external edits, or re-indexes unchanged files every scan | `test-workspace-reconcile.mjs` — reconcile is ASYNC + THROTTLED by contract (yields to the event loop every `batchSize` files, bounded by `maxFiles`, fired via `setImmediate` off the turn path, debounced ≤ once/workspace/`LILY_WORKSPACE_RECONCILE_MS`) so it can't affect UX; per-source `source_stamps` (mtime+size) SKIP unchanged files, (re)index new/edited external changes, and evict stamps whose files are gone; opt-in `LILY_WORKSPACE_AUTOINDEX=1`, fail-open. Complements slice-2 turn-driven ingest (catches edits made outside the agent) |
| Workspace version protection treats Git as a customer-installed prerequisite or silently loses protection for large workspaces | `test-workspace-git-dependency.mjs` + `test-workspace-version-control.mjs` + `test-runtime-pack-internal-dependency.mjs` — the hidden Git runtime pack is resolved before system Git, downloaded and health-checked on demand, large Git-backed saves batch paths below command-line limits, and any missing artifact or failed install falls back to the existing local snapshot backend; internal dependency packs never leak into ordinary dependency settings [gate: workspace-version-protection] |
| Proactive workspace retrieval dilutes ordinary-turn context, injects stale/deleted content, or makes strong models dumber | `test-workspace-autoinject.mjs` + `test-turn-orchestrator.mjs` — OPT-IN (`LILY_WORKSPACE_AUTOINJECT=1`) and injected ONLY on substantive `grounded`/`coverage` turns (never casual/fast chat → zero dilution on normal turns, like ①); `retrieveWorkspaceContext` returns a BOUNDED (`maxChars`) block, freshness-verified (deleted files never cited), explicitly labeled "RETRIEVAL, not proof — open the file to confirm" so it stays a pointer the model must ground, and fail-open (any error → inject nothing → baseline) |
| Memory reinforcement entrenches a once-popular memory, lets usage frequency surface an irrelevant memory, or drops/dumbs recall | `test-memory-reinforcement.mjs` — OPT-IN (`LILY_MEMORY_REINFORCE=1`); the boost is BOUNDED (≤ `MAX_BOOST`=8, vs relevance*25 in the ranker) so it only reorders already-relevant items and a relevant unboosted item ALWAYS beats an irrelevant max-boosted one (proven by test); recency-weighted so stale-but-once-popular items decay (no rich-get-richer); usage is recorded only for memories actually SELECTED for injection; store is per-project, LRU-capped, fail-open; NOTHING is ever deleted — reinforcement only re-weights. Default off → ranking byte-identical to today |
| Cross-session memory dropped on compaction | `test-compaction-memory-export.mjs` / `test-compaction-memory-plugin.mjs` — inject memory, fail open |
| Real-embedding memory recall degrades or slows recall when the embedding endpoint is unset, keyless, or failing | `test-memory-vector-index.mjs` — semantic recall ships DEFAULT-ON in code (`embeddingEnabled` defaults true; only explicit `LILY_MEMORY_EMBEDDING=0`/`false`/`off` disables — a source-controlled default since the bundled settings file is gitignored/regenerated) but is KEY-GATED and fail-open, so it can only improve on the lexical baseline: `resolveEmbeddingConfig` returns null (→ lexical hash, zero egress, zero added latency) whenever the flag is `0` (kill switch) OR no API key resolves — so keyless / BYOK-only users NEVER fire a doomed 401; base/key/model default to DashScope compatible-mode + the shared `DASHSCOPE_API_KEY` (override via `LILY_EMBEDDING_BASE_URL`/`_API_KEY`/`_MODEL`), resolvable through remote-config so the managed gateway can flip it without a code change; `makeEmbeddingCaller` returns null on incomplete config; `semanticRelevanceMap` returns null on any error/wrong-shape response; `buildContextMemoryAsync` fingerprint is identical to the sync lexical `buildContextMemory` when disabled/keyless/failed; vectors cached per provider+model so recall re-embeds only the query. Live-recall gate `scripts/validate-embedding-recall.mjs` PASSED 3/3 vs lexical 1/3 (DashScope text-embedding-v3) before default-on |
| Memory-usage chip mis-renders, blanks a turn header, or affects the answer | `test-memory-usage-summary.mjs` + `test-turn-memory-chip.mjs` + `test-turn-live-turn-adapter.mjs` + `test-turn-article-frame.mjs` — the "🧠 记忆 N" chip is READ-ONLY over the per-turn contextMemory trace, so it cannot change any model's reasoning; `summarizeMemoryUsage` returns null on bad/absent input (fail-open → chip hidden) and is kill-switched at source by `LILY_MEMORY_USAGE=0`; `renderTurnMemoryChip` only draws on sealed turns that actually used memory, is idempotent (reused, not duplicated), removes stale chips, and its call site is wrapped so a render error can't blank the status/header; `memoryUsage` rides in `record.meta` (no schema migration) and round-trips on reopen via `liveTurnFromRecord` |
| Autonomous solution-learning spams the approval queue or pollutes memory with wrong/turn-specific "lessons" | `test-auto-memory-proposals.mjs` — solution distillation is OPT-IN (`LILY_MEMORY_LEARN_SOLUTIONS=1`; off → zero behavior change) and HIGH-PRECISION: only `turn.completed` turns that overcame ≥1 tool error with a substantive (≥20 char) problem qualify, so trivial one-shot wins never propose; explicit user intent (记住 / correction) always wins over autonomous distillation; every proposal still flows through the existing HUMAN-APPROVAL queue (`promoteMemoryProposalsFromRecord` → user approve/dismiss → `appendLearnedConvention`), so nothing unverified is auto-injected into active memory; deterministic (no model call, never blocks the turn); deduped + capped (≤50) + fail-open (promote wrapped in turn-archive) |
| Improvisation fallthrough (parse/learned-flow fails → model writes a script) | scheduled-task: recognized intent shows the card / interval floored, never improvises; web-system: no learned flow → "needs re-learn", not ad-hoc |
| Model mirrors internal execution scaffolding back as its reply: on operational/resumed/continued turns it dumps an "Objective / Work State / Completed / Active(none) / Blocked / Next Move / Relevant Files" status report (plus a blanket evidence disclaimer) instead of answering | `test-task-contract.mjs` + `test-session-followup-context.mjs` + `test-session-bootstrap.mjs` — `PLATFORM_BASELINE_RULES` (every active task contract), the short-follow-up layer, and the resume notice all carry a positive reply-shape rule: the reply IS the deliverable in prose; objective/plan/work-state/next-step are internal tracking, never rendered back; continuations build on prior work without re-sending the previous conclusion. Lily asserts the known `record.terminal` status instead of asking the model to infer it. Evidence-grounding discipline is preserved; only genuinely unsupported claims are flagged inline, no blanket disclaimer on grounded answers. Fail-open: wording only, worst case = today. Code-level backstop (2026-07-22, `test-status-scaffold.mjs`): `src/main/status-scaffold.js` detects the rigid header structure (≥4 known headers incl. ≥2 anchors, first line a header) and STRIPS the scaffold prefix on all three paths — the streaming gate (`turn-runtime-event-router.js`, holds the head until proven clean; a confirmed scaffold never fails open), the terminal finalizer (before the evidence gate/persist; a purely-scaffold reply is replaced by a plain note, salvage keeps the real reply), and history load (`opencode-conversation-source.js`, rewrites prefix dumps instead of dropping the whole message). Red line: an ambiguous boundary keeps the original verbatim |
| Shallow learning → hollow, unusable capabilities | `test-web-system-har-contracts.mjs` — query-POST classified read + request fields → params; SPA render-wait before snapshot |
| Web learning depends on the model remembering every professional step | `test-web-system-learning-orchestrator.mjs` + `test-web-system-learning-skill.mjs` — one default `learn_web_system.cjs` orchestrator plans contract discovery, bootstrap HAR, JS intelligence, source-seeded expanded scan, HAR merge, auth recipe, and finalizer; normal learning forbids ad-hoc here-doc/browser scripts |
| Learned web skills still pop browser pages during normal use | `test-web-system-api-execution.mjs` + `test-web-system-learning-skill.mjs` — all-API execution stays HTTP/browser-free; API fallback is skipped unless `--allow-browser-fallback` is explicit; browser primary plans return `BROWSER_EXECUTION_DISABLED` unless `--allow-browser` is explicit |
| SPA learning misses APIs hidden in page JavaScript bundles | `test-web-system-frontend-source-intelligence.mjs` — analyzes large same-domain JS asset sets, fetches missing protected JS bodies with storageState, extracts endpoint+method hints, persists them as non-executable `apiHints`, and auto-generates frontend source from `scan.harPath` in the finalizer |
| Active API-coverage probing mutates data, bypasses permissions, or over-claims completeness on a contract-less system | `test-web-system-probe-coverage.mjs` — `probe_api_coverage.cjs` verifies frontend-hinted endpoints READ-ONLY (only GET/HEAD ever sent; POST/PUT/PATCH/DELETE hints recorded unverified, NEVER executed); 401/403 respected + recorded as `access:"forbidden"`, never retried/bypassed; templated detail endpoints filled only with an id HARVESTED from a verified collection (no guessing); domain allowlist enforced; bounded probe count + rate-limit + timeout; authed session cookies only (no UA/webdriver spoofing); merges WITHOUT overriding authoritative/HAR contracts; session-gated (skipped without one) + fail-open; reports honest verified/gated/stale delta and still records remaining gaps in `health.json` (never claims complete) |
| Web automation breaks on session expiry / pagination / CSRF / writes | `test-web-system-{auth-refresh,pagination,csrf-rotation,idempotency,param-binding}.mjs` — all opt-in, capped, fail-safe |
| Optional runtime installation corrupts or weakens the base execution environment | `test-runtime-pack-installer.mjs` + `test-runtime-packs.mjs` + `test-runtime-pack-settings-ui.mjs` + `test-runtime-pack-main-progress-ui.mjs` + `test-runtime-release-policy.mjs` — failed installs leave no target dir; missing artifact dirs are not treated as installed; bundled read-only packs work without userData install state; Python packs enter PYTHONPATH only when marked `pythonPath`; native/browser packs enter PATH/env through catalog metadata; settings UI cannot uninstall bundled packs; main chat stays background-silent for normal install progress while failures remain visible; release scripts require platform runtime + LibreOffice instead of shipping a package that downloads runtime on first use |
| Background Office rendering opens a Windows terminal or blocks on an unavailable network printer | `test-runtime-health.mjs` + `test-render-document-profile-uri.mjs` — Windows LibreOffice probes and all bundled Office adapters prefer `soffice.exe` over the console-only `soffice.com`, use no-window subprocess flags, preserve headless/invisible conversion, and disable synchronous printer detection. Conversion failures remain explicit and keep the prior document-delivery fallback instead of silently dropping rendering. |
| UI-first workflow makes natural-language tasks feel blocked or less intelligent | `test-capability-broker.mjs` + `test-chat-native-dependency-flow.mjs` + `test-skill-catalog.mjs` — capability context is chat-native, fail-open, bounded, and only injected for operational turns; the composer must not import runtime-pack preflight/install code; main/subagent guides prefer capability contracts over telling the user to click UI first |
| Live rendering queues several forced scroll-to-bottom callbacks, so the user's first upward wheel gestures appear ignored and older messages require repeated scrolling | `test-scroll-geometry.mjs` + `test-scroll-autofollow.cjs` + `test-message-live-render-model.mjs` — each panel coalesces delayed auto-follow, navigation intent versions invalidate stale callbacks, an upward wheel detaches immediately, and the explicit Latest action still re-enables follow. Pure geometry plus a real Electron scroll panel guard both behavior and DOM timing. `test-conversation-minimap-continuity.mjs` (2026-09-15 demo) — the right-hand question rail keys ribs by render key, so two questions asked inside one turn (插话) resolve to two different bubbles instead of stacking on the first; a question committed during a live turn changes the render signature so the newest one appears; the rail shows from the second question; clearing it on a session switch invalidates the cached signature so it comes back; a resize rebuilds the offsets; and an older-page merge no longer overwrites a steered question. The same wording asked in two different turns also stays two questions: the text+time equivalence fallback (which reconciles the same message arriving from two sources) no longer matches across turn ids, so "继续" asked twice — or asked with an unparsable timestamp — is two ribs, and the rail's overflow shows a thin scrollbar instead of none. [gate: conversation-scroll-control] |
| Static AGENT guide grows without a runtime cap, so a large skill catalog or provider contract can crowd out user messages before the task even starts | `test-agent-guide-i18n.mjs` — `buildAgentGuideContent()` must enforce `AGENT_GUIDE_MAX_BYTES` at runtime, keep base/provider guidance ahead of the skill index, truncate oversized synthetic skill catalogs with an explicit notice, and never rely only on today's bundled catalog size |
| Mobile Command runtime emits ad-hoc error codes that the phone, OpenAPI, or docs cannot interpret | `test-mobile-command-error-contract.mjs` — runtime Mobile Command `MC-ERR-*` codes must exist in both `mobile-command-error-recovery-catalog.md` and `mobile-command.openapi.yaml`; catalog/OpenAPI enums must stay identical |
| Mobile Command kill switches or evidence-gated routes fail open and leave remote authority available | `test-mobile-command-kill-switch.mjs` + `test-mobile-server-final-shape.mjs` — configured-off capabilities return `MC-ERR-CONFIG-FEATURE-DISABLED` with `chat_only` fallback at route boundaries; unaccepted push/diagnostics/artifact-content/live/voice/native routes are registered but typed-disabled, not 404 or accidental success |
| Mobile Command docs claim exact machine-readable contracts but schema references drift or disappear | `test-mobile-command-schema-references.mjs` — API matrix refs must resolve against OpenAPI, WebSocket/DataChannel event schema, and native bridge schema; matrix HTTP operations must exist in OpenAPI |
| Mobile Command push/diagnostics contracts quietly grow raw user data, paths, screenshots, clipboard, or unredacted support payloads | `test-mobile-command-privacy-redlines.mjs` — evidence-gated push and diagnostics schemas must stay metadata-only: explicit diagnostics consent, redacted bundle/snapshot, minimal push receipt/open event fields, and no raw content/path/body/text/header fields in those payloads |
| Mobile Command telemetry, customer status, or support diagnostics become free-form and leak provider details, paths, content, stack traces, URLs, or unredacted support data | `test-mobile-command-observability-contracts.mjs` — observability/support JSON schema must compile, accept only bounded telemetry/status/diagnostics fields, enforce resolved/fallback status invariants, require redacted diagnostics manifests, and reject unknown sensitive/free-text fields |
| Capability context bloats ordinary chat and slows or weakens model reasoning | `test-capability-broker.mjs` — ordinary casual text does not inject capability context; files, dependency advisories, grounded/coverage turns, or explicit operational requests do inject bounded context |
| Default compact capability context is truncated before showing representative non-Office or bundled Office capabilities | `test-capability-broker.mjs` — default `compactCapabilityContext` must keep key fail-open routes plus representative app, mail, research, runtime, and bundled Office skills such as `anthropics-xlsx` inside the bounded prompt budget; focused contexts still use task-ranked ordering |
| Task-focused recommendations exist but are invisible in real turns because the capability context injection trigger is narrower than the catalog router | `test-capability-broker.mjs` + `test-turn-orchestrator.mjs` — operational domains covered by the recommendation graph, including mail, stock/source research, app/script building, UI/code/intent evaluation, and Office/media work, must trigger bounded capability context injection; casual chat must still avoid injection, and real engine payload trace must expose the recommended skill ids |
| Focused capability context re-sorts the recommended graph, so the prompt shows a supporting skill before the top task skill | `test-capability-broker.mjs` + `test-turn-orchestrator.mjs` — focused `compactCapabilityContext` must preserve `recommendSkillCapabilityGraph` order; app creation must show `lily-app-builder` before `lily-browser-qa`, stock research must show `lily-stock-research` before source synthesis, and UI quality must show `lily-ui-quality` before browser QA in the actual engine payload |
| Top-tier system-intelligence requests such as "系统更聪明" or "最终形态" are treated as casual chat or generic browser QA, so the agent misses deterministic routing/capability-quality checks | `test-task-contract.mjs` + `test-capability-broker.mjs` + `test-turn-policy.mjs` + `test-turn-orchestrator.mjs` — these requests must activate an evidence-backed `agent_quality` / `architecture_audit` contract, require file search/read evidence, and put `lily-intent-eval` / `lily-skill-quality-gate` ahead of generic browser QA in the real engine payload |
| Active session skills pollute task-focused recommendations even when unrelated to the current request | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — `activeSkillIds` may boost already-relevant skills, but must not turn a zero-relevance skill into a focused recommendation; an active mail skill must not appear in PDF extraction recommendations |
| Capability status only says which tools are visible, hiding why a relevant tool is unavailable and causing the agent to guess or downgrade | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — `lily_capability_status` must keep backward-compatible available tool names, but also expose `toolDetails`/`unavailableTools` with machine-readable reasons such as `SKILL_NOT_ACTIVE`, `MAIL_BRIDGE_UNAVAILABLE`, and `BROWSER_RUNTIME_UNAVAILABLE` |
| Capability status lists unavailable tool names and reasons but omits what the tool actually does, forcing the agent to infer capability from terse names | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — every `lily_capability_status.toolDetails[]` / `unavailableTools[]` item must include the tool description alongside availability and reason |
| Capability status says `SKILL_NOT_ACTIVE` but omits which skill gate is missing, forcing the agent to infer enablement requirements from separate fields | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — tool details must include `missingSkillIds`, with inactive gated tools naming the exact missing skill ids and available tools returning an empty list |
| Capability status says a connector/runtime is unavailable but omits the exact status key/value, so the agent has to guess which host state must change | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — connector-gated tools must include `connectorStatusKey`/`connectorStatusValue` such as `mailConnected:false`, and runtime-gated tools must include `runtimeStatusKey`/`runtimeStatusValue` such as `browserAvailable:false` |
| Platform-only broker status reports `SESSION_REQUIRED` but omits whether a tool actually needs a session, so the agent may confuse missing session with missing skill or connector setup | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — every tool detail must include `requiresSession` and `sessionAvailable`; platform tools remain available with `requiresSession:false` even when no session context exists |
| Tool visibility is mistaken for broker-handler executability, causing the agent to call placeholder handlers that return not-wired/unavailable errors | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — tool details must include `brokerHandlerAvailable` and `brokerHandlerError`; platform broker tools with real handlers report available, while placeholder/external-surface tools disclose their broker handler error code |
| Tool status discloses that the broker handler is not wired but omits the intended execution surface/server, leaving the agent unsure whether to use mail MCP, learned-web-system MCP, browser runtime, or the broker itself | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — tool details must include `executionSurface` such as `tool_broker`, `mail_mcp`, `learned_web_system_mcp`, or `browser_runtime`, plus `mcpServerName` when the execution surface maps to a registered MCP server |
| Execution surface/server routing is hardcoded in a central group switch, so adding a new tool family silently gets the wrong surface or no server name | `test-tool-broker-registry.mjs` — broker tools must declare `executionSurface` / `mcpServerName` in their tool definitions or dynamic tool generation; status may read those fields but must not rely on broad group-name guessing for known tool families |
| Active skills require optional runtime packs, but status only lists installed packs, so the agent cannot tell which pack must be installed to regain full capability | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — `lily_capability_status.runtimePacks` must expose `requiredByActiveSkills` and a deduped `missing` list; installed packs must not be reported missing |
| Task-focused capability recommendations list required runtime packs but not the current missing/install gap, so the agent may try a degraded path before installing the right pack | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — focused `lily_capability_list` calls must expose `runtimePacks.requiredByRecommendedSkills` and deduped `missing` based on current installed packs; default unfocused lists must stay bounded |
| Unfocused capability lists skip runtime-pack probing for budget reasons but return empty arrays that look like a real "nothing installed / nothing missing" result | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — `lily_capability_list.runtimePacks.evaluated` must be `false` for default unfocused calls and `true` for focused query/file calls; `lily_capability_status` is always evaluated |
| Missing runtime packs are exposed only as opaque ids, so the agent has to guess what the pack is or call extra tools before making an install decision | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — capability list/status runtime pack gaps must include `missingDetails` from `PACK_SPECS` with id, category, localized label, description, and size estimate while preserving the compact `missing` id list |
| Capability recommendations identify missing runtime packs but do not say whether the install tool is currently available, so the agent may ask the user or hunt for scripts unnecessarily | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — focused `lily_capability_list.runtimePacks.installToolAvailable` must reflect actual visibility of `runtime_pack_install`, matching `lily_capability_status` |
| Missing runtime pack metadata still leaves the agent to hand-build install tool arguments, risking wrong pack ids or unsafe install behavior | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — every `runtimePacks.missingDetails[]` item must include `installAction: { tool: "runtime_pack_install", args: { packId }, destructive: true, requiresConfirmation: true }` so the next step is machine-readable and visibly confirmable |
| Learned web-system tools are registered in MCP but omitted from capability list/status, so the agent cannot see natural-language system operations and may improvise scripts | `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — broker-visible learned web-system tools must appear consistently in `tools/list`, `lily_capability_list.tools`, and `lily_capability_status.toolDetails` when the learned skill is active |
| First-party skills decay into one-off prompt hacks that bypass platform capability contracts | `test-skill-capability-contracts.mjs` + `test-long-task-supervisor-migration.mjs` — operational skills must start from natural language, fail open, route long work through `lily_process_jobs`, expose `job_status`/`job_logs`, avoid UI-first operation, and forbid ad-hoc runtime installs when a Lily pack exists |
| User-imported workspace skills overwrite platform skills or leak into unrelated workspaces | `test-workspace-skill-import.mjs` + `test-skill-catalog.mjs` — standalone workspace skill imports normalize non-learned ids into `learned-*`, reject protected bundled skills and dependency folders, mark imports as workspace-only, bind them to the active project, and fail without mutating existing skills |
| Lily platform catalog skills are mistaken for OpenCode native `skill` commands, so catalog capabilities such as `lily-*` or bundled `anthropics-*` appear "not found" | `test-opencode-config-builder.mjs` + `test-opencode-runtime-reducer.mjs` + `test-turn-orchestrator-steer.mjs` + `test-skill-catalog.mjs` + `test-capability-broker.mjs` + `test-runtime-pack-preflight.mjs` + `test-web-system-learning-intent.mjs` — OpenCode native `skill` is denied in runner permissions; AGENT.md/index/subagent prompts say session catalog entries are platform capability guides, not native skills; dependency/web-learning prompts route through Lily MCP/tools/scripts instead of native `skill <id>`; reducer-level fallback recognizes catalog-backed skill IDs, annotates native `skill <id>` failures with the Lily guide path, and emits a bounded steer recovery so the task does not stop at "not found" |
| Registry-declared specialist Office skills are omitted from task-focused recommendations, causing the agent to use generic tools for PDF forms, structured document drafting, evidence lookup, PPT visual QA, or spreadsheet analysis | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — focused capability recommendations must put `lily-pdf-form` first for explicit PDF form fill/validation, surface `anthropics-doc-coauthoring` for structured document writing, put `lily-document-query` first for document evidence lookup, put `lily-ppt-design-qa` first for PPT layout/overflow review, and put `lily-excel-data-analysis` first for spreadsheet analysis/cleanup/chart work, while still excluding unrelated spreadsheet/PDF/template/DOCX skills; PPT export-to-PDF must not route through PDF extraction |
| Referenced spreadsheets without file objects are mistaken for code repair because words like `fix` and `formula` look like implementation work | `test-capability-broker.mjs` — current/uploaded/attached workbook or spreadsheet formula work must put `lily-excel-data-analysis` first, retain `anthropics-xlsx`, and omit `lily-code-repair` even when no file object is present |
| Runtime-pack installation / enablement is treated as generic Office work, so missing OCR/Docling/large-document capability repair is delayed or improvised | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — focused recommendations for dependency/runtime-pack/OCR/Docling enablement must put `lily-runtime-packs` first while preserving the relevant PDF extraction context; failure must stay fail-open to bundled capabilities |
| Vendored authoring skills drift back to latin-centric or dark-by-default output, so first-pass Word mixes per-machine CJK fallback fonts and first-pass decks ship dark/unstyled | `test-office-authoring-contract.mjs` + `test-office-style-helper.mjs` — overlays + `lily-office-intent` must pin the latin+eastAsia font pair and light-first decks to `resources/runtime-scripts/lily_office_style.py`; helper selftest runs runtime-backed (skips gracefully without the bundle) |
| Cross-built runtime bundle ships build-machine absolute paths (pyvenv.cfg home/executable, bin shims), so the customer-machine venv python dies and every pack health check false-fails after a successful install | `test-runtime-python-fixup.mjs` — `ensureVenvCfgFixed` rewrites stale/placeholder cfg paths on-device (win32 only, mac stays sealed), never touches healthy cfgs, and degrades silently on read-only/partial installs |
| Image OCR is mistaken for scanned-PDF extraction because the text says "scan/OCR", causing PDF tools to be recommended for `.png/.jpg` files | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — image OCR must put OCR/runtime support first and retain image context, but must not recommend `lily-pdf-extraction-router` or `anthropics-pdf`; scanned PDFs still keep the PDF route |
| Referenced image transform without a file object is mistaken for image QA, Office/PDF routing, or creative generation; generic OCR text suppresses PDF context | `test-capability-broker.mjs` — current/uploaded/attached image resize/convert/export/webp work, wording variants like "make smaller", "save as jpeg", and "change to png", plus background removal/cutout requests like "remove background", "remove bg", and "抠图去背景", must put `lily-runtime-packs` first through catalog-declared hints and must not route as Office/PDF or creative generation; textual `OCR` alone must not make PDF runtime enablement lose PDF extraction context |
| Visual creation / prompt-writing requests are reduced to image QA or generic fallback instead of using creative direction and prompt-building skills | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — image/poster/video creation must put `lily-creative-director` first, prompt-writing/enhancement must put `lily-prompt-enhancer` first, and image review must keep `lily-image-qa` first without letting generic "generate" steal spreadsheet chart work |
| Source-backed/current research, stock analysis, skill review, or intent routing tests are mistaken for document evidence lookup, browser QA, coding work, or generic fallback | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — stock/financial research must put `lily-stock-research` first and keep source synthesis support; general source-backed/current research, including latest news, browser rankings, and API price comparisons with citations, must put `lily-research-synthesis` first; skill quality review must put `lily-skill-quality-gate` first; intent routing evaluation must put `lily-intent-eval` first; and `lily-document-query`/browser QA/coding must not steal no-file research just because the user asks for evidence/citations or the topic contains words like browser/API |
| Capability routing overfits a small hardcoded keyword/company-name list, or truncates/catalog-matches hints too literally before multilingual and word-form variants can match, so real variants fail to receive focused context | `test-capability-broker.mjs` — registry-declared `capability.matchHints` must survive contract normalization into the skill graph and drive both focused recommendations and `shouldInjectCapabilityContext`; ticker/portfolio, current research variants such as latest news/API prices/带来源/给出处, runtime/image/media variants such as image resize/background removal/video transcode/image resizing, English UI audit, English SaaS dashboard variants, and engineering variants such as missing test/bug risks must route through catalog-declared hints instead of adding company names or broad verbs to broker code; preserving match hints must not expand the compact prompt context |
| Positive routing hints have no negative counterpart, so a broad word such as `prompt` steals intent-routing evaluation requests | `test-capability-broker.mjs` — registry-declared `capability.avoidHints` must survive contract normalization into the skill graph and suppress matching skills; English skill-route evaluation must put `lily-intent-eval` first and must not include `lily-prompt-enhancer` |
| Broad generic words in capability routing steal specialized artifact review or Office QA tasks | `test-capability-broker.mjs` — image artifact-defect review must route to `lily-image-qa`, not app creation; PPT slide overflow / visual consistency with a deck file must route to `lily-ppt-design-qa`, and generic UI quality must not outrank the presentation-specific QA path |
| Media/runtime enablement is mistaken for creative video generation because generic `video` words trigger creative routing | `test-capability-broker.mjs` — ffmpeg/video processing pack enablement must put `lily-runtime-packs` first and must not include `lily-creative-director`; creative routing requires an explicit creative action/object such as poster, cover, storyboard, prompt, visual concept, or generation/design wording |
| Referenced media without file objects falls through to Office/PDF fallback instead of runtime media processing | `test-capability-broker.mjs` — current/uploaded/attached video/audio compression or transcoding must put `lily-runtime-packs` first and must not include `lily-office-intent` or `lily-pdf-extraction-router` even when no file object is present |
| Mail drafting/search, UI quality review, or runtime error repair fall through to generic Office/browser routing | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — email drafting/search must put `lily-mail-assistant` first, UI visual quality review must put `lily-ui-quality` first while retaining `lily-browser-qa`, and TypeError/build/test/runtime repair must put `lily-code-repair` first without stealing image/document review tasks |
| App creation, component implementation, or engineering discipline review falls through to browser QA, Office, document drafting, or visual creative routing | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — new app/page/tool creation must put `lily-app-builder` first while retaining `lily-coding-core` and browser QA support, component/code implementation must put `lily-coding-core` first, and engineering rules/anti-overdesign/capability-regression/code-review requests, including English overengineering/capability-regression and diff-review/missing-tests/missing test coverage/bug risks variants, must put `lily-engineering-rules` first without being mistaken for proposal/document drafting, generic Office routing, or creative `cover` work just because the text contains `coverage` |
| Script creation, template fill, or media transcode tasks fall through to Office/browser/creative-prompt routing | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — script/batch-file creation must put `lily-app-builder` first with coding support, Word/template placeholder filling must put `lily-template-fill` first while retaining DOCX tooling, and video/audio trim/transcode work must put `lily-runtime-packs` first without being mistaken for visual creative direction |
| Office artifact creation is mistaken for app creation because of broad "create/build" verbs | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — PDF creation/editing must put `anthropics-pdf` first, Word report creation must put `anthropics-doc-coauthoring` first with DOCX tooling, and PowerPoint creation must put `anthropics-pptx` first; `lily-app-builder` must only trigger on explicit app/page/tool/script creation, not Office deliverables |
| Document read/summary is mistaken for evidence lookup, or evidence lookup is weakened into generic file tooling | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — ordinary PDF/DOCX/PPTX/XLSX read-summary tasks must put the corresponding `anthropics-*` file tool first while preserving routers/verification, but explicit query/search/cite/evidence requests over uploaded documents must still put `lily-document-query` first |
| Explicit document delivery verification is mistaken for document drafting, or generic verification steals PPT-specific QA | `test-capability-broker.mjs` — current/uploaded report verification for pagination/table layout/links/rendering must put `lily-document-verify` first and omit `anthropics-doc-coauthoring`; PPT slide overflow/layout QA must still put `lily-ppt-design-qa` first instead of generic document verification |
| Browser QA / localhost smoke tests are mistaken for high-risk web-system learning or generic fallback routing | `test-capability-broker.mjs` + `test-tool-broker-registry.mjs` + `test-tool-broker-mcp.mjs` — focused recommendations for console errors, frontend smoke tests, localhost, responsive layout, and web QA must put `lily-browser-qa` first and omit `lily-web-system-learning`; explicit learn/automate-a-system requests must still put `lily-web-system-learning` first |
| Short web-system abbreviations match inside unrelated English words and steal document evidence lookup | `test-capability-broker.mjs` — `oa` must be word-boundary matched so text like `uploaded contract` does not trigger web/browser routing; uploaded/attached contract evidence lookup must put `lily-document-query` first and must not include `lily-browser-qa` |
| Non-high-risk first-party skills exist in the catalog but are not installed/enabled by default | `test-skill-catalog.mjs` — non-high-risk Lily skills such as stock research, skill quality, and intent eval must be `defaultEligible`; only high-risk/on-demand skills such as mail and web-system learning may stay default-off |
| Long-running primary work is mistaken for failure, causing the agent to switch to a weaker/secondary path | `test-opencode-agent-session.mjs` + `test-loop-detector.mjs` + `test-skill-catalog.mjs` — active foreground tools extend the no-progress lease and emit observable progress instead of force-ending; loop hints require progress evidence before route changes; AGENT.md states "slow is not failure" and forbids secondary/degraded approaches until explicit failure, user stop, or proven no-progress |
| Pre-turn context compaction hangs and freezes the turn forever at "Preparing to compact conversation context…" (unbounded `await server.summarize` on a stuck/slow model call; try/catch catches errors but not a hang) | `test-opencode-agent-session.mjs` — `compactContext` bounds the summarize call with `runWithTimeout(..., compactionTimeoutMs())` (90s default, ≥15s floor, `LILY_COMPACTION_TIMEOUT_MS`); on timeout it rejects → caught → returns false → the orchestrator's existing fail-open path runs the turn WITHOUT compaction (baseline). Compaction is memory maintenance, never a turn gate |
| A single giant tool result (e.g. read/write of a 6 MB file) balloons the model context — then compaction has to summarize the blob (slow/hangs) and the whole session degrades. ROOT cause behind the compaction hang; the timeout above only bandages it | `test-large-output-guard.mjs` — engine plugin `resources/opencode-plugins/large-output-guard.js` runs in `tool.execute.after` (its `output` is model-facing), caps oversized tool output to head+tail + writes the FULL text to a `.lily-work/tool-output/` sidecar the model can re-read on demand. Budget `LILY_TOOL_OUTPUT_MAX_CHARS` (32k default, ≥8k), kill switch `LILY_TOOL_OUTPUT_GUARD=0`. Fail-open: any error / null output leaves it untouched; small output untouched; non-text parts survive. Registered in `session-runner-pool.js` plugin list |
| Steer ("插话") drops/abort-restarts a message, or hides/merges a visible follow-up after restart, instead of injecting into the running turn | `test-turn-orchestrator-steer.mjs` + `test-session-runtime-store.mjs` + `test-message-store.mjs` + `test-opencode-conversation-source.mjs` + `test-message-render-keys.mjs` — on by default, kill-switch `LILY_ENABLE_STEER=0`; engine-reject / kill-switch both degrade to the queue (today's behavior); accepted steers persist as same-turn user messages with `meta.steer`, survive busy sync, projection recovery, official-history merge, and render-key de-dupe |
| Conversation minimap navigator breaks the chat / desyncs from the transcript | `test-conversation-minimap.mjs` (model) + `conversation-minimap-regression` in `test-renderer-import.cjs` (DOM); DOM-derived + try/catch teardown → any failure removes the rail, chat scrolls as today |
| **DATA LOSS:** session index wiped — root cause was (a) non-atomic write → interrupted write leaves a corrupt file, (b) corrupt read silently treated as "empty", (c) `load()` then auto-creates + `saveImmediate()` overwrites it, (d) `reconcile` prunes all sessions when projects fail to load | `test-session-load-recovery.mjs` + `test-session-save-guard.mjs` — ATOMIC write (tmp→rename) + rolling `.bak`; corrupt read is quarantined & recovered from `.bak` (not emptied); failed load bails (no auto-create/save); empty project set never prunes; `_guardSessionCollapse` refuses overwriting a healthy/corrupt index with a collapsed one |
| Reopened conversations look empty because the UI waits for OpenCode official history before showing the local latest page | `test-opencode-conversation-source.mjs` + `test-renderer-import.cjs` — initial conversation loads use a local-first latest page and request official OpenCode history in the background; older pagination and official refresh remain available |
| Reopening a finished conversation appends a DUPLICATE of the last turn: a rich local assistant turn keeps its answer in `record.assistantText` with empty top-level `content`, so the background official-history refresh (plain text, different key, no inherited turnId) fails every text/key dedup and is appended below | `test-conversation-pagination.mjs` + `test-session-runtime-store.mjs` + `test-opencode-conversation-source.mjs` — assistant dedup compares `content \|\| text \|\| record.assistantText` across all three merge layers (renderer page merge, renderer committed-store, main-side projection merge), so the official copy collapses into the one rich turn instead of duplicating on every reopen |
| Switching conversations blocks the UI while preparing OpenCode runner/session guides on slower Windows filesystems | `test-session-switch-fast.mjs` + `test-turn-orchestrator.mjs` — session switch changes visible state immediately and only warms the runner in the background; sends, rewinds, skill changes, and other execution paths still call the strong `ensureSessionRunner` path before acting |
| **DATA LOSS:** streamed assistant text disappears if the app crashes before `assistant.final` / terminal archive | `test-message-store.mjs` + `test-opencode-conversation-source.mjs` + `test-session-runtime-store.mjs` — `assistant.delta` is projected durably into SQLite; open projected turns are recovered as visible `turn.stalled` assistant messages across reopen; OpenCode official history merges local projections even while the runner is busy, so partial answers are not hidden by an incomplete official page |
| Non-essential renderer module statically imported into the bootstrap chain (app.js→message.js→…) can blank the whole app on load failure | message.js loads the minimap via guarded `import().catch()` (not a static import) → a broken optional module degrades to "no minimap", never "no chat/sessions" |
| **DATA LOSS:** renaming the product moves Electron's name-derived userData folder, orphaning every install's sessions/licenses/model config/memory (this app has already been terminal-chat-claude → ai-super-terminal → Lily Workbench) | `test-main-single-instance-lock.mjs` — asserts `src/main.js` pins `userData` to a fixed `<appData>/lily-workbench` and that the pin runs BEFORE `bindRuntimePaths()`; changing that folder string fails the build. (Legacy pre-pin folders are still swept by scan-based `legacyUserDataRoots()` — `test-data-migration.mjs`.) |
| Media-gen (image/video/speech) distribution must not break old clients/servers or leave a device with no media | `test-client-config-service.mjs` (resolveMediaSelection): no `config.media` → all key-backed providers + server default (today); selection gated by available keys; default falls back; old clients ignore the additive `effectiveConfig.media`; client `serviceSelection` null on old servers → today's behavior. `test-agent-env-media.mjs`: server-delivered media gateway env (`LILY_IMAGE_PROVIDER`, `LILY_VIDEO_PROVIDER`, `LILY_SPEECH_PROVIDER`, DashScope/Ark/Kling/MiniMax/Zhipu proxy URLs and short tokens) survives spawn env conversion so generation skills can actually call the gateway. `test-admin-media-provider-surface.mjs`: admin config surfaces built-in Lily image/video/speech services and profile rules can select Lily + speech without leaking upstream endpoints. `test-media-provider-settings.mjs`: client settings keep configured first-party Lily media visible even when a stale media allow-list only named DashScope. `test-media-provider-contracts.mjs`: server-delivered media request contracts are additive, scoped to actually available providers, can be generated from the upstream OpenAPI schema, and are injected into client runtime env. `test-agent-guide-i18n.mjs`: AGENT.md exposes current media/search providers and all selected-provider contract parameters instead of hardcoded vendor guesses. `test-media-gateway-providers.mjs` + `test-media-generation-skills.mjs`: Lily GPU private/relative asset URLs are rewritten through the gateway, downloaded with the short Lily token, and speech/video generation can execute from server-delivered contract defaults without leaking 127.0.0.1 result URLs to clients. |
| User-selected media providers are bypassed after an error, causing the agent to silently switch to another vendor | `test-agent-guide-i18n.mjs` — AGENT.md exposes image/video/speech/search provider selections, dynamically describes media skills using the selected provider, and explicitly forbids automatic provider fallback after a configured provider error; the agent must report the error and ask the user whether to retry, switch provider, or provide a key. |
| Web access guide advertises a non-executable search/fetch command because cross-skill script placeholders are left unresolved | `test-agent-guide-i18n.mjs` — generated websearch/webfetch inline guide must contain concrete `websearch.cjs` and `webfetch.cjs` paths for every locale and must not leave `{{WEBSEARCH_SCRIPT}}` / `{{WEBFETCH_SCRIPT}}` placeholders in AGENT.md |
| Search env resolution assumes Electron-only globals and crashes in plain Node helpers before the server-delivered IQS env can be applied | `test-search-settings.mjs` — `getSearchSpawnEnv()` must keep the managed `iqs` default and not throw when `LILY_USER_DATA_DIR` is set but `process.resourcesPath` is absent; server-delivered `WEBSEARCH_IQS_API_KEY` / `WEBSEARCH_IQS_API_URL` must still win when present |
| Rankings/latest/prices/news/roles/releases/statistics are treated as memory-answerable general knowledge, unsupported prose streams to the customer before its evidence check, a no-search constraint is bypassed, failed search calls count as evidence, or the model invents links/items/scores after searching | `test-external-fact-grounding.mjs` + `test-answer-evidence-finalizer.mjs` + `test-turn-orchestrator.mjs` — high-risk external facts receive an additive `external_fact` task contract automatically (while local UI/code/release work and creative chat retain their existing routes); their answer prose is buffered until final claim assessment, and a rejected answer is replaced by one concise scope question or an honest evidence gap, so unsupported claims never enter the visible final or archive; required evidence must come from a SUCCESSFUL websearch/webfetch/live API or authoritative supplied document; answer URLs and ranked items must occur in this turn's evidence, while external years/percentages/versions/latency values are grounded by default. Explicit no-search instructions are honored and never auto-retried; ambiguous rankings and completed research attempts also avoid duplicate retries. A well-scoped memory-only fact may request ONE side-effect-free verification retry, guarded by rescue one-shot/cooldown and both retry kill switches. Gate/finalizer errors fail closed to the honest external-fact fallback, while non-external turns retain their prior fail-open behavior. |
| External claims depend on hard-coded domain vocabulary, so an unfamiliar certification, authorization, membership, or status question bypasses the evidence gate; a model semantic plan cannot activate the host gate; a source merely mentioning an entity is mistaken for proof of the requested predicate; the gate only refuses after repeatedly searching low-authority sources; or a long research turn completes dozens of reads but loses its final synthesis and makes the user replay everything | `test-semantic-external-fact-contract.mjs` + `test-general-claim-verification.mjs` + `test-model-verification-plan.mjs` + `test-public-enterprise-grounding.mjs` + `test-external-evidence-recovery.mjs` + `test-parent-task-closure-recovery.mjs` + `test-external-fact-grounding.mjs` + `test-answer-evidence-finalizer.mjs` + `test-evidence-risk-tiers.mjs` + `test-evidence-entailment-judge.mjs` + `test-tool-semantics.mjs` + `test-tool-call-rescue.mjs` + `test-opencode-conversation-source.mjs` + `test-platform-intent-eval.mjs` + `test-turn-orchestrator.mjs` — existing deterministic profiles remain as high-precision fast paths, but correctness no longer depends on adding domain terms. The selected model may declare any bounded semantic `claimKinds` plus `externalFact=true`; the host safely activates only a non-operational general turn, preserves every user constraint and host route, and enforces declared `authorityHosts` and `evidenceAnchorGroups` per named/listed entity. Structured list extraction is language- and suffix-independent. A real web/API observation also promotes an initially general turn into a generic external-evidence floor, including failed research attempts, so the final answer cannot fall back to memory after searching. Research first discovers the source owner, changes strategy after low-authority results, verifies whether a requested classification is formal, and delivers a verified result or explicitly partial supported subset. Malformed plans or recovery context retain the strict baseline; local code/document/operation tasks cannot be model-rerouted. A repairable evidence gap gets one changed-strategy retry that inherits only bounded successful read-only external evidence from the immediately completed turn. If a research turn instead dies after tool evidence exists but before the parent answer closes, the durable one-shot parent closure reuses completed searches/reads and asks only for missing evidence and final synthesis, never a wholesale replay. Its internal prompt is hidden, and a durable supersession marker prevents the first pre-gate engine answer from returning after history reload. Unsupported conclusions are replaced with an honest limit. [gate: external-claim-evidence] |
| Image/PDF reading is mistaken for media generation, `识别` is parsed as the negation `别`, failed extraction counts as evidence, or guessed attachment details stream before verification | `test-content-task-intent.mjs` + `test-task-contract.mjs` + `test-send-preflight.mjs` + `test-evidence-ledger.mjs` + `test-evidence-gate.mjs` + `test-answer-evidence-finalizer.mjs` + `test-task-run-policy.mjs` — one semantic content-intent owner resolves operation/source/output before routing; read/recognize/summarize requests use `content_extraction`, while create/modify/convert retain artifact routes. Native vision, bridge recognition, and document extraction emit explicit complete/available/partial/unavailable source coverage; failed reads never satisfy `source_content`. Source answers are buffered until the gate passes, unsupported claims are replaced with an honest read-failure answer, and proven prior-turn source context may continue as available but never masquerades as a fresh complete extraction. Intent failure leaves existing routing and attachments intact. Demo repairs (2026-09-15): `test-content-source-gate.mjs` — 文件夹/文件目录 is a directory, not a document, and a `content_extraction` turn with nothing attached (and nothing inherited) requires a real `file_read` instead of attachment-only `source_content`, so a tool-grounded folder answer is delivered instead of being replaced by the canned read-failure line and its stream is no longer buffered; an actual unread attachment is still refused. `test-attachment-staging-race.mjs` — a dropped file is staged asynchronously, so sending now waits for staging (bounded, fail-open), the send button reflects it, a drag with no disk path gets drag-specific copy, and a failed document extraction reports the extractor's own reason or a named timeout instead of an opaque command failure. `test-paste-keeps-text.mjs` — a path pasted as TEXT stays text: the plain-text clipboard flavour is no longer matched against the disk on the composer paste path, and the pasted characters are always inserted; real file copies still attach through the OS file formats. Second pass (same day): routing that replaces the user text (character/agent authoring, web-system learning) now re-applies every preflight extraction (`turn-preflight-context.js`) instead of discarding the attached document with it; a document that cannot be read states the cause in the user's language (`document-failure-copy.js`: legacy .doc, runtime not ready, timeout, the extractor's own reason) instead of a bare skipped chip; and a permission the mode auto-rejects (plan mode, unattended internal turns) announces itself (`permission-denial-copy.js`) instead of reaching the model as a generic unreadable-path error. 文档内嵌图片识别 (2026-09-15)：`test-document-image-recognition.mjs` 钉住只存在于像素里的内容不再静默丢失——纯图片的 Word/幻灯片不再抽成空、图片按文档顺序落在原段落之间、有文本层的 PDF 页也会读其中的图、扫描件整页 OCR 行为不变；重复出现的图只识别一次并以短引用标注，装饰性小图跳过但在结尾如实汇总；没有 OCR 运行时只降级为"文字 + 一行说明"，绝不给每张图打噪声标记，也绝不让抽取失败。 `test-document-image-vision.mjs` 钉住第二层：配置了视觉识别时，文档内图片的描述**加在 OCR 文本旁边而非取代它**（OCR 对字符权威，视觉解释图意），描述只落在自己那张图的标记之后、不串位，优先识别 OCR 读得最少的图，有每文档上限，并声明描述来自另一个模型；开关关闭、无密钥、桥故障、描述为空这四条路径都原样返回 OCR 文本。 **2026-09-18 现场再修一处变笨路径**:一份 3 MB 的 docx,跑了 23 个步骤、18 段思考,交付却只剩一句拒答模板。终审把「一个字节没读到」和「读到了一部分」当成同一种情况处理——整段替换模型的答案。可部分读取意味着**字节读到了**,门禁自己都标着 `hasEvidence: true`、只因答案没声明范围才判失败——缺的是一句话,不是那份工作。现在三分:一个字节没读到 → 替换;读到部分但答案自称「完整/全部」→ 替换(那是对没人看过的页面的虚假断言);老实只讲读到那部分 → **保留答案 + 追加范围声明**(含已读/总数)。声明措辞按门禁自己的 `PARTIAL_SOURCE_DISCLOSURE_RE` 写,并三语逐条验证——**门禁认不出的声明就不是声明**,中文初版正是栽在这上面。**但那仍只是「更诚实」,不是「读完再答」**:平台对它检测到的每一种可恢复缺口都会续作一轮(待办未完、必需工具未确认、声称的文件不在盘上、模型静默),唯独「附件只读了一半」这个**已检测、可恢复**的缺口没有恢复路径——五个消费方全都只是**描述**它。现在它走**同一条一次性重试路线**(终审置 trigger → `SOURCE_COVERAGE_INCOMPLETE` → 策略 → 提示 → 一轮跟进),不新增第二套机制;判据全部来自**结构化的 observed/total**,与答案措辞无关(第一版正是靠读答案散文猜覆盖率才出错);提示独立成文,因为外部事实那六条(搜权威源、只引打开过的链接、逐项证据映射)对一份已经在盘上的文件毫无意义;这一轮**继承上一轮已读到的证据**,所以它是**接着读**而不是从头读——而这一步第一版是**空操作**:`sanitizeEvidenceTool` 要求工具 replay-safe,可 replay-safe 回答的是「**重跑**一遍安全吗」,而继承根本不重跑任何东西;抽取工具又恰好没被标成 replay-safe(`lily_file_intelligence` 还带一个写索引的动作),于是它们产生的观测在**看 kind 之前**就被全部丢掉,「接着读」实际继承到了 null。现在按**用途**决定继承范围:外部事实那条保持原有严格判定,来源内容这条只按 kind 过滤——允许集合里只有观测类(`file_read`/`file_search`/`tool_observation`/`knowledge_base`),没有 `file_write` 也没有 `command`,所以动作的结果永远进不来、不会让模型误以为这一轮已经写过文件。否则会把已读的 12 个重读一遍,若缺口本就来自硬限制(体积/页数/超时),就会在同一个位置再停一次——白付一轮、最后还是那条范围声明。这一轮走的是**续作**而非**重放**:重放在「本回合不是无副作用」时会被拒,而**读过文档的回合永远不是无副作用**(抽取工具未被标为 replay-safe),所以按重放派发的话,它**对唯一要救的场景永远发不出去**。续作也是更对的形状——模型保留自己已读到的内容,只被要求补上还差的那一步;并且续作仍然携带**它自己的指令**,否则通用续作提示只会说「接着做」,把「去读**剩余**部分」这句唯一有用的话丢掉。只跑一轮、不链式、被打断/失败/无剩余来源/非抽取任务一律不触发,兜底仍是那条范围声明——**用户任何时候都不会两手空空**。kill switch `LILY_SOURCE_COVERAGE_RETRY=0`;回归 `test-source-coverage-recovery.mjs`。抹除分支同时收窄:**否定的整体性词是宣称完整的反面**(「未能完整解析」「没有完整列出」),而**已经自报范围的答案按定义不可能在宣称覆盖全部**——后者直接问门禁自己的 `disclosesPartialSourceScope`,不另留一份模式。只匹配词本身曾把「这份材料未能完整解析,以下只覆盖读到的部分」整段抹掉——**那正是在如实交代本条要抓的那种缺口**,比它要防的编造更糟。 续读回合第四次修（2026-09-19，编排器级闭环 `test-tool-call-rescue.mjs`）：真实预检的覆盖计数单位是**文件数**，一份 30 页 PDF 只读到 12 页时 sourceCount=observedCount=1，续读判定以「没有剩余可读」拒绝——现场那种单文件截断恰恰永远触发不了。截断现在是账本汇总里独立的事实（`truncated`），判定「有剩余」= 有未打开的文件 **或** 已打开的文件被截断；范围备注不再打印 1/1（那等于一边说没读完一边声称读完了），改说「开头部分（内容被截断）」；先前回合带过来的上下文（status=available）不再计入分母（原本会把新附件写成 1/2）。闭环测试驱动真实编排器+终结器+救援表+恢复运行时、只假引擎，断言唯一要紧的效果：引擎收到第二条说「被截断、读剩余」的内部续跑提示，且第一轮答案连同诚实备注已交付。 [gate: attachment-content-grounding] |
| Colloquial software requests such as “写一个牛逼的程序” retain model/tools but miss the coding task contract, so testing, workspace delivery, and truthful completion become optional | `test-program-task-intent.mjs` + `test-task-contract.mjs` + `test-platform-intent-eval.mjs` — a focused programming semantic owner combines action, software target, workspace/code attachments, and quality requirements; only an action+target pair activates `code_change`, so ordinary chat, emails, and “程序员排行” do not become coding tasks. The generic semantic draft carries implement/debug/refactor/optimize/inspect plus answer/artifact/workspace-change output modes; unknown or malformed signals leave the existing classifier and strong model/tool baseline intact. [gate: task-intelligence-baseline] |
| User intent exists only as transient prompt advice, terse continuation loses the prior goal, stale tasks are resurrected across unrelated chat, or `turn.completed` is presented as verified completion without satisfying delivery criteria | `test-intent-contract.mjs` + `test-task-contract.mjs` + `test-turn-orchestrator.mjs` + `test-task-run-policy.mjs` + `test-tool-call-rescue.mjs` + `test-tool-broker-{registry,mcp}.mjs` + `test-session-memory.mjs` + `test-session-runtime-store.mjs` + `test-turn-view-renderer.mjs` + `test-platform-intent-eval.mjs` + `test-capability-readiness.mjs` + `test-turn-dependency-readiness.mjs` — additive Intent Contract v3 keeps a bounded host-owned goal, relation (`new/continue/refine/correct/cancel`), deliverables, success criteria, constraints, and capability needs; exact continuations inherit contract id/objective and increment revision, corrections become explicit amendments, and a later non-task answer clears the inheritance anchor. The same selected model may optionally refine semantics through `lily_intent_contract_commit`, but the host preserves identity/routing and monotonically retains baseline deliverables, criteria, constraints, capabilities, and risk; malformed/absent calls change nothing. This one host-owned idempotent MCP remains replay-safe so it cannot disable evidence recovery; all unknown/external MCP tools remain side-effect guarded. Legacy archives synthesize a compatible contract; malformed history or summary/store read failure falls open to current-turn deterministic classification with the original selected model, tools, context, and user text unchanged. TaskRun keeps legacy `status`/events for old clients while adding persisted/rendered `completionStatus`: required evidence/test gaps are `delivered_unverified`, observed work is distinct, and only verified evidence becomes `verified_complete`. [gate: intent-to-capability-plan] 2026-09-23 补：失败必须说得出原因。`test-turn-failure-visibility.mjs` 钉住回合结束的原因进入**快读路径**而不是只留在事件流里——投影表此前只记「失败了」，导致十几种互不相干的成因（仅法律知识包一处就抛出十余种）在实践中无法区分，用户全部只看到同一句话；同时钉住原有的清理仍然成立：真实结束仍会清掉本回合早先派发恢复留下的过期状态，但不再连原因一起清掉，成功回合不留失败码、未提供则绝不臆造、超长码截断以免撑大每一行、传入畸形数据也绝不打断持久化路径。并钉住支持报告能跨会话按成因归并计数并点名（同一码出现三次以上判为缺陷而非偶发）、七天窗口之外的旧故障不再反复上报、读取历史本身出错时报告照常产出——诊断永远不能成为压垮系统的那一环。 [gate: truthful-completion] |
| Native-vision model toggles are saved but stale config profiles still route images through the weaker bridge path | `test-client-config-service.mjs` — provider metadata `nativeVision:true` and profile `capabilities.vision:true` both expand to preset `capabilities.vision:true`; stale generated `vision:false` cannot suppress the provider's current native-vision capability |
| Background service startup must not weaken OpenCode foreground Bash or leave invisible detached processes | `test-process-jobs-core.mjs` + `test-process-jobs-mcp.mjs` + `test-process-job-protocol.mjs` + `test-mcp-config.mjs` — adds an optional `lily_process_jobs` MCP with PID/log/health/stop; MCP unavailable guidance falls back to normal foreground shell; OpenCode `bash` lifecycle is untouched |
| Long tasks look stuck because the platform cannot show liveness, phase, or output hints | `test-process-job-observability.mjs` + `test-process-jobs-core.mjs` — process jobs expose compatible `status` plus normalized `state`, progress-derived `phase`, `heartbeatAt`, `outputFiles`, `recoverable`, logs, and existing PID/log paths without changing foreground tool behavior |
| Long work is lost, duplicated, crosses conversations, attaches to a reused PID, fills disk, or never resumes after restart/sleep | `test-long-task-store.mjs` + `test-long-task-scope.mjs` + `test-durable-process-jobs.mjs` + `test-process-job-hardening.mjs` + `test-long-turn-virtual-clock.mjs` + `test-long-task-recovery.mjs` + `test-long-task-wakeup.mjs` + `test-long-task-session-wakeup.mjs` + `test-long-task-resource-policy.mjs` + `test-long-task-migration.mjs` — scoped HMAC capabilities, SQLite CAS/fencing, nonce heartbeats, process-tree stop, progress leases, durable exactly-once continuation, owner/session isolation, quotas, retention and conservative legacy migration are mandatory; ordinary foreground tools fail open when the optional job service is unavailable |
| Transient engine liveness snapshots are rendered as heavy floating cards, making "still running" UI visually louder than completed tool rows — and stale "still running" lines persist after tools finish, even in saved history | `test-engine-notice-policy.mjs` + `test-turn-renderable-timeline.mjs` + `test-turn-notice-entry.mjs` + `test-turn-process-timeline-model.mjs` + `test-turn-live-clock-patch.mjs` — `longWait`/`toolProgress` keep the lightweight `is-liveness` style but render only WHILE TRUE: `toolProgress` needs a running tool, `longWait` must be the newest timeline entry, and neither reaches the sealed transcript; the heartbeat tick is a surgical patch (status line + `patchLiveToolClocks` text updates on running rows), never a full-article re-render, so elapsed time stays out of the visual signature |
| Foreground upload/download commands look dead or require feature-specific UI — or shell discard sinks such as Windows `NUL` are misrendered and persisted as fake `Download: nul` cards | `test-work-progress-protocol.mjs` + `test-opencode-runtime-reducer.mjs` + `test-turn-process-timeline-model.mjs` + `work-progress-notice-regression` in `test-renderer-import.cjs` — generic progress inference emits optional `workProgress` notices for recognizable transfer output/commands; real discard sinks normalize to no artifact path and fall back to today's tool row without changing command execution; ordinary filenames such as POSIX `null`, real downloads, and structured/live progress remain visible; sealed legacy discard-sink notices are hidden by an exact, reversible read-time compatibility guard |
| Model traffic may silently bypass Lily Gateway or be impossible to audit | `test-model-route-audit.mjs` + `test-opencode-model-config.mjs` — final resolved `LILY_*` env is classified as gateway/direct/invalid with redacted URL/key-kind diagnostics; route audit is attached to OpenCode config diagnostics and runner diagnostics without changing routing |
| Model grading or recovery makes the selected model less capable, repeats side effects, loses the current turn, or lets a lite model stop before verified delivery | `test-capability-grading.mjs` + `test-model-compatibility-probe.mjs` + `test-model-self-heal.mjs` + `test-tool-call-rescue.mjs` + `test-subtask-resilience.mjs` + `test-opencode-agent-session.mjs` + `test-context-budget-manager.mjs` + `test-session-memory.mjs` + `test-mcp-config.mjs` + `test-opencode-message-parts.mjs` + `test-model-eval-policy.mjs` — destructive lite behavior requires confirmed repeated evidence; full/standard remain byte-identical; lite support retains the strong-default executable MCP surface and only adds guidance/verified compatibility recipes; recovery is current-turn anchored and side-effect guarded; retained-context pressure includes history plus the new turn; the lite execution protocol survives measured prompt limits; shared broker context cannot leak a conversation; evals require a baseline and Lily persona |
| OpenCode protocol selection falls back to URL guessing and loses tool calls on OpenAI-compatible providers | `test-opencode-model-config.mjs` + `test-client-config-service.mjs` + `test-model-settings-secret-storage.mjs` + `test-scheduled-tasks.mjs` — service presets explicitly deliver OpenCode provider id/npm/base/protocol; customer custom models/gateways persist protocol; legacy URL heuristics are migration fallback only |
| A valid managed model ID is silently rewritten to another model, so the UI selection and billed upstream model disagree | `test-opencode-model-config.mjs` + `test-model-gateway-openai-adapter.mjs` — protocol normalization may remove only a protocol-invalid legacy suffix; official IDs such as `deepseek-v4-flash` survive admin storage, client OpenCode config, and gateway forwarding unchanged |
| Edge service DNS/routing failure silently makes remote config fail and leaves the model on direct fallback | `test-service-client-edge-fallback.mjs` — UAE edge network failure falls back to the canonical domestic service for bootstrap and signed service calls, pins the runtime service base to the reachable host, and keeps the agent route auditable |
| OpenCode resume id crosses Lily conversations, causing one chat to inherit another chat's history, tools, or skills | `test-resume-binding.mjs` + `test-resume-continuity-guard.mjs` + `test-ensure-session-runner-resume-reset.mjs` — every new resume id is bound to the Lily session/project/workspace/skill-set/version/first-user fingerprint; identity mismatches and unapproved version changes clear the resume and start fresh with local-history rehydration. The explicit 1.18.29 to 1.18.30 forward upgrade preserves an otherwise valid binding; legacy unbound resumes fail open to a recent-history continuity guard instead of silently trusting the engine cache |
| Recoverable model/file failures poison one Lily conversation so the user must create a new chat to recover | `test-opencode-agent-session.mjs` + `test-turn-orchestrator.mjs` — safe failures replay inside the same turn; unsafe/resumed/attachment-linked visible failures invalidate only the bottom engine session, clear the persisted resume id, terminate that runner view, and keep the Lily chat/history intact so the next message starts clean in the same conversation |
| Screenshot/image turns leak raw base64 to a non-vision model, or model-side 413 is exposed to the user instead of being compacted/replayed | `test-opencode-message-parts.mjs` + `test-opencode-server-manager.mjs` + `test-opencode-agent-session.mjs` — image file parts require explicit native-vision opt-in; non-native image sends stay text/path/vision-result only; `Request Entity Too Large` at dispatch or model-event time isolates a fresh engine session and replays the current request without a sticky visible failure |
| Malformed or empty OpenCode completions are treated as completed answers and then poison summaries or compaction memory | `test-turn-error-classify.mjs` + `test-turn-orchestrator.mjs` + `test-session-memory.mjs` + `test-compaction-memory-export.mjs` + `test-remote-config-gateway-token-expiry.mjs` — leaked `<tool_call>/<function>/<parameter>` fragments and empty assistant completions become retryable protocol failures, are never stored as assistant memory, are never injected back into compaction memory, and expired Lily gateway tokens invalidate remote config so sends refresh instead of running on stale credentials |
| An interrupted historical turn already persisted an empty assistant message, so a strict OpenAI-compatible provider rejects every later request before the model or tools run and Retry can never recover | `test-empty-assistant-history-guard.mjs` — the engine plugin `empty-assistant-history-guard.js` repairs truly provider-empty assistant rows in `experimental.chat.messages.transform` before both ordinary and compaction model calls by adding one bounded neutral text marker; message ids/order and valid text remain byte-identical, tool-only messages remain untouched so call/result pairing is preserved, the repair is idempotent, and failures or `LILY_EMPTY_ASSISTANT_HISTORY_GUARD=0` retain baseline behavior [gate: opencode-history-compatibility] |
| Stale, malformed, or placeholder Lily managed gateway tokens make default models fail with 401 while user BYOK direct models still work | `test-opencode-agent-session.mjs` + `test-turn-error-classify.mjs` + `test-remote-config-gateway-token-expiry.mjs` — exact `MODEL_GATEWAY_TOKEN_INVALID/EXPIRED` and gateway-routed 401 refresh remote config, rebuild the runner env, restart the engine session, and replay once; `$LILY_GATEWAY_TOKEN` placeholders and malformed `lilygw.*` cache values force send-preflight refresh; generic BYOK 401 remains a visible API-key auth error |
| Skill presets eagerly install every heavyweight optional dependency before the user asks for that capability | `test-skill-presets.mjs` + `test-runtime-pack-preflight.mjs` — presets only select skills; task text/files decide required versus enhancing packs, and ordinary PDF work keeps heavy engines optional |
| A runtime pack installs successfully but the current runner/MCP keeps its stale environment, so the model cannot use the new capability | `test-runtime-pack-coordinator.mjs` + `test-runtime-health.mjs` + `test-runner-live-config.mjs` — installs are deduplicated and health-checked before activation; an idle pre-dispatch runner is refreshed only after a verified install |
| Dependency preparation replays the user request or dispatches it twice, duplicating side effects | `test-turn-dependency-readiness.mjs` + `test-turn-orchestrator.mjs` — user input is admitted and committed first, preparation runs before runner creation, then exactly one engine payload is sent; planner failure keeps the baseline path |
| A transient network failure restarts a large dependency download from zero or fills the disk | `test-runtime-pack-download.mjs` — stable `.part` files resume with Range, bounded retry handles transient failures, ignored Range restarts safely, and disk headroom is checked before network transfer |
| A downloaded artifact is marked installed before its imports/binaries/browser can actually run | `test-runtime-health.mjs` + `test-runtime-pack-installer.mjs` — known packs are probed in staging before atomic activation; failed health checks preserve the previous version and never write healthy installed state |
| The catalog/release advertises an optional capability for a platform whose artifact was never verified there | `test-runtime-pack-release-matrix.mjs` + `test-runtime-release-policy.mjs` — build publishers write the lock only after same-target probes; formal release preflight is strict across darwin-arm64, darwin-x64, and win32-x64, and one-click upload also compares production metadata |
| Dependency assistance makes a lite model lose executable surfaces or changes the strong model's base config/context | `test-capability-grading.mjs` + `test-mcp-config.mjs` + `test-turn-dependency-readiness.mjs` — lite retains the complete executable MCP surface and receives additive recipes; standard/full remain on the strong baseline, while readiness adds only bounded task context and fails open to baseline |
| Mobile Command malformed, unsupported-version, or oversized remote frames reach local admission and mutate a Lily session | `test-mobile-protocol-version.mjs` + `test-mobile-agent-bridge.mjs` — unsupported protocol versions, oversized text, and too many attachments are rejected before `admitExternalCommand`; local Lily chat remains the strong default, and remote failure cannot create a queued turn |
| Mobile Command phone can only target the desktop foreground chat, or a selected mobile target steals/changes the desktop active session | `test-mobile-agent-bridge.mjs` + `test-mobile-pair-web.mjs` + `test-mobile-pairing-wiring.mjs` — phone requests a bounded current-project session list, selects a mobile target session, sends `lilySessionId` on commands, and desktop validates the selected session while falling back to the active session without changing desktop UI focus |
| Mobile Command voice entry is advertised as production/native ASR or silently fails on unsupported browsers | `test-mobile-pair-web.mjs` — phone exposes only browser dictation as text-entry convenience through `SpeechRecognition`/`webkitSpeechRecognition`, logs unsupported/permission failures visibly, and keeps production voice/ASR gated |
| A huge pasted/typed message (a data dump, e.g. 100 texts / a big log) is sent verbatim into the model context instead of being persisted and retrieved from — bloating history and the window at the source (the top-tier pattern is ingest→workspace→retrieve, not dump) | `test-large-input-staging.mjs` — `large-input-staging.stageLargeInputText` (wired into `opencode-server-manager.sendPrompt` before the prompt body) stages an oversized message to a content-addressed workspace file (`.lily-work/inbox/`) and replaces the ENGINE text with a compact directive + head/tail preview + path that routes the model to the existing `lily_file_intelligence` (inspect/index/query) for selective retrieval; the FULL input is preserved on disk (nothing lost) and Lily's own message store keeps the user's original text (UI unchanged); only triggers above a high char threshold so normal messages pass through untouched (never dumber), fail-open leaves the text unchanged, kill switch `LILY_LARGE_INPUT_STAGE=0` |
| A data-heavy session grows past the model's context window and deadlocks — too big to run AND too big to self-compact (compaction must send the whole head to the summarizer, which also overflows), so the engine retries build/compaction in a loop that keeps appending and growing; the worst bloat is a `write`/`edit` tool INPUT holding a whole large file, which the engine's `truncateToolOutput` never trims (input, and build-path output, are untouched) | `test-context-window-guard.mjs` — the engine plugin `context-window-guard.js` on `experimental.chat.messages.transform` bounds oversized part CONTENT (tool input/output, text/reasoning) to a per-part cap and, if the whole request still exceeds a token budget, tightens largest-first until it fits — before BOTH the turn and compaction model calls, so compaction can finally shrink and the loop breaks; the budget is MODEL-AWARE (session-runner-pool derives `LILY_CONTEXT_TOKEN_BUDGET` from the active model's real context window via `resolveContextBudget` × 0.85 headroom, scaling 32k→1M) so a big model is never over-trimmed and a small one never overflows; whole messages and tool-call/result PAIRING are never dropped, content that fits the real window passes through byte-identical (never dumber), and it is fail-open with a kill switch (`LILY_CONTEXT_GUARD=0`) and a conservative fallback budget when the window is unknown |
| A non-image attachment (JSON/XML/data file) is sent as a raw file part to a model whose provider only accepts image file parts, so the engine's AI SDK throws `AI_UnsupportedFunctionalityError` pre-gateway and the whole turn/compaction fails — worst on unknown custom/BYOK models, and once such a part is in a session's history every later turn AND compaction stays stuck | `test-opencode-message-parts.mjs` + `test-opencode-server-manager.mjs` + `test-turn-error-classify.mjs` + `test-filepart-text-coercion.mjs` — three fail-open layers: (1) client file-part eligibility is capability-driven (`filePartMimeAllowed` reads the preset's declared `capabilities.filePartMimes`), default DENY for every non-image mime, text-like files inline as fenced text and oversized/unknown ones fall back to a source-path note; (2) the engine plugin `filepart-text-coercion.js` on `experimental.chat.messages.transform` coerces every non-media file part (image/pdf left alone) to inline text just before both the turn and compaction model calls, so ALREADY-poisoned sessions self-heal model-agnostically and nothing is lost; (3) the unsupported-file-part error is classified as `ATTACHMENT_UNSUPPORTED` (non-retryable, actionable) instead of being relabeled a dead model or a network drop. All three default-safe with kill switches (`capabilities.filePartMimes`/`LILY_FILEPART_COERCE=0`) and degrade to baseline text/path, never a raw file part |
| Intent, routing, prompt, tool-safety, runtime, skill installation, or completion behavior changes in separate files and silently drifts; oversized coordinators keep accumulating responsibilities until fixes become risky | `test-capability-gate-registry.mjs` + `test-architecture-boundaries.mjs` + `test-context-budget-manager.mjs` + `test-external-command-runtime.mjs` + `test-opencode-agent-session.mjs` + `test-opencode-session-policies.mjs` + `test-skill-catalog.mjs` + `test-skill-guide-sync.mjs` + `test-subagent-runtime-projection.mjs` + `test-task-run-kernel.mjs` + `test-turn-runtime-event-router.mjs` + `test-platform-intent-eval.mjs` + `run-capability-gate.mjs` - machine-readable capability ownership records each baseline, fail-open route, and guard test; the architecture manifest ratchets every existing source hotspot so it may shrink but not grow, rejects new source files above 500 lines, prevents main/renderer cross-imports, enforces intent-before-readiness, preserves one source of truth for tool semantics and runtime contracts, and keeps mobile admission, context compaction, runtime event projection, terminal finalization, subagent projection, TaskRun state transitions, guarded turn recovery, OpenCode liveness/history/failure policy, and bundled-skill synchronization out of their coordinators. The multi-turn golden set exercises production intent, policy, and capability-planning code, including continuation/refinement/correction, stale-task breaks, rankings, optional PDF enhancements, and explicit runtime needs. [gate: architecture-ratchet] |

| Generated/edited Office or PDF files are called polished or verified after merely being written, an optional Node generator is missing even though the bundled Python route exists, a QA retry replays file-authoring side effects, or CJK output loses its font | `test-document-delivery-gate.mjs` + `test-turn-artifacts.mjs` + `test-tool-call-rescue.mjs` + `test-document-fonts.mjs` + `test-office-authoring-contract.mjs` — document creation requires a real output file; verification additionally requires deterministic reopen, render, and actual page-image inspection (all pages up to 12, first/last plus at least 6 for longer files). Missing QA triggers at most one path-scoped continuation that inherits the existing artifact and never replays the original authoring request. Word/PPT/Excel/PDF scratch authoring stays on the bundled Python stack, direct PDF drawing receives a discovered CJK font path, and any failure preserves the file as `delivered_unverified` instead of making a false quality claim. [gate: office-document-delivery] |

| A model writes the substantive Markdown deliverable, calls TodoWrite only to mark the task complete, then emits a short acknowledgement; sealing the turn treats that acknowledgement as the answer and collapses the real report into process history | `test-turn-terminal-narrative.mjs` - a substantial report separated from the closing text only by TodoWrite bookkeeping is promoted into one Markdown-safe terminal narrative. Evidence-changing tools never promote earlier prose; short progress and malformed timelines fail open to the current behavior. The model output, tool execution, and live stream remain untouched. |

| Publishing one Windows or Mac build for an already-current universal version rewrites the signed static `latest.json` with only that platform, a slow older release overwrites a newer concurrent release after its large upload finishes, or a Windows installer URL contains spaces rejected by Microsoft Store | `test-release-admin.mjs` + `test-release-version-guard.mjs` + `test-release-artifact-naming.mjs` - a single-platform publish can merge only a same-version, signature-verified base manifest; the new artifact replaces its platform while untouched platforms remain. Invalid signatures and mismatched versions fail loud. `release-one-click.mjs` retrieves that base and checks remote version monotonicity both before artifact publication and immediately before moving latest pointers, so an older release can never replace a newer one. Windows NSIS, immutable release, and auto-update artifacts share the space-free `LilyWorkbench-<version>-x64` name while macOS keeps its existing product name. |

| Character card parsing, session binding, world-book activation, or context compilation changes a native turn, crosses conversations, displaces Lily guidance, exposes private card data, or lets imported executable behavior run | `test-character-worlds-store.mjs` + `test-character-card-parser.mjs` + `test-character-binding-isolation.mjs` + `test-character-context-injection.mjs` + `test-character-worlds-capability-gate.mjs` + `test-character-worlds-concurrency-stress.mjs` + `test-character-worlds-policy.mjs` + `test-character-world-book-store.mjs` + `test-character-world-book-import.mjs` + `test-character-world-book-activation.mjs` + `test-character-world-book-compile.mjs` + `test-character-world-book-decorators.mjs` + `test-character-world-book-ipc.mjs` + `test-character-persona-store.mjs` + `test-character-persona-context.mjs` + `test-character-authoring.mjs` + `test-character-authoring-ipc.mjs` + `test-character-switch-events.mjs` + `test-character-agent-draft.mjs` + `test-character-workspace-portability.mjs` + `test-character-compaction-isolation.mjs` + `test-character-card-fuzz.mjs` + `test-character-worlds-performance.mjs` + `test-character-agent-task-parity.mjs` + `test-character-scene-memory.mjs` + `test-character-group-modes.mjs` + `test-character-group-compile.mjs` + `test-memory-index-generations.mjs` + `test-character-library.cjs` — native conversations send byte-equivalent Lily/OpenCode prompt bodies with no card parsing or extra model request; bindings are local, optional, immutable-revision pinned, conversation/owner-scoped, and snapshotted in the same SQLite transaction as turn admission, so retries, recovery, queue promotion, and steer inherit by trusted source turn id and never re-read the latest binding; compiled character context is a bounded lower-authority suffix of the per-request system field after the protected Lily prefix — never user text, visible history, or file parts — separately budgeted with no guaranteed minimum; every failure mode (disabled policy, missing binding, missing revision, parser/macro/compiler error, over-budget, provider without safe system context) fails open to the byte-identical native turn with exactly one dispatch and unchanged tools, skill IDs, model, permission mode, files, evidence context, subagent surface, user text, and output reserve; embedded world books import bounded and inert, activation is a deterministic pure resolver over the admitted snapshot plus the durable timed checkpoint, activated entries render only inside the same lower-authority envelope, and every world-book failure mode (missing or corrupt book revision, resolver error, over-budget world content) fails open to the byte-identical no-book compiled body of the same bound character with a single dispatch; world-book timed checkpoints persist only after successful turn finalization, never advance on failed turns, and rewind purges them; world-book IPC is read-only with whitelisted summaries (no raw book content or mutation surface crosses the bridge); personas are local, owner-scoped, immutable-revision pinned, narrative-only (never account/authorization state), and compile into the same lower-authority envelope in the §10.3.1 slot, and every persona failure mode (missing, corrupt, or over-budget pinned persona revision) fails open to the byte-identical no-persona compiled body of the same bound character+book with a single dispatch; every authoring mutation (create/edit/history/restore/duplicate/archive) flows through one validated owner-scoped domain API that creates new immutable revisions, so an already-admitted turn keeps its pinned snapshot and byte-identical compiled body after any edit and update-available requires an explicit apply; imported scripts/plugins/regex scripts/unknown macros never execute and unknown card data is preserved inertly; `LILY_CHARACTER_WORLDS=0` and the signed rollout policy restore native behavior without touching stored data; an agent draft (tool disabled, kill switch, hostile or over-cap draft input, missing authoring service, malformed injected context block) fails closed to a coded error with NO durable write and the native turn stays byte-equivalent; a workspace pack import with a missing referenced revision or hostile character-worlds section degrades to a native conversation + diagnostic and never leaves a half-imported library (entities import only through validated writers with canonical-hash dedup; bindings restore only when their referenced revisions imported) [gate: character-worlds-isolation] |

| Unknown tools or shell commands are replayed automatically after a failure, or retry, rescue, evidence, and TaskRun code disagree about side effects and replay safety | `test-tool-semantics.mjs` + `test-tool-call-rescue.mjs` + `test-tool-broker-registry.mjs` — unknown tools and shell commands are never replayed automatically; unknown semantics are treated as external-side-effectful and not replay-safe, so a failed turn is recovered by nudging the model or asking the user, never by silent re-execution [gate: tool-semantics-single-source] |

| Main, persistence, and renderer drift apart on the versioned RuntimeEvent and TaskRun contracts, so a producer writes an event or TaskRun field a consumer silently cannot read | `test-runtime-contract-manifest.mjs` + `test-runtime-event-schema.mjs` + `test-session-runtime-store.mjs` — existing event names and TaskRun fields stay compatible; unsupported versions and malformed typed producer payloads are rejected before persistence or rendering [gate: runtime-contract] |

| A selected character reaches the model only as inert narrative data, the model denies the active role, character layers exceed the provider system budget, or the UI reports a selected/bypassed role as active | `test-character-role-activation-contract.mjs` + `test-character-context-compiler.mjs` + `test-character-context-injection.mjs` + `test-character-agent-task-parity.mjs` + `test-opencode-agent-session.mjs` + `test-runtime-event-schema.mjs` + `test-session-runtime-store.mjs` + `test-character-session-control.mjs` — the host compiles a fingerprint-bound trusted activation contract separately from lower-authority narrative data, applies it only after the protected Lily kernel at the final OpenCode request boundary, and keeps tools, permissions, safety, evidence, exact facts/code/paths/numbers/citations, task rigor, and requested formats outside role style. Invalid, unsupported, compile-failed, or over-budget role layers fail open to byte-identical native Lily and emit a bounded metadata-only `bypassed` receipt; only final-boundary `applied` evidence may render as active, scoped by session, turn, and immutable revision. Lily-authored character drafts explicitly self-check the complete quality contract instead of saving a thin name-only role [gate: character-role-activation] |
| Character receipt or preview controls render visibly but an active conversation layer intercepts clicks, or an IPC failure leaves a silent dead button | `test-character-result-card.cjs` + `test-character-preview-controller.cjs` — the preview banner stays above the active session hit-testing layer while reserving message space; activate/exit actions expose busy state, refresh versioned preview state after failure, show a retryable error, and retain the active preview until an explicit action succeeds [gate: character-preview-controls] |

| Internal prompt layers (rehydration, memory, advisories, capability context) crowd out or silently truncate the original user request | `test-engine-message-layers.mjs` — normal-size prompt layers and the full visible user message remain byte-identical; only oversized internal layers are bounded, and explicit truncation diagnostics are preserved [gate: prompt-envelope] |

| AI character/persona/world-book creation reports a Markdown, JSON, or workspace file as a created library entity, loses its persistence requirement while queued/retried, or keeps using a stale disabled MCP policy after rollout refresh | `test-character-authoring-intent.mjs` + `test-character-authoring-availability.mjs` + `test-required-tool-completion.mjs` + `test-character-agent-draft.mjs` + `test-opencode-agent-session.mjs` + `test-character-binding-isolation.mjs` + `test-official-character-catalog.mjs` + `test-official-character-picker.mjs` + `test-character-worlds-ipc.mjs` — creation from the library carries an explicit language-independent entity kind; natural-language creation is conservatively inferred; the requirement survives durable queue recovery and manual retry; a disabled policy gets one managed config refresh before failing explicitly; MCP policy changes recycle the idle serve while resuming the same conversation; only `lily_character_draft` with `ok:true` satisfies completion; failed persistence gets bounded correction and then an explicit not-saved result; character, persona, and world-book drafts all use the validated immutable library; official identity comes from trusted versioned source metadata and installation is idempotent. Non-authoring/ambiguous requests keep native Lily behavior [gate: character-authoring-persistence] |

| A shared OpenCode serve or MCP broker borrows the focused/previous conversation's identity, skills, workspace, permission mode, or owner authority | `test-runtime-identity.mjs` + `test-runtime-identity-plugin.mjs` + `test-tool-broker-runtime-identity.mjs` + `test-opencode-runtime-identity-wiring.mjs` + `test-agent-runtime-isolation-stress.mjs` — every broker call carries a host-injected HMAC-signed, expiring, audience-bound runtime identity tied to principal/workspace/project/session/turn/task/agent/attempt; the broker strips the token before business handlers and derives visibility only from verified claims; termination/recycle revokes the grant; missing identity remains explicit platform-only; `LILY_RUNTIME_IDENTITY_V1=0` restores the prior route [gate: request-scoped-runtime-identity] |

| Two workers claim one subtask, a crashed worker keeps a permanent lease, a live multi-day worker is orphaned, dependencies unlock early, retries replay unsafe work, or one session reads another agent mailbox | `test-agent-task-graph.mjs` + `test-agent-task-graph-store.mjs` + `test-agent-runtime-isolation-stress.mjs` — TaskRun projects to a durable DAG with revision-CAS claims, rolling attempt-bound lease renewal, explicit replay safety, deterministic dependency release/cancellation, orphan handling, and session-scoped acknowledged mailboxes; no global single-run lock is introduced; `LILY_AGENT_TASK_GRAPH=0` preserves the current TaskRun path [gate: durable-agent-task-graph] |

| A partial/corrupt checkpoint is presented as restorable, restore mutates state before integrity validation, fork changes the source session, or an external side effect is silently replayed | `test-runtime-checkpoint.mjs` + `test-runtime-checkpoint-store.mjs` + `test-runtime-checkpoint-service.mjs` + `test-agent-runtime-recovery.mjs` — canonical manifests hash every component; prepare/commit is atomic and idempotent; restore accepts committed manifests only, validates and durably stores tracked-file components, captures the real pre-restore state, rolls files back if later adapters fail, and exposes non-reversible effects as unresolved; fork records immutable lineage and copies immutable component data; `LILY_UNIFIED_CHECKPOINTS=0` leaves existing OpenCode revert/diff behavior unchanged [gate: unified-runtime-checkpoint] |

| Headless automation depends on renderer internals, emits an unstable stream, dies when a client disconnects, cannot resume/fork/cancel, or silently changes the legacy JSON format | `test-headless-runner.mjs` + `test-lily-sdk.mjs` + `test-agent-runtime-control-server.mjs` — the `lily` executable keeps legacy `--json`, adds versioned NDJSON `--stream-json`, session/cursor resume, fork and timeout cancellation, and publishes a renderer-free Node SDK; a mode-0600 loopback control plane reuses Lily's durable admission/event store while dedicated tool-policy runs retain the legacy engine fallback [gate: headless-agent-protocol] |

| A public lifecycle hook recursively invokes itself, leaks secrets into audit, hangs a turn forever, bypasses a native-tool denial, mutates identity/authority, or fails with the wrong security policy | `test-public-hooks.mjs` + `test-public-hook-persistence.mjs` + `test-public-hook-bridge.mjs` + `test-turn-orchestrator.mjs` — hook declarations explicitly bound event/type/input schema/timeout/authority/failure policy; payloads and audits redact secret keys; recursion is rejected; observation failures fail open while security failures fail closed; mutable output is restricted to allow/reason/bounded context; command hooks use absolute argv with no shell or credential environment, HTTP hooks require an explicit origin allowlist, and a signed request-scoped loopback bridge enforces native tool and compaction hooks; declarations reload from local atomic config and audits are durable; `LILY_PUBLIC_HOOKS_V1=0` bypasses public hooks [gate: public-agent-hooks] |

| Auto routing guesses quality, changes a saved model's identity, crosses task/draft ownership, bypasses revocation, or loses background work and usage | `test-model-selection.mjs` + `test-model-selection-ipc.mjs` + `test-model-picker-state.mjs` + `test-model-execution.mjs` + `test-model-execution-runner.mjs` + `test-usage-reporter-models.mjs` + `test-opencode-shared-server.mjs` + `test-context-window-guard.mjs` + `test-turn-queue-recovery-task-core.mjs` + `test-turn-terminal-cas-recovery.mjs` + `test-renderer-import.cjs` + `test-turn-archive.mjs` + `test-model-catalog-lifecycle.mjs` + `test-model-identity-migration.mjs` + `test-model-ownership-steer.mjs` + `test-model-ownership-composer.mjs` + `test-opencode-runtime-ownership.mjs` + `test-opencode-bundled-usage.mjs` + `test-usage-provider-persistence.mjs` + `test-long-task-session-wakeup.mjs` - recommended Auto preserves the strong baseline when metadata is absent, never when a valid catalog explicitly revokes models. Stale catalogs refresh once; manual/custom pools never widen. Stable model/connection identities are independent of defaults; legacy selections migrate only with matching evidence. Steer claims precede preflight, failed drafts return only to their unchanged originating session, and background recovery inherits its source turn. Private profiles drive main/helpers, budgets and usage; work leases survive view replacement, compatible sessions share without sharing credentials, and invalidated runners cannot restart. Owned child/compaction events contribute deduplicated provider/model usage through local and server persistence. Public metadata excludes secrets; bulk staging and context maintenance remain available. `test-model-pin-override.mjs` — a retry or continuation replays the SOURCE turn's model so continued work stays on the model that produced it, but a manual pick made AFTER that turn overrules the pin: switching away from a failing model and pressing 重试 must not run on the failing model again (2026-09-16). An automatic selection, a re-pick of the same model, and an unreadable selection store all keep the pin. [gate: auto-model-selection] |
| A packaged app reports installed local software as missing because Finder/Explorer did not provide an interactive-shell PATH, or fixing discovery leaks host credentials / admits relative paths | `test-executable-paths.mjs` + `test-spawn-env-software-discovery.mjs` + `test-spawn-env-allowlist.mjs` — Lily keeps bundled runtimes and verified packs first, then adds existing absolute platform, installer, inherited, and bounded login-shell executable directories. Relative/missing/duplicate/oversized entries are rejected; login-shell discovery receives only platform basics and never host credentials or loader hooks. Failure falls back to known system paths. [gate: local-software-discovery] |

| Optional collaboration disables AI work, crosses accounts after refresh/shutdown, fabricates delivery after timeouts, drops old history, or exposes corrupted decrypted files | `test-collaboration-startup.mjs` + `test-collaboration-store.mjs` + `test-collaboration-sync-engine.mjs` + `test-collaboration-service-lifecycle.mjs` + `test-collaboration-read-ack.mjs` + `test-collaboration-read-checkpoint.mjs` + `test-collaboration-read-recovery.mjs` + `test-collaboration-activity-projection.mjs` + `test-collaboration-message-mutations.mjs` + `test-collaboration-mutation-transport.mjs` + `test-collaboration-message-operations.mjs` + `test-collaboration-message-operation-ipc.mjs` + `test-collaboration-message-operation-recovery.mjs` + `test-collaboration-operation-evidence.mjs` + `test-collaboration-create-intent.mjs` + `test-collaboration-create-ack.mjs` + `test-collaboration-create-receipt.mjs` + `test-collaboration-receipt-view.mjs` + `test-collaboration-mention-candidates.mjs` + `test-collaboration-reply-snapshot.mjs` + `test-collaboration-reply-snapshot-input.mjs` + `test-collaboration-reply-cache.mjs` + `test-collaboration-reply-cache-service.mjs` + `test-collaboration-message-time.mjs` + `test-collaboration-workspace-package.mjs` + `test-collaboration-unknown-recovery.mjs` + `test-collaboration-history-pagination.mjs` + `test-collaboration-history-roundtrip.mjs` + `test-collaboration-history-view.mjs` + `test-collaboration-target-history.mjs` + `test-collaboration-visible-history.mjs` + `test-collaboration-visible-history-service.mjs` + `test-collaboration-direct-projection.mjs` + `test-collaboration-recovery-integrity.mjs` + `test-collaboration-ipc.mjs` + `test-collaboration-directory-refresh.mjs` + `test-collaboration-transfer-runtime.mjs` + `test-collaboration-save-transfer.mjs` + `test-collaboration-transfer-service.mjs` + `test-collaboration-attachment-send.mjs` + `test-collaboration-transfer-retirement.mjs` + `test-collaboration-transfer-ipc.mjs` + `test-collaboration-attachments-ui.cjs` + `test-collaboration-attachment-locales.mjs` + `test-collaboration-timeline.cjs` + `test-collaboration-reply-ui.cjs` + `test-collaboration-reply-navigation.cjs` + `test-collaboration-reply-locales.mjs` + `test-collaboration-mentions-ui.cjs` + `test-collaboration-mention-navigation.cjs` + `test-collaboration-mentions-locales.mjs` + `test-collaboration-encryption.mjs` + `test-account-resilience.mjs` — feature-off preserves the workbench, SQLite is isolated, account identity and stopped continuations are fenced, uncertain delivery retains its original durable key, and typed edits/revokes require validated commit evidence without changing message creation order. Authorized history uses explicit pages and conditional revision checkpoints; encrypted container output is authenticated before publication, read ACKs use only the validated server watermark, and collaboration workspace imports enforce bounded archives and isolated new targets while ordinary imports retain their baseline. Live signed-route/PostgreSQL and private-bucket acceptance remain separate release requirements. [gate: collaboration-local-first-reliability] |

| An unsent message edit leaks plaintext, crosses accounts, overwrites a newer window generation, or survives access revocation | `test-collaboration-edit-draft.mjs` + `test-collaboration-edit-draft-ipc.mjs` — edit drafts are separate from composer drafts, encrypted per account/conversation/message, generation-CAS protected, restart durable, strictly bounded at IPC, fenced after stop/service replacement/revocation, and transactionally deleted when access is removed. Inline edit/revoke UI remains a separate acceptance slice. [gate: collaboration-edit-draft-foundation] |
| Collaboration replaces or crushes the primary workbench, starts open unexpectedly, or loses drafts while adapting to a narrow window | `test-collaboration-right-panel.cjs` — Collaboration is a separate default-collapsed right panel, reserves no width while closed, uses docked/overlay shell modes, presents one IM level at a time, and removes the legacy three-column center replacement. Existing renderer collaboration tests preserve drafts, recovery, revocation and unchanged-workbench fallback behavior. [gate: collaboration-right-panel] |
| base.css declares a type scale and a radius scale, and a whole-app audit found them used 26 and 159 times against ~500 literal font sizes (30 distinct values, 25 below 11px) and ~250 literal radii (40 distinct values) — so the "字体大小：大" setting reached almost nothing and every feature was hand-tuned, which is what "looks generated" measures as | `test-ui-style-scale.mjs` — HARD rule: a literal that is a scale value must be the token (value-preserving, so never a reason not to; migration verified by a computed-style fingerprint of every settings page, identical at default size, 916/950 elements now scale in large mode). RATCHET: off-scale sizes, sub-11px text, weights outside 400/500/600/700, literal shadows and magic z-index counted per file in `ui-style-scale-baseline.json`, may only go down; regenerate with `--write-baseline` only after lowering a count deliberately [gate: ui-style-scale] |
| The every-turn agent guide is the ONLY channel that tells the model which skills exist (the engine's native skill loading is unused — `_opencodeSkillPaths` returns `[]`), and its skill index is built last from the bytes the fixed prefix leaves over, so growing the always-inlined prefix silently deletes skills from the model's view. Measured 2026-09-04: Arabic with the full catalog already sits at 41976B, 85.4% of the 49152B budget, leaving room for ~30 more skills | `test-agent-guide-headroom.mjs` — builds the real guide for bundled and bundled+full-catalog across zh-CN/en/ar and asserts (1) zero skills dropped for budget and zero left undescribed, both previously silent, (2) under a 95% watermark, (3) a per-configuration byte RATCHET in `agent-guide-headroom-baseline.json`, regenerated only on purpose with `--write-baseline`. The prior assertion checked only `bytes <= AGENT_GUIDE_MAX_BYTES`, which the builder guarantees by amputating the index, so it could not fail however many skills were discarded. `test-skill-capacity-notice.mjs` covers the user-facing half: the runtime measurement writes nothing and reports `null` rather than `Infinity` with no skill indexed, and it reaches the skills page via `skills:list`, where a measurement failure returns `null` instead of breaking the list — so the worst case is the old silence, never a broken page [gate: agent-guide-headroom] |
| Rules are PUSHED, not pulled — a rule the model was never shown is a rule it violates — yet the mandatory guide bodies were one monolithic string per locale, so nothing inside them could be conditional and Windows PowerShell rules shipped in every prompt on macOS and Linux where they cannot apply | `test-agent-guide-blocks.mjs` — a manifest may split its guide into addressable blocks with an `appliesTo` condition, and pruning may never happen on a guess: an unknown condition key, a section that would prune to nothing, and a manifest with no blocks all keep the full `body`, which stays in the manifest as the authoritative text and the fallback. Verified across zh-CN/en/ar that a win32 install is byte-for-byte identical and darwin differs by exactly the Windows section (480B zh, 455B en, 0B ar — it never had one); blocks must rejoin to `body` byte-for-byte so they cannot drift into shipping different rules [gate: agent-guide-blocks] |
| There was NO ruler for whether a guide change made the model dumber: no rule-adherence eval, no skill-discovery eval, and the live eval harness injected only `buildAgentBasePersona()`, never the assembled guide — so it exercised none of the rules and no skill index | `test-agent-guide-rule-contract.mjs` — ten non-negotiable rules anchored on distinctive shipped phrases in `agent-guide-rule-contract.json`, verified across 3 locales x 3 platforms (9/10 present everywhere; `windows-shell` 2/9, exactly as `agent-guide-blocks` intends). Surfaced a real gap now recorded rather than tolerated: the Arabic guide never had a Windows section, so Arabic users on Windows get no PowerShell rules. `test-guide-eval-cases.mjs` keeps the live suite (`npm run eval:guide`, own baselines under `baselines-guide/`, mandatory canary keyed on a machine-specific catalog path so a run that never saw the guide fails as SETUP rather than passing on model priors) honest offline: every grader must score its own pass/fail fixtures and reject empty output, every discovery prompt must share no wording with its target skill's description, every rule case must point at a tracked rule [gate: agent-guide-rule-adherence] |
| The user picks 全自主 and the model still ends its turn asking them to choose — reported from the field with the model's own task list at 8/10 — because `permissionMode` drove tool auto-approval and runtime identity only and never reached the prompt, so the model never knew autonomy had been selected | `test-agent-autonomy-guidance.mjs` — only an exact `"full"` appends a directive (ask/plan/unknown add nothing, so those sessions are unchanged), stray whitespace cannot silently downgrade the choice, unlisted locales fall back to English never Chinese, and the wiring must pass the session's real mode from inside a `catch` so a failure leaves guidance as before. Registered in `agent-guide-rule-contract.json` with `source: "autonomy-guidance"` so the contract checks it against its real carrier across all three modes. Live A/B on deepseek-v4-pro, 5 runs per arm, neutral prompt: 2/5 without the directive, 5/5 with it. `test-agent-guide-rule-contract.mjs` enforces the required-in-full / absent-in-ask-and-plan half [gate: agent-autonomy-mode] |
| A leaked tool call kills a turn that already did work: the `MALFORMED_TOOL_CALL_TEXT` rescue REPLAYS the user's request, which is only safe when every tool was replay-safe — so the reported turn (148 tool calls in, files already edited) got no rescue at all and surfaced the raw failure | `test-tool-call-continuation.mjs` — continuing the same engine session instead re-does nothing and asks only for the missing next action. Narrow by construction: only this code, only when the run was NOT side-effect free (read-only turns keep the cleaner replay), never for other healable codes, and `LILY_TOOL_CALL_CONTINUATION=0` restores the previous failure. The side-effect guard still refuses every replay, and the decision must sit BEFORE that guard or the continuation is unreachable — asserted by position, since a gate that matched anywhere let the content silently revert to the user's message [gate: tool-call-continuation] |
| IM lists showed the raw conversation id for every 1:1 ("一大长串") and people without a nickname as handles or placeholders; the Teams view rebuilt every section on every sync event and jumped | `test-collaboration-identity-facets.mjs` + `test-collaboration-list-stability.cjs` — a conversation is named after its other member(s), a person by nickname → enterprise login → server-masked phone → handle, never a raw id (phone masked before it leaves the server; leak guards tightened to reject the raw keys while admitting the masked facet); `teams.update()` fingerprints its inputs (presence in, `onlineUntil` out) and skips byte-identical rebuilds AFTER revocation handling, keeps open forms across a real rebuild; `preserveScroll` is tested against a rebuild that forces layout while empty, because a synchronous refill never loses the offset and the first assertion was vacuous [gate: collaboration-identity-and-list-stability] |
| Adding an official OpenAI model failed with a generic "保存失败": the probe hard-coded `max_tokens`, its retry only shrank the number, the 400 body was flattened to `HTTP_400`, and five other call sites plus the server gateway hard-coded the same field | `test-openai-request-shape.mjs` + `test-model-probe-request-shape.mjs` + `test-model-gateway-request-shape.mjs` (+ the existing `test-model-compatibility-probe.mjs` and `test-model-gateway-openai-adapter.mjs` keep the default shape byte-identical) — the request shape is LEARNED from the server's own 4xx (which parameter it refused), never assumed from a model name or host; adapted once per lesson, bounded; persisted on the preset and handed to the runtime as `LILY_MODEL_REQUEST_SHAPE`; unrelated rejections surface their redacted code/param/message; the output budget is raised (512→4096) only when the server reports exhaustion (`finish_reason=length`, nothing produced) and probe verdicts carry what was observed; when the endpoint says "use /v1/responses" for tools, the probe verifies on the Responses surface, records `api=responses`, and the engine runs that preset on the official SDK's Responses route (`test-opencode-model-config.mjs` keeps every other preset on the compatible SDK) — verified live with a real official key, probe and a headless engine tool call; generic lessons (drop/rename any refused field, tool_choice→auto, strip rejected schema keywords) plus server-delivered hints (`test-request-shape-remote-hints.mjs`) and a redacted report of anything unlearned (`test-model-probe-telemetry.mjs`) mean a new provider quirk is a server setting, not a desktop release; a Codex/Responses proxy that refuses `max_output_tokens` is handled by applying the learned corrections to the final wire body after the chat→Responses translation (`test-model-probe-responses-no-limit.mjs`); the OpenCode engine runs a learned-Responses preset on the official SDK and never sets `limit.output` for an output field the probe learned is refused, so probe-pass implies turn-works (`test-opencode-model-config.mjs`) [gate: openai-request-shape-adaptive] |
| A workspace with 76 active skills failed every turn (`RUNTIME_IDENTITY_FIELD_INVALID: capabilities are invalid`): the token's skill authorization cap was 64 (too small), the field was activeSkillIds not capabilities, and the raw code reached the user | `test-runtime-identity-skill-cap.mjs` — over-limit is its own code `RUNTIME_IDENTITY_TOO_MANY_SKILLS` naming the real field and count; the user sees 'too many active skills this conversation, disable some'; the legacy string still maps; the cap is raised 64→256 (an authorization scope must hold the enabled set; per-turn relevance is applied at the guidance layer, never by filtering this scope); a malformed field stays `RUNTIME_IDENTITY_FIELD_INVALID`, now named [gate: runtime-identity-skill-cap-legible] |
| `protectSecret()` silently wrote API keys and mail passwords as Base64 when the OS secure storage was unavailable | `test-secret-storage-guard.mjs` + `test-model-settings-secret-storage.mjs` — a NEW secret is refused with `SECRET_STORAGE_UNAVAILABLE` (named in the UI), plaintext only by explicit `LILY_ALLOW_PLAINTEXT_SECRETS=1`, existing records still read and are reused so switching/relabelling presets keeps working 一处修八处漏（2026-09-19 审计）：同样十五行的 protect/unprotect 复制了八份，上次的修法只落在模型密钥和邮件密码两份，设备签名私钥（`service-client`）、账户 refreshToken（`account-manager`）、远程配置缓存（`remote-config`，含网关令牌）、许可证（`license-state-store`）、网站凭据（`web-credential-store`）六份仍在静默 Base64。现在 `secret-storage.js` 是唯一碰 safeStorage 的模块（`collaboration/local-keyring.js` 用它包自己的 AES 密钥、本就拒存），八处全部改问它，`test-secret-storage-guard.mjs` 扫源码禁止第二份；写入方各自按「拒存但不变笨」处理：设备身份本进程内存有效、登录本次有效下次重登、配置缓存留在内存下次重取、许可证激活返回 `SECRET_STORAGE_UNAVAILABLE`。新增覆盖：`test-web-credential-store.mjs`（拒存断言）、`test-license-clock-rollback.mjs`、`test-collaboration-local-keyring.mjs`。 [gate: secret-storage-no-silent-plaintext] |
| Runtime events are INSERTed and UPDATEd but never DELETEd anywhere in the repo, and clearing a conversation removes only its messages — so every deleted conversation leaves its whole event stream behind forever, on the main-process database every turn writes to. Measured on a real install: **12 GB** holding 1,156 messages and 3,917,891 events across 155 sessions, of which only 29 still had messages — 2,542,720 events (64.9%) orphaned from 135 deleted conversations, almost all per-token streaming telemetry (`process.event` 1.46M, `task.step.progress` 1.16M, `assistant.thinking.delta` 937k) | `test-runtime-event-retention.mjs` — `clear()` now drops the session's events, and a bounded `pruneOrphanRuntimeEvents` drains the existing backlog from startup maintenance. Safe by construction: the only reader is always scoped to a session id, and the prune touches only rows whose session has no surviving message. Bounded per call so a large backlog spreads across rounds instead of blocking a startup, and fail-open so maintenance can never break one; the gate also asserts the prune is actually SCHEDULED, since a prune nothing calls is the same defect as a message nothing renders. The historical backlog now drains SILENTLY in the background, bounded by the PRIMARY KEY so each chunk is an index range scan (30 ms per 20,000-seq window vs **19,835 ms** for the full-table `type IN (...)` DELETE the obvious query produces), with a schema_meta cursor that resumes across restarts and stops once finished; the gate asserts the QUERY SHAPE in source and via `EXPLAIN QUERY PLAN`, because a type-only DELETE removes the same rows and passes any behavioural test. The older orphan scan is superseded and OPT-IN (`LILY_PRUNE_ORPHAN_EVENTS=1`) because the first version deleted row by row and, `node:sqlite` being synchronous on the main process, froze the UI for two minutes after a restart while removing exactly 1,000,000 rows; whole-session deletes cost ~0 ms but DISCOVERING orphans costs a full table scan (3,133 ms via GROUP BY, 2,821 ms via DISTINCT) that cannot be made cheap there — so `clear()` prevents new orphans at zero cost and the historical backlog is never touched at startup by default [gate: runtime-event-retention] |
| An ungraceful exit (Ctrl-C in dev, a crash, a force quit) skips `before-quit`, so the shared `opencode serve` survives reparented to init, holding userData files and a port — and nothing ever looked for what the previous run left. Measured on a real dev machine: **7 orphaned serves, oldest 40 days**, dragging 24 stale Electron children, 587 MB resident, 19 processes holding userData at once (the same class as the Windows updater's "could not be closed") | `test-orphan-serve-reaper.mjs` — reaped at startup, with the value in what it REFUSES: the command must start with exactly this install's bundled binary **and end at a word boundary** (a bare `startsWith` reaped `.../opencode-old serve`), the parent must be gone (`ppid 1`), and the pid must not be ours. A live instance's serve is parented to that instance so it can never match, and the call sits AFTER the single-instance lock — which is what proves a parentless serve is an orphan and not a concurrently starting instance. A relative binary path selects nothing rather than guessing; win32 is a deliberate no-op since a dead parent's pid is not rewritten to 1 there; fail-open with `LILY_REAP_ORPHAN_SERVES=0` [gate: orphan-serve-reaper] |
| Disk is never reclaimed from `messages.db`: deleting rows returns pages to SQLite's freelist but never shrinks the FILE, so an install that once bloated stays bloated (measured: **12.14 GB holding 1,156 messages**, 7.34 GB of it `runtime_events` payloads, 99.5% reclaimable) — and plain `VACUUM` rebuilds in place under an exclusive lock, which is not something to do to a customer's 12 GB file | `test-message-db-compaction.mjs` — `VACUUM INTO` reads the source and writes a compacted copy, so nothing is write-locked and the disk needed is the size of the RESULT; the swap is a rename run BEFORE the store's first open, the only moment it cannot race a reader (asserted by position). Five gates before the swap: worth copying, freelist actually reclaimable, `PRAGMA integrity_check`, row counts of all six user-data tables MATCH, then rename with the original kept under `.precompact` and restored if the rename fails. Any refusal deletes its copy, so a failed attempt cannot eat the disk it was freeing. Verified end to end: a 247 MB fixture that stayed 247 MB after deleting 99.8% of its rows came out at 0.5 MB with every row intact [gate: message-db-compaction] |
| An enterprise cannot hand out the seats it paid for: `POST members` takes `{userId | phoneE164}` but `organization_members` references `users(id)`, so an unregistered phone was a dead end (`USER_NOT_FOUND` 404) and HR had to wait for every employee to sign up and add them one at a time. The design doc never covered the case in 284 lines | `test-enterprise-invitations.mjs` — an invitation records the intent and the seat is granted at that person's next login. Purely additive (migration 043 adds one table; `organization_members` untouched), and a REGISTERED phone still becomes a membership immediately. Login never depends on it: redemption runs AFTER the login transaction commits, in its own try, so a failure leaves the invitation pending for next time. Each redemption takes a `FOR UPDATE` re-read so two simultaneous logins cannot both grant the same seat; a disabled org DEFERS rather than burning a paid seat; owner is refused explicitly rather than silently downgraded; uniqueness is a PARTIAL index so audit rows never block a re-invite [gate: enterprise-seat-invitations] |
| A company cannot create accounts for its staff: identity is phone-only (`users.phone_e164 NOT NULL UNIQUE`, SMS-only login), so a user row cannot exist unless the person personally holds a phone and completes SMS — a company that bought seats could not issue any | `test-enterprise-accounts.mjs` — a login-name + one-time-password identity the company issues; forced change on first login; the account is OWNED by the org (removal/disable locks the login, a phone user who merely joined keeps theirs). Additive: `phone_e164` nullable with a CHECK that every user still has some identity, partial unique index on login names. scrypt with per-user salt, 5-strike lock that rejects even a correct password, unknown name indistinguishable from wrong password by code AND timing, current password required to change, owner never issuable, and BOTH hash write sites counted — a regex matching either let plaintext at one slip through [gate: enterprise-issued-accounts] |
| Creation was self-serve only: an operator who signed a customer could not open the enterprise — someone at the customer had to register, create, and become owner themselves. §7.1's "admin stays out of internal affairs" was about MEMBERS, not creation, but it blocked the sales flow | `test-admin-enterprise-create.mjs` — `POST /api/admin/enterprise/organizations` creates and hands off to a first owner named exactly one way: a registered phone, or an account the platform issues (the ONLY caller of `allowOwner`; the enterprise-side path never passes it, so a company admin cannot mint owners). Unregistered phone → `OWNER_NOT_REGISTERED`, never a silently invented seat. Behind `assertAdmin`, audited with `ownerIssued`, initial password never persisted, and the admin surface still has NO `/members` endpoints — the boundary that mattered is kept. Closed loop in `admin-enterprise-create-integration.mjs` on a real Postgres with real migrations: create → owner logs in with the one-time password → forced change → visible in both lists [gate: admin-enterprise-create] |
| A self-configured (BYOK) model that can recognise images could not actually use them: presets had no capabilities field, so images from a vision-capable custom model were always forced through the Qwen-VL translation bridge and the model never saw them | `test-model-vision-capability.mjs` — on save/update the endpoint is probed with a tiny image (`probeVision`) and the result is persisted as `capabilities.vision`, with a manual toggle in the add-model form as the fallback when probing is off or the server false-negatives. The flag reaches every consumer: `customPresetRecord` carries it (the bug — it was dropped, so `getActivePreset()` never saw it), the selection catalog and `normalizeOption` keep it on `route.model.capabilities.vision`, `activePresetSupportsVision`/`allowImageFileParts` route images straight to the model, and the engine sets `modalities.input` to include `image`. Fail-open: no flag means the bridge path as before; `LILY_PROBE_VISION=0` disables auto-probe and leaves the manual toggle 2026-09-19 补：探测用的那张 1x1 PNG 本身必须结构合法（签名、逐块 CRC、IEND 齐全、无尾部多余字节），并且要能通过一个**真正解码图片**的端点。旧常量被截断且 IDAT 校验和是错的，宽松解码器收下、严格端点回 400，于是一个图片识别完全正常的端点被探成 vision:false，该预设下每张图都丢掉原生视觉通道、降级走弱路径。 [gate: model-vision-capability] |
| A user with their own OpenAI-compatible image endpoint could not generate images at all: `lily-image-generation` only had adapters for dashscope/lily/volcengine/kling/minimax/zhipu, so a self-configured OpenAI / DALL·E / gpt-image endpoint had no path | `test-model-image-generation.mjs` — a new `openai` adapter POSTs `/images/generations` with the configured model, decodes `b64_json` into files, accepts a returned url, self-heals a server that rejects `response_format` (gpt-image-1) by retrying once without it, and fails LOUDLY with a named error on a missing key. Purely additive: a new opt-in BYOK provider in Settings → image providers (endpoint+key+model) that `media-provider-settings` maps to the exact env vars the adapter reads (`OPENAI_IMAGE_API_KEY`/`BASE_URL`/`MODEL`); every existing provider and the service path are untouched [gate: model-image-generation] |
| A ChatGPT-plan Codex account could chat and recognize images but not GENERATE them in Lily, and the Codex relay is not one of our standard image services so it should FOLLOW the model, not sit beside DashScope | `test-model-image-generation-codex-relay.mjs` — a per-model `capabilities.imageGen` toggle routes image generation through that model's OWN connection: `activePresetImageGenEnv()` emits `LILY_IMAGE_PROVIDER=codex-relay` + `CODEX_RELAY_BASE_URL/API_KEY/IMAGE_MODEL` from the active preset (an explicit media choice still wins, spread after). The `codex-relay` adapter drives `/chat/completions` (relay auto-injects the Responses `image_generation` tool), streams (non-streamed 502s behind nginx), buffers, and extracts every `data:image` data-URI into real files for the media pipeline (no ~2MB transcript bloat); a non-imageGen model emits no image env; missing config or image-less reply fail loudly. Proven end to end against the live relay (2.1MB 1254×1254 PNG) [gate: model-image-generation-codex-relay] |
| Adding a custom model meant typing the model id by hand and guessing what the address served — there was no way to ask the endpoint what models it has | `test-model-endpoint-discovery.mjs` — a Fetch-models button + `discoverEndpointModels` query the endpoint's standard `GET /models` (OpenAI `Bearer` or Anthropic `x-api-key`) and populate a datalist so the user picks a real id. Convenience only and FAILS SOFT: any 401/403/404/network/empty returns `{ ok:false }` (never throws, never blocks save) and the manual field stays as the fallback; capabilities (vision/tool-call/shape) remain the save-time probe's job. Verified live against the relay (37 models) [gate: model-endpoint-discovery] |
| 产物里混进了无关文件：一个视频回合把渲染中间件（`.lily-work/` 下 18 个 webm 分镜）和 `/tmp` 里的临时 html 和 `output/` 的真正交付物并排列成"产物" | `test-turn-artifact-relevance.mjs` + `test-turn-artifacts.mjs` + `test-turn-artifact-model.mjs` — `turn-artifacts.js` 此前是工作区层里唯一不认识内部目录的消费者（`workspace-index` / `workspace-export-planner` / `workspace-grounding-gate` / `workspace-document-mentions` / `learned-context` 各自都有一份排除表），它唯一的排除只覆盖五个 skills 目录；工具来源的路径也从不做工作区围栏，所以磁盘上任意绝对路径都能进来。现在按"位置"而非"来源"判定：工作区内的 scratch 目录（`.lily-work`、`node_modules`、`.git`、缓存与虚拟环境）和工作区外的系统临时目录是过程材料，被用户点名的绝对路径导出（例如桌面）仍是交付物，`dist`/`build`/`release` 也仍是交付物（"帮我打个包"交付的就是构建产物）。不变量：过程材料只在本回合还有别的产物时才隐去；若本回合只产出过程材料，则全部保留并标记为 compact，产物列表永不清空。 [gate: deliverable-relevance] |
| 中文 PDF 整份丢字：`LILY_CJK_FONT_PATH` 指向 ReportLab 无法嵌入的 PostScript/CFF 字体，macOS 前两个候选（PingFang、Hiragino Sans GB）和 Linux 的 Noto CJK 都是 CFF，只按"文件存在"挑字体必然落到嵌不进去的那个，然后静默回落 Helvetica | `test-document-fonts.mjs` + `test-office-style-helper.mjs` + `test-office-authoring-contract.mjs` — 宿主与 Python 两侧都改为读 sfnt 表判轮廓格式（`glyf` 可嵌入、`CFF `/`CFF2` 不可），跳过不可嵌入的候选并补上确实可用的 STHeiti/Songti/wqy；探针读不动时返回 unknown 视为"无意见"而非拒绝；一个可嵌入的都没有时仍返回从前会返回的那一个，失败模式不比基线更差。Python 侧 `register_cjk_font()` 的自检把探针结论与真实 ReportLab 逐个对账。验收 2026-09-16 DEF-001。 [gate: cjk-pdf-font] |
| 文档问答静默说谎：目录索引给 .docx/.xlsx/.pptx/.pdf 只建一个 metadata 分块却按"已索引"计数，查询文档里确实存在的文字返回空且不给任何理由，于是空结果被读成"文档里没有这个内容"；这些文件带的 `indexPolicy: paragraph-index` 是没有任何代码兑现的承诺 | `test-document-content-index.mjs` + `test-file-intelligence-index.mjs` + `test-workspace-knowledge-store.mjs` — Office/PDF 现在走 `extract_document.py` 建真正的内容分块（`index_path` 工具显式开启，它跑在自己的子进程里；主进程上的大文档提示、自动索引、对账三条路径保持原样不阻塞回合）。提取在文件大小、单文件超时、整轮文件数、整轮墙钟四个维度上都有界，任何失败都回落到原来的 metadata 分块并带上具名原因。索引结果返回 `metadataOnly`，查询命中为空时附带 `metadataOnlyDocuments` 与说明（从分块本身推导，所以本次改动之前建的索引也会如实回答）。`LILY_INDEX_DOCUMENT_CONTENT=0` 原样还原旧行为。验收 2026-09-16 DEF-003。 [gate: document-content-index] |
| 浏览器能力自相矛盾：web-automation 包自检 installed/ready/health.ok 全绿，broker 却对 browser_open 报 `BROWSER_RUNTIME_UNAVAILABLE`——解析在"基础包里有没有 node 二进制"这一步就返回了，根本没走到包；而 playwright 的模块树与浏览器目录来自两个不同安装（dev 树的 1.59.1 要 chromium-1217，包里是 1228） | `test-browser-runtime-availability.mjs` + `test-mcp-config.mjs` + `test-spawn-env-web-runtime.mjs` — 应用自身二进制在 `ELECTRON_RUN_AS_NODE` 下就是 Node 解释器（另外四个内置 MCP 早就这么起），所以基础包没有 node 目录不再等于关闭浏览器自动化：装好的包自带 @playwright/mcp、playwright 和浏览器，缺的只是解释器。MCP 条目把 `NODE_PATH` 钉死在 cli 所属的那份安装上。spawn 环境把 playwright 三个变量当作一个整体：包提供时整组胜出，基础包与 dev 树只在包缺席时填补，绝不混搭。自带 node 仍然优先；包和基础包都没有时仍是干净的 no-op。验收 2026-09-16 DEF-004 / DEF-005。 [gate: browser-runtime-availability] |
| 交付的 Office 文档不完整：工作表没有页面设置，浮动图表被分页线劈开，导出 PDF 多出一页只剩纵轴标题；文档标题用 Title 样式承载，pandoc 转换后进元数据、从正文消失；运行时根本没装任何 Python 绘图库，文档/数据技能却承诺"生成图表" | `test-office-style-helper.mjs` + `test-office-authoring-contract.mjs` — `style_xlsx_print()` 把带图表的工作表在横纵两个方向都缩到一页（分页线因此无处可劈），纯数据表仍向下延展，自检覆盖保存重开后的持久化；真实 LibreOffice 渲染对照为 4 页降到 3 页、图表不再重复出现在两页上。docx 覆盖层写明正文标题用 Heading 1 而非 Title 样式，转别人的文件时加 `--standalone` 或自己补回。matplotlib 进入运行时依赖并写入构建校验的 import 清单，`configure_matplotlib_cjk()` 复用同一套字体解析让中文标签不是豆腐块；缺库时只返回 None，图表降级为工作簿原生图表。三项都是加法，失败不会让工作簿或文档生成失败。验收 2026-09-16 DEF-002 / DEF-006 / DEF-007。 [gate: office-delivery-completeness] |
| LibreOffice 转换静默失败：`soffice --convert-to docx` 对 HTML 源退出码 0、什么都不写，只在 stderr 打一句 `no export filter ... aborting`；只看返回码的调用方报告成功，交出一个不存在的路径 | `test-office-conversion-guard.mjs` + `test-office-authoring-contract.mjs` — 成功的判据改成「产出了非空文件」而不是返回码；源格式能被多个模块加载时（HTML 默认走 Writer/Web，只会导出 PDF）自动用显式输入过滤器重试一次；仍然没有产物就抛出异常并带上 LibreOffice 自己那句话。渲染器不再自带一份转换逻辑，全平台共用这一条路径。验收 2026-09-17 DEF-04。 [gate: office-conversion-no-silent-failure] |
| 工具结果按行数限量而不按字节限量：2 行、60202 字节的文件请求「2 行」就把 60KB 全部灌回模型。行数不是体积的代理，超大工具结果正是长会话把自己撑爆上下文的路径 | `test-tool-result-budget.mjs` + `test-file-intelligence-core.mjs` — 采样与按行抽取都加了返回字节预算（默认 8KB，调用方可在 512B–256KB 间调整）。裁剪一定可报告、可续读：给出丢弃字节数与 `nextLine`，从 `nextLine` 继续能精确接上。永不返回空——单行超过整个预算时按字符边界切，多字节字符不会被切成乱码。原本就放得下的结果形状逐字节不变。整份读取也不再在实际只读了前缀时谎称 `coverage: full`。验收 2026-09-17 DEF-07。 [gate: tool-result-context-budget] |
| `inspect_file` 只看扩展名：内容其实是 PDF 的 `fake_document.docx` 被判成 `kind: document` 且毫无提示，直到很久以后在抽取器里以一句看不懂的错误崩掉 | `test-file-type-honesty.mjs` + `test-file-intelligence-core.mjs` — 读文件头比对容器签名（%PDF / PK / OLE2 / PNG / JPEG / GIF / RTF），名实不符时返回 `detectedFormat` + `extensionMismatch` + 说明。**刻意只做建议不改判定**：`kind` 仍由扩展名推导，因为索引、抽取路由和二进制护栏全都依赖它，用启发式翻转它会在全仓库范围内改变行为。读不动文件头、扩展名没有签名承诺、字节本身不声明格式——这三种情况一律不出具任何建议，绝不冤枉。验收 2026-09-17 DEF-09。 [gate: file-type-honesty] |
| 中文填进 PDF 表单字段：值写对了、渲染不可靠。`/NeedAppearances` 只是请求阅读器用自己的字体重绘，是提示不是保证；忽略它的阅读器画出的是空白，而回读 `/V` 根本分辨不出来 | `test-pdf-form-cjk-render.mjs` + `test-pdf-form.mjs` — 写完之后把产物栅格化，逐个检查中文字段矩形里有没有墨迹，结果带 `cjkRender: {checked, verified, blankFields[], warning}`，空白字段连字段名和矩形一起报出来。`--flatten-cjk` 用平台已校验的中文字体把值直接画到页面上并重新栅格化确认，**刻意做成可选**，因为遵守该标志的阅读器会把值画两遍。纯拉丁填写完全不触发这条路径，形状逐字节不变；栅格化不可用时如实返回 RASTERIZER_UNAVAILABLE 而不是宣称已验证。同时钉住技能里那条路由：不要用 reportlab 的 `canvas.acroForm` 造带中文默认值的表单（它的 PDF 字符串转义是 0–255 查表，任何中文字符直接 KeyError）。验收 2026-09-17 DEF-01 / DEF-02。 [gate: pdf-form-cjk-visible] |
| 捆绑的图像库按文件名保存多页 PDF 会崩（Pillow 12 的插件懒加载只注册 PDF 插件，JpegImagePlugin 从未导入，DCTDecode 时 KeyError JPEG）；我们捆绑的 Pillow 11.3.0 尚无此机制，本地不复现 | `verify-runtime-bundle.mjs` 的构建期探针 + `test-office-authoring-contract.mjs` — 这是一道前向护栏：构建时真的存一份两页 PDF，升级 Pillow 若把这个缺陷带进来会在构建期报警（strict smoke 下直接失败）并写明修法（降级钉版，或要求所有调用方显式传 format=PDF），而不是让它流到用户手上。验收 2026-09-17 DEF-03。 [gate: runtime-image-pdf-save] |
| 契约点名的助手 agent 够不着：PDF 契约写「用 lily_office_style.py 的 register_cjk_font()」，给的却是仓库相对路径 `resources/runtime-scripts/...`，从工作区根本解析不了。agent 只能猜，这次猜到了 `/Applications` 里 9 月 8 日的 0.1.166 旧安装，如实报告函数不存在——契约和它点名的模块来自两个不同的构建，中间没有任何东西把它们绑在一起 | `test-runtime-helper-path.mjs` + `test-office-authoring-contract.mjs` — 宿主导出 `LILY_RUNTIME_SCRIPTS`（运行中这个构建的绝对路径），所有 overlay 与技能正文改用它，技能脚本也从环境变量取而不是从自己所在目录往上爬。测试就是那根绳子：契约里点名的每个助手文件必须在导出路径下存在，点名的每个函数必须真的定义在其中之一。已反向验证——把路径改回相对写法，测试立刻失败。验收 2026-09-17 D-S06-01 / D-S06-02。 [gate: runtime-helper-path] |
| 托管作业的 stderr 恒有宿主噪声：作业经由应用二进制以 Node 模式启动，Chromium 自己的日志器在作业代码开跑前就往子进程 stderr 写了 114 字节，于是每个作业的 stderr 都非空——而「stderr 非空」正是判断作业成败最直觉的方式，退出码 0 的成功作业看起来像失败 | `test-job-observation.mjs` + `test-process-jobs-core.mjs` — 日志文件本身一个字节都不改（它是诚实记录），改的是不再把宿主写的东西当作作业的输出交回去：只匹配宿主日志器自己的行格式，移除了多少行多少字节一并报出；整份 stderr 只有宿主噪声时明确标 `stderrHostNoiseOnly`。真正失败的作业仍然明显失败，作业自己打印的类似内容一律保留。验收 2026-09-17 D-SUP-01。 [gate: job-stderr-host-noise] |
| 退出码 0 不等于干了活：worker 静默忽略了传给它的规模参数，3 秒内处理 0 项、退出码 0，所有监控一片绿——从用户角度，这和「长任务中途停了」完全无法区分 | `test-job-observation.mjs` + `test-process-job-observability.mjs` — 作业状态新增 `workObserved` / `workEvidence`，**只陈述事实，不下判断**：看见正数进度、正数百分比或产出文件才算观察到工作；报了 `0/30000` 或只报了阶段名，说明 worker 活着，不说明处理了任何东西，因此不算。纯加字段，作业状态、退出码和既有字段一律不变，合法的快作业也不会被冤枉成失败。验收 2026-09-17 D-S10-01。 [gate: job-work-evidence] |
| 同一份中文内容导出的多个 PDF 各自穿不同的字体：声明的 `Microsoft YaHei` 在 macOS 上不存在，LibreOffice 各个模块按自己的规则替换——实测同样内容从 Word 出来是 ArialUnicodeMS、从 PowerPoint 出来是 SimSong | `test-office-style-helper.mjs` + `test-office-authoring-contract.mjs` — 实测确认：声明一个本机确实有的字族，两种格式嵌入的字体完全相同。`installed_font_families()` 直接读字体文件的 name 表枚举本机字族（659 个 / 0.07 秒，不依赖任何第三方库），`resolve_cjk_document_family()` 据此挑一个存在的。**刻意不改默认值**：交付物是 Office 文件、读者在 Windows 时，声明 Microsoft YaHei 才是对的；只有交付物是本机导出的一组 PDF 且必须观感一致时才传这个字族。读不到已装字体集时原样返回首选字族，行为与从前一字不差。验收 2026-09-17 D-S06-03。 [gate: cross-format-cjk-consistency] |
| 中文字体只对一半内容生效:`style_docx` 把 `w:eastAsia` 写进了 run 和样式,却没删掉同一个 `w:rFonts` 上的 `w:eastAsiaTheme`——主题引用在 LibreOffice 解析器里优先级更高,而默认主题的东亚字体槽是空的。实测同一份文档正文是 STSongti、每个标题回退 ArialUnicodeMS;PPT 的原生图表文字不是 run,`style_pptx` 根本够不着,坐标轴、图例、图表标题全部回退 PingFangSC | `test-office-style-helper.mjs` + `test-office-authoring-contract.mjs` — 三处一起补齐:`_set_rfonts` 先删除四个 `w:*Theme` 属性再写显式字体;`_set_theme_fonts` 填上 theme1.xml 的 major/minor 东亚槽(兜住后续编辑新增的内容);`_set_chart_fonts` 把字体显式写进图表部件,并补一个 chartSpace 级的 `c:txPr`——实测证明只补主题对图表无效,必须写进图表部件。双向验证:修复前 DOCX→PDF 是 ArialUnicodeMS + STSongti、PPTX→PDF 含 PingFangSC;修复后两者的中文字体集合完全相同,D3 的跨格式不一致随之消失。改不动的主题或图表部件直接跳过,文档照样生成。验收 2026-09-17 D1 / D2 / D3。 [gate: cjk-theme-font-chain] |
| 每个已装运行时包都挂在同一条 PYTHONPATH 上,于是包自带的核心库副本对**每一个** Python 进程生效:agent 写的代码拿到的 numpy 是 2.4.6、Pillow 是 12.2.0,而我们声明并测试的是 2.5.3 和 11.3.0。后果实测两条——多张图片存成多页 PDF 直接 `KeyError: JPEG`;依赖审计把四个库读成越界,而运行时本身完全合规 | `test-runtime-pack-isolation.mjs` + `test-document-extractor-pro-pdf-gating.mjs` + `test-runtime-packs.mjs` — 这不是排序问题,三种排法都试过:包在前 Pillow 坏,venv 在前 rembg 坏(它的 numba 拒绝高于 2.4 的 numpy),纯隔离也不行(rembg 缺 onnxruntime、large-document 缺 numpy,只有 venv 有)。**成立的只有一种:venv 解释器 + 恰好一个包。**包自带的版本对它自己带的东西生效,venv 补齐其余,没在用包的进程根本看不见包。通用 agent 环境不再挂任何包(`LILY_RUNTIME_PACK_DIRS` 仍然导出,给刻意的调用方和修复提示用);抽取器只在调用方显式选了 `LILY_PDF_ENGINE=pro-pdf` 时才挂 pro-pdf——顺带消掉一个没人发现的跨包冲突:docling 的 numpy 和 rembg 的 numba 本来一直在同一条路径上争;`runtime_pack_list` 的修复提示改成单包环境。未装或不认识的包只是不贡献任何东西,不会让调用失败;`LILY_RUNTIME_PACK_GLOBAL_PYTHONPATH=1` 原样恢复旧行为。验收 2026-09-17 D5 / D9。 [gate: runtime-pack-isolation] |
| 扫描件静默丢失全部内容:PDF 页渲染成 PNG 通常落在 palette(P) 模式,OCR 把这样的文件读成"一个字都没有"。一张完整的营业执照返回空字符串且 `ok: true`——PDF 页那条路径早就在 OCR 前归一到 RGB,独立图片这条没有 | `test-extraction-honesty.mjs` + `test-document-image-recognition.mjs` — 独立图片入口现在和渲染页一样先归一到 RGB。实测同一张 pdfplumber 存出的 P 模式 PNG:修复前 0 字符,修复后 38 字符命中统一社会信用代码。另加一条诚实判定:OCR 空结果但图像明显有墨迹时返回 warning,而不是让空字符串冒充"这页没有文字";真正空白的图不会被冤枉。打不开做归一时,照旧直接让 OCR 读文件,不比从前差。验收 2026-09-17 DEF-01。 [gate: image-ocr-colour-normalisation] |
| 抽取失败给不出任何信息:加密 PDF 返回 `PdfminerException: `,冒号后是空的,既分不出加密还是损坏,也就无从知道该不该要密码 | `test-extraction-honesty.mjs` + `test-document-translator.mjs` — 原始异常类型和消息逐字保留,另加 `errorCode` 与可操作提示:加密 PDF 得到 `PDF_ENCRYPTED` 和"先用密码解密,文件没有损坏,重试没用"。认不出的原因一个字都不加,所以损坏绝不会被误标成加密——实测损坏 PDF 仍然保留它自己那句可读的 `No /Root object!`。验收 2026-09-17 DEF-03。 [gate: extraction-failure-diagnosis] |
| 超过 5 MiB 的纯文本完全进不了索引:24 MB 的 JSONL 返回 `NO_INDEXABLE_CONTENT`,用户拿不到任何可检索证据;而且"过大"和"二进制"共用同一个 reason,连"请先切分文件"这种可操作提示都给不出 | `test-document-content-index.mjs` + `test-file-intelligence-index.mjs` — 超过上限的文本改为索引头部并如实标注:`coverage: sampled`、`filesTruncated`、以及"已索引前 X / 共 Y 字节,切分文件或调高 maxFileBytes 可获得完整覆盖"。实测 11.1 MB 的 JSONL 从"零证据"变成 679 个分块且检索命中。二进制仍然拒绝,但有了自己的 `binary_content` 原因,两者从此可区分。上限之内的文件索引结果逐字节不变。验收 2026-09-17 DEF-02。 [gate: large-text-index-coverage] |
| 早就结束的老对话,每次重启都冒出新提示要你重发几天前的某句话:应用重启会把在飞的回合判成「结果未知」并要求手动重发,这部分是对的;错的是**防重复宣告的去重键只在内存里,而它要挡的恰恰是重启**——重启把它清空。同时没有任何代码把行挪出 `status IN (dispatching, promoted, accepted)` 这个集合,于是每次重启都把该会话历史上所有未知结果重新发现一遍。实测:228 次宣告只涉及 10 个回合,一条 09-01 的回合四天里被宣告 52 次,每次重启受影响数量还在涨(2→3→4→5) | `test-announce-once-across-restart.mjs` + `test-turn-queue-recovery-task-core.mjs` — 事实改由持久化的行承载,内存只是它的投影:行已经是 `outcome_unknown` 说明上一轮已经宣告过,本轮静默收养;仍在飞的回合才是新结果,宣告并写下来。写在宣告之前,所以崩在两步之间最多多宣告一次而不是永远。用户看到的列表内容完全不变,新产生的未知结果照常立即宣告——沉默不能变成失聪。持久化失败时照旧宣告,即今天的行为,记账绝不吞掉警告。验收 2026-09-17 现场库取证。 [gate: announce-once-across-restart] |
| 重新打开会话后,一个已经结束的回合在「答案」位置显示的是整段流水:工作时写的叙述、工具卡片被移除后留下的成片空行、最后才是结论,读起来就像它结束之后还在自说自话。根因:**答案与过程的分离是内存里 timeline 的属性,而落盘时 timeline 被整个丢掉**,重载后拼接的整串原样进了答案槽位。而且 `truncateString` 截尾保头,长回合被截断时丢掉的正是结论 | `test-answer-survives-reload.mjs` + `test-turn-view-renderer.mjs` + `test-runtime-event-retention.mjs` — 不存整个 timeline,只存**分割点**:最后一个文本块就是答案,它之前的都是叙述。一个字符串的成本,重载时据此重建两段,恢复实时那一刻的版面——答案回到答案位,叙述回到过程区,并且答案单独存所以截断也带不走它。叙述里的空行是工具卡片被拿走留下的痕迹、不是模型写的,只在叙述里合并;答案逐字节原样透传,因为那是模型自己的排版,别处也依赖这一点。改动之前保存的记录渲染结果一字不变,真实 timeline 永远优先于重建。验收 2026-09-17,一个 57 分钟、224 次工具调用的回合。 [gate: answer-survives-reload] |
| 任务已经总结完毕,后面又冒出两段话:那是平台自己发的待办连续性提问(「Task continuity check: the native todo list still has unfinished todo items」)和模型对它的回答。本该把这种内部回合整个隐藏,但识别方式是**匹配一句写死的英文原文**,清单里只有一条——平台后来自己新增的内部提示因此认不出来,问题和答案双双进了对话 | `test-internal-prompt-provenance.mjs` + `test-opencode-conversation-source.mjs` + `test-todo-progress-overlay.mjs` + `test-compaction-continuity-plugin.mjs` — 匹配散文撑不住:任何新增、改写、翻译过的内部提示都会漏。改成**在构建提示的地方打标记、按结构识别**,标记随文本一起持久化,重载后依然成立。标记里带 kind,因为两类的处理恰好相反:`self_check` 是平台自问,回答是脚手架,整个回合隐藏;`recovery` 是平台重新索要丢失的工作,**回答正是用户要的交付物,必须保留**。未打标记或 kind 不认识时按安全的那一侧处理。**2026-09-18 收口成结构**:平台自发的提示共三处,当时只有一处贴了标记,另两处(交付物不完整纠正、必需工具未落库纠正)发的是裸文本,它们的提问于是像用户问的一样进了对话。现在全部经由唯一缝隙 `src/main/platform-prompt.js` 在**发送那一刻**盖章,并由 `test-internal-prompt-provenance.mjs` 扫描源码钉死规则:这三个文件里任何 `.sendPrompt(` 要么在缝隙内、要么携带的是用户原文(挂起载荷 / steer / 引擎重建后的重试),将来新增的续作绕不过去。缝隙默认 kind 是 **recovery 而非 self_check**——recovery 只隐藏平台的提问、**保留模型的回答**,因为那是用户要的活;答案本身确属脚手架的,必须显式声明。用户输入的文本永不经过缝隙,所以用户自己的消息不可能被隐藏。同时把按措辞识别的两件事**拆开**——它们此前共用一个 `LEGACY_` 名字,掩盖了其中一条永远消不掉:「Continue if you have next steps…」是**引擎自带**的续跑提示,我们既不构造也无法打标记,按措辞认它是**与第三方的永久接口**,不是遗留,且只整句匹配;「Task continuity check…」才是我们自己打标记之前的遗留,只够到历史。后者从**前缀匹配**改成**多片段签名**(必须同时含 `continuation attempt:`)——前缀匹配意味着用户只要粘贴那句话再接内容,他自己的消息就会被吞掉,而这正是本条门禁存在的理由。测试样例改用**构造器的真实输出**去掉标记,而不是手写简写:手写样例会和平台实际发出的文本漂移,那样规则就是在对着虚构的东西做测试。已经落盘的历史用一条前缀规则追溯修好(该提示内嵌实时计数,永远无法整句匹配),opencode 自带的那条自动继续提示行为一字不变。在用户真实会话数据上验证:适配器输出的 4 块收敛成 2 块,反向确认原代码仍是 4 块。验收 2026-09-17 现场取证。 2026-09-23 补：平台发给自己的提示词也不算用户历史。`test-resume-continuity-guard.mjs` 钉住 resume 连续性比对排除平台/引擎自撰的提示词——它们以用户消息身份存在于引擎侧、却从不出现在 Lily 自己的历史里，一串自检就能把真实用户消息挤出比对窗口，读成历史不匹配并白白丢掉整个 resume（现场 2026-09-23）。判定用的是写入时打的标记而非文案匹配，且在解包**之前**判定（标记完整时归属才无歧义）；两种标记都算平台所写，恢复类提示词的答案仍归用户、问题本身不算。红线在反方向：被引擎包装过的消息仍是用户的、用户引用了内部提示词的原话仍是用户的，两者都必须继续参与比对——断言检查的是**匹配原因**而非仅仅结果，因为误删会让两侧同时清空、以相反的理由给出相同的"通过"。真正不相关的两段历史仍judged为不匹配。 [gate: internal-prompt-provenance] |
| 客户 0.1.176 正常、0.1.177 启动即「智能工作台 (未响应)」,会话 553 / 788 / 1450 条:`ARTIFACT_SCHEMA_VERSION` 4→5 让**每一个**会话的完成标记失效,启动时的产物回填于是重走全部历史。让它从「启动慢」变成「卡死」的是两个性质——**一个会话是一整块同步工作**(全部消息逐条解压、重新派生、写回,循环结束前主进程什么都答不了);**完成标记只在整段循环跑完之后才写**,所以强退一次就永远回到原点,每次启动都卡 | `test-resumable-enrichment.mjs` + `test-artifact-freshness.mjs` — **先拆掉承重**:显示不再依赖迁移跑完。读取会话某页时,过期记录在内存里就地重新派生后返回(实测一页 50 条 **14 ms**,而把它们写回要 **205 ms**——所以只推迟写,不推迟算)。端到端证明:把真实库全部派生字段剥掉、后台一条不跑,打开一页得到 **24/24 条按当前 schema 显示,而库里仍是 0 条已升级**。于是 schema 升版既不可能让界面显示错误,也不可能为了显示正确而必须在启动时干活;后台那趟仍然有界可续跑,但**降级成纯粹的缓存预热**。在此之上,原来的修法是:工作单位从「一个会话」改成「一片消息」,每片之后立刻持久化游标:强退最多丢一片。片的大小不写死——实测成本按**每条**平滑估算再换算成批量(同一条记录在 macOS 热缓存上约 1 ms,Windows 过杀软要数倍),并且**片内也按时钟收口**,因为成本是长尾的(少数记录要 stat 几十个路径),条数挡不住尾部。解压是唯一的大头(实测 200 行取压缩字节 8 ms、解压 32 ms),所以 `messageSlice` 的解包改成惰性,时钟才真正管得住它。会话按最近更新倒序处理,用户最可能打开的那个先好。同一份真实库(1506 条,剥掉派生字段让回填真有写库工作),用心跳探针量**事件循环延迟**这个「窗口还答不答话」的直接指标:旧形状最大 1081 ms、p95 917 ms、中位 112 ms,>100ms 采样 12 次、>500ms 4 次;新形状最大 881 ms、**p95 80 ms**、中位 30 ms,>100ms 3 次、>500ms 1 次。最大值只降一点,因为它是**单条记录的下限**:库里那条 13 MB 的 envelope 一条就要 1022 ms(解压 67 + 派生 397 + 写回 558),而中位数只有 5 ms——单条记录的重写无法再切分。超过 512 KB 的记录因此独占一个 tick,不再叠在已经花完预算的一片上。这条下限是公开的已知项,不是漏洞:一次性的 0.9 秒顿卡远在 Windows 判定「未响应」之前,而客户遇到的是分钟级反复冻结。**自查又挖出三个自己埋的坑并各自双向验证**:seq 读不出来时游标不前进 → 同一批行被永久重读(造出来是 4 个 tick 重复处理同 3 条),现在无进展即停该会话且不写标记;versions 缺一半会生成 `aundefined` 后缀 → 清扫把**合法游标全删**、所有会话回到从零重来(正是本条要修的行为被自己复活),现在缺版本号就拒绝构建或删除任何键;回填抛异常原本整会话放弃且游标不落盘 → 每次启动都在同一条毒记录上失败、该会话永远回填不完,现在按**单条**失败开路、跳过并计数(那一条保留已存产物=baseline)。最后一个写死的场景数字也去掉了:512 KB 那个隔离阈值改为按**实测每字节成本**换算(本机算出 488 KB,与原先拍的数字几乎重合;慢 10 倍的机器自动降到 64 KB 下限),并补上 kill switch `LILY_SESSION_ENRICHMENT=0` | [gate: resumable-enrichment] || 历史大的用户每次冷启动都要盯着检查页看约 4 秒:那道门是唯一的阻塞步骤,而它的开销只有一件事——`PRAGMA integrity_check` 逐页读完整个文件。实测 918 MB 冷缓存:全量 3973 ms,而门里其余所有判断(文件头、schema、热日志回滚、恢复回执、消息/会话计数)加起来 33 ms。页面超 800 ms 就显形,所以大库必看 | `test-startup-admission.mjs` + `test-database-recovery.mjs` —— **校验没有被砍掉,砍掉校验正是丢库的方式**;它只是挪出关键路径:门用「同样的判断减去逐页扫描」放行,全量扫描在窗口打开后立刻在后台跑,**每次启动的覆盖率一字不变**,变的只是用户等不等它。仍然阻塞的情形一个不少:上次没走到 `before-quit`(崩溃、强退、断电——恰恰是可能出现半截写入的场合)、上次后台校验失败或没跑完、标记缺失/损坏/版本不符(含首次启动与从旧版本升级)。标记在**启动时**就置脏、退出时才置净,所以崩溃不需要被任何人捕获。后台校验失败会落盘,下次启动自动变回阻塞门并带着候选副本进入修复流程。kill switch `LILY_STARTUP_FAST_ADMIT=0` 恢复旧行为;所有不确定一律答「校验」,标记坏掉最多让启动退回原来那么慢。同时把产物回填接进**已经存在**的进度通道 —— 客户那次看到的「未响应」本可以是「正在整理历史记录 361/1515(可继续使用)」:管道和进度条早就在,只接了旧版迁移一个生产者。进度以**消息**为单位(会话为单位时,那条 1450 的对话会让进度条整段不动),续跑只计剩余(实测 total 1515 与真实消息数完全吻合,单调不减,收尾 done===total) | [gate: startup-admission] || 回答已经结束,画面里却还在冒东西:一个回合被渲染成**三张卡片**,三张的待办清单是同一份、停在不同进度。用户给的关键事实是「退出重启就消失」——重载时一条记录只生成一个 article,**所以重复从来没进过数据**,是活动渲染的产物。取本机真实库对账:那次提问之后只有**一条** assistant 记录(2514 字,单个 turnId)。根因是代码里**根本不存在「一个 turnId 只能有一个 article」这条不变量**:活动路径 `ensureLiveArticle` 用 Map 按 turnId 复用,提交路径却 `createElement("article")` 无条件追加,从不看列表里已有什么;两张卡能不能合并,取决于**另一处启发式** `liveTurnRenderMode` 是否恰好返回 `remove-duplicate`。它返回别的值、或提交路径再跑一次(重渲染/分页/投影更新),就多一张 | `test-one-turn-one-article.cjs`(真实 Electron DOM,16 项)+ `test-turn-view-model.mjs` —— 把「判断」换成「不变量」:新增 `turn-article-mount.js` 作为回合进入列表的**唯一入口**,按 `data-turn-id` **就地替换**已有卡片(就地,所以已完成的回答不会跳到对话底部)。这样任何生产路径都无法再添第二张,`liveTurnRenderMode` 从正确性要求降级为优化。刻意收窄:没有 turnId 的 article(通知、定时草稿、绑定卡)不参与合并;协调只在单个会话的列表内进行;优先级只有一个方向——**已提交的卡片压过活动外壳**,绝不反向,所以真实答案不可能被自己的空壳覆盖。同一个病还有第三种形态,现场证据给出了完整事件序:`assistant.final(A) → turn.completed(A) → user.committed(B) → turn.started(B)`。`runtime.liveTurn` 是**单个槽位**,而清理旧活动卡的唯一时机是「它还占着槽位、且它的已提交卡片恰好已到达」;错过这个窗口,B 一 `turn.started` 就把槽位顶掉,**A 的活动卡从此再没有任何代码会看它一眼**,并且因为新用户气泡按「插在当前活动卡之前」定位,那张空卡就落在了新提问的下面。回合**结束**那一刻同样会多一张:已提交卡片落地时活动卡还占着槽位。并且那份 `liveArticles` 旁路 Map 已被**删除**:它是 DOM 已经持有的状态的第二份拷贝,两者会漂移,报上来的空卡正是这种漂移(条目活得比节点久,或节点没有条目指向)。现在 `is-live`/`is-sealed` 由视图模型每次渲染互斥维护,**列表本身既是显示也是记录**,从 DOM 派生就不可能不一致——这是删机制而不是加机制。**运行时的补救扫描随后也被删除**:它允许坏状态先出现再收拾,也顺带掩盖了制造它的生产者。取而代之的是**源码层规则**(与内部提示那条同一套办法):凡是造出回合卡、又会碰列表的模块,必须经由挂载点;纯工厂(完全不碰列表)放行。实测注入一个绕过挂载点的假生产者,规则当场拦下。于是重复**造不出来**,而不是被扫掉。身份判据同时收窄为 **(turnId 且 是回合卡)**——用户气泡为了 minimap 定位也带同一个 turnId,只认 id 会把提问和答案当成同一个对象,实测后果是助手卡整个不渲染。原先那条与槽位无关的判据(同一回合的已提交卡片是否已在列表里)——在就删活动卡,不在就留着。运行中的回合按定义没有已提交卡片,因此永远删不到;这条永远只是去重,绝不会变成「答案消失」。真实 DOM 26 项,含按现场事件序复现的孤儿卡与结束时重复两种。之所以跑真实 DOM 而不是 jsdom:这是个 DOM bug,替身把 replaceChild 或选择器语义搞错就什么都证明不了 | [gate: one-turn-one-article] || 长任务验收现场:模型写过的 15 KB 生成脚本,在历史里被裁成「头+尾」,于是模型**照着这个视图重写**了它——补出来的中段自成一体,但供应商名从设计稿的 云山/汇谱/明远 漂成了 云杉/慧璞/铭远,而且全程没有任何报错,因为写入工具忠实地写了模型发来的东西。根因有两层:其一,**五个守卫各自发明了自己的 `[lily: …]` 占位符**,而拦截「把占位符当正文写回」的兜底只认识其中两个,第五个生产者对它天然隐形;其二,**裁剪只看大小、不看用途** | `test-history-elision.mjs` + `test-context-window-guard.mjs` + `test-live-file-history-guard.mjs` + `test-large-output-guard.mjs` + `test-empty-assistant-history-guard.mjs` —— 统一到 `lib/history-elision.cjs` 一个契约:一处构造、一个识别谓词、一条扫源码的规则(这条规则上线当场就抓出了我没数到的第五个生产者)。更关键的是判据从「多大」换成「**做什么用**」:工具输出、历史回答是**给模型理解的**,头+尾摘录仍然有用;而写入类工具的输入是**给模型复现的文件正文**,摘录读起来像完整文件,模型就会凭想象补中段——对这类内容,**指针严格优于摘录**:它不可能被误当成整体,还告诉模型整体在哪。指针同时便宜得多(一行 vs 数千字符),所以上下文压力下省出来的预算留给了真正还值得留的部分——**这是变聪明,不是变省**。每条标记都说清三件事:删了什么、真东西在哪、接下来该做什么;识别锚定在开头,所以正文里**引用**标记的真实文件不会被误伤 | [gate: history-elision-contract] |
## Enforcement

Desktop reply-cache companion guards: `test-collaboration-reply-cache.mjs` and `test-collaboration-reply-cache-service.mjs` keep server-owned quotes inside the existing encrypted local message envelope and outside durable send intent. Source masks commit with sync cursors, cover old/future references without decrypting full conversations, survive restart, and preserve unavailable-before-revoked privacy. Legacy/own-revoked replies do not revoke unrelated references. History generation fences and strict IPC protect account/membership/bootstrap boundaries and late errors. The two signed PostgreSQL message-fields/reply-snapshot integrations exercise SQLite reopen, real bootstrap completion, IPC, revoke events and pre-join recipient masking; OS key wrapping remains a fixture, not native two-app acceptance. Source-mask retention cleanup remains Task20.

Reserved snapshot input guard: `test-collaboration-reply-snapshot-input.mjs` runs the actual HTTP parser and explicitly rejects server-owned snapshot fields without changing unrelated legacy unknown-field handling.

Reply-snapshot companion guard: `test-collaboration-reply-snapshot.mjs` covers server-derived immutable quotes, separate fixed-purpose encryption, bounded Unicode excerpts, receipt replay, batched recipient authorization and revocation/deletion placeholders. `server/scripts/collaboration-reply-snapshot-integration.mjs` separately exercises signed HTTP with real migrations through 041, source/reply edits, ciphertext transplantation, membership history boundaries, rejected client-supplied snapshots and transaction rollback. Desktop quote caching/revocation masking and retention-time ciphertext cleanup remain separate requirements; this server slice does not claim those are complete.

Message-intent companion guards: `test-collaboration-create-intent.mjs`, `test-collaboration-create-ack.mjs` and `test-collaboration-message-time.mjs` cover reply/mention metadata through encrypted drafts, complete immutable outbox intents and original-device recovery. Invalid create responses cannot confirm delivery or release the conversation barrier. History-first settlement retains stable command identity and newer revisions; only explicit server timestamps count as message creation time. New creates are limited to 32 KiB, without preventing recovery or reads of previously committed 64 KiB messages. `server/scripts/collaboration-message-fields-integration.mjs` separately exercises signed HTTP, real collaboration migrations, malformed successful responses, receipt recovery and SQLite reopen, including private history boundaries. The old-size upgrade case seeds an existing encrypted message and valid receipt fingerprint; it is not a test of an old released binary. Send-time encrypted quote snapshots, mention-candidate UI and full Electron acceptance remain separate requirements.

Durable-read companion guards: `test-collaboration-read-checkpoint.mjs`, `test-collaboration-read-recovery.mjs` and `test-collaboration-activity-projection.mjs` protect read intents separately from the message outbox, original-device recovery, monotonic confirmation, coalesced higher observations, persisted retry backoff, clamped ACK recovery and exact authorized snapshot counts. Pending refresh generations survive stale bootstrap/reopen and prevent cursor ACK before the current own-read projection is recovered; a hanging read does not block text delivery. `server/scripts/collaboration-unread-integration.mjs` uses real collaboration migrations, signed HTTP, a 600-message fixture, two devices and desktop SQLite reopen; it verifies that a 200-message cache does not truncate unread/mention counts, private membership bounds apply, and the exact second-device read projection is saved before cursor ACK. This does not replace viewport/focus interaction or two full Electron-client acceptance.

Mutation-recovery companion guard: `test-collaboration-message-mutations.mjs` and `test-collaboration-mutation-transport.mjs` exercise encrypted edit/revoke intents in the existing outbox, typed commit evidence, immutable creation order and revision-conditional history completion. `server/scripts/collaboration-mutation-desktop-integration.mjs` separately exercises real PostgreSQL with the shipped collaboration migrations (033/037/038/040/041), signed HTTP commits, dropped edit/revoke responses, desktop SQLite reopen, receipt recovery and authorized history. It also checks real revision conflicts, durable editing intent/reason across restart, the safe operation IPC view and explicit skip before the next queued create is sent exactly once. Account prerequisites and OS key wrapping remain test fixtures; simulated dropped responses do not substitute for full deployment migration, full Electron or private-bucket acceptance.

Operation-view companion guards: `test-collaboration-message-operations.mjs` and `test-collaboration-message-operation-ipc.mjs` protect additive SQLite migration, bounded account/conversation operation reads, strict request/response partitions and finite error codes without expanding the lightweight state broadcast. Only persisted edit intents expose their own editing text; credentials, device identities and raw error details are excluded. `test-collaboration-operation-evidence.mjs` checks that a positive ACK dominates a late receipt failure even when the outbox state has not changed yet, and that retry accounting/restart cannot revive the stale failure reason. These foundations do not substitute for inline edit/revoke, unsent editing drafts or recovery-button interaction acceptance.

Edit-draft companion guards: `test-collaboration-edit-draft.mjs` and `test-collaboration-edit-draft-ipc.mjs` protect the separate encrypted unsent-edit record, restart recovery, cross-window generation CAS, account/conversation isolation, transactional revocation cleanup, and strict renderer boundaries. This foundation does not itself provide inline edit/revoke UI or conflict comparison acceptance.

Enterprise-revocation companion guard: `test-collaboration-directory-refresh.mjs` and `server/scripts/collaboration-enterprise-http-integration.mjs` — legacy authenticated enterprise/admin member and organization mutations commit revocation, directory events and recipient cursors atomically. Trusted actors have explicit checked provenance rather than fake devices. Desktop directory refresh survives event/snapshot crashes and cannot ACK before authorized history hydration. The PostgreSQL fixture covers dual authentication, transaction-time session/role rechecks, rollback, lock ordering and download-ticket revocation races; it does not access production or a real object bucket.

Native-transfer companion guard: `test-collaboration-transfer-runtime.mjs` + `test-collaboration-save-transfer.mjs` + `test-collaboration-transfer-service.mjs` + `test-collaboration-transfer-ipc.mjs` — optional transfer initialization leaves text collaboration available, native selection never grants renderer path authority, and account/scope/purpose guards fence late work. Save reauthorizes on the server, authenticates plaintext and publishes without overwriting an existing file. Eight closed IPC commands expose safe views only; the verified-file broker remains main-only. Attachment-message admission is additionally guarded by `test-collaboration-attachment-send.mjs`: explicit send persists a stable encrypted intent, verified files enter the existing outbox, and cancellation or a changed device cannot replay the command. `test-collaboration-transfer-retirement.mjs` guards authenticated Team transfer retirement before key destruction so revoked journals cannot block unrelated personal uploads after process restart. This guard does not substitute for power-loss durability, workspace import or live-bucket acceptance.

Social-interaction companion guard: `test-collaboration-social-journal.mjs` + `test-collaboration-social-command-revocation.mjs` + `test-collaboration-social-directory-actions.mjs` + `test-collaboration-social-ipc.mjs` + `test-collaboration-social-ui.cjs` + `test-collaboration-social-navigation.cjs` + `test-collaboration-social-locales.mjs` — explicit friend/conversation intents persist encrypted before transport, unknown outcomes and same-intent retries retain their command IDs across restart, and scope revocation removes scoped commands transactionally. Account/service replacement fences IPC and DOM callbacks; post-confirmation revocation cannot return an obsolete conversation target, and later manual conversation, section or workbench navigation prevents a pending social completion from stealing focus without cancelling its durable intent or discarding drafts. Friend chat resolves canonical membership IDs; member controls use authorized server projections and every mutation remains server-authorized. Actual Electron tests cover confirmations, navigation, draft preservation, late revocation and unchanged-workbench fallback. `server/scripts/collaboration-conversations-http-integration.mjs` additionally exercises signed PostgreSQL friend/group commits with dropped responses and desktop SQLite restart recovery.

Social-directory companion guard: `test-collaboration-directory.mjs` + `test-collaboration-directory-ipc.mjs` — account-isolated SQLite projections honor explicit legacy friendship lists while retaining unknown pending/block state; complete versioned snapshots replace the full relationship directory. Malformed directory payloads roll back with their cursor, only caller-owned blocks reach the view, Team revocation removes roster/cache, and read-only IPC/preload allowlists exclude private profile fields and transport credentials. Signed PostgreSQL bootstrap coverage lives in `server/scripts/collaboration-conversations-http-integration.mjs`.

- **AGENTS.md Rule 13** makes this gate binding for every task.
- Each vector above has an automated guard; the full suite (`npm run test:unit`
  + `test:renderer`/`test:runtime`/`test:service`/`test:skills`) must be green.
- Any new capability-affecting feature adds its own (1) failure-mode test proving
  it degrades to baseline and (2) closed-loop guard, and registers it here.
- "Closed-loop verified" means tested. "Looks fine" / "should work" does not pass —
  state honestly what is automated vs what still needs a live/build verification.

## Enterprise account and pool closure (2026-09-05)

IM detail presentation closure (2026-09-07): `test-collaboration-page-polish.cjs` checks visible native confirmations above hidden list roots, cancel/Escape and surface teardown without dispatch, stable confirmation on directory refresh, visible friend/member failures and retry using the original command ID, late-result fencing, successful drawer-local refresh, directory/search empty states, and real narrow/dark action geometry and background contrast. `test-collaboration-create-dialog.cjs` also checks live locale changes preserve drafts and focus. Local controlled APIs are not production or installed two-client acceptance. [gate: collaboration-detail-presentation]

`test-enterprise-web-flow.mjs` guards response envelopes, redirect propagation, credential receipts, role-gated pages, overseas enterprise routes, and spendable balance display. `server/scripts/admin-enterprise-create-integration.mjs` exercises real migrations, first-password-change enforcement, owner reset protection, revoked sessions, personal-first/full-request organization fallback, employee ledger identity, concurrent debits and idempotent retries, plus gateway HTTP against a real localhost upstream. Existing account-free license/trial/static gateway paths and personal wallet priority remain the baseline; enterprise authorization never downgrades model selection or tools.

Reaction recovery companion guard (2026-09-05): `test-collaboration-reaction-recovery.mjs` exercises encrypted SQLite reaction commands through the real outbox/transport, ACK settlement without message revision, lost-response recovery after reopening SQLite, exact message/emoji/active evidence, definitive-rejection rollback and unblocking the next command. `test-collaboration-reactions.mjs` and `test-collaboration-ipc.mjs` retain explicit empty reaction state; `test-collaboration-timeline.cjs` checks actual Electron add/remove clicks and clearing the final chip after history merge. Existing create/edit/revoke receipt guards remain required. These are synthetic interaction/recovery tests, not a claim of two installed-client acceptance.

Overseas enterprise password login: `test-enterprise-overseas-login.mjs` and `test-service-client-bootstrap.mjs` keep the dedicated account entry accessible while preserving disabled personal SMS and purchase policy. Existing domestic SMS remains available; explicit enterprise disable remains honored.

Account nickname: `test-account-nickname.mjs` verifies Unicode length, invalid input, active-account checks, self-only mutation and renderer save feedback; `server/scripts/admin-enterprise-create-integration.mjs` proves authenticated device binding, profile persistence and reread after login. Login identifiers and Lily IDs remain unchanged.

Enterprise directory and expiring online presence: `test-enterprise-presence.mjs`, `test-enterprise-presence-websocket.mjs`, `test-enterprise-roster.cjs`, `test-account-nickname.mjs` protect self-only nicknames, offline fallback, real heartbeat identity, multi-device disconnect and executable member UI. [gate: enterprise-directory-presence]

Redis presence backend (2026-09-07): `test-collaboration-presence-redis.mjs`, `test-collaboration-presence-query.mjs`, `test-collaboration-presence-routes.mjs`, `test-collaboration-presence-gateway.mjs`, and `test-collaboration-presence-hints.mjs` guard bounded advisory reads, unknown-on-failure, exact authenticated session/device leases, block-over-Team authorization, trailing hint delivery, and socket shutdown. Opt-in `test-collaboration-presence-redis-live.mjs`, `test-collaboration-presence-query-live.mjs`, and `test-collaboration-presence-redis-restart.mjs` verify actual isolated local Redis/PostgreSQL and actual Redis stop/start; these do not claim physical two-machine acceptance. `test-collaboration-redis-deploy.mjs` guards authenticated loopback-only bounded ephemeral storage. PostgreSQL messages/outbox remain authoritative; Redis failure must not stop durable messaging. Migration 046 is additive and must precede the new API. Production rollout and full-client UI acceptance are separately recorded in the presence implementation plan. [gate: collaboration-redis-presence]

Todo-progress evidence overlay (2026-09-05): `test-todo-progress-inference.mjs` + `test-todo-progress-nudge-plugin.mjs` + `test-todo-progress-overlay.mjs` (with the existing `test-live-task-strip.mjs` / `test-turn-todo-entry.mjs`) — the task-list card is no longer hostage to model discipline. A step is shown as done by evidence only when a successful tool call's input carries every identifier unique to that step; generic steps, shared identifiers, failed attempts and output-only mentions never count. The engine plugin nudges the model after N quiet tool calls with the evidenced steps named, asking for a status-only update; turn end turns a stale "in progress" into "unconfirmed" and may run one bounded, quote-verified model pass. Everything is an overlay: the model's own list is never rewritten, the baseline rendering is byte-identical without a taskRun, and every layer fails open with its own kill switch. Live verification against a real weak-model turn is still pending. [gate: todo-progress-evidence-overlay]

视觉桥回信(2026-09-19):`test-vision-translator.mjs`(真实传输层,仅桩 fetch)+ `test-send-preflight.mjs` + `test-document-image-vision.mjs`。现场:DeepSeek 分析图片,芯片只说「已跳过图片解析」,随后主模型报网关错误页;库里 `workProgress` 记着真实原因「Vision API returned no readable image content」,但 `visionSkipped` 通知没有带 `detail`,渲染层本来就会显示它。往下查是更深的一层:**2026-09-08 的请求形状重构把 `callVisionApi` 从「取 `choices[0].message.content` 归一化后返回」改成了「返回整个响应对象」**,可读性检查于是把 `{id, choices, usage}` 判成空,**平台自己的视觉桥自那天起对每一张图都失败**,而桥的测试全绿——因为它们直接注入 `translate`,从未跨过这条缝。修法在缝上:文本在传输层一处提取(`visionReplyText`),两条调用路径(聊天附件、文档内嵌图)共用;回信为空时带上服务端的 finish_reason(content_filter / length),而不是一句泛话;`visionSkipped` 通知带上桥记下的原因。新增的检查走真实传输层,回退源码即当场复现现场那句错误。 [gate: vision-bridge-reply]

回合对 runner 的预留(2026-09-19):`test-runner-turn-reservation.mjs` + `test-runner-live-config.mjs` + `test-turn-orchestrator.mjs` + `test-opencode-agent-session.mjs`。现场:闲置 3 小时的会话发「继续」,得到「engine session was recycled」失败卡,随后救援回合才正常跑。库里事件序:ensure 之后 1 秒,回合前压缩就已经撞上 RUNNER_TERMINATED,发送再撞一次,然后等满 12 秒 dispatch 宽限才判失败。根因是**不存在「已被回合认领的 runner 不算空闲」这条不变量**:从 ensure 到 send 之间(视觉桥、文档抽取、路由、回合前压缩)runner 不 busy,于是八处各自手写 `isAlive() && !isBusy()` 的「终止空闲 runner」路径(设置保存、登录、权限模式、技能同步、运行包刷新)都把它当成可以回收的,回合握着的引用随即变成尸体。修法:runner 自己拥有唯一的空闲判定 `isIdle()`(活着、不忙、未被认领),认领是**回合自身存活的谓词**(仍是当前回合且未终结)而不是需要记得释放的标志,所以中止/被顶替/终结的回合按构造自动放手;八处调用全部改问 runner,并用扫源码的规则禁止再手写;没有引擎时的 dispatch 失败立即确认(原来白等 12 秒 SSE 证明)。副作用是修正:技能切换原本会把**正在跑**的 runner 也终止掉,现在只丢弃死的、留下忙的与被认领的,变更在下一次发送由 ensureProcess 的配置指纹自然生效。 [gate: runner-turn-reservation]

聊天补全回信统一读取（2026-09-19）：`test-chat-completion-reply.mjs` + `test-vision-translator.mjs` + `test-model-vision-capability.mjs` + `test-model-compatibility-probe.mjs` + `test-evidence-entailment-judge.mjs`。审计视觉桥回归时发现同一类问题散在六处：六个模块各自手写 `choices[0].message…`，兼容探测丢掉内容分段数组，视觉探测只看 HTTP 状态——网关 200 返回 HTML 错误页也会被判「支持读图」（OICM+ 网关就这么坏过）。现在只有 `chat-completion-reply.js` 读回信（文本/推理链/工具调用/finish_reason/是否是补全），六处全部改问它，扫源码规则禁止再手写；探测判「回信是补全」而不是「状态是 200」。 [gate: chat-completion-reply-seam]

JSON 落盘原子化（2026-09-19）：`test-json-file.mjs` + `test-license-update.mjs` + `test-model-settings-secret-storage.mjs`。审计：60 个模块各自 `fs.writeFileSync(file, JSON.stringify(…))`，14 份自己的 `writeJson`，没有一份是原子的——崩溃/强杀/断电落在写入中间就是半截文件，下次启动读成 `{}`；会话索引丢库那次治的是症状，这才是病根。另有 13 处各自手搓 temp+rename，命名、清理、权限位各不相同。现在 `json-file.js` 是唯一实现（同目录临时文件 + rename，transient 的 Windows 锁重试），67 处全部改走它，每处保留原来的字节形态（缩进/尾换行/0600）；扫源码规则禁止再出现第二份（两处有据可查的例外：`flag: "wx"` 的只创建标记、注入 fs 的 Playwright 状态存储）。测试用真实写失败证明旧内容完好且无临时残留。 [gate: json-file-atomic-write]

文件种类单表（2026-09-19）：`test-file-kinds.mjs` + `test-vision-translator.mjs` + `test-send-preflight.mjs` + `test-turn-artifacts.mjs` + `test-turn-artifact-model.mjs` + `test-attachment-preview-model.mjs` + `test-content-task-intent.mjs` + `test-capability-broker.mjs`。审计：「什么是图片」在 14 处各定一份、8 种不同的表——`.heic` 对任务路由是图片、对视觉桥不是；`.svg` 对渲染层是图片、对桥不是；`.tif` 三处有三处无；文档扩展名另有 17 份，ext→mime 表 5 份，文件名正则 5 处又各抄一遍。现在 `src/shared/file-kinds.mjs` 是唯一的表（主进程 require、渲染层 import 同一文件），分组**按用途命名**——是不是图片 / 浏览器能不能显示 / 视觉桥能不能读 / 是不是办公文档——每个消费者问它自己的问题，不再拼扩展名；mime 与文件名正则都从表派生；扫源码规则禁止再出现第二份列表（两处有据可查的例外：按扩展名键的魔数签名表、抽取器的能力清单）。顺带把主进程里同名的 `file-kinds.js`（diff/文件树的文本判定）改名为 `text-file-kinds.js`，避免两个「file-kinds」。 [gate: file-kinds-single-table]

引擎通知目录（2026-09-19）：`test-engine-notices.mjs` + `test-engine-notice-policy.mjs` + `test-send-preflight.mjs` + `test-turn-user-wait.mjs` + `test-content-source-gate.mjs`。审计：18 个模块 28 处手写 `{code, level, panel, replace, replacesCode, done, detail}`，每个作者都得记全所有字段——`detail` 有 8 处没带（视觉「已跳过」芯片 11 天没有原因），`panel` 也时有时无；渲染层另存一份可见性策略，靠注释「与主进程保持同步」。现在 `src/shared/engine-notices.mjs` 是目录：每个 code 声明一次它的层级、面板可见性、替换槽、是否结束阶段；调用点只说 code 和当下的 detail（动态槽位如逐工具/逐子任务仍由调用点给）；两个进程的策略都从目录派生。两处一致性修正：`visionPreparing` 与 `documentPreparing` 一样成为实时面板行；压缩「完成」行按目录替换「准备」行。扫源码规则禁止再手写通知对象，且每个 `engineNotice("x")` 的 code 必须在目录里。 [gate: engine-notice-catalogue]

文字系统判定单一定义（2026-09-19）：`test-script-detection.mjs` + `test-tool-call-rescue.mjs` + `test-document-delivery-gate.mjs` + `test-context-budget-manager.mjs` + `test-memory-vector-index.mjs`。审计：17 处各写一份汉字字符类、7 种写法（只有基本区 / 带扩展 A / 带兼容区 / 带假名 / 直接写字面字符），`answerLanguage` 两份一模一样——同一句话对回答语言是中文、对范围备注不是、对 token 估算算一半。现在 `src/shared/script.mjs` 是唯一定义（汉字类 = 扩展 A + 统一区 + 兼容区，`answerLanguage` zh/ar/en，`instructionLanguage`），17 处、两份副本、4 处 `instructionLanguage === "zh"` 全部改走它；区间用 \u 转义写死，编辑器的规范化不可能把 U+F900 变成 U+8C48 而把字符类扩到半个 Unicode；扫源码规则禁止再手写。 [gate: script-detection-single-definition]

失败回合单一形状（2026-09-19）：`test-turn-failure.mjs` + `test-turn-orchestrator.mjs` + `test-turn-archive.mjs` + `test-turn-start-guard.mjs` + `test-tool-call-rescue.mjs`。审计：14 处手写 `turn.failed` 载荷，字段名 `code` 7 处、`errorCode` 10 处、两个都写 3 处、漏 `failed: true` 1 处；5 个读取方各自写一份 `errorCode || code` 兜底——而 `code` 在 runner 载荷上又是**进程退出码**，这个兜底可能把退出状态当成错误码。现在 `turn-failure.js` 的 `turnFailure()` 是唯一构造（failed / assistant / errorCode / errorCategory / retryable + 站点已知的附加信息），`failureCodeOf()` 是唯一读取（唯一还认识旧字段名的地方，且数字退出码永远不算错误码）；扫源码规则禁止再手写载荷字面量或第二份兜底。 [gate: turn-failure-shape]

事件类型清单出自契约（2026-09-19）：`test-runtime-contract-manifest.mjs`。审计：`turn-event-types.js` 与 `runtime-event-bus.js` 各手抄一份「可无回合发出」「回合结束后仍可记录」的事件清单，与 `runtime-contract.json` 已双向漂移（文件多了 `turn.parent_closure_recovery`、少了 `context.compactionDecision`）。现在 schema 从契约派生四组集合，两个文件只转引；清单测试断言派生集合与契约一致且无本地副本。 [gate: event-type-lists-from-contract]

瞬时网络故障签名（2026-09-19）：`test-network-errors.mjs` + `test-turn-error-classify.mjs` + `test-serve-diagnostics.mjs`。审计：6 处各写一份 socket/DNS/超时错误码与短语（下载重试、模型探测、serve 诊断、失败分类、会话失败策略、协作离线判定），fetch 栈新出的 `UND_ERR_*` 要在 6 处各加一遍才算加上。现在 `src/shared/network-errors.mjs` 是传输层签名的唯一定义，各站点在其上组合自己的范围（5xx、overload、鉴权）；扫源码规则禁止第二份码表。 [gate: transient-network-signature]

小助手单一副本（2026-09-19）：`test-shared-helpers.mjs`。`codedError` 7 份一字不差、`isPlainObject` 5 份一字不差，各归一处（`coded-error.js` / `plain-object.js`）；三份消息格式不同的 `codedError` 变体不是副本，留在原地。 [gate: helper-single-copies]

会话读取预算（2026-09-19）：`test-conversation-read-budget.mjs` + `test-turn-archive.mjs` + `test-resume-binding.mjs` + `test-resume-continuity-guard.mjs` + `test-media-result-tracker.mjs` + `test-opencode-conversation-source.mjs`。客户报「一千多条消息的会话打开卡几秒」。真实库实测（365 条会话及其 4 倍拷贝 1460 条）：分页打开本身只要 50 ms 主进程 + ~300 ms 端到端，卡的不是分页——是**每一条热路径都在整段解包**：`getConversation()` 把全部 envelope gunzip+parse，1460 条一次 565 ms，而调用方只要尾部 12 条（媒体去重）、第一条用户消息（resume 绑定）、最后 8 条用户文本（resume 连续性）、六十个回合（回合意图）——每次发送都付一次，引擎 runner 活着时每次打开再付一次；另外一页 50 条 13.6 MB JSON，66% 是进程事件的原始 stdout，面板从不渲染。修法：`getRecentConversation`/`getFirstUserMessage` 有界读（565→82/0/14 ms），热路径全部改走；进程事件在归档时按面板已用的紧凑形状存，旧记录在读取时投影（一页 13.6→5.6 MB）；扫源码规则禁止热路径再整段读。 [gate: conversation-read-budget]
会话切换成本（2026-09-19）：`test-session-switch-warmup.mjs` + `test-committed-message-equivalence.mjs` + `test-session-runtime-store.mjs` + `test-json-file.mjs`。客户报「切换会话时卡」。真实应用 CDP 实测：每次切换后 150 ms 主线程跑一次不起引擎的 runner 预热，首次 201 ms、之后每次 45–70 ms——其中 `resolveSessionSkillIds` 一次 **读 831 个文件**（每个已装技能的启用判断都重读全部 12 个内置 manifest，45 个技能 × 12），`writeSessionAgentGuide` 的签名计算再算一遍，`writeActiveMcpConfig` 每次预热都 temp+rename 重写 `opencode-mcp.json`；Windows 下每次 open 过一遍 Defender、rename 撞瞬时锁退避 300 ms，就是客户看到的秒级。修法：manifest 目录按文件自身 mtime+size 校验（每技能一次 stat，未变零读取，改了下一次读就见，写入方不用记得失效；每次返回独立对象，改动不污染缓存），内置版本每进程读一次而状态归一每次照跑；AGENT.md 与 MCP 配置先比对再替换（重启后内容未变不再写盘）；`stage-profile.js` 一个开关看分段耗时，预热超过 100 ms 记日志。实测 `resolveSessionSkillIds` 25→2 ms、831→0 次读；预热后的 guide 检查 27→3.8 ms，零写盘；真实应用每次切换的预热 45–70→~10 ms。渲染进程 CPU profile 再抓到另一半：切入缓存了 359 条的会话时，`syncCommittedMessages` 判定「来的这条是不是已缓存的某条」花 **565 ms**——每一对都重新正则归一化两段文本三遍，而 30 分钟时间窗这个最便宜的门放在最后；一千条会话就是客户看到的秒级。修法：判定谓词抽成 `committed-message-equivalence.js`，文本面向按消息对象派生一次（按派生字段校验，原地改动会重读），时间窗先于任何文本比较；谓词答案不变。实测 1000 条历史合并 50 条官方页 + 去重：毫秒级。 [gate: session-switch-cost]

时间线文字块单一定义（2026-09-23）：`test-turn-streaming-blocks.mjs` + `test-turn-process-group.mjs`。一个 84 分钟、396 次工具调用的长任务完成后,最终回答上方仍排着 6 条「批次 9 完成,进入批次 10…」式的进度旁白,读起来像做完了还在说。查到三处:①工具调用之间的纯换行被封成独立文字块——35 条里 28 条是空白,占掉只存 100 条的归档窗口,挤走真实工具历史;②「哪一块是答案」在三处各写一份扫描(注释写着 mirrors renderer),空白尾块会被当成答案,真答案在旁白里再出现一次、重载时存成空答案;③回合封存后旁白不随步骤折叠。修法是删重复、不加过滤:主进程与渲染器两份一模一样的块构建逻辑合并为 `src/shared/timeline-blocks.mjs`(与 file-kinds 同一模式),两条规则只在这里——块从第一个可见字符开始、答案是最后一个有可见文字的块;封存且有步骤组时旁白按书写顺序进折叠组、排在步骤之前。不变量:打开的块内部空白逐字节保留,完整答案另存逐字节;运行中旁白原位;没有步骤组时旁白不藏进空组;规则之前归档的空白块在显示层照旧跳过且永不当答案。反证:三处各自撤回测试均失败。 [gate: timeline-blocks-single-definition]

压缩结果以引擎为准（2026-09-24）：`test-compaction-outcome.mjs` + `test-opencode-runtime-ownership.mjs` + `test-opencode-agent-session.mjs`。一个长会话当天 11 次压缩全记为失败,原因同为 `summarize failed: OPENCODE_HTTP_TIMEOUT after 30000ms`;引擎库里同期的压缩消息显示每一次都做完了(37–75 秒)。根因:同一个等待有两道界限——会话声明 90 秒,SDK 传输表里另有写死的 30 秒,内层永远先到。后果三连:成功的压缩向用户报「跳过」、按「最近失败」退避了该做的压缩、「上次压缩」停在上午所以同一压缩被反复发起(26 次)。修法:等待界限只有 `runtime/compaction-timeout.js` 一个主人,SDK 表删去 summarize 项;等待结束不等于结果——超时错误携带仍在运行的调用(即引擎工作租约),结算后按真实结果记账(`compaction-outcome.js`);运行中会话处于 compacting,决策给出 `compaction_in_progress`,不叠第二次摘要;调用方区分「仍在收尾」与「失败」,前者不记假压缩、提示结束转圈。不变量:窗口内完成照旧记成功、真实错误立即记失败、本轮拿不到摘要照旧不压缩继续;serve 死亡经租约拒绝记为失败,迟到结果永不记成功;进行中标记只在进程内存。反证:四处各自撤回测试均失败。 [gate: compaction-outcome-truth]

看门狗区分睡眠与卡顿（2026-09-24）：`test-app-watchdog.mjs`。当天两条「main event loop lag」(136,715 与 2,841,042 ms)与系统电源日志的合盖睡眠逐秒吻合,照信排查就会去找一个从未发生的卡死。墙钟睡眠时照走、单调时钟(macOS mach_absolute_time / Linux CLOCK_MONOTONIC)停走,二者之差就是睡眠:卡顿与渲染心跳陈旧改用单调时钟计量,差值记为 `system_sleep`;powerMonitor suspend/resume 作第二信号,唤醒时重置两个基线,两路看到同一次睡眠只记一次。不变量:真卡死两钟同走照旧报警;只注入墙钟的测试行为不变;无 powerMonitor 时单靠钟差。反证:改回墙钟计量测试失败。 [gate: watchdog-sleep-not-lag]

进程信号身份（2026-09-20）：`test-process-signal-guard.mjs` + `test-process-jobs-core.mjs`。客户报主窗口变成「This window could not load · killed · 15」，运行一段时间自己出现。实测 Electron 41：渲染进程收到 SIGTERM 就是 `killed · 15`（SIGKILL 是 9），所以是有人给渲染进程发了 SIGTERM。平台里三条路径（删会话清理长任务、长任务 stop、`lily_process_jobs` stop）按**记录下来的 pid** 发信号，发之前只做 `kill(pid, 0)` 活着检查——pid 被系统回收后对谁都为真，mac 重启后早启动的渲染辅助进程很容易拿到旧任务的 pid；`stopPidTree` 进程组失败还会回退成直接对该 pid 发 SIGTERM。修法：所有对记录 pid 的信号只走 `stopRecordedProcess`，永远拒绝自己的进程树（main、父进程、`app.getAppMetrics()` 的全部辅助进程），记录带进程身份（启动时间+命令指纹）时必须仍然匹配；拒绝即视为任务已结束，不等待、不升级；新任务在 spawn 时记录身份；扫源码禁止其它裸信号。 [gate: process-signal-identity]
渲染进程自愈（2026-09-20）：`test-renderer-process-recovery.mjs` + `test-window-appearance.mjs`。同一现场的另一半：渲染进程死后主窗口永远停在兜底页——「重新加载」按钮是 `location.reload()`，刷新的是 data: 兜底页自己；文案是给协作弹窗写的；退出只在 `LILY_DEBUG_RENDERER` 下记日志、不上报，生产库里一条记录都没有。修法：正在运行的页面渲染进程一退出，窗口自己重载真实页面（每分钟最多两次），只有反复死、clean-exit 或首帧前崩溃才显示兜底页；按钮导航到 `lily-recover://reload` 由守卫拦截并重载真实页面、同时重置预算；主窗口有自己的文案；每次退出无条件记日志并上报 runtime diagnostics（reason、exitCode、运行时长、是否自愈）。 [gate: renderer-process-recovery]

Persistent IM task cards (2026-09-14): `test-remote-task-cards.mjs` guards encrypted reopen, participant/revocation checks, monotonic snapshots and delayed-ACK exact-content deduplication. `test-remote-task-cards-ui.cjs` guards panel-independent projection, DOM identity and stale account responses. Existing timeline, visible-read, reply UI and workflow UI tests passed. Local-session cards, durable background hydration, history pagination and live dual-device acceptance remain pending. [gate: collaboration-task-cards]

Task source session identity (2026-09-14): `test-remote-task-project-source.mjs`, `test-remote-task-session.mjs`, and actual Electron `test-workspace-collaboration-entry.cjs` cover identity-only menu propagation, encrypted pre-I/O origin persistence, same-session retries, invalid/archived/execution-session rejection and real session-index restart. Association cannot start an engine or alter an existing running session. Local-session card rendering remains pending. [gate: collaboration-task-source-session]

Local-session collaboration cards (2026-09-14): `test-remote-task-cards.mjs` and `test-remote-task-project-source.mjs` guard account/device/session-scoped encrypted projection, both-party reopen and revocation. `test-session-task-cards-ui.cjs` exercises the actual message panel, rebuild-preserved nodes, engine-context exclusion, cached-state labeling and logout/teardown fences. Expanded `test-remote-task-cards-ui.cjs` checks original-draft recovery and task-detail routing. Full renderer import regression passed with optional IPC fixture warnings; no live-service acceptance claim. Persistent background hydration and exhaustive task history remain pending. [gate: collaboration-session-task-cards]

Durable task hydration (2026-09-14): `test-remote-task-hydration.mjs` verifies real SQLite migration/backfill, event/cursor atomicity, retry deadlines, encrypted snapshots, newer-generation and stop fences, denial masking, UI-independent startup/live events and service timer retry. A hung task GET must not block ordinary chat sync. Existing store/sync/client-service/conversation-hydration and 27 lifecycle regressions pass. Historical discovery after bootstrap and full task pagination remain pending; no power-loss or live-device claim. [gate: collaboration-task-hydration]

Task history pagination API (2026-09-14): real PostgreSQL `server/scripts/collaboration-tasks-integration.mjs` verifies 107-task traversal with timestamp ties and changing progress, candidate-page advancement under denial, and per-task/actor authorization. Migration 048 supports immutable creation-order scans. Missing and unauthorized get share COLLAB_TASK_ACCESS_DENIED; corrupt data retains a retryable unavailable error. Route, signed policy/version and client transport tests pass. Persistent desktop scans and local card pagination remain pending. Guard tests: `test-collaboration-policy.mjs`, `test-remote-task-ipc.mjs`, `test-remote-task-policy.mjs`, `test-remote-task-routes.mjs`. [gate: collaboration-task-history-api]

Persistent desktop task discovery (2026-09-14): `test-remote-task-history.mjs` verifies 1150 encrypted SQLite snapshots, bounded page fetches, durable cursor restart, atomic snapshot/checkpoint rollback, bootstrap and stop fences, denial reconciliation, and UI-independent service startup. Existing store/sync/client/lifecycle tests pass. The current projection retains the newest 1000 cards and signals more history; older-card UI pagination and batched completion reconciliation remain pending. [gate: collaboration-task-history-discovery]

Task-card pagination (2026-09-14): `test-task-card-pagination-ui.cjs` exercises both conversation surfaces against real IPC, workflow validation and encrypted SQLite with 1150 cards, failed-page retry, refresh/page-load concurrency, stable DOM anchors, a newly added task and logout. `test-remote-task-history.mjs` and project-source tests cover local traversal and closed cursors. Shared-workspace dual-client workflow regression passes with its documented transport/account/editing fixtures. A4 implementation is complete; full-service/physical-device and large-file acceptance remain D3. [gate: collaboration-task-card-pagination]

Task Git baseline primitive (2026-09-14): real `test-remote-task-git.mjs` verifies isolated parentless history, manifest-only capture, exact bytes, immutable CAS/restart/concurrency, inherited-Git-environment isolation, file-integrity/link/control rejection and Git fsck. ZIP bundle, Git runtime dependency and existing workspace version-control regressions pass. Workflow invocation, Git worktree execution and object transport are not yet connected; B1 remains incomplete. [gate: collaboration-task-git-baseline]

Task Git workflow integration (2026-09-14): real Git and two-account SQLite workflow tests verify matching sender/receiver baseline commits, isolated worktree execution, clean baseline index, exact blob materialization despite attributes, restart/missing-final-save reuse without resetting edits, and rejection of incomplete worktrees. Shared-workspace Electron dual-client UI passes with documented fixtures. B1 implementation is complete; contribution commits/object transport (B2/B3), automatic partial-materialization recovery (C/D) and full-service/physical-device acceptance remain pending. Guard tests: `test-remote-task-git.mjs`, `test-remote-task-workflow.mjs`. [gate: collaboration-task-git-workflow]

Task Git contributions (2026-09-14): real Git changeset tests and two-account SQLite workflow tests verify explicit operations, baseline ancestry, inventory-gated deletion, unrelated-file exclusion, immutable refs and frozen restart/upload reuse. Shared-workspace Electron dual-client regression passes with documented fixtures. Contributions are currently device-local with ZIP transport; B2 still needs all-empty delivery support and explicit edited-rename identity/edge-case coverage. B3 object transport and C/D remain pending. Guard tests: `test-remote-task-changeset.mjs`, `test-remote-task-workflow.mjs`. [gate: collaboration-task-git-contributions]

All-delete task delivery (2026-09-14): real SQLite workflow and shared-workspace Electron dual-client UI verify empty-result preparation/submission, empty Git tree with deletion records, explicit delete confirmation, application and full rollback. All-filtered input is blocked; ordinary empty workspace/input import remains rejected. Generic workspace import and preparation/source regressions pass. Fixture transport is unchanged; B2 edited-rename/boundary verification and B3/C/D remain pending. Guard tests: `test-remote-task-bundle.mjs`, `test-remote-task-dual-client-ui.cjs`, `test-remote-task-workflow.mjs`. [gate: collaboration-task-empty-delivery]

Edited task renames and delivery replay (2026-09-14): real Git/SQLite workflow verifies same-file move plus complete rewrite across restart, with before/after path/hash identities, and an accepted-but-response-lost delivery replay after subsequent working edits. Unit/integration cases reject malformed/duplicate identity and inode reuse with different birth time; legacy/unsupported identity remains conservative delete+add. Device identity stays out of renderer/network/commit payloads. No live cross-platform identity or physical-client claim; B2 boundary audit and B3/C/D remain pending. Guard tests: `test-remote-task-changeset.mjs`, `test-remote-task-workflow.mjs`. [gate: collaboration-task-edited-rename]

Task contribution boundary audit (2026-09-14): real Git/SQLite workflow verifies file/directory replacement contributions, filtered ancestor and symlink blocking, actual EACCES, sparse-inventory rejection on legacy ZIP and a persisted validation gate that prevents uploading a draft interrupted before omission checks. Baseline/worktree, changeset, bundle and final shared-workspace Electron UI regressions pass. B2 implementation is complete within the current regular-file manifest contract; actual Git transport (B3), safe transition application/recovery (C) and Office/scale/full-service acceptance (D) remain pending. Guard tests: `test-remote-task-workflow.mjs`. [gate: collaboration-task-contribution-boundaries]

Git bundle transport primitive (2026-09-14): real independent-repository tests verify baseline/ancestor reconstruction, incremental unchanged-object reuse (2,098,202-byte baseline vs 727-byte delta in the recorded run), unrelated-task exclusion, absent prerequisites, corrupted bytes/internal packs, conflicting refs and idempotent import. This is local Git plumbing only; server descriptors/ACL, missing-object query, encrypted network transport and task workflow integration are not yet connected. B3/C/D remain incomplete. Guard tests: `test-remote-task-git-transport.mjs`. [gate: collaboration-task-git-bundles]

Task Git descriptor service (2026-09-14): contract/route/service tests and real PostgreSQL integration verify encrypted descriptors, immutable command replay, fixed task-input prerequisites, task-scoped missing-pack queries, bystander/removed-peer denial, revoked-input rollback and encrypted input-route consistency. Object ciphertext verification is seeded in PG; real pack/ancestry checks remain the separate local Git transport tests. Desktop protocol negotiation and encrypted Git pack transport are not connected yet; B3/C/D remain pending. Guard tests: `test-remote-task-contract.mjs`, `test-remote-task-routes.mjs`, `collaboration-tasks-integration.mjs`. [gate: collaboration-task-git-descriptors]

Desktop Git metadata and rollout gate (2026-09-14): real SQLite plus actual IPC-handler tests verify private Git input/delivery views, renderer stripping/injection rejection, mode checks and immutable encrypted descriptor replay. Client query and policy/remote-cache gates pass with Git rollout default off. Existing ZIP workflow/card/IPC regressions pass. Actual workflow bundle upload/download/materialization and full network/service verification are still pending; B3/C/D remain incomplete. Guard tests: `test-collaboration-client-service.mjs`, `test-collaboration-policy.mjs`, `test-remote-task-git-desktop.mjs`, `test-remote-task-policy.mjs`. [gate: collaboration-task-git-desktop]

Task Git workflow transport (2026-09-14): opted-in workflows export/import baseline and incremental bundles, validate ancestry and trees, and bind cache reuse to encrypted task/object proofs. Git/ZIP workflow, mixed-mode restart, real Git materialization and ordinary/all-delete Electron UI tests pass. PostgreSQL integration verifies cache hits cannot bypass object revocation. Workflow/UI transport and accounts remain fixtures; combined real encryption/network/service acceptance remains pending. B3/C/D are incomplete. Guard tests: `remote-task-ui-fixture.cjs`, `test-remote-task-dual-client-ui.cjs`, `test-remote-task-git-transport.mjs`, `test-remote-task-workflow.mjs`, `collaboration-tasks-integration.mjs`. [gate: collaboration-task-git-workflow-transport]

Git encrypted HTTP byte path (2026-09-14): test-remote-task-git-encrypted-transfer.mjs passes actual encryption, loopback multipart/Range networking, independent account journals/Git repositories, accepted-part response-loss restart, ciphertext corruption recovery, incremental size savings and cached-ticket revocation. Existing ZIP taskFiles tests pass. Provider, object API and ACL are fixtures; production TLS and combined signed domain-service verification remain unproven. B3/C/D remain incomplete. [gate: collaboration-task-git-encrypted-http]

Git task signed domain integration (2026-09-14): collaboration-objects-http-integration.mjs now covers real encrypted desktop task workflows, device signatures, Fastify routes and PostgreSQL task/object verification/binding with shared identity. Lost create/delivery ACK restarts, immutable incremental delivery, ancestor reuse, application/rollback, outsider denial and revoked cached input pass alongside existing attachment/sync/log-secrecy checks. Provider and native selection are fixtures; Fastify uses injection. B3 implementation is complete; C and D, including physical clients, production TLS/provider and scale acceptance, remain pending. Guard tests: `collaboration-task-git-http-fixture.mjs`, `collaboration-transfer-http-fixture.mjs`. [gate: collaboration-task-git-signed-domain]

Integration intent and writer lease substrate (2026-09-14): test-task-integration-intents.mjs verifies encrypted immutable intents, target serialization across chains/workspace mappings, two SQLite connections and competing processes, monotonic generations, expiry/stale-worker fencing, renewal/retry/completion/cancel and scope retirement. Existing store/local recovery regressions pass. Coordination is per account/database and caller-supplied canonical target; no production event/turn or filesystem writer is connected yet. C1/C2/C3/D remain incomplete. [gate: collaboration-integration-intents]

Authorized integration discovery (2026-09-14): test-task-integration-discovery.mjs verifies task hydration/history and actual service startup create deduplicated pending shared intents from fresh authorized Git snapshots, with persisted original-session/root checks. Missing bindings retain retries and visible safe cards; stale history cannot cancel newer delivery intents. SQLite reopen, withdrawn review cancellation, privacy, existing hydration/history and lease regressions pass. This does not admit engine turns or modify files; C1 orchestration and C2/C3/D remain incomplete. [gate: collaboration-integration-discovery]

Shared Git candidate prerequisite (2026-09-14): test-shared-git-integration.mjs passes real Git explicit-base merges, immutable two-parent candidates/materialization, contributed-attribute isolation, text/binary conflict refusal, exact-commit validation gate, CAS head advancement and publish retry without touching private W. Existing Git baseline/transport regressions pass. Validation is an injected test callback; durable validation/outbox, remote publication, conflict turns and production worker wiring are not implemented. C1/C2/C3/D remain incomplete. [gate: collaboration-shared-git-candidate]

Local shared publication recovery (2026-09-14): test-shared-publication-recovery.mjs verifies real Git atomic head/receipt publication plus encrypted SQLite candidate, exact validation and outbox journals. Pre/post-Git exceptions, later head advancement, outbox-write rollback/reopen, failed/expired validation and fresh revocation pass. Existing Git and lease regressions pass. Domain authorization/validation remain injected test callbacks; production workers, model conflict admission and network publication/outbox delivery are not connected. This is not power-loss or global transaction proof. C1/C2/C3/D remain incomplete. Guard tests: `test-shared-git-integration.mjs`. [gate: collaboration-shared-publication-recovery]

Background integration preparation (2026-09-14): production service recovery now runs a bounded, leased worker through private fresh task authorization and verified Git acquisition. test-integration-worker.mjs verifies real Git/SQLite preparation, explicit no-validator wait, configured test-validator recovery/publication, retry deadlines, stop fencing and scope cleanup. Signed Fastify/PG integration starts the actual service from a freshly authorized discovered intent and verifies encrypted acquisition/preparation without changing private source files. ZIP/Git workflow and store/history/lease/publication regressions pass. Formal validators, model conflict turns, visible retry/status and remote publication delivery remain incomplete with C1/C2/C3/D. Guard tests: `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-integration-worker]

Integration status and retry (2026-09-14): closed main-process projections now expose durable integration progress on existing cards and details, including original-binding/validation waits and local publication awaiting remote sync. Real SQLite tests verify same-intent fresh-authorized retry, stale/running/withdrawn/completed fences and metadata isolation; Electron verifies status/localization, single-flight/failure handling and late-response navigation fences. Existing worker, cards/history/hydration/IPC, ZIP/Git workflows and actual SQLite/IPC 1150-card Electron pagination pass. Renderer domain/retry calls remain fixtures; formal validation, conflict turns, remote publication and C3/D remain incomplete. Guard tests: `test-remote-task-cards-ui.cjs`, `test-task-integration-discovery.mjs`, `test-task-integration-status.mjs`. [gate: collaboration-integration-status]

Candidate validation evidence (2026-09-14): actual integration workers now bind isolated candidate bytes to workspace/input Git identities and persist immutable encrypted reports for integrity, host project checks and post-check content hashes, including runtime/policy versions. Real Git/SQLite regressions catch a clean merge with inconsistent related values, changed candidate bytes, invalid tree and late results; publication hashes resolve to persisted evidence, which survives reopen. Final signed Fastify/PG service integration verifies default integrity evidence and explicit missing-policy wait without touching private source. Project policies/provider/native selection in tests remain fixtures. Installed project checker selection, code/Office semantic validation, bounded sandbox execution, bound-session evidence projection and C1/C2/C3/D remain incomplete. Guard tests: `test-candidate-validation.mjs`, `test-integration-worker.mjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-candidate-validation]

TaskCore host integration entry (2026-09-14): the existing durable background queue/local assistant branch can now await a closed main-process integration operation after TaskCore creation. Actual orchestrator tests (fixture session/runner adapters) verify foreground preservation, no engine dispatch or invented user message, terminal failure and late-generation fencing. Real MessageStore SQLite verifies deterministic turn-ID dedupe/reopen, recovered operation envelope and explicit attempts; owner-change checks pass. Native service/worker admission and its cross-store recovery are not connected yet; C1/C2/C3/D remain incomplete. Guard tests: `test-integration-turn-recovery.mjs`, `test-turn-orchestrator.mjs`. [gate: collaboration-integration-turn]

Native integration admission (2026-09-14): main startup now queues worker execution into the original session's TaskCore through a persisted encrypted admission journal. Real SQLite tests verify ACK-loss/reopen, stable attempts, cancellation/manual retry, explicit inactive unknown-dispatch recovery, live-turn preservation, bootstrap wait and stale-receipt fences. Signed Fastify/PG test now uses actual SessionManager/MessageStore/TurnOrchestrator/TaskCore and the real worker, including queued session-store reopen, Git validation evidence and unchanged private source. Provider, native project/window/runner availability and foreground activity are adapters; no model or installed physical clients are exercised. Project validators, isolated conflict engine work, final cross-account writer coordination, remote publication and C3/D remain incomplete. Guard tests: `test-integration-admission.mjs`, `test-integration-worker.mjs`, `collaboration-native-turn-fixture.cjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-native-integration-admission]

Bounded typed JSON and native syntax checks (2026-09-14): deterministic JSON object conflicts now produce immutable SharedGit candidates while preserving other file changes; ambiguous fields, arrays, unsupported precision and mixed conflicts remain pending. Real Git/SQLite worker tests require actual candidate validation after resolution. Production candidate validation additionally runs bounded parse-only JavaScript checks using the app's Node runtime, preserving explicit coverage and encrypted diagnostics. Actual Node and Electron tests verify no candidate execution, module scopes, clean environment, limits, timeout termination and cancellation. Malformed syntax fails even with a permissive project callback; syntax success alone still waits for project policy. Existing signed Fastify/PG regression passes with prior provider/native selection fixtures. General trusted project tests/type checking, isolated model resolution and C1/C2/C3/D acceptance remain incomplete. Guard tests: `test-candidate-javascript.mjs`, `test-candidate-validation.mjs`. [gate: collaboration-typed-and-syntax-validation]

Original validation inputs (2026-09-14): production host validators can read bounded immutable B/H/D files independently of candidate deletions, with exact Git/blob/content-hash provenance in encrypted reports. Real Git/SQLite regressions verify removed assertions still detect a candidate regression, separately advanced-H checks, absence versus failure, unsafe identity refusal, oversized/request limits, close/fencing and swallowed-read failure visibility. Worker/publication recovery and existing signed Fastify/PG regressions pass with their prior fixture scope. This does not discover/run every test or retain a trusted check policy across future tasks; that policy, model integration and C1/C2/C3/D remain incomplete. Guard tests: `test-candidate-validation.mjs`, `test-validation-original-inputs.mjs`. [gate: collaboration-original-validation-inputs]

Persistent original check policy (2026-09-14): a native identity-only selection workflow pins original Node test bytes in encrypted immutable policy revisions, with a CAS current pointer and account/workspace/project/physical-root binding. Real Git/SQLite tests cover later-task reuse, edits, concurrent updates, authorization/root/source changes and reopen/revocation. Actual Electron UI tests cover three-language selection/count/trust text and single-flight/cancellation with picker/API fixtures. Candidate-v4 receipts distinguish missing validation from named policies and bind selected check identity; workers fence policy changes and cannot substitute unrelated callback approval. Final signed Fastify/PG regression passes with prior fixture limits. Dedicated execution, coverage, automatic wakeups and the remaining C1/C2/C3/D acceptance are not complete. Guard tests: `test-candidate-validation.mjs`, `test-integration-check-policy-workflow.mjs`, `test-integration-check-policy.mjs`, `test-integration-worker.mjs`, `test-remote-task-cards-ui.cjs`. [gate: collaboration-persistent-check-policy]

Pinned Node check execution (2026-09-14): the actual worker restores selected original tests into a separate candidate copy and executes them under macOS OS restrictions plus Node permissions, with structured per-file counts, output/time/sampled scratch limits, process-group cancellation and post-run content verification. Real Node 22/Electron Node 24 tests reject removed/weakened assertions, empty/skipped checks, restricted file/process/network access and resource overflow; actual persisted-policy replacement cancels running checks. SQLite evidence/reopen and atomic policy-install/wakeup rollback pass. Signed Fastify/PG now verifies automatic selection-to-second-original-session-TaskCore execution and exact local publication while private files remain unchanged; provider/native selection adapters remain fixtures. Worker, admission/turn recovery, status, publication recovery and all 27 lifecycle checks pass. Other OS execution, independently pinned helpers/dependencies, model conflict repair, remote publication/CAS/outbox, private A/W/M/undo, Office and scale/physical-client acceptance remain incomplete with C1/C2/C3/D. Guard tests: `test-integration-check-policy-workflow.mjs`, `test-integration-check-policy.mjs`, `test-integration-worker.mjs`, `test-node-check-execution.mjs`, `test-node-check-policy.mjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-pinned-node-execution]

Server integration qualification (2026-09-14): migration 049 and signed owner-only target/claim/renew/release APIs add a workspace-wide PostgreSQL lease with database-clock expiry, monotonic generations and exact head/task/delivery/device binding. Real PostgreSQL tests verify two-device and multi-task exclusion, expired takeover, stale/head fencing, receipt replay without lease extension, object/device revocation, object expiry, review withdrawal and rollback. Signed Fastify/PG tests use actual verified encrypted Git packages and main client transport to prove committed-but-lost claim ACK recovery, renewal and release; existing TaskCore/local publication and logger-secrecy regressions pass with provider/native selection fixtures. All 27 lifecycle checks pass. Native use of server qualification, merged-object publication with atomic lease/head CAS, remote download/outbox recovery and the remaining C1/C2/C3/D requirements are still incomplete. Guard tests: `test-integration-lease-routes.mjs`, `test-remote-task-ipc.mjs`, `collaboration-integration-lease-fixture.mjs`, `collaboration-objects-http-integration.mjs`, `collaboration-task-git-http-fixture.mjs`, `collaboration-tasks-integration.mjs`. [gate: collaboration-server-integration-qualification]

Remote shared publication (2026-09-14): migration 050 and signed APIs atomically bind an encrypted workspace pack, advance H with lease/generation/revision CAS, release qualification, and commit owner-only event/receipt fanout. Real PostgreSQL tests cover race/replay, complete rollback, final-clock expiry, encrypted routing, retired incremental dependencies and a bounded 64-publication chain. The signed HTTP fixture publishes a candidate validated by actual original-session TaskCore Node checks, loses/replays its ACK, then decrypts and imports the actual pack into an independent Git repository with exact tree/parent checks. Post-publication sync now covers task and workspace account hints. Existing task transport, crypto, route/client/service, transfer and lifecycle regressions pass. Provider/native selection remain fixtures. Native automatic server qualification, canonical-chain reconciliation and outbox acknowledgement are still absent; C1/C2/C3/D remain incomplete, as do installed two-client and scale/Office acceptance. Guard tests: `test-collaboration-sync.mjs`, `test-integration-lease-routes.mjs`, `test-remote-task-ipc.mjs`, `test-shared-git-transport.mjs`, `collaboration-objects-http-integration.mjs`, `collaboration-publication-fixture.mjs`, `collaboration-publication-http-fixture.mjs`, `collaboration-task-git-http-fixture.mjs`, `collaboration-tasks-integration.mjs`. [gate: collaboration-shared-remote-publication]

Canonical shared-head acquisition (2026-09-14): the main workflow can freshly authorize and import the bounded encrypted publication chain, verify Git M/H/D/B ancestry and exact trees, and atomically CAS local H with a persistent monotonic remote checkpoint. Independent Git tests cover incremental reconstruction, stale/changed metadata, scope loss, concurrency, retained unsent candidates and post-transaction interruption. Final signed HTTP/PG tests exercise the production workflow and an independent canonical receiver. Existing worker/transport/publication/service regressions pass. This is snapshot acquisition, not automatic remote publishing or an acknowledged outbox; cross-task initial-H provenance and remote-first worker integration remain next. C1/C2/C3/D and full-client/Office/scale acceptance remain incomplete. Guard tests: `test-shared-head-sync.mjs`, `collaboration-publication-http-fixture.mjs`. [gate: collaboration-canonical-shared-head]

Native remote-first publication (2026-09-14): with the explicit sharedPublicationProtocol runtime policy, the original-session worker claims/renews server qualification, acquires canonical H, validates, resumes encrypted pack upload, confirms the immutable remote publication, then completes local Git/SQLite/outbox state. Final signed Fastify/PG/TaskCore acceptance observes real renewal during Node checks and recovery from a deliberately lost publication ACK through a later native attempt. Real Git/SQLite adapter tests add database reopen, actual incremental second publication and renewal-failure fencing; existing lifecycle/admission/status/publication regressions pass. Protocol advertisement, cross-task initial-H provenance and migration of old completed local journals remain unfinished, so rollout is not enabled. C1/C2/C3/D and full-client/Office/scale acceptance remain incomplete. Guard tests: `test-integration-worker.mjs`, `test-remote-publication.mjs`, `test-shared-publication-recovery.mjs`, `collaboration-publication-http-fixture.mjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-native-remote-publication]

Durable initial shared baseline (2026-09-14): migration 051 anchors the original owner input object with separately encrypted workspace provenance. Signed owner-only baseline reads/downloads survive source-task cancellation and old-assignee departure, while source expiry/revocation and current device/scope remain enforced. Independent Git and actual signed HTTP/PG/encrypted-transfer tests verify original H acquisition when the current task has a different B, plus cancelled-task and cached-source denial. Legacy targets restore only exact B through 64-task pages with encrypted native cursor/reopen and lost-ACK recovery; explicit retry can restart an exhausted scan. Unclaimed task B is refused as canonical H. Existing native publication/worker/transfer/route/crypto/lifecycle regressions pass with provider/native-selection fixtures. Old completed-journal migration and protocol advertisement remain next; C1/C2/C3/D, installed full-client/Office/scale acceptance remain incomplete. Guard tests: `test-remote-baseline-recovery.mjs`, `test-remote-task-routes.mjs`, `test-shared-head-sync.mjs`, `test-task-integration-status.mjs`, `collaboration-baseline-fixture.mjs`, `collaboration-baseline-http-fixture.mjs`, `collaboration-objects-http-integration.mjs`, `collaboration-task-git-http-fixture.mjs`, `collaboration-tasks-integration.mjs`. [gate: collaboration-durable-initial-baseline]

Legacy publication upgrade and protocol negotiation (2026-09-14): a bounded indexed startup pass preserves original local journals/outboxes and atomically revives completed local-only intents for native remote confirmation. Current validation runs again; downgrade cannot complete remotely qualified work locally. Only confirmed publication retires the old outbox in the same transaction as its sent replacement and completed intent. Real Git/SQLite faults/reopen/page tests and actual signed Fastify/PG/original TaskCore upgrade with lost publication ACK pass; policy/client/store/lifecycle regressions pass. COLLABORATION_SHARED_PUBLICATION_ENABLED defaults off and controls signed sharedPublicationProtocol=1 advertisement plus configured integration APIs; desktop validates prerequisite versions. No production flag was enabled. Expired orphan/long-offline recovery, simultaneous installed clients/global writer coordination, model repair, private A/W/M/undo, Office and streaming/scale remain incomplete with C1/C2/C3/D. Guard tests: `test-collaboration-policy.mjs`, `test-legacy-publication-migration.mjs`, `test-remote-publication.mjs`, `test-remote-task-policy.mjs`, `collaboration-objects-http-integration.mjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-legacy-publication-upgrade]

Expired publication upload recovery (2026-09-14): signed owner status/init replay identifies only expired unbound orphans without restoring credentials. Native recovery re-probes cached uploads, requires a newer live grant before replacing an uncertain publication object, retains encrypted retirement history and reuses the candidate/validation. Generic denial, explicit expiration, revocation and object-identity changes fail closed. Transfer tests and real Git/SQLite reopen tests pass; signed Fastify/PG/native TaskCore acceptance covers expiry immediately before publish, a fresh encrypted replacement, lost commit ACK and one confirmed publication. Provider/native selection remain fixtures. Physical orphan cleanup, other long-offline staging cases, simultaneous full clients/global coordination, model repair, private A/W/M/undo, Office and scale remain incomplete with C1/C2/C3/D. Guard tests: `test-collaboration-object-recovery.mjs`, `test-collaboration-transfers.mjs`, `test-remote-publication.mjs`, `collaboration-objects-http-integration.mjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-expired-publication-upload]

Missing publication staging recovery (2026-09-14): native publication can finish an already verified upload without its local package directory. After qualified retirement, a missing package is rebuilt from the same immutable Git candidate/prerequisites with a fresh actual descriptor; retained-manifest ciphertext loss can also request fenced replacement after current owner status. Real Git repack/reopen tests cover changed pack bytes, unchanged validation, symlink refusal and an export-before-journal failure. The signed Fastify/PG/TaskCore fixture deletes the complete staging directory before publication, then verifies encrypted reconstruction and lost-ACK confirmation. Transfer and worker companions pass. Unknown init identities/missing manifests and device adoption remain fail-closed; cleanup, global/full-client coordination, model repair, private A/W/M/undo, Office and scale remain incomplete with C1/C2/C3/D. Guard tests: `test-collaboration-transfers.mjs`, `test-remote-publication.mjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-missing-publication-staging]

Unknown init identity recovery (2026-09-14): with a retained encrypted manifest, missing ciphertext no longer blocks replay of the original init command/key. The client recovers the same object, rechecks current owner status and either finishes existing remote data or returns an identified missing-stage result to the lease-fenced publisher. Verified/bound init replay returns current metadata without upload credentials. Thirty-five transfer tests and signed Fastify/PG lost-ACK/file-deletion and stale-ID simulations pass, along with the native publication chain and affected companions. Changed bytes and stale/cancelled/revoked/device-mismatched state fail closed. Missing manifests/device ownership, cleanup, global/full-client coordination, model repair, private A/W/M/undo, Office and scale remain incomplete with C1/C2/C3/D. Guard tests: `test-collaboration-transfers.mjs`, `collaboration-transfer-http-fixture.mjs`. [gate: collaboration-unknown-init-recovery]

Task upload manifest journal (2026-09-14): task-owned uploads persist their immutable identity and latest state in encrypted SQLite before writing the file manifest. Missing manifests/directories restore on the original authorized device; cancellation, revision checks, removal tombstones and cleared removed keys prevent stale resurrection. Nine SQLite/filesystem fault tests and existing transfer/workflow/lifecycle regressions pass. Signed Fastify/PG/native TaskCore acceptance deletes both upload and publication staging, then confirms qualified encrypted replacement and lost-ACK recovery. Provider/native selection remain fixtures. Already-lost unjournaled identity and device adoption are not inferred; cleanup, global/full-client coordination, model repair, private A/W/M/undo, Office and scale remain incomplete with C1/C2/C3/D. Guard tests: `test-task-transfer-journal.mjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-task-transfer-journal]

Object expiry and cleanup executor (2026-09-14): a bounded SQL sweep retires expired objects, erases wrapped keys and waits out issued credentials before a row-locked private-store deletion. Retry/backoff, idempotent post-delete rollback recovery and retained orphan provenance are implemented; bound live objects and foreign owners remain protected. Unit signing/lifecycle tests and real signed Fastify/PG cleanup faults pass alongside native TaskCore publication recovery. Migration 052 supplies partial cleanup indexes. The server worker is default-off and wired to startup/shutdown; actual Qiniu deletion and deployment were not performed. Global/full-client coordination, model repair, private A/W/M/undo, Office and scale remain incomplete with C1/C2/C3/D. Guard tests: `test-collaboration-object-cleanup.mjs`, `test-collaboration-object-recovery.mjs`, `collaboration-object-cleanup-http-fixture.mjs`, `collaboration-objects-http-integration.mjs`. [gate: collaboration-object-cleanup]

Cross-account application writer (2026-09-14): every task apply/rollback and local startup recovery uses one nonwaiting OS-user SQLite write lock, independent of account/profile and covering nested roots. Process death releases the lock without expiry; busy admission leaves no partial journal and retains a retryable UI plan. Node 22/Electron Node 24 independent-process tests verify stalled writes, SIGKILL recovery, stale-preview rejection and separate account journals; ZIP/Git workflows, encrypted recovery and Electron retry UI pass. This locks cooperating application brokers only; foreground engine/editor writers, independent A/W/M/undo, simultaneous full clients, model repair, Office and scale remain incomplete with C1/C2/C3/D. Guard tests: `remote-task-ui-fixture.cjs`, `remote-task-live-acceptance.mjs`, `test-local-writer.mjs`, `test-remote-task-application.mjs`, `test-remote-task-recovery.mjs`, `test-remote-task-workflow-ui.cjs`, `test-remote-task-workflow.mjs`, `test-remote-task-writer.mjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-local-application-writer]

Independent local candidate (2026-09-14): confirmed native remote publication now prepares W′ from explicit last-synchronized A, current W and shared M in private staging. Nonoverlapping text and supported JSON merge; private edits/deletions remain, and unresolved/binary conflicts stop application. Encrypted candidate/A records survive reopen and missing staging, with per-attempt fencing and remote revision checks; A never advances on preparation. Real Git object inventory verifies no private blob enters shared storage. Node/Electron candidate and recovery tests plus signed HTTP/PG/TaskCore preparation pass with provider/native-selection fixtures. Automatic local validation/application, foreground writer admission, completed-intent startup reconciliation, A receipt/undo and local card projection remain pending, as do full clients/Office/scale. C1/C2/C3/D remain incomplete. Guard tests: `test-integration-turn-recovery.mjs`, `test-local-materialization.mjs`, `test-task-local-candidate.mjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-independent-local-candidate]

Private candidate validation (2026-09-14): native remote publication now runs separate W′ validation using an immutable private Git commit, existing pinned original tests and sandboxed Node execution. Token/policy-bound encrypted evidence distinguishes passed, failed and missing checks; weakened candidate tests cannot approve W′. Node 22/Electron Node 24 and shared-check regressions pass, with signed HTTP/PG/TaskCore coverage. Private validation does not advance A or apply files. Foreground write coordination, automatic local application/receipt/undo, restart reconciliation, local card projection, Office, full clients and scale remain incomplete with C1/C2/C3/D. Guard tests: `test-integration-turn-recovery.mjs`, `test-local-candidate-validation.mjs`, `collaboration-task-git-http-fixture.mjs`. [gate: collaboration-private-candidate-validation]

Version restore writer coordination (2026-09-14): Git and snapshot version restores now hold the same OS-user SQLite lock as task apply/rollback across their awaited write and failure-recovery operations. Busy restoration uses the existing version-busy error; version-save scheduling is unchanged. Node/Electron real restore tests, independent child-task exclusion and existing version/application regressions pass. Foreground engine activity remains outside this lock; cross-profile engine completion/crash ownership, automatic W′ application/A receipt/undo and the remaining full-client/Office/scale requirements are unfinished. C1/C2/C3/D remain incomplete. Guard tests: `test-local-writer.mjs`, `test-remote-task-writer.mjs`, `test-version-task-writer.mjs`, `test-workspace-version-control.mjs`. [gate: collaboration-version-restore-writer]

Owned engine exit confirmation (2026-09-14): shared-server shutdown retains its child handle after leader exit and returns an idempotent awaitable process-group receipt. Actual Node/Electron tests prove a surviving tool writer delays confirmation until group absence; denied probes/timeouts/Windows dispatch do not claim success. Existing server/kill/reaper regressions pass. This excludes detached tools and separate job actors and does not yet serialize foreground admission with application. Automatic W′ writes/A receipts/undo and C1/C2/C3/D full acceptance remain pending. Guard tests: `test-engine-exit-confirmation.mjs`, `test-shared-server-exit-confirmation.mjs`. [gate: collaboration-engine-exit-confirmation]

DOCX private candidate policy (checkpoint 56): `test_office_merge.py` and `test-office-local-candidate.mjs` cover conservative paragraph/table-cell merging through real Python, opaque part-byte preservation, conflicts, hostile packages, unavailable runtime and unchanged W. `test-local-materialization.mjs` rejects cache reuse under a changed merge policy. This is the private A/W/M chain only; shared Office merge, rendered fidelity, full format coverage and automatic application remain unaccepted. C/D stay open.

Admitted local materialization (checkpoint 57): `test-local-materialization-apply.mjs` proves real broker writes and atomic encrypted applied/A receipts, refusal on stale W/base/policy and writer contention, and rollback after receipt failure. The signed HTTP/native fixture now applies independently validated W′ and advances A to M using fixture-owned writer admission. Production foreground admission is not supplied, so automatic desktop application is not enabled. Applied materialization cannot use legacy rollback; contribution-specific undo and installed-client acceptance remain pending.

Foreground group admission (checkpoint 58): `test-foreground-writer.mjs` uses real detached groups to prove registration before execution, blocking after leader exit, owner-IPC crash cleanup and admission after confirmed group exit. Actual engine and both job launchers register; legacy stop verifies identity and waits for group shutdown. Tests inject private coordination paths. This is POSIX registration/admission, not yet warm-profile draining, automatic local retry, Windows proof or full-client acceptance.

Automatic local continuation (checkpoint 59): POSIX source wiring now connects registered foreground admission to native application. `test-idle-foreground-drain.mjs` preserves active/unknown views and SDK work, retires idle profiles and clears advisory hints. `test-local-application-retry.mjs` covers durable completed-shared retries and the shared-generation/local-validation race. The signed native/HTTP fixture requires an actual terminal waiting turn and a distinct successful application turn after foreground release. Production switches and installed clients were not changed; undo, persistent local card status, Windows/old-process proof and full acceptance remain open.

Local task-card status (checkpoint 60): native integration projection verifies task/delivery/source binding and requires a matching applied receipt. `test-task-integration-status.mjs` checks closed IPC and independent SQLite reopen. Actual Electron session-card and detail tests show foreground waiting separately from shared publication, then local applied status in English, Chinese and Arabic; cached/account/navigation behavior remains covered. Contribution-specific undo and the remaining C/D acceptance are not claimed.
Contribution undo (checkpoint 62): `test-local-contribution-undo.mjs` (Node 22 and Electron Node 24) proves that the identity-only rollback of a successful local materialization applies a durable contribution inverse through the same broker and admission writer: disjoint later edits survive, overlapping edits refuse without writing, A and shared history never rewind, completed undos replay as history and are never rolled back, interrupted inverses recover to the pre-inverse W by identity and at startup, deleted files return with their recorded mode inside the immutable binding, the same shared M never re-applies an undone contribution, and personal undo survives a revoked conversation. `test-task-integration-status.mjs`, `test-remote-task-workflow-ui.cjs` and `test-session-task-cards-ui.cjs` cover the closed `undone` projection and three-language detail/card states. Whole-file rollback of applied or undone materializations stays refused. Not claimed: full-client or cross-profile acceptance, Windows, or Office inverse beyond the existing DOCX conservative merge.
Pinned check imports (checkpoint 63): `test-check-imports.mjs` and `test-node-check-policy.mjs` (Node 22 and Electron Node 24) prove that test infrastructure imported by the selected checks is pinned from the same baseline and restored over the candidate, so weakening a helper instead of a test is refused; code under test that a check imports is recorded with change evidence, not restored; third-party packages resolve only through the project's own installed `node_modules`, exposed read-only inside the seatbelt and Node permission sandboxes, wait with `DEPENDENCIES_UNAVAILABLE` when absent, and a candidate shipping its own copy of a declared package fails. Nothing is installed. Non-Node runtimes and non-macOS sandboxes remain `SANDBOX_UNAVAILABLE`.
Conflict model repair (checkpoint 64): `test-conflict-repair.mjs` and `test-integration-repair.mjs` (Node 22 and Electron Node 24) prove that unresolved shared conflicts reach the requester's configured model only after deterministic Git/JSON merging, with the task goal, three-way and diff3 evidence and prior answers; that only complete marker-free resolutions for requested paths become candidate resolutions, still gated by the same validator before H advances; that business decisions become one question per file (`decision_required`) answered through the identity-only `answerIntegration` command which re-admits the same intent; that invalid model output and an unconfigured model leave the conflict visible; and that questions/answers are encrypted at rest. `test-task-local-candidate.mjs` covers the same resolver on the private A/W/M chain. The model never approves, never sees unrelated files, and cannot weaken checks. Office repair and full-client acceptance are not claimed.
Check-failure repair (checkpoint 65): `test-integration-repair.mjs` proves that when pinned checks fail, exactly one bounded model repair per requester answer set may rewrite only the files the delivery itself changed (checks and helpers excluded), that the amended candidate keeps the H and D parents, chains its identity to the failed candidate and must pass the same validator before H advances, that repairs touching unchanged files are refused, and that a question instead of a fix becomes a decision the requester answers to unlock one more attempt. Failed candidates stay recorded as evidence.
Office merge (checkpoint 66): `scripts/test_office_merge.py` (bundled Python), `test-office-local-candidate.mjs` and `test-office-shared-candidate.mjs` (Node 22, Electron Node 24; skip with notice without `LILY_TEST_OFFICE_PYTHON`) prove typed part-level three-way merges for Word, Excel and PowerPoint including macro-enabled packages on both chains: disjoint paragraph/cell/slide edits merge, same-paragraph/same-cell/same-slide edits, row insertion or deletion, both-side appends, shared formulas and relationship conflicts are named conflicts, opaque and macro parts are kept verbatim, metadata never blocks content, formulas taken from one side force recalculation on open, and with the bundled LibreOffice the merged package must render to at least one page before acceptance. PDFs are never merged (`pdf_source_preferred`); packages are never offered to the model. Not claimed: visual page comparison, chart/drawing semantics, macro execution, installed-client acceptance.
Streamed Git materialization and online-only inventory (checkpoint 67): `test-remote-task-git-scale.mjs` (Node 22, Electron Node 24; 15,001 files, 28.3 MB, report in `docs/research/2026-09-14-collaboration-git-scale.json`) proves tree enumeration streams beyond the former 1 MiB buffer, blobs materialize through one `cat-file --batch` process with hash and size guards, sparse worktrees hold no placeholders, absent online-only entries are never deletions, edits to unmaterialized files are refused, materialization is idempotent and exact, and the capacity policy refuses before writing when free space minus reserve is insufficient. `test-remote-task-workflow.mjs` (Git mode) covers inventory-first receive with the `inventory`/`materialize` commands and assignee-only access; ZIP mode keeps refusing sparse input. Limits are operator policy with hard ceilings. Not claimed: sidebar file tree, OS placeholders, agent pre-command materialization, multi-GB acceptance.
Concurrent multipart upload (checkpoint 68): `test-collaboration-transfer-concurrency.mjs` proves that under `LILY_COLLAB_UPLOAD_CONCURRENCY=4` at most four parts are in flight, each part is uploaded exactly once across a lost response and recovery, completion stays ordered and the plaintext round-trips; the default remains sequential so `test-collaboration-transfers.mjs` keeps its exact dispatch-order and stop/cancel/revoke fences.
Measured scale and faults (checkpoint 69): `test-collaboration-scale-report.mjs` (Node 22 and Electron Node 24) records 256 MiB encrypt/multipart/decrypt timings, in-flight parts, RSS and runtime versions with a lost part recovered by resume, and a SIGKILLed materialization process leaving no completion claim and an intact object store; `server/scripts/collaboration-objects-http-integration.mjs` passed against the verification PostgreSQL container on 2026-09-14. `test-remote-task-application.mjs` proves an externally locked destination yields the retryable `LOCKED` code with untouched bytes and clean rollback. Still not claimed: two full clients against a real service, installed packages, physical machines, Windows.
