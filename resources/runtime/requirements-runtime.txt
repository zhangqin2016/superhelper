# Pre-installed into bundles/<platform>/runtime/venv for document & data skills.
# Playwright package only (Chromium is not bundled — run playwright install if needed).
pandas>=2.2,<3
openpyxl>=3.1,<4
xlsxwriter>=3.2,<4
Pillow>=10.4,<12
imageio>=2.34,<3
numpy>=1.26,<3
markitdown[pptx,pdf,docx]>=0.0.1a2
pypdf>=4.0,<7
reportlab>=4.2,<5
pdf2image>=1.17,<2
lxml>=5.0,<6
defusedxml>=0.7,<1
python-docx>=1.1,<2
python-pptx>=1.0,<2
pdfplumber>=0.11,<1
playwright>=1.40,<2
# Consumer-friendly PDF/scan path (no torch — stays light on ordinary machines):
# digital PDFs use pdfplumber/pypdfium2; scanned pages & images use RapidOCR
# (onnxruntime, no torch). Heavy ML layout engines (Docling) are intentionally
# NOT bundled — they raise RAM/CPU beyond an ordinary consumer laptop.
pypdfium2>=4.0,<5
rapidocr-onnxruntime>=1.3,<2
# Template fill: render .docx templates with {{ }} / {% %} placeholders (Jinja2)
# from structured data. Light — docxtpl rides python-docx + jinja2, no new heavy
# deps. Powers the lily-template-fill skill.
docxtpl>=0.16,<1
