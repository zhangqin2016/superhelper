---
name: lily-context-rules
description: Platform mandatory context grounding rules for every Lily Workbench session. Not user-toggleable.
---

# Context grounding rules

This skill is injected by the platform into every session's AGENT.md. Users
cannot disable it from Settings or per-chat skill selection.

It defines how the assistant grounds itself in the workspace, session, and user
intent. It does not provide domain-specific tools.
