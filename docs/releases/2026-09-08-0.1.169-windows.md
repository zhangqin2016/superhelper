# Lily Workbench 0.1.169 Windows Release

Published Windows x64 from commit `2ac1fa30`, based on upstream `d98a1561`.

## Changes

- Includes the delayed collaboration panel/back-button focus ownership fix that
  arrived after the 0.1.168 Windows build.
- Stops confirmed repetitive text or unchanged process-job observations while
  preserving received output. Confirmed loops do not automatically replay tasks.
- Reduces background-job observations by identity and fences stale process
  callbacks by generation. Background processes are not killed by poll detection.
- Existing model/tool configuration and database WAL compaction protection remain.

## Verification

- Passed on Windows: process-job-generation (6 cases), process-job-turn-guard,
  job-observation, loop-detector, turn-loop-guard, task-execution-progress,
  collaboration-center-lifecycle and capability-gate-registry.
- The real Electron workspace-entry test passed, including immediate handoff and
  delayed panel-focus ownership. Windows profile locks were reported explicitly.
- OpenCode agent-session and turn-orchestrator suites passed. WAL compaction,
  architecture (998 source files) and Windows installer guards passed.
- Release runtime preflight and Windows payload verification passed: 13,478 files,
  approximately 1575 MB unpacked, Windows engine and base Python runtime included.
- All 13 changed source/resource files since the previous Windows build match
  packaged ASAR content byte for byte. Package version is 0.1.169; OpenCode CLI
  and SDK are 1.18.21. Main executable and installer Lily icons visually verified.
- Installer, auto-update installer and blockmap return HTTP 200 with exact sizes.
  Public manifest and stable feed match local bytes. Service API version, size
  and SHA-256 match the installer. Release row: `rel_8f2b1qaqz4rnj1`.
- One-click release exited 0. CDN refresh succeeded; a stale first manifest read
  resolved on bounded retry. Independent public verification passed afterward.
- During publication, upstream added `dd57f599` for external packaged-resource
  fallback. It is synced locally but is not part of this frozen Windows build.
  A separate smoke test executed the actual packaged Windows executable with
  Electron 41.7.0 and loaded both observation consumers from its ASAR successfully;
  the published Windows artifact does not reproduce the missing-resource failure.
- No fresh-machine installation, full Windows repository suite, or real-model
  customer-loop acceptance was performed. Authenticode status is `NotSigned`.

## Artifact

- File: `LilyWorkbench-0.1.169-x64.exe`
- Size: 554696658 bytes
- SHA-256: `42f2a3e18bac2343f436a2256d3ea33ea4b43d92d865b7535c9c4f6fa15e234c`
- Blockmap: 572029 bytes
- Download: https://qny.lanrensoft.cn/app/updates/win32-x64/0.1.169/LilyWorkbench-0.1.169-x64.exe
- Stable feed: https://qny.lanrensoft.cn/app/auto-updates/win32-x64/stable/latest.yml
