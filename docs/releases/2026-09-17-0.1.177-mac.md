# Mac 0.1.177

## Status

Built and verified. **Publication was attempted and stopped at the first gate;
nothing reached production.** The one-click release exited before any upload
because the environment has no release-admin credential:

```
[release-one] server release publish requires RELEASE_ADMIN_TOKEN
              or RELEASE_ADMIN_EMAIL + RELEASE_ADMIN_PASSWORD
```

One line of log, no qshell invocation, no bytes transferred. Online users remain
on 0.1.176. Everything else the release needs is in place: the license private
key, a logged-in qshell for the lanrensoft bucket, both architectures' artifacts,
and a full dry run that completed end to end.

Following the precedent recorded in `2026-09-08-0.1.170-mac.md`, no attempt was
made to retrieve the credential from the production server. None was obtained,
displayed or persisted.

To finish, supply the credential to that one command only:

```bash
RELEASE_ADMIN_TOKEN=… npm run release:one -- --version 0.1.177 \
  --target mac --upload --skip-build --skip-catalog-publish --notes "…"
```

The order is immutable artifacts, then server rows, then the update pointers,
then a CDN refresh and public verification. The pointers move second to last, so
an interruption anywhere before that leaves production untouched. The CDN refresh
is the step to watch: qshell's route to the Qiniu management API is broken on
this machine, and `latest-mac.yml` is edge-cached for a year, so a failed refresh
means the release is invisible. `scripts/qiniu-ipv4-admin.mjs` is the fallback.

The skill and workspace-app catalog is deliberately not part of this release,
matching the shape of 0.1.176. The skill changes from this session ship inside
the application bundle regardless.

## Scope

Source: `767be025`.

The version was bumped because Mac 0.1.176 is published and immutable; a second
artifact with that name but different contents would be indistinguishable from
the real one.

## Artifacts

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| `dist/Lily Workbench-0.1.177-arm64.dmg` | 649675022 | `6a942702b03bbf19e56c5f436ca2507ccfd6001ca8fd885e8f7b5c89238890e3` |
| `dist/Lily Workbench-0.1.177-x64.dmg` | 712415419 | `e4ef6ddbbbaea06cd1e10a8e0781214aa0e524d635d37d54cfd7e7615cf93c7d` |

Update-feed zips were produced alongside each dmg and are unused by this build.

## What it carries over the published 0.1.176

Twelve substantive fixes, covering every defect in five acceptance reports plus
the two conversation defects found in the user's own database:

- Deliverable relevance: a turn shows what it delivered, not every file it touched.
- 2026-09-16 capability acceptance, all seven findings.
- 2026-09-17 platform acceptance, all nine.
- X-Test: a conversion never destroys a file it did not produce.
- Stress test: an extraction says what it found, an index says what it covered.
- Runtime packs: one pack per process, and only when that pack is the job.
- A contract only names a helper the running build actually has.
- A lost turn is announced once, not once per restart.
- A reloaded turn shows its answer in the answer slot.
- A prompt the platform sends to itself is marked, not guessed.

## Verified in the built packages, not assumed

- Every module added this session is inside `app.asar`: the internal-prompt
  marker, the runtime-pack runner, job observation, the record timeline, the file
  signature reader, the document content extractor.
- The three changed Python helpers are present in unpacked resources.
- **matplotlib 3.11.2 is installed in both runtimes** — 61 packages in the arm64
  venv, 66 in the x64 interpreter. It reached these packages through
  `requirements-runtime.txt`, not by hand, which is what makes it reproducible.
- pandas 2.3.3 and Pillow 11.3.0 are inside their declared ranges, confirming
  the earlier "manifest drift" reading was pack shadowing rather than drift.
- Both dmgs mounted and verified; no macOS junk files in either.
- The whole build logged zero errors.

The two architectures lay their Python runtime out differently, which is by
design and worth knowing when inspecting them: arm64 keeps packages in
`runtime/venv`, while the cross-built x64 installs them into the base
interpreter and leaves `venv` as a 12 KB shim.

## Not done

- Not notarized, not signed with a distribution certificate, not uploaded.
- No installed-client acceptance: nothing here has been run from the .app itself.
- LibreOffice is not bundled on either architecture; it installs on demand as a
  runtime pack.
