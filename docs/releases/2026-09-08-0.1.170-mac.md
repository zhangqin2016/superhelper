# Mac 0.1.170 release preparation

## Status

Both architectures built and verified. Immutable artifact upload is in progress;
production release rows and latest update pointers have NOT been promoted.
Publication requires an authorized release credential in the publishing environment.
The current process has no `RELEASE_ADMIN_TOKEN` or admin login environment.
An attempted server `.env` credential retrieval was rejected by the security
reviewer before execution; no credential was obtained, displayed or persisted.
No alternate credential extraction was attempted.

## Scope and source

- Build source: `a909e6f25f3ed0a5f97c9b5fb412030eae0107ac`, plus version metadata
  changing package and lockfile from 0.1.169 to 0.1.170.
- Fixes recursive task recovery, recovery admission identity, stale startup
  state and duplicate failed history after an explicitly interrupted task.
- macOS Apple Silicon and Intel DMGs plus automatic-update ZIPs only.
  No server business-code deployment, website restart, Windows artifact/feed
  upload, or skill/app catalog publication.
- Previous public manifest was 0.1.169. Do not copy an older Windows artifact
  into a manifest labeled 0.1.170: the static updater uses the manifest-level
  version. The standard per-target publisher includes same-version platforms
  only; independent Windows server rows and auto-update feed stay unchanged.

## Verification

- Full discovery suite: **913/913 scripts passed**, exit 0, 505 seconds.
  Log: `/private/tmp/lily-mac-0170-unit.log`.
- `npm run test:release`, architecture boundaries (1002 files, 52 ratchets),
  capability registry (75 gates) and `git diff --check` passed.
- Online Mac runtime-pack preflight passed on retry. The initial direct
  connection timed out before building; the script restored version metadata.
  The next run passed without skipping the gate. 20 production artifacts still
  lack offline lock entries, as in the previous release; online availability
  does not certify those missing immutable lock entries.
- Successful build and upload dry-run exited 0:
  `/private/tmp/lily-mac-0170-build-retry.log`. The dry-run prints upload commands
  but does not upload or modify any latest pointer.
- Both DMGs mounted and passed Electron Framework and archive-metadata checks.
- Per architecture, 911 tracked source/plugin files match source byte for byte;
  both packaged Electron executables loaded the actual conversation source,
  repetition guard and execution-progress modules and exercised interruption
  authority successfully. Intel execution used Rosetta, not physical Intel hardware.
- Both app bundles passed deep strict signature verification. Signatures remain
  ad-hoc; Apple Developer ID / notarization credentials are not configured.
  This is NOT Apple-notarized, Gatekeeper clean-machine acceptance.
- Prior real-model source-client UI acceptance is documented in
  `../superpowers/plans/2026-09-08-native-task-ui-acceptance.md`. This packaging
  step did not repeat logged-in full-app UI/model testing inside the installer.

## Artifact identity

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| Lily Workbench-0.1.170-arm64.dmg | 613044982 | `d46852e0e67cfaf58343d286f353f39c3352e35022ed9df891f18da01fd3f6f8` |
| Lily Workbench-0.1.170-arm64.zip | 510168399 | `60d549cbedd5517a3c0a361effb5cc0fb76b61d9525c93b33527fbd4d75bbddb` |
| Lily Workbench-0.1.170-x64.dmg | 669130928 | `3804e71e7af5bc1040031dd1e1345fb952b61acc41a5af0dab6d51d9de8e3add` |
| Lily Workbench-0.1.170-x64.zip | 550470668 | `0701336011802d3bdddcf19fc6543fb3b9d5db32fac51d7c392ff44b9705d713` |

Local Qiniu fingerprints (computed by `qshell qetag`, for remote comparison):

- ARM DMG: `lp4eBMCyGJOgsjEXU_M4QsP32UNL`
- ARM ZIP: `lgyf8AcvfkrZ_e0RBae9xGQp-F5r`
- Intel DMG: `lv6lwz7jyM5QoM2X8hPiRO6ei0ej`
- Intel ZIP: `lsspdu7sBbepfY3EWZT2zpQii-Ik`

All four exact new object keys were checked with `qshell stat` before upload
and did not exist. Upload logs are `/private/tmp/lily-mac-0170-upload-{arm64,x64}-{dmg,zip}.log`.

## Remaining publication steps

1. Finish immutable uploads and verify remote size/Qiniu fingerprint against
   local artifacts, plus public download availability.
2. Configure an authorized release token or login via the supported environment;
   do not extract administrator credentials from unrelated storage or logs.
3. Recheck remote latest version for concurrent publication, then use the
   existing one-click publisher with `--version 0.1.170 --target mac --skip-build
   --skip-catalog-publish --upload`; retain online preflight and public verification.
4. Verify signed manifest, Mac API release identities and update-feed hashes.
   Preserve older artifacts for rollback. Do not mark published before these pass.
