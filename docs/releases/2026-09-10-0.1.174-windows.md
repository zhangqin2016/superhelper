# Lily Workbench 0.1.174 Windows Release

Published Windows x64 from frozen commit `5cf192d5`. This is an immutable
rebuild of the 0.1.173 stable source with a new version number; no capability
or dependency changes were introduced.

## Verification

- OpenCode version alignment, Windows installer guard and capability registry
  passed. The capability registry covered all 80 documented gates.
- Formal online release preflight passed for runtime-pack metadata, runtime
  health, installer, IPC refresh, settings and the common publisher.
- Windows payload verification passed: 13479 files, approximately 1575 MB
  unpacked, with the Windows engine and base Python runtime present.
- Packaged ASAR reports version 0.1.174 and OpenCode SDK 1.18.29. The packaged
  OpenCode CLI reports 1.18.29.
- Main executable and installer Lily icons were extracted and visually verified.
- No fresh-machine installation, full Windows repository suite, live provider
  test, or installed-client auto-update/restart acceptance was performed.
  Authenticode status is `NotSigned`. Existing npm audit findings were unchanged.

## Publication

- The standard one-click workflow built the package and uploaded the immutable
  installer. The duplicate auto-update upload was replaced by a Qiniu server-side
  copy of that verified object; the blockmap was uploaded separately.
- Release row: `rel_pneuu8ei4tvnsw`. Catalog publication was skipped because the
  source catalog is unchanged from 0.1.173.
- Remote-version guard verified 0.1.173 before 0.1.174 pointers were written.
- CDN refresh completed through the forced-IPv4 Qiniu admin tool with HTTP 200.
- Public manifest and stable feed exactly match local files. Both installer URLs
  and the blockmap return HTTP 200 with exact sizes. The public API version, URL,
  size and SHA-256 match the local artifact.
- Immutable and auto-update installers share Qiniu hash
  `llQylFO6qzBmjuL1BWobLnCkkejo` and exact size 554752053 bytes.

## Artifact

- File: `LilyWorkbench-0.1.174-x64.exe`
- Size: 554752053 bytes
- SHA-256: `fe419ad7b7bde00806c9203c5b1eb5c8c6884be8a83b0d7f413941b05b421e7a`
- Blockmap: 572246 bytes
- Download: https://qny.lanrensoft.cn/app/updates/win32-x64/0.1.174/LilyWorkbench-0.1.174-x64.exe
- Stable feed: https://qny.lanrensoft.cn/app/auto-updates/win32-x64/stable/latest.yml
