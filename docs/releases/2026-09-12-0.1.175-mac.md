# Mac 0.1.175 release

## Status

Published and independently verified on 2026-09-12. Apple Silicon and Intel DMGs,
auto-update ZIPs, server release rows, the signed latest manifest and both stable
Mac feeds are live. Existing Windows 0.1.175 URL, SHA-256 and size were preserved.
No Windows artifacts, skills, apps or server business code were deployed.

The prepared Mac 0.1.174 was never uploaded. During authorization, Windows
0.1.175 went live. Mac was rebuilt as 0.1.175 without changing application source
or dependency declarations, so publication could preserve the signed Windows
entry rather than overwrite a newer manifest. User explicitly authorized Qiniu
upload and online publication. Both package version files now read 0.1.175.

## Source and verification

Source is HEAD `47c7feed` plus the existing session-database safety and
conversation render-order fixes, including their untracked new modules. Release
preparation aligned the missing render-order capability registry/document entry.

- The prior full run passed 925/932 in 523s; all seven failures caused by an
  interrupted Python rebuild passed on targeted rerun after runtime restoration.
  See `2026-09-12-0.1.174-mac.md` for the exact evidence and limits.
- 84 capability gates and documentation anchors passed after the version bump.
- Source fingerprint differed only in package.json/package-lock.json version.
- Both newly built ASARs match all 914 current source/package files. Both
  bundled OpenCode executables report 1.18.29.
- Both app/engine signatures and mounted DMG checks passed. The previously
  verified complete Python runtime was reused, not re-downloaded or reduced.
- Standard publisher ran online dependency preflight again, uploaded all four
  versioned artifacts, preserved the signed Windows entry, created Mac release
  rows, then uploaded latest pointers. Catalog publication was explicitly skipped.
- Qiniu uploads used a temporary process-scoped IPv4 CONNECT proxy with
  end-to-end HTTPS validation. CDN refresh used the repo IPv4 admin tool and
  returned HTTP 200 / code 200 / success for all three metadata URLs.
- Independent verification used plain public URLs, not cache-busted responses:
  manifest signature, per-platform API URL/SHA-256/size, exact stable-feed bytes,
  ZIP SHA-512, all four download Content-Length/Content-MD5 and actual ranged
  download bytes match local artifacts. Windows metadata matches the preserved
  pre-existing signed entry. Verification exited 0.

## Artifact identity

| File in dist/ | Bytes | SHA-256 |
| --- | ---: | --- |
| Lily Workbench-0.1.175-arm64.dmg | 637948308 | `1bed0f5048eda28b5011564e8b7205719e6be4119324209d8b35cf47beda6b4c` |
| Lily Workbench-0.1.175-arm64.zip | 531570677 | `0ce71cc0d7e51b0829b62cb38c7e200b01333c100aa52cfe336a2b3665bd9189` |
| Lily Workbench-0.1.175-x64.dmg | 684580721 | `c901633c29953589da4ec46c9062a1c2cb5669151578a5c2d07f87b1bc5c2b50` |
| Lily Workbench-0.1.175-x64.zip | 565415056 | `06d8f86efdcfc1cd2ca2852cd771a5d5f6e825afa4f678a03a1c316e93e317da` |

Mac server rows: `rel_jyfxfoyigzo9hd` (darwin-arm64),
`rel_hxj1nhch1ckie7` (darwin-x64).
CDN request: `6aa53a11ae4f84419ce9ff5a`.

## Acceptance limits

Still ad-hoc signed, with no Apple Developer ID identity or notarization.
The earlier isolated full-app startup on this host blocked in macOS Keychain
`SecItemCopyMatching`; no security controls were bypassed. Full installed-client
startup, live-model execution and installed-client update/restart acceptance are
not claimed. Intel executable checks ran under Rosetta (Bun AVX warning), not
physical Intel hardware. Publishing and download verification do not remove
these acceptance limits.

Logs: `/tmp/lily-mac-0175-build.log`, `/tmp/lily-mac-0175-publish.log`,
`/tmp/lily-mac-0175-cdn.log`, `/tmp/lily-mac-0175-online-verification.log`.
