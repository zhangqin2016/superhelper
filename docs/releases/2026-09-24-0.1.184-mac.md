# Mac 0.1.184 release

## Scope

Mac ARM64 and Intel only, built from `6bcc491f` plus the existing local account,
memory settings and IPC changes. OpenCode remains 1.18.30. No Windows, server,
or separate skill catalog release. Version files advanced from 0.1.183.

## Verification

- Online Mac dependency preflight passed. Offline strict preflight initially
  refused missing local lock entries; it was not bypassed.
- Both architectures built with the existing Mac packaging pipeline.
- Full suite: 1119/1119 passed in 887 seconds.
- Both DMG and update ZIP application trees match; signatures verify and
  packaged resources match source. Packaged Python rendered charts on both.
- Packaged source comparison passed for 1122 source/package files on each
  architecture. Both engines reported 1.18.30.
- GUI acceptance remains incomplete: the isolated ARM64 application blocked
  in `SecItemCopyMatching` / macOS keychain content access. No permission or
  encryption bypass applied. Intel execution uses Rosetta, not Intel hardware;
  it emitted Bun's AVX warning. No authenticated model turn was verified.
- Signatures are ad-hoc, not Apple Developer ID notarization.

## Artifacts

| Architecture | DMG SHA256 |
| --- | --- |
| ARM64 | `ebc390330df68ca1830778c853f208cf7f196e1ea7879c53b2afe8a315a16b28` |
| Intel | `1e04aff37ba2e904afbc1374340a017c493f57ae6e565e8d492f76c33b995601` |

## Publication Status

Published after the user confirmed production credential use. Both Mac
installers and both version-named update ZIP files are uploaded to Qiniu;
both Mac release records and stable feeds have been updated.
ARM64 release: `rel_trqkez5qklvklg`; Intel: `rel_7d554rwd219vjl`.
CDN refresh returned HTTP 200 for both Mac feeds.
Final public verification passed for both release APIs, stable feeds, ZIP
SHA512 values, download lengths and object hashes. A transient Intel request
timeout recovered on retry. Windows 0.1.182 API/feed and the shared signed
manifest match the pre-publication baseline. No installed automatic-update
and restart cycle was performed.
Evidence: `/tmp/lily-mac-0184-online-verification.log`,
`/tmp/lily-mac-0184-finalize.log`, `/tmp/lily-mac-0184-cdn.log`.
All four public downloads match local sizes and Qiniu object hashes; evidence:
`/tmp/lily-mac-0184-download-verification.log`.
Local credentials returned HTTP 401. After explicit approval, the existing
production release token was read from the actual `deploy/baota/.env` and used
only in process memory; no secret was printed or saved. No server configuration
was changed. Shared latest.json and Windows paths were not written.

Direct installer URLs:

- ARM64: https://qny.lanrensoft.cn/app/updates/darwin-arm64/0.1.184/Lily%20Workbench-0.1.184-arm64.dmg
- Intel: https://qny.lanrensoft.cn/app/updates/darwin-x64/0.1.184/Lily%20Workbench-0.1.184-x64.dmg

Evidence: `/tmp/lily-mac-next-tests.log`,
`/tmp/lily-mac-0184-{build,artifact-check,smoke,upload}.log`,
`/tmp/lily-mac-0184-startup-sample.txt`.
