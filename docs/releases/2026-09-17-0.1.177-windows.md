# Windows 0.1.177 Release

- Published: 2026-09-17, stable, win32-x64 only.
- Built source: `f0a5f91d` on `feat/opencode-engine`.
- OpenCode CLI and SDK: `1.18.30`; packaged CLI version verified.
- Installer: https://qny.lanrensoft.cn/app/updates/win32-x64/0.1.177/LilyWorkbench-0.1.177-x64.exe
- Size: `567600777` bytes.
- SHA256: `1aaaef5e3c3189ef09d8efdd741e6b99597df6c445a7bb974832a7e0f8d2201e`.
- Platform release ID: `rel_mvi8adrwacc7id`.

## Release Preparation

- Aligned package-lock metadata with package version 0.1.177.
- Retained the Windows-compatible font selection fix in the document image recognition test.
- Fixed Windows LibreOffice profile arguments using `Path.as_uri()`, including spaces, fragment characters and Chinese path coverage.
- Real LibreOffice HTML-to-DOCX/PDF conversion selftest passed, including failure and collision checks. Conversion startup remained slow on this machine.
- Bundled CPython 3.12.10 and 67 packages. Build completed with 14,535 unpacked files.

## Verification

- Online release preflight passed. The final resumed build skipped only the already-completed preflight.
- Runtime matrix verification found three locked entries and ten production entries absent from local locks; online resolution passed.
- OpenCode version alignment, Windows installer guard and capability registry checks passed (110 gates/document anchors).
- Focused tests passed for tool-result budgets, file-type honesty, internal prompt provenance, persisted answers, restart announcement behavior, runtime helper paths and runtime-pack isolation source checks.
- Packaged files matched source bytes for internal prompt markers, runtime-pack runner, runtime event persistence, turn timeline and the LibreOffice conversion helper.
- Both executable and installer icons were extracted and visually confirmed as Lily.
- Signed latest.json and Windows latest.yml were published and CDN refresh accepted. Public downloads of both manifests matched local bytes exactly without cache-busting.
- Both installer routes returned HTTP 200 and 567600777 bytes; blockmap returned HTTP 200 and 585182 bytes.
- Public release API returned 0.1.177 with matching URL, SHA256, size and Windows feed URL.
- Catalog synchronization completed: 15 uploaded, 16 unchanged, one metadata synchronization. Optional daily-stock-analysis workspace app skipped because its source was absent.

## Limitations

- Installer is not Authenticode-signed.
- Clean-machine installation and installed application auto-update/restart acceptance were not performed.
- The complete repository test suite was not run. Installed runtime-pack execution checks were skipped; font checks probed zero real fonts, so they are not evidence of full CJK rendering acceptance.
- No macOS release or server binary deployment was performed.
