# Mac 0.1.169 release

## Scope and source

- macOS Apple Silicon and Intel DMGs plus automatic-update ZIPs. No server
  business-code deployment, website restart, Windows upload, or catalog update.
- Build source: `c290497a`, including repetition/liveness fixes, process-job
  generation fencing, prior collaboration focus repairs and packaged-resource
  fix `dd57f599`. Later documentation and identical parallel Windows version
  metadata merges do not change the packaged source bytes.
- Source and release preparation pushed to GitHub `feat/opencode-engine`
  through `f314eb69`. The parallel Windows version-preparation commit
  `2ac1fa30` was preserved, not overwritten. The later Windows release record
  `2ac62e28` reports successful actual packaged-consumer loading on Windows,
  without reproducing the Mac resource omission. That is separate Windows
  evidence, not a Windows test performed by this Mac release task.

## Verification

- Final full capability run: **279 scripts passed**, exit 0.
  Log: `/private/tmp/lily-mac-0169-capability-publish.log`.
- Release regression suite passed. Online Mac runtime-pack preflight passed;
  20 production artifacts remain outside the offline lock, as in 0.1.168.
- Initial unpublished package inspection found the shared observation helper
  outside ASAR while host consumers required an ASAR-relative path. A packaged
  layout regression reproduced `MODULE_NOT_FOUND`. External-resource loading,
  source fallback and missing/invalid-helper fail-open behavior now pass; both
  architectures were rebuilt before any upload.
- Both rebuilt packages passed signature validation and DMG mount checks.
  Per architecture, 895 source/plugin files match source. Packaged Electron
  executed both observation consumers and loaded the actual shared helper.
- Both packaged OpenCode binaries report 1.18.21. Intel ran through Rosetta;
  Bun emitted its existing CPU/AVX warning. Physical Intel hardware was not tested.
- Signatures are ad-hoc, not Apple Developer ID notarization. No claim of
  Gatekeeper/clean-machine, logged-in full-app UI, or live-model task acceptance.

## Artifact identity

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| Lily Workbench-0.1.169-arm64.dmg | 611873739 | `8a4db891b184b6f7cae4e2da8a152aecda7f15d44f1c243eeebfa8f62619168b` |
| Lily Workbench-0.1.169-arm64.zip | 510164358 | `51ca778014fb65750cb69382a0d51a4fc876815f77ad3ee96b227b88fd044114` |
| Lily Workbench-0.1.169-x64.dmg | 669726930 | `01386b15f2d1cff74fdf5c9108162fdb33cd13ae22370647d1a8860ad5b77490` |
| Lily Workbench-0.1.169-x64.zip | 550466617 | `c8704f2430dd91ee1eb150cbd9158973af9f441e71b8db8034a1b67b5d35d199` |

## Publication

- Published both DMGs and update ZIPs to the existing Qiniu release bucket.
  Mac release rows: ARM `rel_8vwvg7mo4diex6`, Intel `rel_189yqasgrg5bdr`.
- Signed public manifest and both Mac update feeds promoted to 0.1.169.
  CDN refresh returned 200/success; one stale initial manifest read recovered
  on the existing bounded retry. One-click release exited 0.
- Windows 0.1.169 was independently published while Mac uploads were running.
  Its signed manifest was verified and merged into the local Mac publication
  manifest before promotion. All three platform entries are retained. Original
  Windows 0.1.168 and concurrent 0.1.169 baselines were saved separately.
- Independent public verification exited 0: public manifest signature validates
  against the bundled key; Mac entries match local signed entries; release APIs
  match DMG version, URL, SHA-256 and size; feeds match local bytes and ZIP SHA-512;
  four public object sizes and Qiniu fingerprints match local artifacts; actual
  ranged downloads match local bytes. Two SSL timeouts recovered on bounded curl
  retry. Concurrent Windows API/feed and manifest entry were preserved exactly.
- Logs: `/private/tmp/lily-mac-0169-publish.log`,
  `/private/tmp/lily-mac-0169-public-verification.log`.
  Artifact evidence: `/private/tmp/lily-0169-public-evidence.json`.
- Rollback: prior 0.1.168 Mac artifacts remain available. Any rollback requires
  deliberate release-row and Mac-feed changes while preserving other platforms.
