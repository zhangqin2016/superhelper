# Lily Mobile Command Pro Existing-System Integration Audit

## 1. Scope And Evidence Rule

This is the repository-truth artifact for MC-SPEC-005. Line references describe the audited HEAD (`5b102d62`) on 2026-07-12. A row marked **verified current** names code or data that exists now; a **planned seam** is additive design work and is not an existing export, route, table, or capability.

Mobile Command production code does not exist. In particular, the current tree has no pairing, remote-session, remote-permission, remote-audit, TURN, upload, or signaling route/table. Existing admin `audit_logs` are not a remote-command audit ledger.

## 2. Verified Integration Matrix

| Concern | Current owner | Verified export/route/table | Current caller | Reuse decision | Required additive seam | Compatibility rule |
|---|---|---|---|---|---|---|
| Device identity | `server/src/services/device-identity.js:13-110`; `server/migrations/001_initial.sql:14-22`; `server/migrations/007_client_config_profiles.sql:43-56` | **Verified current:** exported key upsert/rotation and signed-request verification; unified `devices.id`, `device_public_keys`, `request_nonces` | Public device/auth/config routes; desktop wrappers in `src/main/service-client.js:530-734` | Reuse `devices.id` and Ed25519 request identity; do not invent separate desktop/mobile device base tables | **Planned:** device-role/pairing relations and rotation/revocation semantics | Existing device IDs and local activation remain valid; ambiguous remote authority fails safe |
| Account sessions | `server/src/routes/public/auth.js:88-111,114-260`; `server/migrations/022_account_wallet.sql:1-63` | **Verified current:** SMS login creates `users.id`, `user_sessions`, and `user_devices`; access token contains user/session/device IDs | `src/main/service-client.js:548-580`; account manager and account IPC | Reuse user and account-session vocabulary; a remote session is not a `user_session` | **Planned:** pairing grant and remote-control session linked to authenticated user/device records | Logout/revocation may remove remote authority but must not corrupt local conversations or licensing |
| License binding | `server/migrations/001_initial.sql:1-32`; `server/src/services/device-identity.js:118-164`; `src/main/license-manager.js:413-426` | **Verified current:** `licenses`, `license_devices`; valid-license fallback by unified device ID; desktop license exports | Public license/config routes; IPC license handlers | Reuse entitlement/binding checks; license is not the user or remote-session primary key | **Planned:** explicit authorization policy combining user, device, session, and entitlement | Stale requested license may fall back only to another valid binding on that device; local license stays usable |
| Configuration | `server/src/routes/public/client-config.js:61-103,106-175`; `server/src/services/client-bootstrap.js`; `src/main/remote-config.js:275-326` | **Verified current:** unsigned `GET /api/client/bootstrap` returns region/routing/features; signed `POST /api/client/config` applies scoped `config_profiles` via `deepMerge`; remote-config exports cache/read/refresh APIs | Desktop startup/config consumers | Reuse both paths for their existing purposes; do not collapse bootstrap policy into profile config | **Planned:** additive, versioned Mobile Command policy fields and kill switches in the proper owner | Unknown/expired remote capability authority disables remote features while desktop baseline remains unchanged; old clients ignore additive fields |
| Network authentication | `src/main/service-client.js:465-525,827-859`; `server/src/services/device-identity.js:50-110` | **Verified current:** private `requestSignatureHeaders`/`serviceFetch`; Ed25519 canonical method/path/timestamp/nonce/body hash; five-minute timestamp window and nonce replay rejection | Only exported service-client wrappers; server routes explicitly call `requireSignedDeviceRequest` | Reuse protocol and wrapper pattern, not nonexistent public transport/signing exports; bootstrap GET remains its current exception | **Planned:** exported narrow Mobile Command client methods or a reviewed shared transport seam plus route-specific auth | Never bypass route authorization because transport is signed; signature/key uncertainty rejects remote authority |
| Route registration | `server/src/app.js:91-114`; `server/src/routes/public.js:14-37` | **Verified current:** `registerRoutes(app)` registers route plugins; `publicRoutes(app)` is the public registrar and directly registers public route owners | `buildApp`, `buildDocApp` | Add public Mobile Command registrar under `publicRoutes`; preserve doc-app discoverability | **Planned:** one cohesive public Mobile Command route plugin/registrar | No hidden production route registration; OpenAPI/doc enumeration and existing route order remain intact |
| Conversations | `src/main/ipc-sessions.js:68-115`; `src/main/turn-orchestrator.js:841-872` | **Verified current:** session list/get/create/switch IPC; orchestrator resolves the existing local session | Renderer via IPC; scheduled tasks and assistant IPC use orchestrator | Reuse existing conversation/session IDs; do not create a parallel remote transcript owner | **Planned:** authenticated remote lookup/projection with session isolation | Remote disconnect never terminates or rewrites the local conversation |
| Turn injection | `src/main/turn-orchestrator.js:841-925,3437`; `src/main/ipc-assistant.js:106,176`; `src/main/scheduled-tasks.js:276` | **Verified current:** `TurnOrchestrator.sendUserMessage`; busy turns queue; requested steer failure falls back to the queue | Assistant IPC, scheduler, retry paths | This is the sole planned command-injection boundary; do not call `OpencodeAgentSession.sendUserMessage` directly | **Planned:** source/auth/idempotency metadata validated before the same method | Feature disabled or bridge failure returns to today's local/queue behavior; preserve ordering and session isolation |
| Runtime events | `src/main/runtime/opencode-message-parts.js:526-535`; `src/main/runtime/opencode-runtime-reducer.js:739-842`; `src/main/turn-orchestrator.js:705-740` | **Verified current:** message-part adapters and reducer normalize engine events; orchestrator emits Lily permission/question/terminal events | `opencode-agent-session` and renderer event bridge | Reuse normalized Lily events, not raw OpenCode SSE payloads | **Planned:** versioned remote projection with redaction, replay cursor, and backpressure | Unsupported remote projection cannot block or mutate local event processing |
| Permission and question | `src/main/runtime/opencode-runtime-reducer.js:739-802`; `src/main/opencode-agent-session.js:1303-1350`; `src/main/turn-orchestrator.js:705-740,1184-1195` | **Verified current:** actual authority prompts are distinct `permission` and `question` effects/events; there is no generic current `approval` subsystem | Runtime reducer → agent session → orchestrator → renderer | Reuse these semantics only after remote authority policy; do not relabel every prompt as approval | **Planned:** remote eligibility, expiry, approver identity, one-shot resolution, and local precedence | Missing/late/ambiguous authority denies sensitive action; local prompt handling remains authoritative |
| Artifacts | `src/main/artifact-registry.js:175-277`; `src/main/turn-artifacts.js:7,152`; `src/main/turn-orchestrator.js:2186-2278` | **Verified current:** workspace-scoped register/resolve/alias manifest; artifacts are derived during turn terminal handling, not emitted as a live complete remote stream | Turn artifact derivation and local-media protocol | Reuse workspace path safety and artifact identity as a local source adapter only | **Planned:** immutable remote descriptor/download authorization and event timing contract | No path escapes; missing remote artifact support leaves the local workspace registry unchanged |
| File staging | `src/main/file-staging-manager.js:67-150,240`; `src/main/ipc-files.js:150-192` | **Verified current:** class stages a local path/buffer and returns random metadata; current path has no chunk protocol, content hash, idempotency key, or risk result | File IPC handlers | Reuse supported-type/local staging after a remote upload is verified; do not treat it as an upload service | **Planned:** chunk/hash/idempotency/quota/risk/scanning layer before staging | Failed/unverified upload never reaches agent execution; current local attachments continue to work |
| App lifecycle | `src/main.js:288-313` | **Verified current:** Electron activate handling; `before-quit` saves managers, terminates runners, stops connector bridge, and clears staging | Electron app events | Add start/stop only through the established lifecycle composition | **Planned:** disposable Mobile Command service with bounded shutdown | Disabled/startup-failed remote service must leave startup and shutdown behavior equivalent to today |
| IPC | `src/main/ipc-handlers.js:42-262`; `src/main/ipc-sessions.js:68-235,385-390` | **Verified current:** domain handler modules register `ipcMain.handle`; session and permission settings already have owners | Preload/renderer | Add only local pairing/status/permission presentation IPC; remote wire protocol does not belong in IPC | **Planned:** a dedicated handler module and preload allowlist after bridge schema acceptance | Additive channels only; no renderer gets direct OS input, keys, or service credentials |
| Logging | `src/main/logger.js:5-38`; `server/src/app.js:85-89`; `server/migrations/001_initial.sql:74-83` | **Verified current:** namespaced console logger; Fastify app logger; admin `audit_logs` table | Main-process modules, Fastify routes/admin audit helper | Reuse operational logger shape; do not reuse admin audit rows as remote authorization history | **Planned:** structured/redacted remote telemetry and dedicated immutable audit owner | Never log tokens, signatures, file contents, transcripts, or sensitive input payloads |
| Migrations | `server/src/db.js`; `server/migrations/001_initial.sql`; `server/migrations/007_client_config_profiles.sql`; `server/migrations/022_account_wallet.sql` | **Verified current:** ordered SQL migrations use additive `create table/index if not exists`; FKs and explicit indexes live in migration files | Server DB bootstrap/migrator | Follow ordered additive SQL convention; no production table exists for Mobile Command today | **Planned:** new numbered migration after the current latest migration, with tables/indexes/retention reviewed in MC-SPEC-006 | No destructive reinterpretation of `devices`, users, sessions, licenses, or config profiles |

## 3. Accepted Repository Terminology And Seams

- `user_id` means `users.id`; an account login is represented by `user_sessions.id`.
- `device_id` means the existing unified `devices.id`. Desktop/mobile role is additive metadata or relation, not a second base device identity.
- `license_id` means `licenses.id`, linked through `license_devices`; it is entitlement context, not account identity.
- A planned `remote_session` is distinct from both local conversation/session identity and `user_sessions` authentication state.
- Server routes enter through `registerRoutes` → `publicRoutes`; desktop commands enter through `TurnOrchestrator.sendUserMessage`.
- Configuration has two current sources with different jobs: bootstrap region/feature policy and signed profile-merged effective config.
- Artifact registry and file staging are local adapters with partial reusable invariants, not remote artifact/upload implementations.

## 4. Confirmed Absences

Repository searches across `server/src` and `server/migrations` found no Mobile Command pairing, remote-session, remote permission/approval, remote audit, TURN credential, upload, or signaling owner. These remain planned domain contracts and migrations. Their names in the implementation map are placement proposals, never verified current interfaces.
