# 企业账户与 Token 池验收（2026-09-05）

已部署版本：`enterprise-20260905-0047-v2`。生产 API `/health` 返回 `{"ok":true}`；API、数据库健康，Web 正常运行。旧镜像和部署环境备份保留。此次仅部署 server/web，无桌面安装包发布。

## 线上 UI 验收

- 原企业 `org_hu1qseaq96gcig`（再次起飞小组）正常显示，负责人登录名 `zcqf`；显示「重新签发负责人初始密码」。没有重置客户原账号密码。
- 通过管理界面创建验收企业 `org_xq8jlq9o8bbioq`（企业闭环验收-20260905），签发负责人 `qa0905_owner`，成功展示一次性初始凭据。
- 负责人通过密码界面登录，进入首次改密页面。实际密码变更通过正式 API 完成；没有把这一步描述为浏览器提交验收。改密表单和跳转另有执行真实页面/动作代码的测试覆盖。
- 通过企业成员页面生成 `qa0905staff_0001` 和 `qa0905staff_0002`，凭据当次显示，刷新后隐藏。第一名员工单次扣费上限 1,000 持久化成功。
- 通过平台管理界面调拨 10,000 Token，期限一天，显示操作成功。企业负责人额度页最终显示 9,906；用量页按员工和模型均显示消耗 94。
- 点击登录页「手机号验证码」后，停留在 `/account/login?mode=sms&next=%2Faccount%2Fenterprise`，显示手机号、发送验证码、验证码及登录表单。未发送实际短信；此次验证入口跳转，不包含短信供应商投递验收。

## 真实员工消耗

员工 `usr_294oe16xhmr2vq` 通过正常登录、首次改密和签名客户端配置取得网关凭据；未使用直接供应商密钥绕过计费。

| 项目 | 结果 |
| --- | --- |
| 模型 | deepseek-v4-flash |
| 请求标识 | qa-enterprise-008464a0-ff68-4df2-a026-ad951dd4b3d3 |
| 网关状态 | HTTP 200 |
| 池余额 | 10,000 → 9,906 |
| 供应商用量 | 输入 87 + 输出 7 = 94 Token |
| 员工用量汇总 | 94 单位 |
| 尝试扣其他企业池 | HTTP 403 |
| 普通员工管理企业账户 | HTTP 403 |

当前报表「次数」按用量事件记录计数：本次预扣与结算产生两条记录，显示 2；这不是两次模型调用。金额/Token 汇总与余额变动一致。员工配额为单次扣费上限，不是累计预算。测试企业和两个员工保留供复核，测试池一天到期。

## 自动验证及范围

页面/服务动作、企业密码登录跳转、地区路由、余额有效期与角色检查、初始凭据恢复入口、认证限流及 HTTP 隔离测试通过。企业创建/账户/邀请、账户会话守卫、模型与媒体网关及制品卫生相关检查通过。Next 生产构建通过，`git diff --check` 通过。

真实 PostgreSQL 隔离 schema 集成验证覆盖创建回滚、登录改密、初始账户限制、负责人凭据恢复边界、成员角色、调拨与扣费、并发余额、幂等与会话撤销，以及实际 HTTP 网关到本地上游夹具。既有企业协作集成测试通过。独立代码审查未发现部署阻断项。未运行与本次 server/web 改动无关的完整桌面测试。

源包 SHA-256：`c7f506a0d82d4e29d80ce025e01cd1d60532fecd39942dbcb7e646e9c70e2497`。报告不保存任何密码、会话或供应商密钥。
