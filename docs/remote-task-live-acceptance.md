# 远程任务真实联调记录 — 2026-09-07

## 环境与边界

- 服务端：`remote-task-20260907-0730`，迁移 `045_collaboration_tasks.sql` 已落库。
- 使用新建独立测试组织、两个企业测试账号和合成文本文件；没有使用已有业务会话或工作文件。
- 服务端任务与工作空间处理开关已开启。签名配置 `rt-acceptance-default-off-20260907` 保持普通用户入口关闭，`rt-acceptance-org-20260907` 仅对测试组织开启入口。
- 凭据仅存本地 0600 临时文件，不写入仓库、截图或测试输出。

## 已通过：真实服务端及私有桶

`scripts/remote-task-live-acceptance.mjs` 使用两个独立的正式 client/store/transfer/workflow 实例。本轮通过 Electron 网络栈运行，实际连接生产 API 与专用私有对象存储，不使用网络 fixture。

- [x] 两账号真实登录，独立设备密钥及请求签名。
- [x] 验证正式配置签名和过期时间；测试账号获得任务开关，无账号配置请求没有任务/工作空间开关。
- [x] 每次建立独立私密会话，真实 bootstrap 写入隔离 SQLite。
- [x] 冻结之后修改原文件；接收方仍取得冻结版本。
- [x] 真实加密上传及服务端验证；故意丢弃成功创建响应后，同一命令重试，服务端仍只有一个任务。
- [x] 私有对象匿名下载返回拒绝；授权下载成功，密文大小及 SHA-256 完全一致。
- [x] 正式传输模块下载、解密、解包；基线只读，协作者拥有独立可编辑副本。
- [x] 真实修改、添加、删除测试文件，上传交付并绑定版本验收；验收不会自动覆盖发起人文件。
- [x] 应用预览包含 add/replace/delete；缺少删除确认时返回准确错误码且文件未变化。
- [x] 明确确认后精确应用；回滚恢复原文件、恢复被删文件、移除新增文件，远端任务保持已验收。

早先普通 Node 网络栈运行遇到 `UND_ERR_CONNECT_TIMEOUT`；正式 Electron 网络栈运行完整通过。测试脚本最初使用错误的传输目录名称，现已修正并增加真实离线模块装配预检。这些失败没有被计入通过结果。

## 本轮修复及回归

- Electron 的 `LILY_USER_DATA_DIR` 在单实例锁之前生效；绝对路径校验、不同目录并行、同目录拒绝重复实例均通过真实 Electron 进程测试。
- 企业密码登录与短信登录一致地刷新账号专属签名配置；刷新失败不伪装成登录失败。
- 登录刷新排在旧刷新之后；旧 Promise 完成不能清除新的刷新任务。
- 账号改变后的迟到配置响应不能写入当前缓存；覆盖 token 取得、配置请求和设备注册重试的异步边界。
- 131 项 IM/远程任务 Node 脚本通过；7 项账号/配置刷新脚本通过；真实 Electron 页面接线、任务交互和实例隔离回归通过。
- 完整窗口发现登录后左下角仍显示未登录：账号设置刷新现在通知账户菜单重读，菜单用刷新代次阻止旧响应覆盖新状态。`test-account-menu-login-refresh.mjs` 覆盖登录、退出和迟到响应，回归通过。

## 尚需验证，不能提前勾选

- [x] 两个完整源码 Electron 窗口通过真实登录控件，同时展示同一已验收的线上任务（2026-09-07T08:03:07.362Z）。主代理逐张核对截图；这不等于模型服务配置无异常，见下。
- [ ] 两个发布安装包/两台物理机器的验收。
- [ ] Windows 实机及掉电持久性。
- [ ] 普通用户全量开放及新安装包发布。

完整源码窗口验证使用 `scripts/remote-task-live-electron.cjs`，隔离 userData/home/documents/运行控制发现文件，关闭跨实例孤儿进程清理；不发起模型任务。首次直接 IPC 登录仅证明一个客户端已认证，不作为真实登录界面通过证据。后续修正自动化等待：DOM 已存在不代表监听器已绑定，账号面板已显示也不代表登录配置刷新已结束。

双窗口任务显示证据分别保存在 `/private/tmp/lily-full-client-a3.e1wdFc/collaboration-accepted-task.png` 与 `/private/tmp/lily-full-client-b4.KzMhSC/collaboration-accepted-task.png`。第二端实际登录结果包含 `modelConfigReady=false / SERVICE_REQUEST_FAILED`，但随后任务策略可用且真实任务显示成功；因此只能计入双端任务显示通过，不能声称模型服务配置检查通过。两端已通过 stdin 正常退出，没有停止用户原有客户端。

后续 a5/b5 配置超时已发现验收环境隔离缺口：正式设备 ID 根据硬件 ID 派生，同机两个全新配置目录获得相同设备 ID、不同密钥；服务端设备注册更新该 ID 的公钥，导致彼此签名失效（实际观测 401）。仅隔离 userData 不等于两台设备。正式默认配置目录的既有设备 ID 与测试 ID 不同，检查未读取或输出私钥。后续同机验收必须同时隔离测试硬件身份与指纹，不修改正式产品的硬件授权策略，不宣称物理双机验收。

最终 a6/b6 复测通过：仅验收启动器为精确 macOS 硬件查询提供独立虚拟身份，正式服务代码自行派生设备 ID、指纹并保存独立密钥，真实网络与业务处理不替换。`test-live-electron-device-isolation.mjs` 验证不同身份/指纹/密钥、重复读取稳定和其他命令原样转发。两个窗口均返回 `full_app_ui_passed`，真实登录结果 `modelConfigReady=true`，侧栏为已登录，任务策略开启；两端同为会话 `conv_06ea4ff4-cfb9-4b7c-9aee-ee309b0e80f3` / 任务 `task_5e87baed-b4d6-403b-9618-8d99c7e03bc9`。主代理核对两张截图后正常退出两个测试窗口。

最终截图：`/private/tmp/lily-full-client-a6.VymD1r/collaboration-accepted-task.png`、`/private/tmp/lily-full-client-b6.9wYsuC/collaboration-accepted-task.png`。第二端仍有模型配置诊断提示条；配置刷新成功不等于模型运行已验证，本轮没有发起付费模型任务，不能据此声明模型服务运行验收通过。原先配置超时已通过独立设备身份复测消除，不应把历史失败抹去。

## 重跑方式

离线装配检查：`node scripts/remote-task-live-acceptance.mjs --credentials <私密凭据文件> --preflight`。

真实联调必须显式传入 `--allow-live-writes`，仅使用专门创建的验收账号。脚本会保留合成任务和加密测试对象在服务器；其本地临时工作目录会清理。完整应用窗口的私有测试目录保留，不能当作用户正式配置目录复用。
