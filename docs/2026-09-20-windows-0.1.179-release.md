# Windows 0.1.179 release

Published 2026-09-20, Windows x64 only. Product code: `e1e4fd38`.
Test follow-ups: `792398b9`, `7a27fb26`. Branch: `feat/opencode-engine`.

## Artifact

- Installer: `LilyWorkbench-0.1.179-x64.exe`, 572607675 bytes.
- SHA256: `BC0217A3B8B1FD2726E97E484F2EF41D934767718803012009A9A2860D8B7D2F`.
- OpenCode: 1.18.30; Python: 3.12.10. LibreOffice remains opt-in.
- Windows Authenticode certificate is not configured: installer is unsigned.
- All seven ICO frames match `resources/icon.ico` in both app and installer.
- 80 changed source/resource files match the final packaged files.
- Packaged runtime verification passed, including base Python imports.

## Verification

Final `run-capability-gate.mjs`: 413 scripts passed, exit 0, no assertion errors
in `.cache/release-179-checks/clean-gate.log`. Release preflight passed, including
online confirmation of ten Windows runtime-pack artifacts absent from the local
lock inventory. This does not certify installing every optional pack.

Two UI tests now use visible Electron windows. The task-center test also uses
the existing collapsed-sidebar layout for its synthetic 420px viewport (the
real main window minimum is 1024px). Its cleanup preserves failure exit codes.
Both tests passed five consecutive focused runs after these corrections and
the final full run. Earlier full-run logs are not acceptance evidence.

Explicit skips remain: retained 1.18.29 native upgrade binary, optional document
image recognition environment, POSIX permissions on Windows, and two runtime
discovery-dependent live extraction checks. Packaged runtime smoke checks are
separate evidence, not a claim these skipped cases ran.

Native UI and 2GiB history evidence and residual risks remain documented in
`2026-09-20-ten-round-native-qa.md` and
`2026-09-20-large-history-responsiveness.md`. This release does not certify all
startup paths, every skill, arbitrary-duration tasks or the exact customer stall.

## Publication

Used the existing Windows-only release pipeline, skipping local skill/app
catalog publication. No macOS build, model configuration or server deployment.
Qiniu upload interruptions recovered before publishing mutable latest pointers.

- Download: https://qny.lanrensoft.cn/app/updates/win32-x64/0.1.179/LilyWorkbench-0.1.179-x64.exe
- Manifest: https://qny.lanrensoft.cn/app/updates/latest.json
- Auto-update feed: https://qny.lanrensoft.cn/app/auto-updates/win32-x64/stable/latest.yml
- Server release id: `rel_6bi3gphfra254a`.

Release pipeline exited 0. CDN refresh succeeded. Public manifest, auto-update
feed and server release were all verified as 0.1.179. Local evidence:
`.cache/release-179-checks/publish.log` (not committed).
