# OpenCode 1.18.30 controlled upgrade

> Execution: superpowers:subagent-driven-development; test-first implementation,
> independent specification and quality reviews, fresh verification before completion.

**Goal:** Upgrade Lily's pinned SDK and shipped engine from 1.18.29 to 1.18.30
without silently discarding validated native session continuity.

**Approved scope:** User approved the preceding four-step controlled-upgrade
recommendation on 2026-09-14. No production release, credential changes, customer
database manipulation or unrelated workspace edits are included. Work in the
existing shared checkout; preserve its existing database/auth/renderer changes.

**Architecture:** Keep SDK v2/shared serve, existing plugins, provider routing and
Lily prompts unchanged. Resume identity checks remain strict; only the explicit
forward transition 1.18.29 to 1.18.30 is eligible for compatibility. Do not infer
compatibility from semver ranges or enable rollback against upgraded storage.

**Tech stack:** Node, Electron, OpenCode prebuilt npm engines, SDK v2, SQLite.

## 1. Resume compatibility

Files: `src/main/resume-binding.js`, `scripts/test-resume-binding.mjs`,
`scripts/test-ensure-session-runner-resume-reset.mjs` (or focused sibling).

- [x] Add failing assertions for forward upgrade acceptance and identity mismatch
  rejection; assert downgrade, unrelated/new versions and malformed versions reject.
  Example: `verifyResumeBinding({...session, agentResumeBinding: {...binding,
  opencodeVersion: '1.18.29'}}, {...binding, opencodeVersion: '1.18.30'}).ok === true`.
- [x] Run `node scripts/test-resume-binding.mjs` and observe expected red.
- [x] Add the minimal explicit pair check to the version comparison, preserving all
  existing identity checks and legacy unbound behavior. Do not rewrite stored data.
- [x] Verify real `ensureSessionRunner` keeps resume ID for the approved pair when
  owned artifacts exist, and still resets a foreign/stale binding.
- [x] Run resume binding, continuity, artifact and runner reset tests; obtain
  specification and code-quality review.

## 2. Version and artifact alignment

Files: `package.json`, `package-lock.json`, `scripts/fetch-opencode-engine.mjs`,
`scripts/test-opencode-version-alignment.mjs`, `docs/opencode-source-version.md`.

- [x] Set test expected version to `1.18.30`; run it and observe old-pin failure.
- [x] Pin SDK/default fetch/documentation to `1.18.30`; use npm to update lock and
  installed SDK without unrelated dependency updates.
- [x] Retain a temporary 1.18.29 native binary for cross-version acceptance.
- [x] Fetch darwin-arm64, darwin-x64, win32-x64 and linux-x64 prebuilt artifacts.
- [x] Run `node scripts/verify-engine-bundle.mjs --platform <key>` for each platform;
  native binary version must execute; foreign artifacts are not native OS acceptance.
- [x] Run version alignment and SDK/config/provider/plugin regressions.

## 3. Native upgrade acceptance and regression

Files: focused opt-in native integration test under `scripts/` if existing tests
cannot cover the upgrade, verification report under `docs/`.

- [x] Use disposable profiles/workspaces only. Create history with old native engine,
  stop it cleanly, restart new engine on the same profile, verify same session/history
  and successful continuation through SDK. No customer profile or secret logging.
- [x] Exercise actual engine HTTP/SSE and a deterministic localhost provider for
  streaming, tool calls and cancellation where practical; clearly distinguish this
  from nondeterministic real-model/customer-task acceptance.
- [x] Run `npm run test:unit`, architecture and capability gate; investigate failures
  and distinguish pre-existing shared-worktree failures from introduced failures.
- [x] Record test counts, artifact verification, unresolved limits and exact scope.
  Do not claim installer publication, Windows execution or customer issue resolution.

## Completion

- [x] Review scoped diff, run `git diff --check`, update version documentation and
  this checklist with results. Preserve unrelated work; do not push or publish.

## Outcome

Source upgrade and local acceptance complete. Final discovery suite 941/941 in
424 seconds; capability gate 324 commands; architecture and version alignment
passed. Native Mac ARM cross-version + Responses test explicitly executed and
passed; parent/child/compaction usage replay test passed. Both independent reviews
approved. An existing auth gate registration omission was repaired in the shared
workspace after its test passed; that unrelated repair is excluded from the
upgrade submission. See `docs/opencode-1.18.30-verification.md`
for limitations, including opt-in skips and foreign-platform execution. Changes
were subsequently authorized for commit and push. Submission is scoped to the
upgrade and separately tested in an isolated staged-tree export; no installer
has been published by this upgrade. The isolated submission tree passed 931/931
full-suite commands (414 seconds), 310 capability-gate tests, architecture, all
four bundle checks and explicit native Mac upgrade/Responses/usage acceptance.
