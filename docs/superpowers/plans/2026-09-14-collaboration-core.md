# Lily collaboration core implementation plan

> **For agentic workers:** Use superpowers:executing-plans, inline execution with checkpoint commits. Resume from the first unchecked item; do not restart completed work. User explicitly authorized implementation without another design approval.

**Goal:** Persistent shared workspaces and conversation tasks, task-owned Git deliveries, automatic validated integration and safe local application for code, Office and large directories.

**Architecture:** Preserve existing collaboration ACL, encrypted SQLite records, command receipts, multipart transport and task orchestration. Separate shared published Git history from the private version vault and from local uncommitted files. Every network/filesystem boundary has a recoverable durable identity; no claim of a global transaction.

**Tech Stack:** Electron/CommonJS main process, ES module renderer, SQLite, Fastify/PostgreSQL, system/bundled Git, bundled Python and LibreOffice.

## Baseline and authorization

- Original worktree was clean detached `06eb1d95` (0.1.12), missing all named collaboration modules, CAPABILITY-GATE.md and memory/MEMORY.md.
- Current branch `codex/collaboration-core` starts at committed `dab5cecb` from `feat/opencode-engine`. Switching required sandbox approval for shared Git worktree metadata; no original working files were changed.
- Original dirty workspace contains database recovery, message rendering and upstream auth work. None of those uncommitted edits were imported. Committed task continuity and collaboration foundations are available; later safety changes need integration review when this branch is eventually merged.
- Research copied verbatim from the explicitly supplied file into `docs/research/2026-09-14-collaboration-git-ai-research.md`. Prototype is interaction reference only.
- No production deployment, install package, original-workspace merge, or external messaging is authorized.

## A — Persistent identity, bindings and cards

- [x] A1. In `task-workflow.js`, persist preparation intent before asynchronous freeze and emit a change after each durable state. Keep failures visible and retry under the same identity after restart. Dedicated `test-remote-task-preparation.mjs` covers deferred freeze, pre-ready send refusal, concurrent retry and SQLite reopen; existing workflow covers upload/receipt recovery. This step does not implement cards or an automatic startup retry worker.
- [x] A2a. Add optional shared workspace identity to server contract/service/routes and additive PostgreSQL migration 047; bind owner/conversation with a composite foreign key. Desktop task views retain identity. Verify real PostgreSQL replay, task/object ACL, rollback and encrypted/relational consistency. Legacy tasks omit identity.
- [x] A2b. Negotiate shared-workspace support in server policy/client before enabling identity-bearing creation. Generate and persist identity in main-process workflow per selected source project and authorized conversation; include it in immutable send intent. Do not send new fields to an older service without capability support. Follow with A3 binding and A4 cards.
- [x] A3. Add encrypted device-local binding records using `task-records.js`; accept only main-resolved project/session IDs, persist canonical directory identity. Extend `task-session.js`, workflow receive/open and main task options so existing projects can own isolated task copies without retargeting an active engine. Git worktrees are B1; full-source/live acceptance remains D3.

  A3a main binding and A3b execution/receive selection completed (checkpoints 4–6). Bound-session task card projection remains A4. Git worktrees replace current isolated ZIP copies in B1.
- [x] A4. Project local pending and authoritative tasks into stable conversation references in collaboration timeline and owning workspace session. Reuse remote-task actions, update a single keyed node by monotonic revision, keep initial order and local materialization distinct. Test actual Electron DOM, navigation/account fences, offline pending and restart duplication.

## B — Git baseline and immutable task contribution

- [x] B1. Add `src/main/collaboration/task-git.js` using existing `workspace-git.js` executable resolution, an isolated collaboration repository and explicit task refs. Capture only authorized frozen baseline paths; never copy `.git` or the private vault. Real Git tests assert private history and unrelated working edits are absent.
- [x] B2. Make task execution use its isolated worktree and known materialization inventory. Build a changeset from explicit path records; absence is deletion only for known downloaded readable baseline files. Rename is represented with source/target identities. Test add/change/delete/rename, mixed local edits, ignored/missing/unreadable input and immutable retries.
- [x] B3. Negotiate Git objects through mature Git plumbing/transport; add ACL-scoped content descriptors and missing-object queries to the existing object service. Reuse encrypted multipart for large file objects, retaining legacy ZIP input/delivery readers. Reject completion until all required objects verify. Test object reuse, lost responses, revoked access and real Git ancestor reconstruction.

## C — Automatic integration and local materialization

- [ ] C1. Persist integration intents in SQLite keyed by workspace/delivery/target; add generation/lease fencing and a workspace-wide writer coordinator. Wire task events to the bound session through `turn-orchestrator.js` and existing turn queue/TaskCore. Foreground turns continue; no focus or engine cwd changes. Test duplicate events, reopen, lease expiry, stale workers and background admission.
- [ ] C2. Build shared candidate M exclusively from published H and immutable D. Deterministic Git/type merge first; unresolved intent goes to the existing AI turn with baseline/goal/evidence. Run candidate validation, CAS publish H→M and persist publication outbox. Recover ref/database acknowledgement gaps by reading actual refs; test simultaneous publishers and target advancement.
- [ ] C3. Build local candidate from A/W/M separately. Extend `task-application.js` journal with immediate hash rechecks, writer fencing, retryable external Office locks and contribution-specific inverse changes. Keep private W out of shared commits. Inject crashes before/after every write and receipt; retain subsequent edits on apply/undo conflicts.

## D — Office and large-directory acceptance

- [ ] D1. Add Python document merge adapter under `resources/runtime-scripts/`, invoked with resolved bundled runtime. Use python-docx/openpyxl/python-pptx for typed structure; preserve untouched package parts and refuse unsupported destructive round trips. Model sees bounded semantic conflicts. Verify Word paragraphs/tables, Excel formulas/row identity, PPT objects and macro/unknown-part preservation. Render/recalculate with existing document verification tools; PDF prefers source.
- [ ] D2. Replace ZIP materialization on the new protocol with streamed manifest/object enumeration, bounded concurrent hashing/upload, quotas, resumable verified cache and lazy materialization inventory. Show online-only entries in Lily tree only. Test missing objects never become empty filesystem placeholders and do not become deletions.
- [ ] D3. Run measured scale/fault cases and two full Electron clients with isolated test profiles/service. Record actual bytes/RSS/timing and runtime versions. Separate fixtures, Git/SQLite/filesystem integration, PostgreSQL HTTP integration and real clients; unavailable environments remain unchecked acceptance items.

## Verification and checkpoints

Next execution target after checkpoint 55: automatically apply the independently validated A/W/M candidate through foreground-writer admission and the process-wide coordinator, then advance A only with its local receipt and implement contribution-specific undo. Private candidate validation now runs pinned checks against a separate local Git commit and persists separate evidence. Original-session remote publication now prepares private candidates automatically; application, completed-intent startup reconciliation and local task-card status remain unfinished. Existing preview/apply/rollback coordinate across accounts and profile roots; foreground engine/editor writes remain outside that broker lock. The bounded object cleanup worker is implemented behind a default-off server flag; physical-provider deployment/acceptance has not occurred. Task-owned transfer manifests now recover from encrypted SQLite journals on the original device; device adoption and already-lost pre-journal identities still require explicit recovery ownership. Expired orphan replacement, missing publication packages and retained-manifest unknown-init/ciphertext recovery now have verification. Continue independently pinned original-test helper/dependency coverage and unresolved model repair before claiming C2. The independent private A/W/M writer and undo, Office fidelity, bounded streaming/lazy materialization and measured scale remain required. Protocol negotiation now exists behind the default-off operator publication flag; it is not full-core or installed-client acceptance.

Each implementation slice: first run a failing intent regression, implement minimally, rerun its suite and affected companions, review diff, commit explicit paths only, update this file with evidence and next action. Never use global `git add .`.

Existing companion baseline: `test-remote-task-{bundle,workflow,session,application,records,recovery,transfer}.mjs`; UI: `test-remote-task-dual-client-ui.cjs`; server scripts under `server/scripts/collaboration-*integration.mjs`.

Self-review: A covers requests 1/3 and ACL; B covers 2/4/5 and private-history boundaries; C covers 6 plus concurrency/recovery; D covers 2/7 and fidelity/scale. Realtime CRDT and block dedupe are explicitly outside this core plan. No stage may be declared complete from helper tests alone. Detailed API/test additions are refined against immediate callers before each slice rather than inventing unverified signatures.

## Execution log

2026-09-14 checkpoint 7 (A4 partial):

- Added encrypted participant-authorized task snapshot projections and stable draft card anchors. Revisions cannot regress; contradictory same-revision responses fail. An authoritative task discovered before send ACK reuses a draft only when task-scoped input object, requester, shared workspace and full creation content match. Projection never resolves the pending command receipt itself.
- IM timeline loads cards without opening the task panel, updates existing DOM nodes, and clears account-owned cards on navigation/logout with generation fences. Cards use existing task detail/create surfaces and remain separate from message sequence/read/reply semantics. Fixed renderer listTasks to use the real preload string-ID signature.
- RED: delayed ACK initially produced two cards. PASS Node: remote-task-cards (real SQLite, reopen encryption, exact-match negative cases, revocation), remote-task-workflow, remote-task-desktop, collaboration-center-lifecycle. PASS Electron: remote-task-cards-ui (including delayed logout response), collaboration-timeline, collaboration-visible-read, collaboration-reply-ui. UI APIs use fixtures; no live-device acceptance claim.
- A4 remains incomplete: originating local session cards, bound-session projections, persistent background event hydration/history pagination, offline freshness indication and actual card-click acceptance remain. Existing task list is capped at 50 and card projection at 1000; do not claim exhaustive history. B/C/D remain outstanding. Next slice must validate card opening and complete source session identity/card plumbing before broader Git integration.

2026-09-14 checkpoint 1:

- A1 implemented. Preparing records now exist before bundle I/O; failed preparation remains encrypted and can resume via `prepare {draftId}`. Ready material is required for send. Live preparation IDs fence duplicate retries within the singleton workflow. Read-only drafts/recoveries bypass mutation locks and do not emit change events. State writes emit immediate lightweight hints. Renderer supports re-preparing a restored draft.
- New red test first failed because draft reads were blocked by preparation's `new` mutation key. After implementation it passes with real SQLite, real successful freeze and intentionally deferred/failing I/O. No child process was killed in this test: reopen is controlled recovery evidence, not power-loss evidence.
- PASS with Node 22.19.0 and `NODE_PATH=/Users/zhangqin/aicode/ceshitermianl/node_modules`: `test-remote-task-preparation.mjs`, `test-remote-task-project-source.mjs`, `test-remote-task-workflow.mjs`, `test-remote-task-bundle.mjs`, `test-remote-task-session.mjs`, `test-remote-task-application.mjs`. SQLite experimental warning only. Dependencies are read-only references; no install or original-workspace mutation.
- PASS actual Electron `test-remote-task-workflow-ui.cjs`, including added failed-preparation/same-draft retry assertions. Invocation: `NODE_PATH=/Users/zhangqin/aicode/ceshitermianl/node_modules /Users/zhangqin/aicode/ceshitermianl/node_modules/electron/dist/Electron.app/Contents/MacOS/Electron scripts/test-remote-task-workflow-ui.cjs`. Sandbox launch exited 134; approved unsandboxed launch passed against temporary test profile. API responses are fixtures, not live service acceptance.
- Review: existing revoked-source test now expects the encrypted pending intent to remain after authority is lost during freeze; prepared material must never be published by that callback. No remote message, package or model turn is dispatched during preparation. Known remaining scope: one workflow's in-memory retry fence is not the cross-process durable lease required by C1.
- Budget breach: oversized rule-file outputs exceeded the user's session token budget (goal tool reported 90,976 at first check). Surfaced to user. Subsequent reads must use narrow ranges and bounded output; checkpoint now for a fresh continuation.
- NEXT: A2 read `server/src/services/collaboration/task-contract.cjs`, task service/route/migrations, `task-view.js`, `client.js` task API and existing server integration tests. Add stable sharedWorkspace identity with immutable ACL scope and compatibility before device binding/card projection. A2–D3 remain unimplemented; no full-core completion claim.

2026-09-14 checkpoint 2:

- Previous turn classified as progress (commit 389805ca and verified preparation recovery), not a wait or blocker.
- A2a implemented: optional sharedWorkspaceId in task contract, closed HTTP create schema and desktop projection. Server creates/reuses a shared workspace within command transaction; owner and conversation cannot change. Replays reauthorize before receipt reuse. Unique insert and post-insert lock recheck cover concurrent first claims. Task read rejects encrypted/relational workspace mismatch. Composite FK enforces the owner/conversation relationship.
- No shared-workspace-wide history/object endpoint exists yet; current two-party task ACL and scoped object broker continue to control access. The ID alone grants nothing. Membership/history capabilities must be designed with B3; do not infer them from this schema.
- RED: new contract test rejected sharedWorkspaceId before implementation. PASS: contract, routes, service (in-memory transaction adapter), desktop (SQLite reopen), workflow (SQLite/files/ZIP, fixture transport), diff check.
- PASS real PostgreSQL `server/scripts/collaboration-tasks-integration.mjs` using existing dedicated `lily-collaboration-verify-pg` container at loopback 55432. Random task_test_* schema created and cleaned. Includes migration 047, concurrent command replay, two tasks/one identity, foreign owner and foreign conversation denial, observer task/object denial, missing input rollback, encrypted identity mismatch, composite FK rejection, transition CAS and revoked replay. Verified object rows are seeded; not HTTP auth/live upload acceptance.
- Server ESM dependencies temporarily resolved with `server/node_modules` symlink to original workspace's installed server dependencies, read-only; link removed after tests. Recreate locally for subsequent server tests, never commit it. Container credentials were passed only in subprocess environment, never saved to repository or printed.
- NEXT: A2b inspect task policy/config and desktop service/client response schemas; advertise a versioned feature before main workflow generates shared IDs. Then A3 select existing project/session without changing running engine cwd. Migration 047 must precede enabling the new server feature in any future deployment; none was requested or performed.

2026-09-14 checkpoint 3:

- Previous turn was progress: scoped workspace identity persisted and tested with PostgreSQL, commit 233308f3.
- A2b complete: server advertises sharedWorkspaceProtocol:1 when final task/workspace/master gates permit. Generic old profiles do not invent support; desktop signed-config projection accepts only exact version one. Service forwards it to workflow.
- Before initial freeze, workflow transactionally reuses or creates an encrypted shared-workspace record keyed by device/conversation/source project/canonical directory. Random remote identity is persisted, never derived from plaintext paths. Source identity survives SQLite reopen and remains separate across conversation permission domains. Draft, create request and confirmed task binding retain it; source project also survives acknowledgement.
- Old service creates omit the field entirely. A previously prepared workspace-bearing draft cannot silently downgrade its immutable intent when protocol support disappears; it returns COLLAB_TASK_PROTOCOL_UNAVAILABLE and remains stored. Completed receipts stay idempotent.
- RED before implementation: protocol advertisement absent; separate SQLite source test lacked persisted identity. PASS after implementation: `test-remote-task-policy.mjs`, `test-collaboration-policy.mjs` (actual desktop config-cache projection), `test-remote-task-workspace-identity.mjs`, `test-collaboration-client-service.mjs`, `test-remote-task-workflow.mjs`, `test-remote-task-project-source.mjs`, `git diff --check`. New identity test uses actual SQLite/files/ZIP and fixture upload/receipt responses; not live server/client acceptance.
- Runtime invocation remains `NODE_PATH=/Users/zhangqin/aicode/ceshitermianl/node_modules node scripts/<test>`. Server ESM tests used the temporary read-only server/node_modules symlink, removed after testing.
- NEXT A3: inspect `src/main.js` taskOptions/openWorkspace, `task-session.js`, session manager persistence, project manager identity, and renderer receive/open actions. Add existing-project/session binding while retaining task execution isolation and immutable input baseline; do not point an active engine at a different cwd or treat unrelated files in the bound directory as task contributions. User still sees one top-level workspace. A4 and all B/C/D remain outstanding.

2026-09-14 checkpoint 4:

- Previous turn was progress (674d1e3c negotiated stable source identities). Added A3a: closed `bind {conversationId,taskId,projectId,sessionId?}` workflow command, authorized through current remote task first. Main process resolves registered project and optional session; explicit session must belong to that project. No directory paths enter IPC requests.
- `resolveRemoteTaskBinding` returns canonical existing directory and an existing selected session, or idempotently creates one idle collaboration session under that same project. It never adds a project, changes focus or modifies an existing running session. Real main taskOptions wires this resolver.
- Mapping is encrypted via task records and keyed by account/device/sharedWorkspaceId. It persists project/session/root and is reused across tasks/reopen. A second task cannot silently change project, session or canonical root. Each task stores workspaceBindingId independently of its isolated workRoot; no task files are copied into the user's project by binding.
- RED tests: missing resolver export and rejected bind command. PASS: `test-remote-task-session.mjs` (real session manager/index save/reopen; running session unchanged; foreign/deleted targets denied), `test-remote-task-workspace-binding.mjs` (real SQLite, fixture task authorization/session resolver), `test-remote-task-workflow.mjs`, `test-remote-task-project-source.mjs`, diff check. No Electron UI claim in this slice.
- NEXT: A3b add bounded main-projected available project/session choices to the existing task card/details and bind before receive. Integrate isolated task execution roots with session/runner startup before routing Open to the bound project; simply opening the bound root would let the agent edit unrelated W while deliveries still read the isolated workRoot, so it is explicitly not implemented as a shortcut. Existing legacy Open stays unchanged until this is connected. A4 conversation cards and B/C/D remain incomplete.

2026-09-14 checkpoint 5:

- Previous turn was progress (289eb6fc, persistent local binding). Added session-workspace.js as the shared main-process resolver. Ordinary sessions retain the original project object. Dedicated task execution sessions retain project identity but resolve path to their task copy, pinned to canonical root/device/inode. Missing/replaced/symlink roots yield no project, never the private root. This is not an operating-system sandbox.
- Centralized engine/diagnostic cwd, turn document/memory/context preflight, resume ownership, session skill and guide paths, learned-skill collection, runtime checkpoints, session-scoped file writes and message-artifact backfill through this resolver. Existing-session binding itself remains read-only.
- Bound helper Open now registers a new dedicated task execution session underneath the selected existing project and keeps its workRoot isolated. Session metadata survives real index reopen. Bound requester Open selects its saved target conversation. Unbound/legacy Open retains its prior behavior. UI choice and file-tree presentation still need integration before calling this complete.
- RED: new resolver module missing; bound task Open failed existing-project assertion. PASS: test-session-workspace, remote-task-session, remote-task-workspace-binding, filetree-security, ensure-session-runner-resume-reset (new isolated cwd/resume and missing-root case), remote-task-workflow, session-skill-tree, full turn-orchestrator, diff check. All invoked as Node tests with the established NODE_PATH. Runner is a test double; actual resume-binding and filesystem paths are exercised. Orchestrator warnings/errors are intentional injected failures and final exit was zero. No live-model/installed-client claim.
- NEXT A3 UI: add main-projected bounded project/session choices and saved binding view to task workflow; use existing task detail/card for selection and bind before receive. On subsequent tasks use saved mapping automatically; don't show a new top-level project. Verify actual Electron selection/restart flow and session file tree shows task scope coherently. Then implement A4 persistent task cards in both collaborative and owning local conversation, followed by B Git baseline/changesets.

2026-09-14 checkpoint 6:

- Previous turn was progress (2a0cff49 isolated session workspace routing). A3 receive UI now calls authorized bindingOptions, presents bounded local project/session names, and binds before receive/open/prepareDelivery. Existing saved mappings bypass selection. Native folder selection is available when choosing another folder; cancellation does nothing, and task authority is rechecked after chooser await before project registration. Renderer sends IDs only, never paths; delayed navigation invalidates callbacks.
- New task receive inherits the workspace mapping. Open inherits mappings on both sides; helper execution remains an isolated copy under the chosen top-level project. Existing running target sessions are never retargeted. Session choices exclude isolated execution sessions. All three locales support selection and explain isolated-copy execution.
- Corrected an earlier plan assumption: `project-tree.js` is a project/session sidebar, and there is no mounted directory browser using preload listDirectory in this baseline. Existing file access appears via session artifacts and file handlers, now rooted correctly. D2 must provide the requested online/lazy file tree; do not claim it already exists.
- RED: bindingOptions unsupported. PASS Node: remote-task-workspace-binding (SQLite reopen, chooser cancellation/revocation, safe projections), remote-task-session, remote-task-workflow; JS syntax/diff checks. PASS actual Electron workflow UI including selected existing session preserved across en/zh-CN/ar, first-receive binding and no repeated bind. PASS dual-client UI in BOTH legacy mode and `LILY_TEST_SHARED_WORKSPACE=1` with actual IPC, SQLite, ZIP/files, native clicks and original source assertions; new mode verifies selected project reaches isolated Open.
- Electron main ignored NODE_PATH for jszip, so initial dual-client launch failed during module loading and remained in its error dialog. Confirmed and terminated only test PID 59226; created temporary read-only node_modules symlink to original installed dependencies, then both runs passed. Symlink removed after tests. No application-user profile, production data or original files were changed.
- NEXT A4: persistent stable task cards in IM timeline and owning local session, independent of task panel being opened. Source prepare currently records sourceProjectId but not originating sessionId; extend identity-only prepare to carry/validate source session and retain anchor before freeze. Use task local intent ID → confirmed task ID mapping without duplicate bubbles; persist participant-authorized task snapshots with monotonic revisions and recover from task.updated after restart. Keep local materialization separate from shared task state. B/C/D remain outstanding.

2026-09-14 checkpoint 8 (A4 source session identity):

- Previous turn was progress: commit 30c99228 and verified IM card projection. This slice carries the originating session ID from the workspace menu through recipient selection, form retries and identity-only prepare IPC. Main validates project/session ownership and rejects missing, archived or isolated execution sessions. Existing running sessions are unchanged; projects launched without a selected session reuse a persisted idle source session.
- Encrypted preparation intent records sourceSessionId before bundle I/O; completed task binding retains it. Retrying a failed preparation must resolve the same session and root, failing before freeze if the original session is gone. No command, upload or model turn is dispatched by source association.
- RED: sessionId prepare rejected by the closed IPC schema. PASS Node: remote-task-project-source (pre-I/O persistence and missing-origin retry), remote-task-session (real session index reopen, running-session immutability and invalid source rejection), remote-task-preparation, remote-task-workspace-identity. PASS actual Electron: workspace-collaboration-entry verifies real menu to prepare propagation; remote-task-workflow-ui verifies existing preparation/retry/binding behavior. Syntax and diff checks pass. Menu test first failed module loading without renderer dependencies; temporary node_modules symlink to installed original dependencies enabled rerun, then was removed.
- NEXT: use sourceSessionId and saved workspace binding sessionId to project authorized cards into local session UI. Read message.js panel lifecycle and reset semantics before adding rendering; keep task content in encrypted collaboration records, not plaintext session messages. Expose a closed session-card query with account/revocation guards, navigation/account invalidation, and open-card routing to the existing task surface. Also finish real card-click tests, offline freshness, durable task.updated hydration/history pagination. A4 and B/C/D remain incomplete; this identity slice alone does not display local-session cards.

2026-09-14 checkpoint 9 (A4 local-session cards):

- Previous turn was progress: 738e4811 persisted source-session identity. Added closed read-only sessionCards IPC resolved against a real local session/project. Encrypted projections select origin drafts by device/project/session and incoming tasks by saved workspace binding. Revoked conversations are excluded, and both source/receiver projections survive SQLite reopen. Binding conversation IDs allow first-task hydration even when the task cache is initially empty.
- Local cards now render inside the existing session scroll panel, outside engine messages. Forced conversation rebuild preserves card nodes; a task-bearing session hides empty-session onboarding. Active panels refresh on collaboration changes; hidden panels avoid network reads and still clear on account/revocation events. Removing a panel unsubscribes listeners. No task content is copied to plaintext session messages or dispatched to an engine.
- Card buttons restore the original draft or navigate to the authorized IM conversation and actual task detail. Opening reports failure when the requested surface could not load. Both IM and local cards label stale remote state with translated Last saved status after an unavailable refresh. All three locales have the label.
- RED: session projection regression failed before forSession existed. PASS Node: remote-task-cards (device/session isolation, source/receiver reopen and revoke), remote-task-project-source (real workflow and safe session-card IPC), remote-task-workflow, remote-task-desktop, remote-task-workspace-binding. PASS Electron: new session-task-cards-ui (actual message module/panel/layout, rebuild, engine-context exclusion, onboarding, cached label, locale, navigation, account fence and teardown); expanded remote-task-cards-ui (real draft/detail surfaces and navigation route); workspace-collaboration-entry, remote-task-workflow-ui, collaboration-visible-read. Full test-renderer-import exited zero with final test-renderer-import: ok; its fixture logs missing handlers for optional IPC channels, so this is renderer regression evidence, not a full-service run. Syntax/diff checks passed. Temporary read-only node_modules symlink removed.
- NEXT: A4 remains incomplete until task.updated events are durably hydrated without any task/IM/local panel being open, and task history pagination removes the latest-50/cache-1000 blind spots. Current UI hydration uses authorized conversation list reads; events are advisory and active UI remains the trigger. Implement a persisted main-process hydration queue integrated with sync/service lifecycle, then restart/lost-event/denied-access tests and dual-client verification. Review plan A4 in full before checking it off. B Git contribution isolation, C dual integration chains/recovery and D Office/scale remain outstanding.

2026-09-14 checkpoint 10 (A4 durable task event hydration):

- Previous turn was progress: 3263ba21 added verified local-session cards. Added SQLite v23 task_hydration, atomically queued with task.updated and the sync cursor. Migration backfills valid retained historical hints at maximum revision; malformed old payloads are excluded. Projection failures roll back the hint queue together with the page.
- Main service recovers on startup, committed task events and a 2-second timer. Each bounded batch handles at most 16 due entries, with persisted exponential backoff up to 30 seconds. Task GET runs outside the ordinary chat sync lane, so a pending task read cannot block chat synchronization. Missing conversation metadata uses the existing serialized authorized conversation hydration lane. Only task IDs/revisions/retry metadata are plaintext; task bodies remain encrypted.
- Generation checks fence newer events and stopped/account-switched services across awaited reads. Older-than-hint responses remain pending. Task-level access denial hides cached task cards; a new hint retains the denial mask until an authorized snapshot commits. Retry work is read-only and does not start an engine, upload, or send a message.
- RED: hydration module absent. PASS new remote-task-hydration with real SQLite v22 upgrade/backfill, page rollback, controlled close/reopen, persisted retry deadline, newer-generation race, stale revision, denial/regrant mask, service startup with no UI, chat-sync independence, stop/late-response fence, real service timer retry and live committed-event hydration. Timer test initially advanced its injected clock before the failure checkpoint was persisted; corrected it to wait for the authoritative attempts row before advancing time. This is controlled restart evidence, not killed-process/power-loss evidence.
- PASS existing remote-task-cards, remote-task-desktop, collaboration-sync-engine, collaboration-store, collaboration-conversation-hydration (10 cases), collaboration-client-service, collaboration-sync, collaboration-service-lifecycle (27 cases), syntax/diff checks. Client/service regression exposed eager task-card initialization against a legacy metadata-only adapter; changed hydration to initialize cards only when its DB/client path is actually used, then passed. Server ESM sync test required a temporary server/node_modules symlink to existing original dependencies, removed after passing.
- NEXT: A4 still lacks exhaustive task discovery/history. New clients or bootstrap cursor replacement can have no retained task.updated events for older tasks; authorized paged task history must seed durable hydration/reconciliation, including reauthorization without a new task revision. Remove the latest-50/card-1000 blind spots through explicit paged APIs and UI pagination/lazy loading rather than unbounded response arrays. Inspect server tasks.list/routes, client task list contract, task-commands and card projection. Add >50, pagination/restart/ACL and dual-client tests before A4 completion. B/C/D remain outstanding.

2026-09-14 checkpoint 11 (A4 authorized task history API):

- Previous turn was progress: 01b27ac8 added durable event hydration. Added authenticated /tasks/history with fixed 50-candidate pages and a strict creation-time/ID keyset cursor. Immutable creation order prevents task progress updates from moving records across page boundaries. Each returned body uses the existing task-level authorization, and a fully denied candidate page still advances its cursor. Added migration 048 chronological conversation index; legacy /tasks/list retains its array response.
- Fixed a production contract mismatch discovered during integration: get previously returned COLLAB_TASK_UNAVAILABLE for both permission refusal and corrupt/unavailable data, while background hydration can mask denied cards only on explicit denial. Missing and unauthorized get now both return COLLAB_TASK_ACCESS_DENIED (no existence distinction); encrypted/routing corruption remains COLLAB_TASK_UNAVAILABLE and retryable by hydration. Legacy list handles both existing omission cases; history skips only explicit denial and propagates data failures.
- Server advertises taskHistoryProtocol:1 only behind enabled/tasks/workspace gates; desktop accepts only exact signed version 1. Added signed client listTaskHistory transport. No desktop discovery caller exists yet, so old services are not sent a new request by existing UI.
- RED: taskHistoryBody export missing. PASS actual isolated PostgreSQL integration with 107 tasks, same-timestamp ties, progress mutation between pages, no duplicates/omissions, bystander empty results, denied-page advancement, revoked actor, missing/unauthorized error parity, plus existing replay/CAS/package ACL/ciphertext checks. Historical task rows and object verification are seeded; not a live two-device acceptance run. Migration 048 was executed only in the random test schema, which the harness dropped afterward. Existing dedicated PG container remains unchanged outside that schema.
- PASS remote-task-routes, remote-task-service, remote-task-contract, remote-task-policy, collaboration-policy (exact desktop history protocol versions), remote-task-ipc (signed cursor transport), remote-task-hydration, syntax/diff checks. Temporary server/node_modules symlink removed.
- NEXT: implement desktop persistent history discovery using the negotiated history capability. Seed/resume per-conversation page cursors on startup/bootstrap, store authorized snapshots atomically with scan progress, and reconcile denied tasks safely without waiting for another task revision event. Then expose local card pagination/lazy loading so the card projection no longer silently truncates at 1000. Verify >50 and >1000 histories, empty authorized pages, restart mid-scan, bootstrap replacement and ACL changes. A4 remains unchecked; B/C/D remain outstanding.

2026-09-14 checkpoint 12 (A4 persistent desktop history discovery):

- Previous turn was progress: 8e7a981d supplied the authorized history API. Added SQLite v24 per-conversation scan cursors and seen-task records. Bootstrap resets scan generations in the same transaction as its cursor replacement; ordinary restart preserves incomplete page positions. Only exact taskHistoryProtocol:1 enables requests. Main startup, bootstrap and the existing task recovery timer drive discovery independently of UI.
- Each recovery fetches at most four 50-candidate pages. Page snapshots and cursor progress commit atomically, with account/scope/generation checks after network awaits and validation of page shape, identities and cursor progress. Failed pages retain their cursor and use persistent backoff. Completed/denied sweeps are retried after five minutes to discover old tasks and changed permissions without a fresh task event.
- Returned snapshots stay encrypted. Previously denied tasks receive a separate fresh task GET before unmasking. At sweep completion, cached tasks absent from all seen pages are masked and queued for individual authorization; absence alone never deletes a task or declares it revoked. Completion reconciliation visits cached task IDs for that conversation synchronously; page fetching is bounded, but this final reconciliation is not yet batched.
- RED: history module absent. PASS new remote-task-history: real SQLite with 1150 encrypted snapshots, four-page batch cap, 23-page traversal, mid-scan close/reopen with no first-page reread, bootstrap-generation fence, retry/malformed response, mid-page transaction rollback, denial masking/absence reconciliation, actual service startup without UI, and stop/late-response fencing. Network history pages are fixtures; server PostgreSQL paging evidence remains checkpoint 11.
- PASS existing collaboration-store, collaboration-service-lifecycle (27), collaboration-client-service, collaboration-conversation-hydration (10), collaboration-sync-engine, remote-task-cards, remote-task-desktop, remote-task-workflow and remote-task-hydration; syntax/diff checks pass. No dependency symlinks or test processes remain.
- Preserved current visible behavior when the new backfill exceeds 1000 cards: the closed card projection keeps the newest 1000 and emits hasMoreCards instead of retaining only the oldest 1000. This is a transitional window, not full UI pagination; controllers do not consume hasMoreCards yet.
- NEXT: finish A4 local card pagination/lazy loading in both IM and owning-session surfaces. Add a closed before-cursor/next-cursor contract, preserve loaded card anchors during refresh, and make older cards reachable with explicit loading/scroll controls. Test >1000 cards through real Electron surfaces, including concurrent updates, navigation, offline and denied scopes. Consider batching final history reconciliation and verify end-to-end history discovery against the real server adapter. Then audit A4 before moving to B Git baselines/contributions. B/C/D remain outstanding.

2026-09-14 checkpoint 13 (A4 card pagination and implementation audit):

- Previous turn was progress: 2f607836 added persistent discovery. Cards and sessionCards now accept a closed before cursor and return at most 100 cards with nextCursor. Both IM and local workspace session surfaces expose Load earlier tasks with loading/retry states in all three locales. Refresh reads through the user's oldest loaded anchor, preserving expanded history while inserting newer tasks. A task-state refresh arriving during an older-page request is deferred until that request settles.
- Pager nodes are excluded from message indexing/read semantics. Existing keyed card nodes survive additional pages and refreshes; account/navigation invalidation clears both cards and pagination. Cursor values cannot introduce filesystem paths, arbitrary page sizes or another scope. Actual workflow responses are paginated, so older cards remain accessible beyond the former 1000-card window.
- RED: local pageTaskCards missing. PASS real SQLite 1150-card traversal without duplicates; strict cursor tests in remote-task-project-source. PASS new actual Electron task-card-pagination-ui using real IPC, workflow validation, encrypted SQLite and both renderer surfaces: all 1150 cards reachable, failed-page retry retains existing cards, refresh during page load, DOM identity, addition of task 1151 preserves oldest loaded anchors, and logout cleanup. Network list replies are explicitly offline fixtures; history was seeded through the real encrypted card store.
- PASS existing remote-task-cards-ui, session-task-cards-ui, collaboration-timeline, remote-task-project-source, remote-task-workflow, remote-task-desktop, remote-task-preparation and remote-task-history. PASS shared-workspace dual-client UI with native clicks through create/retry/accept/receive/open/deliver/approve/preview/apply/rollback, actual IPC/SQLite/ZIP/source-file assertions; transport/accounts/editing remain fixtures. Diff/syntax checks pass. Temporary root node_modules symlink removed.
- A4 implementation audit: pre-freeze durable pending identity is checkpoint 1/8; original-anchor ACK dedupe and monotonic snapshot revisions are 7; source and receiving-session projection plus engine-context separation are 9; offline labels and actual action routing are 9; durable event/history recovery is 10–12; >1000-card visibility is 13. Actual DOM, account/navigation and controlled SQLite restart tests cover the stated A4 validation. A4 is now checked as implemented. Physical-device/full-source service acceptance remains D3; no global goal completion claim.
- Known performance follow-ups: local card paging currently builds the authorized conversation projection before selecting its 100-row response; history sweep completion visits cached task IDs synchronously. These are not constant-memory task-count algorithms. Batch/index those paths if D3 scale evidence requires it; do not confuse card pagination with D2 large-directory streaming or file-tree materialization, which remain unimplemented.
- NEXT B1: inspect workspace-git.js executable resolution and current task-bundle freeze/receive inventory, then implement task-git.js with a separate collaboration repository and explicit baseline/task refs. Prove with real Git that private .git/vault history and unrelated local dirty files never enter task history. Follow B2 changesets, B3 incremental transfer, C dual merge/application chains and D Office/scale. Keep the full A–D objective active.

2026-09-14 checkpoint 14 (B1 Git baseline storage primitive):
- Added TaskGit with the existing WorkspaceGit executable/runtime resolver, a separate bare repository, task-identity-hashed refs, isolated per-capture index and parentless deterministic baseline commits. Explicit frozen manifests are the sole input; Git attributes/filters do not transform bytes. Streams validate SHA-256/size and file identity before importing verified temporary bytes. Control files, links, hardlinks and overlapping roots are rejected.
- Sanitized inherited GIT_* variables and disabled global/system Git configuration; no source .git/version-vault refs or object alternates are imported. update-ref uses a zero-old-value CAS; identical retry/concurrent capture resolves the existing ref and differing input fails without replacing it.
- Verified real Git test-remote-task-git.mjs: exact CRLF bytes, only authorized paths, no parent/private commit, immutable retry, new-instance restart, concurrent capture, hostile inherited Git paths, corruption/control/link rejection and fsck. Existing test-remote-task-bundle.mjs, test-workspace-git-dependency.mjs and test-workspace-version-control.mjs passed; diff check passed.
- B1 remains unchecked: the primitive is not yet invoked by task-workflow. No claim of task execution using worktrees or transmitted Git ancestry. Capture uses one process per file and the existing 10,000-entry manifest contract; this is not D2 scale completion. Orphan unreachable objects after rejected input/ref conflicts are harmless but eventual storage reclamation remains to be designed with retained-ref safety.
- NEXT: wire authorized frozen preparation/receive into this Git baseline store and persist its immutable descriptors in encrypted task records. Replace receive's second ZIP extraction with a task-isolated Git worktree and preserve execution-root identity checks, recovery/retry and legacy ZIP readers. Then explicit B2 contribution records and B3 object transport, C integration/application and D Office/scale/full-client acceptance.


2026-09-14 checkpoint 15 (B1 workflow and isolated Git execution):
- Send now captures the frozen draft baseline before upload and stores its descriptor in encrypted draft/task records. The sender's ref uses the stable local draft identity (available before remote task creation); receiver refs use remote task identity. Both independently produce the same parentless commit from the verified ZIP manifest. Existing ZIP wire readers remain compatible.
- Receive persists the immutable input snapshot, Git descriptor and chosen execution path before worktree creation. It uses a real detached Git worktree with a baseline index, writing verified blob streams directly to preserve exact bytes despite .gitattributes. A repository-owned readiness marker binds baseline/ref and directory dev/ino. Repeat receive and restart validate/reuse the same worktree without resetting files, including when creation succeeded but final SQLite acknowledgement was lost. Existing legacy working copies remain usable; no in-place migration of active legacy execution directories is attempted.
- Real Git test-remote-task-git.mjs verifies clean index, preserved CRLF despite attributes, edited-worktree reuse and explicit incomplete-worktree rejection without reset. Real two-account SQLite/filesystem test-remote-task-workflow.mjs verifies equal owner/helper Git baselines, worktree execution, missing final-save recovery, encrypted restart, delivery/application and undo. Desktop, project-source, preparation and session regressions pass. Shared-workspace Electron dual-client UI passes create/retry/accept/receive/open/deliver/approve/preview/apply/rollback with actual IPC/SQLite/filesystem and documented transport/account/editing fixtures; this is not D3 full-service/physical-device acceptance.
- B1 implementation scope is complete. Partial materialization before the readiness marker currently fails visibly as COLLAB_TASK_GIT_WORKTREE_INCOMPLETE; automatic safe recovery of that state remains required under C/D fault recovery. No power-loss durability or OS process sandbox claim. Per-file Git process overhead and ls-tree buffer/manifest caps remain scale limitations for D2/D3.
- NEXT B2: derive immutable contribution commits from baseline plus explicit add/modify/delete/rename records. Use known readable materialization inventory to distinguish deletion from unavailable data. Exclude unrelated current workspace edits; preserve immutable delivery retries and frozen bytes. Then B3 authenticated missing-object/object transfer, C shared/local integration chains and D Office/scale/full-service acceptance.

2026-09-14 checkpoint 16 (B2 explicit contribution records and workflow):
- Added task-changeset.js: validates canonical manifests and a readable materialization inventory, emits before/after identities for add/modify/delete/rename, preserves absent undownloaded baseline files, and refuses modification of an unmaterialized baseline path. Identical-content unique pairs and case-only moves become renames; ambiguous or edited moves currently remain explicit delete+add (no guessed intent).
- TaskGit now builds contribution trees from the immutable baseline plus only explicit changed records, in a private temporary index/work directory. Contribution commits have exactly the task baseline parent and include canonical operation records in their message. Delivery identity binds an immutable ref using zero-old-value CAS. Baseline descriptors include canonical manifest hashes; old descriptors are reconstructed from the frozen baseline when preparing a contribution.
- Receive saves known fully materialized paths. prepareDelivery persists its Git contribution after the existing omitted-file guard; submitDelivery reconstructs a missing contribution from frozen bytes before upload, so older prepared drafts cannot bypass it. Git descriptors stay encrypted/main-only. ZIP transport remains unchanged; sparse inventories are refused by the legacy ZIP submission path to avoid turning unknown absence into remote deletion.
- Verified test-remote-task-changeset.mjs with real Git: all four operations, baseline ancestry, offline-path preservation, unrelated-file exclusion, immutable retry/conflict, forged base-manifest rejection, unknown-materialization rejection and ambiguous/case-only rename cases. Git/worktree regression and two-account SQLite workflow tests pass, including frozen contribution persistence across restart/upload and exclusion of unrelated sender working edits. Project-source and preparation regressions pass. Shared-workspace Electron dual-client UI regression passes using documented account/network/editing fixtures; no production/physical-device claim.
- B2 remains incomplete: the current ZIP freezer rejects all-empty deliveries, so deleting every input file is not yet end-to-end supported. Edited renames need explicit source/target identity support beyond delete+add representation; add tests for that and for missing/unreadable input, file/directory transitions and immutable uncertain delivery replay. B3 is still required to transport actual Git objects/ancestry; current contribution refs are device-local. C/D are unchanged pending.
- NEXT: complete the B2 gaps above, then B3 object negotiation/transport. Maintain separation between published shared integration and private local application in C; finish Office fidelity, streaming/lazy materialization and full-service/fault/scale acceptance in D.

2026-09-14 checkpoint 17 (B2 all-delete delivery):
- Task delivery freeze/import now explicitly opts into empty payloads; task creation/input and ordinary workspace import retain default nonempty validation. The allowEmpty setting is main-only and passed through the strict package preflight/extraction wrapper into workspace import. Empty delivery remains a real metadata ZIP and an empty Git result tree with explicit deletion records, never fabricated placeholder files.
- Extended real two-account SQLite workflow to delete every authorized baseline file, prepare/submit/approve, require deletion consent, apply, and restore each original SHA-256 on rollback. An all-filtered credential-bearing result is still blocked as COLLAB_TASK_DELIVERY_OMITTED. Bundle tests verify default empty-input rejection and explicit empty-delivery round trip.
- Shared-workspace Electron dual-client UI with LILY_TEST_DELETE_ALL=1 passes actual prepare/submit/preview/confirmation/apply/rollback actions and filesystem assertions. Transport/account/editing remain fixtures; no full-service or physical-device claim. Generic collaboration-workspace-package and workspace-share regressions pass after resolving their ESM imports using the existing temporary dependency symlink (initial runs did not start due missing jszip resolution). Preparation and project-source regressions pass; temporary dependency symlink removed.
- B2 remains unchecked pending edited-rename identity and remaining boundary/uncertain-delivery tests. NEXT: inspect existing file-operation/identity facilities; use verifiable source/target identity for edited moves without inferring intent from similarity alone. Cover missing/unreadable materials, file/directory transitions and immutable delivery replay. Then B3 actual Git-object transfer, C shared/local integration and D Office/scale/full-service acceptance.

2026-09-14 checkpoint 18 (B2 edited-rename identity and uncertain replay):
- Newly materialized worktrees record regular, single-link file identity as device/inode/creation-time nanoseconds in the readiness marker and encrypted local task record. Unsupported zero/missing identities are omitted. Bundle capture records identity from the already-validated open source descriptor, alongside its frozen bytes. Identity fields remain main-only and are not part of transferred ZIP metadata, renderer projections or Git commit messages.
- Changeset construction pairs deleted and added paths with unique matching captured file identity before the existing exact-content pairing. This preserves an edited rename, including after process restart, with explicit before/after paths and distinct SHA-256 values. Duplicate/malformed/out-of-manifest identities fail validation. Different creation time prevents pairing a recycled inode; legacy records without initial identity remain conservative. Atomic-save/copy-delete moves that replace file identity still produce complete delete+add records; they are not claimed as identity-proven renames. No cross-platform live identity guarantee beyond the tested host, nor filesystem sandbox claim.
- Real two-account SQLite/Git workflow test covers move + full content rewrite + restart + one rename contribution. Added uncertainty fixture: server commits delivery then drops the response; later working edits and client restart must replay the original command, frozen ZIP and immutable Git contribution. Existing apply assertions prove the later helper edit is excluded. Real Git changeset, baseline/worktree, bundle, project-source and preparation regressions pass; syntax and diff checks pass.
- B2 remains unchecked pending boundary audit: file/directory transitions, missing/unreadable materialization and complete delivery retry coverage. NEXT run those cases, resolve actual failures, then move to B3 Git-object descriptors/negotiation/transport. Continue C shared/local integration and D Office/streaming/full-service acceptance without treating local Git refs or fixture tests as end-to-end completion.


2026-09-14 checkpoint 19 (B2 boundary fixes and implementation audit):
- Fixed omitted-input classification to walk each ancestor without following links. File-to-directory replacement is an explicit deletion of the old file; directory-to-file replacement permits old child deletion only when the ancestor file is actually included in the delivery. Omitted/filtered ancestors and skipped parent links remain blockers. EACCES never becomes absence.
- Added durable deliveryValidated after the omission checks. If a process stops after saving the frozen draft but before validation/contribution completion, direct submit cannot upload it without this marker (or an already persisted contribution from the validated path). Existing legacy prepared drafts without either proof must be prepared again; automatic orchestration/recovery remains C. This closes a reproduced validation-bypass window without re-reading later working edits into an immutable delivery.
- Real SQLite/Git workflow now covers file/directory transitions and their resulting Git contents, filtered ancestor replacement, parent links pointing to missing external children, actual host EACCES, incomplete inventory rejection on legacy ZIP, and interrupted pre-validation submission with zero upload admission. The secret-filter test uses a scannable .txt ancestor; the existing scanner is extension/size scoped, so this evidence does not assert exhaustive secret detection. Previous all-delete, identity-proven edited rename, immutable lost-response/restart replay and unrelated-owner-edit exclusion remain in the same passing test.
- Full B2 targeted audit passed: task baseline/worktree tests, changeset tests, bundle input omission/race tests and the expanded workflow test. Final shared-workspace Electron dual-client UI passes after the validation marker change; its transport/account/editing fixtures remain documented. B2 implementation is complete for the current authorized regular-file manifest contract. Unsupported/identity-replaced moves remain correct delete+add records. Large-scale sparse transport, cross-platform identity acceptance and process/power-loss recovery are not claimed.
- NEXT B3: inspect server/src/routes/public/collaboration-objects.js, services/collaboration/objects.js/object-repository.js/object-key-broker.js and client transfer-runtime/taskFiles. Add scoped Git content descriptors/missing-object negotiation and actual object transfer/reconstruction, using mature Git plumbing and existing encrypted multipart. Keep legacy ZIP readers. Verify object reuse, missing/corrupt objects, lost responses, revoked access and commit ancestry across independent repositories. C/D remain incomplete; in particular safe local application of file/directory transitions belongs to C3 despite B2 correctly recording them.

2026-09-14 checkpoint 20 (B3 Git bundle transport primitive):
- Inspected object routes/service and taskFiles transfer admission: current objects are bound to a task input/delivery object ID, so additional Git packs require explicit protocol and authorization changes. No existing admission was bypassed.
- Added main-only task-git-transport.js using TaskGit's resolved Git runtime: bundle v2 export from one exact task baseline/contribution ref, ancestor prerequisites for incremental objects, SHA-256/size descriptor and exclusive output creation. Import first copies/verifies bounded bytes into owned staging, verifies the bundle header/ref/prerequisites, runs Git bundle verification/fetch/fsck, and publishes the task ref with zero-old-value CAS. Existing refs cannot be overwritten; retries converge on the same commit. The module itself does not grant network/task authorization.
- Real test-remote-task-git-transport.mjs uses independent sender/receiver/empty/conflicting/damaged repositories. It proves exact parent reconstruction, unchanged 2 MiB object reuse (last run baseline 2,098,202 bytes; delta 727 bytes), unrelated-task-history exclusion, missing prerequisite rejection, descriptor/hash mismatch rejection, internally corrupted pack rejection despite matching outer SHA-256, no ref publication on failed import, conflicting target preservation and duplicate import after lost acknowledgement.
- B3 remains incomplete: this primitive is not yet wired to task-workflow, the encrypted transfer manager or server. No network/ACL/revocation/PG acceptance claim. Export/import retains the legacy 256 MiB object ceiling, bounded 64 KiB bundle header, and 256 prerequisite cap; resumable export admission, richer object reuse and D scale recovery remain pending. Invalid fetched objects can remain unreachable in the owned repository; safe reclamation needs retained-ref coordination.
- NEXT: add a signed Git protocol capability plus task-scoped content descriptor/object bindings and missing-object negotiation. Preserve existing object-service encryption, command idempotency and fresh task ACL on registration/query/download, including reuse across authorized deliveries without granting unrelated task objects. Wire bundle descriptors through immutable input/delivery records and taskFiles; then real PostgreSQL/HTTP and two-client reconstruction/revocation tests. C/D remain pending.

2026-09-14 checkpoint 21 (B3 server descriptors and scoped missing query):
- Reused existing task input/delivery object binding instead of introducing a parallel permission model. Added canonical closed Git descriptor validation and optional inputGit/deliveryGit command fields. Descriptors persist inside encrypted task state and participate in existing idempotency fingerprints. Input packs must be full baseline bundles; Git deliveries must name exactly that input commit as their prerequisite. Tasks cannot switch between Git and ZIP delivery modes.
- Added authenticated /tasks/git/missing. It freshly authorizes both task parties through service.get and returns only that task's baseline/selected-delivery pack descriptors absent from the caller's bounded haveCommits list. No global object/hash lookup; unknown hashes cannot discover another task's data. This reports required packs, not a guarantee that the object store is currently available; downloads retain object-service checks.
- Git delivery admission now locks/checks the original bound input object before binding a new delta. Revoked/expired/mismatched inputs fail and leave the delivery object unbound. Encrypted task reads also require inputSnapshotId to agree with the database input object route, preventing descriptor/routing drift.
- Contract and route tests pass, including closed fields, fixed prerequisite, no mode downgrade, bounded/deduplicated haveCommits and authentication boundary. Existing service transaction-runner regression passes. Expanded real PostgreSQL test passes encrypted descriptor round trip, same-key create/submit replay, descriptor mutation rejection, input-route corruption, bystander denial, scoped missing packs, revoked prerequisite rollback and both-party revocation. Object verification state and ciphertext hashes are seeded fixtures in this PG test; real bundle bytes/ancestry were separately verified in checkpoint 20. No full HTTP/object-store/client acceptance claim.
- B3 remains incomplete. No signed Git capability advertisement has been enabled; current desktop workflow still uses ZIP and does not retain/use the new Git descriptors. NEXT wire a signed protocol capability, private main task projections/commands, client missing query, and actual encrypted bundle upload/download/import. Keep renderer input authority closed. Verify reconstructed baseline/contribution manifests, immutable retries, revocation and independent repositories through real client/service tests. C/D remain pending.

2026-09-14 checkpoint 22 (B3 desktop contracts, query client and rollout wiring):
- taskView now validates Git input/delivery metadata and can retain it for main-only reads; task commands remember a separate sanitized card projection. IPC taskResult strips both input and delivery descriptors. Malformed or missing Git metadata fails validation instead of silently becoming a ZIP task.
- Main task submit accepts a separate internal descriptor argument while renderer taskCommand remains closed. Git-vs-ZIP mode and exact input prerequisite are checked against a freshly authorized task before journaling. The existing encrypted command journal/fingerprint includes the descriptor, and retries replay the same data after restart.
- Added client.missingTaskGitObjects using the existing signed/authenticated invocation path. Added optional taskGitProtocol:1 to policy/config normalization, an independent COLLABORATION_TASK_GIT_ENABLED environment gate (default off), and server client-config gate wiring. Master collaboration, tasks and workspace gates still apply; old/unknown protocol versions do not invent support. No environment or deployment setting was enabled.
- Verified new real SQLite/actual IPC-handler desktop test: internal metadata retention, renderer stripping/injection rejection, mode-downgrade rejection, malformed/missing descriptors and identical encrypted command replay. Client/service, policy/remote-cache, task policy/contract/IPC, task cards and full existing ZIP workflow regressions pass. These tests cover the gate and existing signed-client path, not a full live signed-config/Git-transfer run.
- B3 remains incomplete: task-workflow still sends ZIP and does not yet consume taskGitProtocol. NEXT wire policy into service workflow construction; persist each draft's negotiated format; export/upload full baseline or incremental contribution bundle while retaining frozen ZIP compatibility; add inputGit/deliveryGit to commands only through main-owned descriptors. On receive/preview use the scoped missing query, verified taskFiles downloads and importBundle, validate expected baseline/parent ancestry, and materialize exact regular-file snapshots/manifests from imported Git blobs for existing worktree/application paths. Tests must prove real independent-repository transfer/retry/revocation, then connect to actual encrypted multipart/service. C/D remain pending.

2026-09-14 checkpoint 23 (B3 workflow bundle transport):
- Opted-in new tasks now upload Git baseline bundles and incremental delivery bundles through taskFiles. Existing prepared ZIP drafts and ZIP tasks retain their protocol across restart; disabling the Git gate prevents Git uploads. The rollout flag remains off by default.
- Receiving imports verified bundles in dependency order into independent task repositories, validates exact parentage and regular-file trees, and materializes bounded blobs directly. Cached commits require an encrypted task/object/descriptor proof before skipping download. Missing-object queries check current bound-object ownership, scope, expiry and revocation even for cache hits.
- Verified Git and ZIP two-account SQLite workflow suites, mixed-mode restart/gate cases, cached-but-unverified commit rejection, frozen/uncertain delivery replay and requester baseline reuse. Real Git transport tests validate materialization, ancestry and malicious control-file rejection. Git baseline, desktop, preparation, project-source and client-service regressions pass. Shared-workspace Electron Git UI runs pass for ordinary and all-delete deliveries. PostgreSQL integration reproduced the cache/revocation failure before the fix and passes after it.
- UI/workflow transport, accounts and editing use explicit fixtures; PostgreSQL object verification is seeded. B3 remains unchecked pending combined real encryption/network/service verification. Full-service/physical-device acceptance, large directory metadata limits and partial materialization recovery remain pending with C/D.
- NEXT: exercise actual encrypted taskFiles upload/download with Git packs, then finish B3 acceptance before implementing durable C orchestration and the two distinct integration chains, followed by D Office/streaming/lazy materialization and fault/scale acceptance.

2026-09-14 checkpoint 24 (B3 encrypted HTTP byte path):
- Added test-remote-task-git-encrypted-transfer.mjs: separate Alice/Bob SQLite stores, transfer journals and Git repositories use real taskFiles encryption, multipart transport and Range downloads over a loopback HTTP provider emulator. Production-shaped HTTPS URLs are remapped only inside the test fetch adapter; this does not test production TLS. Object API, task reads and ACL remain explicit fixtures.
- Verified a random 6 MiB unchanged file is transmitted in the baseline once; the incremental encrypted delivery was 1,071 bytes versus 6,294,173 baseline bytes. The provider stores part one then drops its response; restart reconciles the remote part and uploads only part two. Corrupted downloaded ciphertext is rejected, restart/retry succeeds, exact result manifest/ancestry is reconstructed, and a cached plaintext still fails after ticket revocation.
- Initial test runs exposed fixture configuration errors (required collaboration-transfer root basename and origin-only multipart upload URL), corrected without production changes. Final test passes; existing real ZIP encrypted taskFiles suite passes all 3 tests with none skipped. Loopback listening requires sandbox escalation. B3 remains incomplete.
- NEXT: extend server/scripts/collaboration-transfer-http-fixture.mjs (already real desktop signed client -> Fastify -> PostgreSQL with only provider simulated) and its collaboration-objects-http-integration.mjs caller to include migrations 045/047/048, task/Git test gates and actual task workflow creation/receive/incremental delivery/cache revocation. This closes the domain-stub gap left by the current loopback byte test. Continue C/D afterwards; do not substitute fixture-only tests for full-service/device/scale acceptance.

2026-09-14 checkpoint 25 (B3 signed domain integration complete):
- Added collaboration-task-git-http-fixture.mjs to the existing signed object integration harness, enabling task/Git gates only in its isolated test configuration and applying migrations 045/047/048. Two real desktop workflows use encrypted SQLite journals, Git repos, taskFiles crypto/multipart, actual signed client requests, Fastify routes, PostgreSQL task/object services and the package broker. Only external Qiniu storage and native project/session selection are fixtures; Fastify uses app.inject rather than a production TLS listener.
- Verified shared identity, actual verified ciphertext binding, lost task-create ACK plus desktop restart without duplication, explicit receiver binding, Git input reconstruction, incremental modify/add/delete delivery, lost delivery ACK plus restart preserving frozen edits, missing query baseline reuse, approval/application/rollback, outsider denial, revoked-input rejection even with all commits cached, and actual download-ticket refusal. Existing attachment service, object/history/sync and credential-free logger/receipt checks pass in the same run. Initial new case correctly refused receive before explicit shared-workspace binding; the test now performs that required user action.
- B3 implementation is checked complete using this combined domain integration plus checkpoints 20–24's Git integrity, compatibility, encrypted HTTP byte/recovery and desktop gate evidence. Default production rollout remains off. Physical clients/TLS/provider acceptance and scale/streaming work remain explicitly D2/D3; no global completion claim.
- NEXT C1: inspect existing TaskCore/turn admission, account task events and session binding; implement durable workspace/delivery/target integration intents with generation/lease fencing and a workspace writer coordinator. Tests must cover duplicate events, reopen, expired leases, stale workers and background admission while preserving an active foreground turn. Then C2 shared H+D publication, C3 private A/W/M application, and D Office/lazy/scale/full-device acceptance.

2026-09-14 checkpoint 26 (C1 durable intent and lease substrate):
- Added main-only integration-intents.js with immutable workspace/delivery/target/chain identity, scope-encrypted baseline/delivery/session/project bindings, deduplicated pending/running/completed/cancelled records, and claim/renew/release/complete/cancel transitions. Existing task_workspace_records provides scope retirement; schema v25 adds opaque target-key leases with retained monotonic generations.
- Writer ownership is serialized per account/database and canonical target identity, including shared/local chains and different workspace/conversation mappings to that target. Expiry, replacement, cancellation and release fence stale journal transitions. A successor increments the generation; completion and release atomically update the intent and release ownership. Independent targets can proceed. The caller must provide canonical target identity and fresh remote authorization; this is not an OS lock or a global database/filesystem transaction.
- New intent regression first failed because the module was absent (after correcting the test helper syntax); it now passes real SQLite deduplication, immutable-input rejection, two connections, two competing child processes granting exactly one writer, expiration before/after successor claim, stale renew/release/complete refusal, successful renewal, retry, cancellation, completed replay, encrypted metadata and scope retirement. Existing collaboration-store and remote-task-recovery tests pass. No deployment or existing source checkout changes.
- C1 remains incomplete: no production callers/event admission or background turn dispatch yet. NEXT connect freshly hydrated authorized task states and existing persisted workspace/session binding to these intents, add restart discovery and bound-session admission through turn-orchestrator/TaskCore with stable command identity. Never steer or change cwd/focus of an active foreground turn. Wire all eventual shared/local writers through the coordinator and add immediate Git/file fencing at C2/C3 boundaries; cross-account physical-target coordination remains part of that boundary audit. C2/C3/D remain pending.

2026-09-14 checkpoint 27 (C1 authorized event/history discovery):
- Added integration-discovery.js and connected it to service task hydration/history when Git protocol and the native source-session resolver are available. Fresh private task snapshots create shared-chain intents for requester-owned review/accepted Git deliveries only after matching the persisted workspace/baseline/project/session and canonical source root. Discovery receives existing explicit session IDs and does not create sessions, mutate files or start an engine.
- Hydration atomically publishes a safe task card and either the integration intent or a retained binding-required retry. Newly authorized cards clear a historical denial even when a local binding is pending. History also discovers intents and queues deferred binding work, covering missed events/restarts. Git descriptors remain main-only; task cards stay sanitized. Fresh changes-requested/cancelled/superseded states cancel obsolete uncompleted shared intents.
- Fixed the stale-history boundary: a page older than an already stored card queues fresh hydration and cannot cancel/discover intents from its old state. The same check protects hydration snapshots older than an existing card. A stopped/new-generation response still uses existing fences.
- Verified new regression after initial missing-module failure: real SQLite reopen, missing-binding retry with visible card, original-session matching, private metadata, repeated event/history dedupe, withdrawn review cancellation, stale-history protection, and real collaboration-service startup discovery without opening a panel. Existing hydration and 1150-task history regressions plus intent/competing-process tests pass.
- C1 remains incomplete: intents are pending only; no automatic engine work or file application is started. NEXT wire durable pending intent admission through the existing turn orchestrator/TaskCore using stable identity and the existing bound session. Preserve active foreground work and verify restart/duplicate/background admission; expose binding-required/discovery issues appropriately. C2 must verify/download D and run deterministic H+D candidate preparation before any AI conflict judgment; C3 must separately handle A/W/M and actual writer boundaries. C2/C3/D remain pending.

2026-09-14 checkpoint 28 (C2 deterministic candidate prerequisite for C1 dispatch):
- Inspected existing turn queue background visibility/durableQueueKey/turn ID and local-assistant paths. A local-assistant turn only projects a completed message; it cannot execute a merge callback. Sending every pending merge as an engine prompt would violate deterministic-first processing. Therefore implemented the shared candidate prerequisite before admitting model conflict work; C1 remains unchecked.
- Added shared-git.js in the isolated task repository: workspace head initialization, explicit baseline three-way merge-tree, immutable candidates with H and D parents, regular-file/control-path/tree-limit validation, exact-commit validation callback and CAS head publication. Ref-based retries resolve already published candidates. It never reads or modifies private W. TaskGit tree inspection is shared with snapshot materialization, which now accepts an explicit bounded parent list for two-parent candidates.
- Private info/attributes overrides contributed merge/filter/text attributes, with built-in text/binary merge behavior; a supplied merge=union cannot suppress conflicts or select a custom driver. Actual Git 2.45.0 supports required merge-tree --merge-base; unsupported execution returns a coded failure. No fallback to a model or arbitrary command occurs.
- New test initially failed for missing module; implementation then exposed absent bare info directory and a conflicting test rival, both corrected. Final real Git test verifies independent text contributions, immutable retry/parents/materialization, unmodified private source, union-attribute isolation, conflicting text and synthetic binary .xlsx bytes, exact validation gate, advanced-head CAS refusal and lost publish-ACK retry. Baseline and bundle transport regressions pass. The binary case tests conflict detection, not Office fidelity.
- C2 remains incomplete: this is local Git plumbing with an injected validator, not configured project acceptance validation, durable validation evidence, publication outbox, cross-device H/M transport or an AI conflict-resolution path. NEXT connect C1 intent worker to verified delivery download, deterministic preparation and durable candidate/validation records. Queue only unresolved judgments into the bound session with stable admission identity, preserving foreground work; reconcile actual refs after crashes. Complete C1/C2, then C3 private A/W/M and D Office/lazy/scale/full-device acceptance.

2026-09-14 checkpoint 29 (C2 local publication recovery and outbox):
- SharedGit now atomically updates the workspace head and creates an immutable publication receipt using Git update-ref transactions. A receipt identifies the exact candidate and expected old head; it remains authoritative after later head advancement. TaskGit command plumbing accepts stdin for this transaction and closes stdin for ordinary commands.
- Added shared-publication.js coordinating an existing fenced intent with fresh authorization, deterministic preparation, a scope-encrypted candidate/validation journal, publishing state, actual Git receipt reconciliation, and an encrypted queued publication outbox. Successful evidence binds commit, policy ID and evidence hash. SQLite atomically records published state, one outbox entry and intent completion. A failed/expired validation cannot become successful evidence; revoked authorization before publication blocks the Git write. Validation failure/conflicts remain recoverable pending work.
- New test first failed for missing coordinator module. Final real Git/SQLite regression injects pre-Git and post-Git exceptions, reopens SQLite with a successor lease, reuses persisted exact validation evidence, advances the head through another publisher, and reconciles the earlier receipt without rewinding. An injected outbox insert failure rolls back SQLite completion; retry recovers one queued outbound record. Failed validation, expired validator and authorization withdrawal are also verified. Shared Git candidate, baseline, bundle transport and multi-process intent regressions pass.
- These are simulated boundary exceptions and SQLite reopens, not abrupt power-loss acceptance. The validator/authorization implementations in the new test are explicit callbacks. The coordinator is not yet connected to production task event workers, configured project validation, model conflict turns or a network outbox sender. Lease guards fence SQLite and check before/after asynchronous work; Git CAS and immutable receipts cover the separate ref boundary, not an atomic lease-clock/Git/filesystem transaction. Cross-account/physical writer coordination remains pending.
- NEXT connect the C1 worker to private verified task delivery acquisition and this C2 coordinator, with durable session admission only for unresolved judgments. Implement configured validation evidence and remote shared-head/publication transport, drain/reconcile the publication outbox, then C3 private A/W/M and D Office/lazy/scale/full-device acceptance. C1/C2/C3/D remain unchecked.

2026-09-14 checkpoint 30 (C1 production background acquisition/preparation):
- Added private workflow authorizeIntegration/acquireIntegrationInput capabilities. They recheck current requester/workspace/delivery/state, original source project/session/root and baseline identity, then use actual taskFiles missing-query/download/import verification to supply the publisher's task repository and exact Git descriptors. These capabilities are not renderer commands and never change the session cwd or write private W.
- Added schema v26 indexed task_integration_work scheduling with existing-intent backfill; enqueue, complete/cancel and scope retirement maintain it. New integration-worker runs bounded eight-item passes, claims/renews writer leases, uses the private workflow acquisition and shared-publication coordinator, persists validation/conflict waits, retries failures with deadlines/three-attempt limit, and fences stopped/expired callbacks. Stops clear renewal timers immediately; generation/state predicates prevent stale scheduling updates from reviving completed work.
- Service startup and its existing two-second task recovery loop now run this worker when Git/source-session/root capabilities exist. A configured main-process validation callback can publish; without one, real candidate preparation stops durably with COLLAB_INTEGRATION_VALIDATION_REQUIRED. There is no invented default approval, engine prompt or network outbox sender. Waiting validation is retried on restart when a validator is configured; user-visible retry/status and conflict admission remain next work.
- New worker test first failed for missing module, then passes actual Git/SQLite preparation, validation wait/no busy loop, configured-validator reopen/publication, durable retry deadline, stopped late acquisition and scope queue cleanup. Signed Fastify/PostgreSQL harness now gives the source its explicit project/session, seeds discovery from a freshly signed task read, starts the real collaboration service, and verifies automatic encrypted acquisition/candidate preparation reaches validation-required while private source bytes remain unchanged. Provider and native project/session selection remain fixtures; this is not live model/physical-client acceptance.
- Existing ZIP and Git workflow, store/local recovery, task hydration/history, discovery, competing-process leases and publication recovery regressions pass. C1 remains unchecked for TaskCore/background conflict admission and visible wait/retry flows; C2 remains unchecked for configured project/semantic validation and remote shared-head/outbox transport; C3/D remain pending.
- NEXT wire formal validation policy/evidence and bounded read-only conflict judgment into the existing bound-session turn queue with stable durable identity, preserving a running foreground turn. Surface pending/validation/conflict/retry state on task cards. Then implement remote publication CAS/outbox delivery/recovery, C3 private A/W/M and D Office/lazy/scale/full-device acceptance. Preserve the full scope rather than treating prepared candidates as completion.

2026-09-14 checkpoint 31 (visible integration state and identity-only retry):
- Added a closed integration status projection for existing conversation/session cards and task details: queued, preparing, validation required, conflict, failed, local publication awaiting sync, synced, cancelled and original binding required. Status exposes only stage/current delivery/canRetry; journals, paths, commit identities and internal errors remain main-only. Card projection indexes intents by task and preserves existing card anchors. English, Chinese and Arabic strings are present.
- Added fresh-authorized integrationStatus/retryIntegration workflow commands. Retry checks the original persisted source binding and current delivery, rechecks queue state transactionally, resets the same pending intent and never creates a new one. Running/completed/cancelled/withdrawn or stale delivery work is refused; missing configured validation has no misleading retry action. Detail UI uses existing single-flight and account/conversation-generation fences and shows retry failures.
- Verified real SQLite status/discovery tests, closed IPC/card projections, original-binding wait/restore/cancel state, deduplicated retries and local-publication-versus-sent status. Electron card/detail tests verify original node identity, failure/queued/validation/publication states, English/Chinese locale change, duplicate clicks and late retry after navigation. Actual SQLite/IPC Electron pagination passes with 1150 cards on both surfaces. Existing cards, hydration, history, IPC, integration worker and ZIP/Git workflow regressions pass. Locale JSON parses with 11 integration keys each and no duplicate keys; git diff --check passes. Initial pagination launch lacked Electron dependency resolution; after temporarily linking existing dependencies the full test passed. Three initial test command names were corrected to their actual remote-task filenames and passed.
- UI retry and task/domain APIs in the focused renderer test remain fixtures; real source selection, production validation, installed dual-device acceptance and remote publication are not claimed. C1/C2/C3/D remain incomplete. NEXT establish explicit trusted validation policy and candidate validation evidence, then bound-session TaskCore admission only for unresolved judgments; add remotely authorized shared publication/outbox transport and independent private A/W/M integration with writer fencing/recovery. Do not substitute a generic passing validator or mark a local Git publication as remotely synced.

2026-09-14 checkpoint 32 (candidate validation evidence boundary):
- Previous turn was progress: 7802903f added visible integration status/retry. Inspection showed publication accepted a trusted callback hash without a persisted check report. Added candidate-validation.js to the actual integration worker: it verifies exact workspace/candidate/baseline/delivery binding and Git ancestry/tree, materializes isolated exact candidate bytes, invokes only the host-configured project checker, then streams hashes again and rejects modified/deleted/extra/symlinked candidate files. It does not read private W or execute contributed commands.
- Reports bind candidate/input versions, pipeline and project policy versions, Git/Node runtime versions, explicit check coverage/results and content evidence hashes. Content-addressed encrypted records preserve previous reports; a separate encrypted latest pointer survives restart. Publication validation hashes resolve to these records. The pipeline version is part of the reusable policy identity, so prior opaque callback receipts cannot bypass newly required validation before publication. Already-published Git receipts retain existing reconciliation semantics.
- No project policy remains an explicit required state after structural evidence is recorded. A failed project check or changed candidate is now COLLAB_INTEGRATION_VALIDATION_FAILED, distinct from absent validation. Policy identity validation matches the worker and UI retry availability. Shared publication retains the failed/required evidence reference; only successful evidence enters the publication outbox.
- RED: new candidate validation test failed for the missing module. PASS real Git/SQLite tests: clean Git merge with inconsistent related JSON values fails a real fixture project check; a passing content check succeeds; wrong tree and modified candidate bytes are refused; evidence is encrypted, hash-verifiable, survives reopen, retains earlier reports, and cannot be saved after stop/fencing. Worker test resolves published receipts to actual persisted check reports and covers missing/failed policy, retry, restart and stop/scope cleanup. Existing publication recovery, status/retry and discovery tests pass.
- PASS final signed Fastify/PG integration after binding checks: actual service startup downloads/prepares the encrypted delivery, records Git/materialization and post-check integrity evidence, leaves original source unchanged and waits for project policy. Existing signed attachment/task/object replay, revocation and log-secrecy checks also pass. Provider/native selection remain fixtures; random PG schema is cleaned. git diff --check passes; temporary dependency symlinks removed.
- This is the production evidence boundary and host-checker adapter, not a complete project validation policy or execution sandbox. Generic code parsing/type checking/test selection, Office validators, bounded/cancellable external-check execution, hard-process-kill temporary-directory reclamation and user-visible evidence/TaskCore projection remain outstanding. Callback policies in tests are explicit fixtures reading candidate bytes, not installed project configuration. C1/C2/C3/D remain unchecked. NEXT wire bound-session orchestration and trusted project checker selection/execution with persisted coverage (including immutable original checks); feed unresolved conflicts/failed checks to that run, then complete remote shared publication/outbox and independent private A/W/M materialization. Do not replace missing semantic validation with a blanket pass.

2026-09-14 checkpoint 33 (TaskCore-backed host integration turn entry):
- Previous turn was progress: 73f916ef persisted candidate validation evidence. Read admission, queue recovery/dispatch, local assistant startup, TaskCore and terminal guards. Existing local assistant turns finalized a fixed string immediately and could not host an asynchronous deterministic integration operation. Added integration-turn.js with a closed account/intent identity, explicit numbered attempt, background queue origin/visibility and no invented user message. The local assistant dispatcher now carries the typed operation through the existing recovery envelope, and startup invokes a main-only handler after building TaskCore. No new engine prompt, cwd or project is introduced.
- The host operation receives an assertActive guard bound to session owner, turn ID, generation, stop and terminal state. Late results cannot finalize replacement turns or changed owners. Result labels are closed, localized English/Chinese/Arabic states; local shared publication remains awaiting sync. Validation failure ends as a failed turn. Missing/throwing handlers use existing guarded failure recovery rather than producing an empty success. Existing queued local assistant dispatch now honors recordUser:false.
- RED real orchestrator test initially failed for absent integration-turn module. Real SQLite admission test then exposed that durableQueueKey requests durable admission but is not itself a store uniqueness key. Followed existing long-task wakeup convention: deterministic turnId includes session/account/intent/attempt. Repeated admission and SQLite reopen now reuse the original row/turn; explicit next attempt has a distinct identity. Did not alter unrelated generic queue/store semantics.
- PASS extended test-turn-orchestrator.mjs: busy foreground turn remains unchanged; background operation starts later with TaskCore present, no extra engine dispatch/user message, successful terminal, late-generation fence and failed host terminal/recovery. Session/engine persistence adapters in this suite are fixtures. PASS test-integration-turn-recovery.mjs with actual MessageStore SQLite dedupe/reopen and recovered operation envelope, closed identity input, explicit attempts and owner checks. Existing turn-start-guard, turn-queue-recovery-task-core and task-core-persistence regressions pass. Expected injected failure logs are part of these suites; diff check passes.
- This entry point is not yet connected to collaboration service startup/worker execution. C1 remains unchecked: NEXT wire service/worker admission to the native orchestrator after bootstrap, persist an admission attempt/receipt so SQLite-to-turn-store gaps and cancelled/unknown turns recover, and reauthorize account/original project/session before execution. Move the existing leased deterministic preparation/validation through the handler; preserve direct worker behavior only for explicitly unsupported embedders, not as a native bypass. Then use isolated bound-session engine work for unresolved conflicts/check failures, select trusted project validation policies, and complete network publication/C3/D. Do not claim automatic model conflict handling, physical-client acceptance or full-core completion from this entry-point test.

2026-09-14 checkpoint 34 (native worker admission and cross-store recovery):
- Previous turn was progress: 398f3d8a added the TaskCore host-operation entry. Native src/main.js now always supplies the integration queue bridge; until IPC/orchestrator bootstrap is ready, work remains pending without consuming retries. Service startup uses a new integration-admission scheduler instead of directly running the native worker. The main-only execute callback routes the original session's turn back to the current account's service. Embedders without a queue capability retain the existing direct worker path; the native app does not bypass admission.
- Added scope-encrypted admission journals with a persisted numbered attempt and deterministic turn ID before the second-store queue call. Lost queue ACKs replay the same identity. Different executed lease generations/new explicit retries receive a new attempt. Cancelled/failed/interrupted queue turns wait for explicit retry; completed turns whose work is still pending (for example target lease contention) can retry. An outcome-unknown receipt is recoverable only when the native bridge explicitly reports the turn is not active; live unknown turns retain their identity. Work generation/state/deadline rechecks and guarded SQL prevent late admission receipts from overwriting newer execution.
- Refactored worker execution into one leased runIntent entry shared with direct recovery. Native execution reauthorizes account, original session/project/root and task/delivery; requires the latest admission turn identity and owning TaskCore guard before acquiring inputs and at subsequent boundaries. Cancellation/fenced turns leave retryable waiting work. Worker lease generation still fences concurrent writers; stop fences both scheduler and worker before SQLite closes. Added safe generic failed-result text in all three locales.
- RED test-integration-admission.mjs failed for missing scheduler. PASS real collaboration SQLite: concurrent recover dedupe, encrypted journal/ACK-loss reopen, explicit attempts, wrong account/session/turn denial, fresh authorization denial, cancelled wait/manual retry, live-versus-restart-only unknown receipt, completed-but-pending retry, native-not-ready budget preservation, late ACK/newer-state protection and stop. Existing real MessageStore dedupe/reopen, orchestrator, worker, discovery and 27 service-lifecycle checks pass. Worker regression now also cancels an owning turn during acquisition and verifies no late publication.
- PASS strengthened signed Fastify/PG integration: actual CollaborationService -> admission -> actual TurnOrchestrator + SessionManager + MessageStore -> TaskCore -> actual worker -> signed encrypted delivery acquisition -> real Git candidate -> persisted validation evidence. A simulated active foreground phase prevents early work. Reopening SessionManager/MessageStore and the orchestrator restores exactly one queued turn; native startRecoveredTurns executes it; persisted TaskCore retains original project/session and a completed preparation turn. No user message is invented and source files remain unchanged. Project discovery/window/runner availability/provider remain test adapters; no engine/model is invoked and no installed dual-device claim is made. Dedicated random PG schema was cleaned; temporary dependency links removed; diff/syntax checks pass.
- C1/C2/C3/D remain unchecked: workspace-wide final writer coordination must cover eventual private and shared writers across physical-target/account boundaries, and unresolved conflicts/check failures still need isolated model work owned by this session. NEXT connect trusted project validation policy selection/execution and bounded conflict context through the existing engine path without changing the foreground session cwd; preserve actual check evidence and immutable original checks. Complete remote publication/outbox then independent private A/W/M application/undo, Office fidelity and measured large-directory/full-client acceptance. Native preparation now runs through TaskCore, but the default absent project validator still correctly waits rather than publishing.

2026-09-14 checkpoint 35 (bounded deterministic JSON conflict resolution):
- Previous checkpoint connected native preparation to the original session's TaskCore. SharedGit preparation now tries a conservative typed JSON adapter before returning text conflicts. Independent object fields, identical changes and one-sided field deletion can merge; conflicting edits/deletion-versus-edit remain unresolved, and arrays are atomic. Only .json regular files present at the same path in B/H/D qualify. Mixed unsupported conflicts remain pending as a whole. This is a deterministic transformation; no model or contributed merge driver runs.
- Reused the existing bounded JSON parser without changing the character-worlds subsystem. V1 accepts object roots with safe integer literals; decimal/exponent/negative-zero/large-number inputs, duplicate or dangerous keys, invalid UTF-8 and excessive depth remain unsupported rather than losing precision or data. Limits are 256 KiB per input/output, 32 conflicted paths, 6 MiB total input and depth 32. These are conservative typed-resolution limits, not the eventual large-file transport limits.
- Resolves exact Git blobs through bounded reads in an owned temporary directory. An isolated index begins from Git's merged tree, replaces only fully resolved conflict paths and preserves other changes. Candidate commit/ref identity includes the versioned resolution digest; retries are immutable and cannot overwrite the existing text-only identity. Shared publication and candidate validation share the same reference calculation. Required project validation, exact-tree evidence, fresh authorization, lease fences, publication CAS and private-source isolation remain in force. Temporary scratch data is removed on normal completion/failure; hard-kill reclamation remains outstanding.
- RED: typed JSON test first failed for the missing adapter; actual Git integration failed because independent fields on one line still returned conflicts. PASS typed unit coverage for independent nested fields, deletion, same-field conflict, atomic arrays, numeric safety, duplicate keys, encoding and size/depth refusal. PASS real Git tests for resolved bytes, preservation of nonconflicting files, retry identity, tampered resolution identity denial, required validation, remaining text/binary/same-field/mixed conflicts, unchanged head/private source and scratch cleanup. PASS real SQLite worker: resolved candidate waits without project policy, then a fixture policy reads actual materialized JSON and allows local publication. Existing candidate-validation and shared-publication recovery tests pass. The added worker test initially omitted its required owning-turn guard; corrected the fixture to provide the existing execution contract.
- PASS existing signed Fastify/PG integration after production changes: encrypted transport/task workflow, actual native SessionManager/MessageStore/TaskCore preparation, durable replay/revocation and logger secrecy. Storage provider/native selection/project-policy fixtures retain prior scope; this run does not add installed-client or model acceptance. Random PG schema cleaned, temporary dependency symlinks removed, diff check passes.
- C1/C2/C3/D remain unchecked. This advances deterministic C2 resolution only; project schema/related-file checks must still approve semantic combinations. NEXT select and execute trusted project validation policies and route remaining conflict/check failures into isolated model work owned by the original TaskCore, without retargeting its foreground engine. Remote shared-head publication/outbox, independent private A/W/M application/undo, typed Office fidelity and measured scale/full-client acceptance remain outstanding.

2026-09-14 checkpoint 36 (native bounded JavaScript syntax validation):
- Previous turn was progress: 3e42837a connected deterministic JSON resolution. Investigation confirmed there is still no installed trusted project test-policy selection. Added a real syntax gate to the production candidate-validation pipeline, without relabeling syntax success as project acceptance. It parses .js/.cjs/.mjs from the exact candidate manifest with the app's own Node runtime; nearest candidate package.json determines explicit module/CommonJS scope. Unspecified .js scope tries CommonJS then module syntax. Other languages remain explicitly outside this coverage.
- The checker sends bytes to Node --check over stdin. Candidate files are never imported/executed, no shell or contributed command is used, and the subprocess receives a minimal environment excluding inherited NODE_OPTIONS/preloads. Input reads check manifest hashes, regular single-link files and size bounds. Limits: 1 MiB per input, 20 MiB read budget, 200 eligible files, 30 seconds per scan, three seconds per parser and 64 KiB output. Runtime failure/limits remain required; syntax errors fail. Owning-turn/lease guards are polled during parsing, and timeout/cancellation kills the parser and waits for close. This parse-only subprocess is not a general execution sandbox for project tests.
- Encrypted immutable reports now retain parser version/runtime, explicit coverage counts, file hashes, actual parse modes, bounded diagnostics and output digests. Failed syntax cannot be overridden by a passing host project callback. Unsupported/unread syntax input cannot become a passing overall validation result. No project policy still means required after syntax success. Candidate-v2 policy identity includes Node version, invalidating older unpublished validation receipts when this pipeline/runtime changes. Existing published receipts retain reconciliation semantics.
- RED: standalone parser test failed for the missing module; actual candidate test showed malformed module syntax still returned required before wiring. PASS test-candidate-javascript.mjs against both current Node and the existing Electron binary in ELECTRON_RUN_AS_NODE mode: syntax success/failure, no code execution, module/CommonJS/nearest-package/unspecified-mode behavior, inherited preload exclusion, input/output limits, real child timeout termination and owning-turn cancellation. Coverage counts distinguish attempted from actually parsed files. PASS actual Git/SQLite candidate validation: syntax error without project policy, permissive callback cannot override error, encrypted diagnostic storage, existing related-file checks/reopen/mutation/fencing. Worker and publication recovery regressions pass.
- PASS existing signed Fastify/PG suite after final pipeline identity changes: encrypted task acquisition, actual original-session TaskCore preparation and validation evidence, replay/revocation and log secrecy. Its provider/native selection fixtures and no-installed-dual-client scope remain unchanged. Random PG schema cleaned, dependency symlinks removed, diff check passes. No tests silently skipped.
- C1/C2/C3/D remain unchecked. This is actual built-in JavaScript parse coverage only, not type checking, immutable original test selection, project semantic acceptance, Python/Office checks or isolated model conflict resolution. NEXT implement trusted project policy selection/execution with original checks pinned independently of contribution edits and route unresolved conflict/check evidence through the original session's isolated model work. Remote shared publication/outbox, independent private A/W/M/undo, Office fidelity and scale/full-client acceptance remain required.

2026-09-14 checkpoint 37 (original validation input provenance):
- Previous turn was progress: 0950b6dd installed native JavaScript syntax checks. Read the source research requirement that models must not manufacture success by removing/weakening existing checks. Host project callbacks previously received only candidate files, making removed assertions/configuration unavailable to them even though the immutable B/H/D objects remained in the task Git store. Added production originalInputs.read({version,path}) to the host validation context, limited to this exact candidate's baseline/head/delivery. No renderer/model tool or arbitrary Git ref/path capability was added.
- The reader verifies candidate and input ancestry, inspects immutable trees and distinguishes proven absence from unreadable/over-limit data. It returns owned byte copies and Git/blob/SHA-256 identities. Reads are serialized, limited to 100 requests, 4 MiB per blob and 64 MiB total; bounded temporary extraction is cleaned. Published-H checks added after B can be read separately from baseline checks. Private W is never consulted. Closing the checker scope prevents late reads; pending reads and swallowed read failures cannot produce passing overall validation.
- Candidate-v3 encrypted reports include immutable original-input provenance: exact versions, path/blob/content hashes, proven absences, request/read/failure/pending counts and digest. This is explicitly input coverage, not proof that a test executed. Existing project-policy evidence remains a separate check. Host callbacks that use no original inputs record zero reads; no default project approval was added. The pipeline version invalidates old unpublished policy evidence.
- RED: standalone original-input test failed for the missing module; actual candidate test failed because the host context had no original-input reader. PASS actual Git: B/H/D lineage binding, independently advanced H with an added check, delivery deletion, private/unsafe identity refusal, owned-buffer immutability, scope close/pending work/fencing, real oversized Git blob refusal before extraction and request limits. PASS actual Git/SQLite candidate validation: a fixture host policy reads the original baseline JSON assertion after the contribution deletes it, compares actual candidate data and rejects the regression; exact original hashes appear in encrypted evidence. Swallowed original-read failures remain a failed evidence check. Existing candidate validation, worker and shared-publication recovery tests pass.
- PASS existing signed Fastify/PG suite after final changes: actual original-session TaskCore preparation and persisted validation evidence, encrypted transport and durable replay/revocation/log secrecy. Provider/native selection and project-policy fixture limits remain unchanged; this is not installed dual-client or model acceptance. Random PG schema cleaned, dependency symlinks removed, diff check passes.
- C1/C2/C3/D remain unchecked. Original-input access is not automatic test discovery/execution or a persistent workspace-wide policy registry, and it does not assert every original test was selected. NEXT implement trusted project validation policy selection with check origins retained across later tasks, then bounded execution of those original checks and isolated model resolution in the original session. Remote shared publication/outbox, independent private A/W/M/undo, Office fidelity and measured scale/full-client acceptance remain required. Do not substitute input-read provenance for semantic/test execution evidence.

2026-09-14 checkpoint 38 (persistent original check policy and native selection):
- Previous turn was progress: 80b70e12 made original B/H/D inputs available to host validators. Added an encrypted, immutable Node-test check-policy registry. A mutable CAS pointer selects an immutable policy revision containing original check bytes, blob/content hashes and baseline origin. Binding includes account, conversation, shared workspace, local project, canonical target and physical directory identity (BigInt device/inode/birth-time fingerprint). Later task baselines do not replace the selected originals. Policy updates preserve earlier revisions for evidence replay; scope revocation removes access.
- Native main now supplies a localized file picker for trusted Node tests. A closed identity-only configureIntegrationChecks workflow action selects files only from the freshly authorized original project, rejects symlinks/outside paths and requires the selected local bytes to match the task baseline. Up to 32 .js/.cjs/.mjs files, 128 KiB each and 1 MiB total are pinned with bounded reads. Source-directory replacement, stale delivery, review withdrawal and running integration fence configuration. The final SQLite transaction rechecks local work state and policy CAS after asynchronous capture/authorization.
- Task details expose selection/update and the pinned file count in English, Chinese and Arabic. The UI explains that selected tests execute project code on a candidate copy and that original bytes persist for later tasks. Raw paths, policy bytes and record IDs stay main-only; cancellation preserves the existing policy. The workflow exposes getIntegrationCheckPolicy only to the main worker, with fresh task/source authorization.
- Worker lookup now honors the saved policy: until the dedicated executor is connected, it remains validation-required instead of allowing an unrelated legacy host callback to approve the candidate. Publication authorization rechecks policy identity and fences a changed policy. Candidate-v4 evidence/cache identity includes configured-versus-unconfigured state, explicit policy ID, Node runtime and selected check-policy ID; reports retain that selected ID. Review found and fixed a pre-existing collision between an absent validator and a host policy literally named unconfigured. Old unpublished validation cannot bypass the selected checks.
- RED registry test failed for the missing module. PASS real Git/SQLite policy tests: original byte persistence across later tasks and source edits, separately bound roots/workspaces, concurrent CAS winner, final transaction guard, fresh denial, invalid paths, immutable prior revisions, encryption, reopen and revocation. PASS real workflow tests with task API/native-picker fixtures: installation and closed summary, cancellation, byte mismatch, outside selection, physical root replacement during picker, withdrawal, running and stale-delivery fences. PASS extended actual Electron cards UI: localized trust text/count, identity-only single-flight configuration, cancelled picker preserves selection, existing card/retry/navigation/logout behavior. Picker interaction/API responses in that UI test remain fixtures.
- PASS candidate validation and worker regressions after final identity changes, including named-unconfigured collision refusal, selected-policy identity, refusal to bypass pinned checks and policy-change fencing. Existing status/retry and all 27 service-lifecycle tests pass with none skipped. PASS final signed Fastify/PG integration: actual original-session TaskCore preparation, main policy lookup, encrypted delivery and evidence, replay/revocation/log secrecy, retaining prior provider/native-selection fixture scope. Random PG schema cleaned, dependency symlinks removed, syntax/diff checks pass.
- C1/C2/C3/D remain unchecked. This commit saves and authorizes the check set; it does not yet execute it. NEXT implement the dedicated bounded/cancellable Node test runner using an isolated candidate execution copy with pinned original tests restored, persist actual execution/coverage and post-run integrity evidence, and wire install/restart wakeups plus policy changes/revocation into the original TaskCore worker. Dependency/test-helper coverage and other languages must remain explicit; do not claim arbitrary project acceptance from a selected filename or exit code alone. Then complete isolated model conflict repair, remote publication/outbox, private A/W/M/undo, Office and scale/full-client acceptance.

2026-09-14 checkpoint 39 (actual pinned Node checks through native TaskCore):
- Previous turn was progress: 26d6d632 saved trusted original check policies. The production worker now constructs a dedicated Node policy from the immutable registry revision. It materializes a separate exact candidate execution copy, restores the selected original test bytes over contributed replacements/deletions, runs them, verifies the whole execution copy again, and records original origins/hashes, execution manifest, runtime, actual test counts/failures and diagnostics in the encrypted candidate evidence. The private source and shared Git candidate are never edited by test restoration. A valid selected policy cannot fall through to an unrelated callback; malformed policies fail closed.
- Execution currently supports macOS sandbox-exec only. The OS profile denies network and writes outside owned scratch; test children additionally use Node permissions to deny process creation, addons/workers and reads outside the candidate/scratch. Environment is rebuilt without inherited credentials/options. The trusted parent supplies the selected files and consumes node:test structured events; each selected file must report registered tests with no failures, skips, todos or cancellations. Plain stdout JSON and exit zero without registered tests cannot approve. Separate report transport, 256 KiB host output, 1 MiB test-output accounting, 10000 events, 32 failures, 16 log excerpts, child/parent V8 heap limits and a 45-second process-group deadline bound execution. Scratch inspection runs inside the OS sandbox, cancels above 256 entries/16 MiB, and repeats at completion; this is sampled growth detection, not a hard disk quota.
- Runtime investigation found two independent issues: Node startup needs read access to the root directory itself, and Node 24 inherits -e from its internal option binding even after process.execArgv is cleared. The runner now copies its trusted bootstrap into a read-only owned file outside scratch and launches that file. Fixed startup probes showed Electron's task-name warning is nonfatal; no extra Mach permission is enabled. Actual execution passes on system Node 22.19.0 and Electron's Node 24.15.0. Node permissions supplement the OS boundary; they are not a malicious-code security boundary by themselves ([Node permission documentation](https://nodejs.org/api/permissions.html)). Structured runner behavior is documented by [Node test runner](https://nodejs.org/docs/latest-v24.x/api/test.html).
- Policy installation and the current waiting intent's wakeup now commit in one SQLite transaction; failed wakeup rolls the policy pointer back. A bounded startup scan of eight rows per recovery pass discovers previously saved policies without running checks outside native admission. The existing original-session queue creates a new stable attempt. Active checking revalidates the current local policy/root/scope and owning lease/TaskCore; policy replacement terminates the process group and prevents publication. Remote authorization is refreshed at the existing acquisition/publication boundaries.
- RED: the new worker scenario initially remained required instead of executing originals; a scratch-growth regression initially timed out instead of being cancelled. PASS real Git/SQLite worker tests on Node and Electron: removed/weakened tests still detect an invariant violation, successful tests publish the exact candidate, saved policies wake after startup, active persisted-policy replacement kills an actually running test, private bytes remain unchanged, temporary copies are removed, and encrypted evidence survives reopen. Runtime tests additionally verify assertion failures, empty/skip refusal, private-file/write/child-process/network denial, fast and sustained scratch growth, output limits, timeout and cancellation. Registry/workflow tests verify atomic wakeup rollback and identity fences. No native assertions were skipped on this macOS host; other platforms explicitly retain validation-required and their native-execution test branches are not claimed as covered.
- PASS signed Fastify/PostgreSQL integration with actual SessionManager/MessageStore/TurnOrchestrator/TaskCore: the first recovered original-session attempt waits for policy, identity-only selection pins baseline tests, automatic wakeup admits a second original-session turn, actual isolated assertions pass, and exact local shared publication/evidence completes without changing private source. Existing signed transport/replay/revocation/logger secrecy checks pass. Provider/native picker/window/project discovery remain fixtures; this is not installed dual-client or remote shared-head synchronization acceptance. Existing worker, candidate validation, policy/workflow, admission, turn recovery, status/retry, shared-publication recovery and all 27 service lifecycle checks pass.
- C1/C2/C3/D remain unchecked. This validates the selected original Node test files, not all project requirements: imported helpers/dependencies are not independently pinned, dependencies are not installed, and non-Node tools or other OS sandboxes are not implemented. NEXT close check-helper/dependency coverage and project validation feedback, then implement isolated model repair only for unresolved conflicts in the original session, remote shared publication/CAS/outbox transport, the independent private A/W/M writer and undo, Office fidelity and measured streaming/lazy/full-client acceptance. Keep local publication labelled as awaiting remote synchronization.

2026-09-14 checkpoint 40 (server workspace integration qualification):
- Previous turn was progress: dddb14c7 connected actual pinned Node checks. Inspection before remote publication found that the existing generation/lease was local SQLite only. Added PostgreSQL migration 049 with one integration target per shared workspace, a baseline head/revision, monotonic generation and an all-or-none device/task/delivery lease. Read-only lookup reports an uninitialized verified task baseline without creating a row. First claim initializes only that baseline; a wrong head/revision rolls back initialization and the command receipt. This does not store or publish a merged Git pack.
- New server qualification service reuses the task service's device, conversation/member and encrypted-task authorization. Only the requesting workspace owner qualifies; being an assignee or conversation member does not grant owner-wide access. Every operation and receipt replay rechecks the current reviewed/accepted delivery and both bound, unexpired input/delivery objects. Mutations use the existing command-runner transaction; operational leases create no chat events. Capability enablement requires the explicit Git-task server flag.
- Claim, renewal and release bind workspace, head/revision, device, task, delivery, lease ID and generation. The fixed 30-second term is computed from PostgreSQL clock_timestamp after locks, never a client clock or supplied duration. Renewal retains generation; expiry or release allows the next claim to increase it. Same-command replay returns the original response without extending or reacquiring a lease. A replayed historical response is not evidence of a current grant: subsequent guarded operations reject expiry, takeover or head movement. Full publication must perform that guard atomically with CAS in the next slice.
- Added closed signed /tasks/integration/get, claim, renew and release routes and main-only client methods, with no renderer-supplied roles, TTL, expiry, paths or credentials. Route regression first failed for missing exports, and client regression first failed for the missing transport method; both pass after wiring. Existing task routes, IPC/client, in-memory transaction-service and collaboration-client regressions pass, as do all 27 service lifecycle checks with none skipped.
- PASS actual PostgreSQL migration/domain tests: two devices race for one winner, different tasks share one workspace qualification, leases cannot transfer to another authorized task, expired takeover increments generation, stale renew/release/head writes fail, claim/renew receipts replay exactly, device/object revocation and object expiry prevent new qualification or replay, withdrawal prevents old receipt use, and failed first-head CAS leaves no target. These domain tests seed verified object rows and use actual encrypted task transitions. PASS complete signed Fastify/PostgreSQL fixture with actual encrypted Git task packages: lookup, committed-but-lost claim response, same-key recovery, renewal, assignee denial and release. Existing actual TaskCore/pinned-test/local publication, transport/replay/revocation/logger-secrecy checks still pass. Provider/native selection remain test adapters; random schemas are cleaned.
- C1/C2/C3/D remain unchecked. The native worker does not yet acquire this server lease; remote merged-object publication, a publication-time generation guard/CAS, canonical head download and outbox reconciliation remain required. Next implement that end-to-end path rather than treating successful qualification as remote synchronization. Also retain original-test helper/dependency coverage, unresolved model repair, cross-account local writer coordination, private A/W/M/undo, Office and measured scale/full-client acceptance. No deployment, package installation or original dirty-workspace merge occurred.


2026-09-14 checkpoint 41 (encrypted remote shared publication and canonical download):
- Previous turn was progress: 22ca601c added server qualification only. Added migration 050: workspace-bound stored objects, immutable encrypted publication records, and a publication pointer on the integration target. Bound objects now belong to exactly one message, task or shared workspace. Shared packs are owner-only within the original conversation/workspace domain; a source task assignee or bystander cannot download the combined owner history. Task binding rejects workspace objects. Read authorization still checks the current device, conversation scope, object state and expiry.
- Shared transport uses the existing bounded standard Git bundle implementation with a separate immutable candidate-ref namespace. A complete first pack carries H/D ancestry; later packs can omit an available published H. Git may list both H and B as bundle prerequisites, so extra shared boundaries must be ancestors of the excluded known head. Task ref validation remains unchanged. Independent real repositories reproduce exact candidate trees and both merge parents, with a small incremental second pack; private input files remain untouched. The existing 256 MiB pack ceiling is unchanged and is not large-file acceptance.
- Closed signed publish/publication routes and main-only clients now exist. Publication checks fresh task/owner/package authorization, B/D identity, expected H/revision, owning device/task/delivery, lease ID and generation. The final SQL CAS uses database clock_timestamp for lease and all required package expiries. Workspace object binding, encrypted immutable metadata, H/revision advancement, lease release, an owner-only workspace.published event, sync fanout and the command receipt share the existing transaction kernel. Receipt/event failures and expiry during commit roll everything back. Replaying a command or the same immutable publication under a new transport command cannot publish twice; changed content is rejected. A historical receipt is never a continuing lease grant.
- Publication envelopes use a distinct AES-GCM purpose and bind immutable publication/conversation identity; reads compare decrypted routing against database columns. Descriptors, validation identity and evidence hash are encrypted in the publication body. Receipt/event payloads contain only stable identity/head/revision. Server verification establishes opaque encrypted-object durability and scoped metadata consistency, not execution of client tests or semantic Git correctness. The native producer must supply actual validation, and receiving Git must verify the downloaded plaintext descriptor, pack graph and tree.
- Owner reads can fetch current or specified immutable publications after the source task changes. Incremental dependencies are checked through their full available chain, bounded at 64 publications; a complete pack is required to reset longer chains. The main-only sharedFiles broker reauthorizes publication and object access even for cached bytes. It returns one verified pack plus its parent identity; automatic chain traversal/import and native canonical-H reconciliation remain next work. Publication revocation/expiry blocks reads and command replay. Complete packs are independent of retired source task packages.
- RED PostgreSQL regression first failed for the missing publish method. PASS real PG domain tests: fenced generation, wrong sources, required initial full pack, concurrent one-winner CAS, immutable retry, receipt/event rollback, final-SQL lease/package expiry, owner-only event fanout, encrypted routing/purpose, revoked and expired parent dependencies, durable read after withdrawal, revoked device denial, and a seeded 64/65-publication ancestry boundary. Domain objects/descriptors and long historical metadata are explicitly seeded; those tests do not establish Git semantics.
- PASS signed Fastify/PG fixture after final changes: original-session TaskCore runs selected pinned Node assertions, the resulting actual candidate is exported, encrypted/uploaded through the real transfer/object broker, published with lease/CAS, replayed after a deliberately lost committed ACK, downloaded and imported into an independent real Git repository with exact tree and parent assertions. Owner metadata/ticket access succeeds; helper access and retired cached downloads fail; private W remains baseline. Added post-publication durable sync verification. This exposed an existing task.updated normalization bug alongside the new workspace event: both identity-only types now declare account event scope, while older projections can ignore them and advance their cursor. The regression was red before that fix. All fixture assertions ran on this macOS host; unsupported platforms explicitly report that native publication coverage did not run.
- PASS existing task bundle, task route/IPC/client/service, message crypto, transfer runtime (9), task transfer (3), sync/client/projection and service lifecycle (27) checks, with no skipped tests on this host. Final signed suite retains logger/receipt secrecy, actual encrypted task workflow and apply/rollback coverage. Provider, native picker/project discovery and transport harness remain fixtures; this is not installed two-client/TLS acceptance. Temporary PG schemas cleaned; dependency symlinks removed before commit; diff checks pass.
- C1/C2/C3/D remain unchecked. Native automatic qualification/publication is not wired, and local published journals/outboxes are not yet acknowledged from this remote receipt. NEXT negotiate the complete protocol, integrate server lease renewal/fencing into the original TaskCore, download/import the bounded canonical publication chain, reconcile H and drive durable upload/publication outbox recovery before completing local work. Preserve fresh policy/root/task checks and handle a remote winner by rebasing and revalidating instead of overwriting it. Remaining requirements include original-test helper/dependency coverage, unresolved model repair, cross-account local writer coordination, independent private A/W/M and undo, Office fidelity, streaming/lazy materialization and measured scale/full-client acceptance. No deployment or installed release occurred.


2026-09-14 checkpoint 42 (verified canonical H acquisition and atomic local mirror):
- Previous turn was progress: 179d3583 committed encrypted remote publication and signed verification. Inspection of the native worker found that existing local publication completes the integration intent before its queued publication-outbox reaches the server. Adding a network upload directly after that completion would reuse stale H when another device has won. This slice supplies the canonical acquisition/reconciliation needed by the remote-first worker; it does not declare that worker/outbox complete.
- Added main-only shared-head acquisition. It reads the freshly scoped target, traverses at most 64 immutable publication metadata nodes, and downloads/imports complete-to-incremental packs through the existing owner-only encrypted broker. It validates workspace/owner/conversation identity, monotonic revisions, parent-publication linkage, descriptor namespace/prerequisites, immutable download metadata, exact M parents H/D, exact D parent B and M tree. Cached objects still receive fresh scope/package authorization. The complete server chain, source task/source-root authorization and target head/revision are rechecked before local CAS. The result is an observed remote snapshot, never a lease or continuing authorization grant.
- Added a bounded Git remote-state blob containing revision, commit and publication identity. Updating shared H and that checkpoint is one update-ref transaction guarded by both previous head and previous state object. Older remote responses cannot rewind a recorded revision; mismatched same-revision identity fails closed. Local concurrent writers are preserved by CAS. The existing local candidate and publication receipt refs remain reachable when optimistic local H is replaced by the canonical server snapshot, so unsent work can be rebased later. No private W writes or outbox completion occur here.
- The real task workflow now exposes acquireIntegrationHead only to main-process callers. It reuses task Git acquisition and original project/session/root authorization, accepts the caller's synchronous lease/policy/TaskCore guard, and returns verified context plus canonical head. It has no renderer command or automatic scheduler hook yet. Revision-zero initialization is accepted only when the exact verified local task baseline is the server's initial head; a missing/different initial anchor is explicitly unavailable rather than guessed. Server target provenance for another task's initial H still needs completion before rollout.
- RED real-Git test first failed for the missing acquisition module. PASS independent-repository reconstruction of a complete first publication plus incremental second publication; exact combined files; fresh and late scope denial; metadata mutation, wrong tree, cyclic/overlong chains and remote movement refusal; revision regression; preserved unsent local candidate/receipt; local writer CAS; runtime reopen; initial-baseline binding; failed H CAS leaves remote checkpoint unchanged; interruption immediately after the Git transaction leaves H and checkpoint together on reopen. API and file transfer in these deterministic domain tests are explicit fixtures; publication checks do not run project tests again.
- PASS final signed Fastify/PostgreSQL fixture: after the actual original TaskCore validation and remote publish, the production workflow acquires the canonical H, and the new acquisition module reconstructs the same published head in another independent Git repository using real encrypted transfers and fresh signed reads. Existing lost-ACK replay, owner-only download/event access, retirement, task apply/rollback, post-publication account sync and logger secrecy still pass. Provider/native selection remain test adapters, not installed-client acceptance. Existing shared merge/publication recovery, shared/task bundle transport, integration worker, two-account ZIP workflow and all 27 service lifecycle checks pass with no skipped tests on this host.
- C1/C2/C3/D remain unchecked. No new protocol is advertised and the automatic worker still has its old local-publication path. NEXT wire remote-first server qualification/renewal, canonical acquisition, exact revalidation, durable pack upload/publish receipt recovery, then local Git/outbox completion under original TaskCore guards; replay must reconcile an already committed remote publication without treating old local receipts as remote proof. Complete initial-H package provenance across tasks/devices and migrate/requeue old completed local journals. Bounded ancestor traversal currently performs per-publication authorization/download calls; it is not a scale/performance result. Preserve unresolved model repair, original-test helper/dependency coverage, global local writer coordination, independent A/W/M/undo, Office fidelity, streaming/lazy materialization and full-client/scale acceptance. No deployment or original dirty-checkout merge occurred.


2026-09-14 checkpoint 43 (remote-first publication through the original native worker):
- Previous turn was progress: a95f08bb added verified canonical H acquisition. Added a native remote-publication attempt behind the explicit sharedPublicationProtocol=1 runtime policy. The service passes that policy into the main workflow, which constructs the attempt with the actual task Git, signed client, encrypted transfer broker and current TaskCore/local lease/check-policy guards. The production worker now invokes canonical acquisition and remote publication before local completion when that policy is present. No renderer path, caller-supplied TTL or validation boolean is introduced. Server/remote-config advertisement is deliberately still absent pending the rollout gaps below; the signed acceptance fixture enables the runtime policy explicitly.
- Claim requests are persisted before dispatch. A dropped claim response is replayed under the same command identity, then a fresh target read must prove the same live device/task/delivery lease and generation. An expired/released historical grant is replaced, not treated as authority. Conservative local deadlines derive from remaining server time minus observed request duration and a margin, using a monotonic host clock. A single in-flight renewal runs every five seconds; failed renewal fences validation and publication. Native shutdown cancels renewal and performs bounded-scope release where the account is still active. Cleanup uses the server lease CAS, so a successor cannot be released by an old attempt.
- Each candidate/evidence identity has an encrypted durable record for the owned pack directory, immutable descriptor, transfer ID, verified object ID, stable publication ID and exact signed request. The first publication exports a complete pack; later publications omit canonical H while the verified dependency depth is below 64, otherwise exporting a full checkpoint. Uploads resume their existing transfer. The request is saved before server publication; neither a network error nor a local Git receipt marks remote success. Confirmation independently reads the immutable server publication and compares owner/scope, task/delivery, object, B/H/D/tree, descriptor and validation identity against that request.
- A confirmed publication is followed by fresh canonical acquisition, then a local Git receipt and one SQLite transaction that records the sent outbox/remote receipt and completes the intent. Recovery runs before a new claim or validation: a committed-but-unacknowledged publication is confirmed and reflected without publishing or checking it twice. Git can now create the receipt for an older acknowledged candidate while verifying a newer canonical descendant H, preserving that newer head. Legacy local-only outboxes remain queued. Existing completed legacy journals are not yet automatically migrated into this new path.
- Ordinary workspace contention and remote head/lease races retain pending work with bounded backoff instead of exhausting the three-error budget. A busy remote lease waits 30 seconds; local TaskCore cancellation/fencing and access denial retain their existing waiting/cancel behavior. The native original-session admission remains the only production execution route.
- RED signed Fastify/PG test first failed because native completion had no remote receipt. PASS final actual SessionManager/MessageStore/TurnOrchestrator/TaskCore fixture: selected original Node assertions deliberately run for 6.5 seconds; a real server renewal receipt is observed during checking; verified encryption/upload and atomic publication occur automatically; the committed publish response is deliberately dropped; another native original-session attempt confirms the immutable publication; local work completes with a sent outbox only then. PostgreSQL contains one publication. Reopening owner state, signed replay, owner-only downloads/events, independent Git/canonical import, revoked-cache refusal, task apply/rollback and logger secrecy continue to pass. Provider/native picker/project discovery remain fixtures, not installed two-client acceptance.
- PASS real Git/SQLite recovery tests with explicit API/transfer adapters: dropped claim and publish responses across database reopen, fresh verification of an expired historical claim, no premature local H/intent/outbox completion, one confirmed publication without repeated checks, an actual incremental second pack combining both deliveries, and live renewal failure preventing any publication request. Shared publication recovery verifies acknowledging an older candidate under a newer canonical descendant; worker regression verifies more than three busy grants can recover. Shared-head, worker, admission, status/retry, existing local publication recovery and all 27 service lifecycle tests pass with none skipped on this macOS host. The initial status-test command used a nonexistent filename; the actual test-task-integration-status.mjs was located and passed. Isolated PG schemas are cleaned and temporary dependency links removed before commit.
- C1/C2/C3/D remain unchecked. NEXT complete server provenance/availability for revision-zero H when it came from another task/device, requeue and reconcile old completed local publication journals (including superseded queued outboxes), and negotiate/advertise the complete protocol through server and remote-config before enabling rollout. Expired orphan uploads and long-offline recovery still need coverage/remediation. Then prove simultaneous full clients and cross-account local writer coordination, finish original-test helper/dependency coverage and unresolved model repair, implement the independent private A/W/M writer and undo, Office fidelity, streaming/lazy materialization and measured scale. Do not substitute the explicitly enabled acceptance fixture for deployed availability or full A-D completion. No deployment or installed release occurred.


2026-09-14 checkpoint 44 (durable initial H provenance and bounded legacy recovery):
- Previous turn was progress: 7c198bc5 committed native remote-first publication. Revision-zero H still depended on whichever task's B existed locally. Migration 051 now retains one immutable workspace baseline reference to the original owner-uploaded task input object, encrypted under a dedicated baseline purpose with workspace/conversation AAD. The first successful claim atomically anchors that exact B; failed initialization leaves no anchor. It does not duplicate provider ciphertext or give stored objects multiple bindings.
- Owner-only baseline metadata and download authority survive cancellation of the source task and departure of its old assignee. Device, conversation/workspace ownership, source task/object routing, encryption identity, bound state and object expiry remain freshly checked. Other assignees and bystanders gain no authority. Initial target reads and historical lease/resolution replays reject retired anchored sources. Published revisions no longer require the old baseline package; their existing full/incremental publication chain remains authoritative.
- Added signed baseline download through the main encrypted transfer runtime. Canonical acquisition validates exact workspace/owner/conversation identity, full baseline descriptor, target commit, bundle integrity, parentless Git ancestry and tree safety, then uses the existing final authorization/target recheck and atomic H/remote-state CAS. An independent repository holding a different current B can acquire the original H without changing its current B or private files. An uninitialized server target is only a proposed task B and is now refused as canonical H, preventing two devices from pinning different revision-zero checkpoints before the first claim.
- Legacy revision-zero targets with no reference have a signed restoration command using the existing workspace/task index and at most 64 encrypted task rows per page. It searches only the same owner/conversation/workspace for an exact full B and an available correctly bound source object, including cancelled source tasks. It never replaces H with the current task's different B. The command uses fresh current-task authorization and target CAS before receipt replay, without events or lease-generation changes. Native attempts persist encrypted request/cursor state before dispatch and process at most one page per attempt; lost ACKs replay the same request. Pending pages retain coordination retries, while exhausted searches report unavailable. A freshly anchored target from another device supersedes old local unavailable state. The existing authorized explicit retry atomically resets an exhausted scan with its work row so restored sources can be searched again.
- RED tests demonstrated the missing durable initial anchor, missing legacy resolver, native attempts claiming before restoration, retry failing to reset an exhausted scan, and unclaimed task B being recorded as canonical. The first new local recovery test initially lacked its conversation projection; that fixture setup was corrected before recording the intended missing-behavior failure. PASS PostgreSQL domain coverage includes different B values, cancelled source/departed assignee, owner-only downloads, source/device expiry and revocation, encrypted routing/purpose, first-CAS rollback, 65 decoy tasks across bounded pages, lost page ACK, unchanged H/generation and fresh denial before resolution receipt replay. Descriptors and verified object rows in these domain fixtures are explicitly seeded.
- PASS actual signed Fastify/PostgreSQL/encrypted-transfer fixture creates two real Git tasks with different B values in one workspace, loses/replays the first claim ACK, cancels the source task, simulates a legacy missing anchor, loses/replays the signed restoration ACK, then imports original H into an independent repository initially holding only the current B. Exact file/parent checks, ordinary cancelled-task denial, owner-only baseline metadata/tickets, private-B preservation and retired cached-source denial pass. The original native TaskCore validation/renewal/publication ACK recovery, independent published-H download, task apply/rollback and logger secrecy remain covered. Provider/native selection are fixtures, not installed two-client/TLS acceptance.
- PASS real Git canonical acquisition tests and encrypted SQLite cursor/reopen tests, existing native remote-publication and integration-worker tests, closed route/client/crypto/transfer checks, explicit retry/status checks and all 27 lifecycle tests, with none skipped on this macOS host. Ordinary object authorization remains compatible with the pre-task migration fixture. Final verification and cleanup are recorded in the commit; no deployed availability or performance result is inferred from bounded fixtures.
- C1/C2/C3/D remain unchecked. NEXT requeue and reconcile old completed local publication journals, including superseded queued outboxes, then negotiate/advertise the complete server/remote-config protocol before rollout. Preserve recovery for expired orphan uploads and long-offline retries, simultaneous full clients and cross-account local writer coordination, independently pinned original-test helpers/dependencies and unresolved model repair. Independent private A/W/M/undo, Office fidelity, streaming/lazy materialization and measured scale/full-client acceptance still remain. No deployment, installed release or original dirty-checkout merge occurred.


2026-09-14 checkpoint 45 (legacy local-publication migration and negotiated remote rollout):
- Previous turn was progress: e10b7ca6 committed durable initial H provenance and legacy baseline recovery. Inspection confirmed that the old publisher completes the local intent and leaves a queued outbox, so ordinary admission cannot revisit it. Added a protocol-gated startup migration that reads at most eight completed work rows per indexed keyset page. SQLite v27 adds the corresponding account/state/intent index. It validates the encrypted intent, original journal and queued outbox bindings, preserves full original journal/outbox snapshots, clears old validation, requeues the same intent and advances its scheduling generation in one transaction. It does not claim a target lease, write Git/private files, authorize remote access or create an acknowledgement. Native admission and the current original-session/source/task/check-policy guards still own execution.
- Migration survives runtime reopen and repeated scans without duplicating intents or resetting target lease generations. Inconsistent publication metadata is retained and marked as a visible per-task migration failure instead of being republished or blocking unrelated admissions. A missing original publication journal is not invented. The scan is a bounded startup pass over existing completed work; it is not a scale benchmark or a guarantee of cooperating with a simultaneously running obsolete client.
- Every native remote attempt now persists that remote confirmation is required. A later policy downgrade cannot route migrated or newly remote-qualified work through the old local-completion shortcut. Re-enabling the protocol wakes work waiting specifically for that requirement. Legacy original validation remains in the snapshot while the revived attempt runs current checks. Remote outbox identity now includes candidate/policy/evidence, so fresh validation of the same Git commit cannot collide with immutable legacy outbox content.
- Only confirmed remote publication and canonical reconciliation allow completion. The sent replacement outbox, superseded old outbox with replacement/receipt references, confirmed migration record, final publication journal and completed intent share one SQLite transaction. A failure during supersession rolls them all back; the old queued record and immutable Git candidate/receipt remain recoverable. Normal remote lost-ACK recovery still confirms the existing immutable publication instead of repeating validation or sending a new result.
- Added COLLABORATION_SHARED_PUBLICATION_ENABLED, default false. When enabled with the existing IM/workspace/task/Git prerequisites and organization policy, the server's final signed-config gate advertises sharedPublicationProtocol=1. Desktop normalization retains only that exact version with the required Git/workspace versions, and main startup already passes the normalized policy into the native service. Profiles cannot bypass operator prerequisites. Configured integration APIs require the publication flag as well as task Git, so an old cached client policy cannot bypass a server-side disable. No environment/deployment/installed client was enabled by this change; the acceptance process sets its own test flag.
- RED real Git/SQLite regression initially failed because the migration module was absent. PASS actual local publication -> completed intent/queued outbox -> atomic requeue -> database reopen -> fresh validation -> confirmed remote completion. Coverage includes pre-commit migration failure, repeated scans, eight-row pages across nine valid synthetic target records plus one inconsistent record, unchanged Git, failed/uncertain remote response retaining the old outbox, downgrade fence, final supersession-transaction failure and recovery, visible invalid history and re-enable wakeup. Remote APIs in this focused test are adapters. Existing real-Git remote-publication tests now start from a genuine legacy local publication and retain dropped claim/publish ACK, expired historical claim, incremental packs and renewal-failure coverage.
- PASS final actual signed Fastify/PostgreSQL/native TaskCore upgrade fixture: first publish locally through the real original-session Node check runner; stop/reopen owner state; enable the native remote policy; startup migrates and admits a fresh original-session attempt; actual checks run again, real lease renewal occurs, ciphertext is uploaded and H is published; the publish response is deliberately lost; another native attempt confirms it, writes the sent replacement and supersedes the old outbox. Actual private files stay baseline. Subsequent signed baseline restoration, independent Git downloads, task apply/rollback, owner-only ACL, revoked caches and logger secrecy pass. Provider/native selection remain fixtures, not installed two-client/TLS acceptance. One fixture initially expected undefined instead of the actual null receipt; another waited for the receipt before the completion transaction and prematurely stopped the live turn. Both fixture assumptions were corrected; final verification waited for published state and native terminal completion.
- PASS server/desktop policy gates (including disabled configured APIs), new migration/recovery, shared publication recovery, admission, worker, status/retry, SQLite store, client/service and all 27 lifecycle tests with none skipped on this macOS host. The configured-service gate fixture initially omitted the required database transaction interface; it now supplies a throwing interface proving disabled endpoints never reach storage. Temporary PG schemas clean themselves; dependency links are removed before commit. No whole-repository, full-client, Office or scale claim is made.
- C1/C2/C3/D remain unchecked. NEXT cover/remediate expired orphan uploads and long-offline recovery, then simultaneous full clients and cross-account writer coordination, independent original-test helpers/dependencies and unresolved model repair. Continue the independent A/W/M private writer/undo, Office fidelity and bounded streaming/lazy materialization with measured acceptance. Negotiated remote publication is now implemented, but default-off operator rollout must not be described as deployed or as completion of the remaining core requirements. No original dirty-checkout merge or installed release occurred.


2026-09-14 checkpoint 46 (expired orphan upload recovery under fresh qualification):
- Previous checkpoint 9514b5c7 committed legacy local-publication migration and protocol negotiation. A verified but unbound candidate expires after its orphan deadline; the old generic unavailable response and cached object ID prevented native recovery. Owner-authorized status and init receipt replay now distinguish this exact condition with object identity and ciphertext size/hash, without issuing upload credentials or changing the object state. Bound objects, revoked/aborted objects, foreign owners and objects with an explicit expiration policy cannot receive this replacement proof. This preserves denied/unknown outcomes instead of treating them as absence.
- Transfer recovery validates the proof against its immutable encrypted manifest, including when the original init ACK was lost. It retains the recovered object ID and reports COLLAB_TRANSFER_ORPHAN_EXPIRED. A second status probe after a missing multipart session preserves the same proof. Ordinary transfer retry does not create a replacement object; the native publication coordinator must decide whether publication is safely fenced.
- Native publication re-probes previously uploaded objects. Replacement requires the exact expired-orphan code and a current live server grant; a persisted publication request must have an older generation and different lease. A same-grant uncertain request refuses replacement. Existing immutable-publication recovery runs before acquisition, so committed or retired publication dependencies do not become permission to republish. Generic authorization/provider failure and a probe changing the recorded object ID are refused. Each publish call permits at most one replacement.
- Under the held grant, one SQLite transaction records the retired transfer/object/descriptor/request in encrypted history and resets the active upload/request. The same candidate bundle and validation remain; preparation checks the plaintext digest and creates a fresh encrypted transfer. A successor can resume this durable state. Old ciphertext remains expired and unbound; this change neither revives it nor implements provider cleanup. No active orphan-cleanup worker was found in server/src, so physical garbage collection remains outstanding.
- RED tests reproduced generic-invalid expiry results and silent object-identity replacement; the missing-multipart expiry case also failed before its fix. PASS transfer suite: 24 tests, none skipped. PASS owner recovery tests plus object client/routes/config/state machine, transfer runtime/service/retirement, shared publication recovery, legacy migration and integration worker companions. The real Git/SQLite remote-publication adapter test covers generic denial, changed object identity, same-grant refusal, new-grant replacement across database reopen, one retired record, retained validation, lost commit ACK, one confirmed publication/outbox, incremental follow-up and renewal failure. Adapter transfers do not themselves prove encryption.
- PASS signed Fastify/PostgreSQL integration: lose init ACK, expire the actual row, replay the original receipt to identify the same orphan without credentials, deny non-owner/revoked/explicit-expiration cases. The original-session TaskCore upgrade fixture expires the verified candidate immediately before the real publish route, then observes a later qualified encrypted replacement, lost successful publication ACK and native confirmation. The old row remains verified/unbound and the published row references a different object. Actual canonical download/import, task apply/rollback, ACL and logger secrecy pass. Qiniu provider and native selection remain fixtures; installed two-client/TLS acceptance is still pending.
- C1/C2/C3/D remain unchecked. NEXT finish long-offline recovery beyond this retained-bundle orphan case (including missing staging and explicit recovery ownership), then simultaneous full clients/global local-writer coordination, independently pinned original-test helpers/dependencies and unresolved model repair. Continue independent private A/W/M/undo, Office fidelity and bounded streaming/lazy materialization with measured scale/full-client acceptance. No deployment, installed release, operator flag change or original dirty-checkout merge occurred.


2026-09-14 checkpoint 47 (recover missing publication staging from immutable Git):
- Previous goal turn was progress: 55b0644e committed expired orphan replacement under fresh qualification. Current inspection found that an unconditional local directory check prevented using an already verified remote upload, and that expired-upload replacement could not rebuild a missing candidate bundle. Publication now touches local package staging only when preparing a new transfer. An existing transfer can reauthorize/probe/complete without its old plaintext bundle or directory.
- An upload-free attempt reconstructs a missing pack from its immutable candidate ref/commit and retained prerequisite boundary. Only that state can replace the pack descriptor; an active transfer/object/publication request prevents representation changes. Git maintenance may change bundle bytes while preserving the validated commit, so a regenerated descriptor records the actual new hash/size rather than assuming byte-identical output. Existing retired-upload records preserve the original descriptor/request. Validation is retained only through the existing unchanged-candidate/policy guards.
- Missing directories use a new owned directory. Existing files with a recorded descriptor are still digest-checked by real encryption preparation; symlinks, hard links, nonfiles and redirected directories are refused instead of overwritten. The preparing state is durably saved before export. An export written before its prepared journal update is not adopted after restart; the next qualified attempt exports into a fresh directory. Uncommitted scratch exports are retained for future cleanup rather than being confused with authenticated publication material.
- Transfer recovery reports COLLAB_TRANSFER_STAGING_MISSING only for missing local ciphertext. With a retained manifest/object identity, native replacement still requires fresh authorized server status and current task/lease guards; an older uncertain publication request must be fenced by a newer generation. Verified remote objects succeed without reading missing ciphertext, and generic denial/integrity failure never authorizes a replacement. Missing manifests, changed device ownership and unknown init identities remain fail-closed; this checkpoint does not infer their lost identity or adopt their receipts.
- RED missing-ciphertext regression returned the old generic failure; RED real Git/SQLite publication regression failed on the missing directory before probing the expired upload. Final review reproduced four cancellation/revocation/stop/revision races during missing-file inspection; a fresh manifest/lifecycle guard now prevents stale callbacks from authorizing replacement. PASS 29 transfer tests with none skipped. Expanded real Git/SQLite adapter test covers removal of the entire old directory, actual Git repack changing bundle hash, retained validation/commit, verified upload ACK loss plus missing directory across reopen without duplicate transfer, known missing ciphertext replacement, refusal to overwrite a symlink, injected failure after bundle export before journal persistence and database reopen into a fresh export. Existing claim/publish ACK recovery, incremental packs, renewal failure, legacy outbox reconciliation and private-file preservation remain covered. Adapter transfers copy actual bundle bytes and check their digest but do not themselves encrypt.
- PASS actual signed Fastify/PostgreSQL/native TaskCore fixture removes the candidate's whole staging directory immediately before dispatching the real publish request; the server expires that first upload. Native recovery gets new qualification, reconstructs/encrypts the same candidate, publishes, loses the successful ACK and later confirms one publication with a sent replacement outbox. The old directory stays absent and the confirmed journal points at new staging. Canonical import, task apply/rollback, ACL and logger secrecy remain verified. Provider/native selection are fixtures; no installed two-client or physical-provider acceptance is claimed.
- PASS affected transfer runtime/service/retirement/manifest, shared publication recovery, legacy migration and integration worker companions. Changed JavaScript syntax and diff checks are part of final checkpoint verification. C1/C2/C3/D remain unchecked. NEXT finish unknown staging identity/recovery ownership and physical orphan cleanup, then simultaneous full-client/global local writer coordination; continue independently pinned original helpers/dependencies, model conflict repair, private A/W/M/undo, Office fidelity and bounded streaming/lazy materialization with measured acceptance. No deployment, original dirty-checkout merge, installed release or rollout flag change occurred.


2026-09-14 checkpoint 48 (recover unknown init identity with retained manifest):
- Previous goal turn was progress: b279a1bc committed missing publication staging recovery. Inspection showed that an init ACK lost before object ID persistence, followed by ciphertext loss, still prevented identity recovery even though the encrypted manifest retained the original command, device, key and metadata. The manager now distinguishes absent ciphertext from changed/unsafe ciphertext, rechecks the current manifest/lifecycle and replays the original init intent. It never generates a replacement command or key to guess the missing identity.
- After recovering an uploading object ID, a missing-ciphertext attempt performs fresh owner status before continuing. Present/verified provider data can finish without local ciphertext; a proven missing upload retains the object ID in COLLAB_TRANSFER_STAGING_MISSING for the existing native lease-fenced replacement path. Network uncertainty, current denial, cancellation, changed device or stale manifest cannot authorize replacement. Changed ciphertext still fails before init dispatch. If the original init never committed, replay can initialize that original identity; this is the existing authorized init operation, not a new read-only endpoint or a proof that no object can be created.
- Init receipt replay can now return freshly owner-authorized verified/bound object metadata without new upload credentials. Ciphertext size/hash must match the immutable local manifest before the desktop adopts that result. Explicit object deadlines, orphan deadlines for unbound rows, current membership/device and revoked object checks remain enforced. The command receipt itself still contains only its original object ID; current authority/state is checked outside the receipt on every replay.
- RED focused tests showed lost-init plus missing ciphertext returning no object identity, and verified replay remaining failed. PASS 35 transfer tests, none skipped, including original command/key equality, no multipart initiation on missing bytes, verified recovery without local bytes, and cancellation/revocation/stop/revision races both before object identity is known and after multipart starts. Current manifest checks run before replay on the missing-file path and before returning any replacement proof.
- PASS actual signed Fastify/PostgreSQL transfer fixture loses an init ACK, deletes staged ciphertext, reopens the manager and recovers the exact object from the one original PG receipt with zero multipart starts. A second case removes the local ID from a retained manifest and deletes ciphertext after actual upload verification; replay returns verified state and the same object without another multipart session. This is controlled stale-journal simulation, not a power-loss claim. The full native TaskCore expired-upload/missing-directory/lost-publication-ACK chain, canonical Git import, task apply/rollback, ACL and logger secrecy also pass. Qiniu provider/native selection remain fixtures.
- PASS object recovery/routes/client, transfer runtime/service/retirement and real Git/SQLite remote publication companions. Final syntax/diff checks and exact dependency-link cleanup precede the checkpoint commit. C1/C2/C3/D remain unchecked. NEXT preserve/recover upload identity when the manifest itself is missing and define safe device ownership recovery, then physical orphan cleanup and simultaneous full-client/global local writer coordination. Independent original-test helpers/dependencies, model conflict repair, private A/W/M/undo, Office fidelity, streaming/lazy materialization and measured scale/full-client acceptance remain required. No deployment, rollout enablement, installed release or original dirty-checkout merge occurred.


2026-09-14 checkpoint 49 (encrypted task-transfer journal and missing-manifest restoration):
- Previous goal turn was progress: d0db6321 recovered unknown init identities when their manifest remained. Task-owned upload identity now has an independent encrypted SQLite journal using the existing account/conversation-scoped task record store. The journal includes original command IDs, device, encryption metadata and current checkpoint; no keys or local paths are added to renderer views. Ordinary attachment manifests and task downloads retain their prior storage behavior.
- Task-upload state is journaled before its file-cache write. A failed file rename therefore cannot lose cancellation or roll back identity. Recovery validates account/scope/device, immutable command identity and monotonic revision; a known older authenticated file advances to the journal, while equal-revision mismatch/newer files fail closed. The initial empty revision-one file is the sole supported unowned-to-device cache transition. Existing intact task uploads seed a journal on authenticated read; an identity lost before ever being journaled is not invented.
- Missing manifests or whole task-transfer directories are reconstructed only from an authorized journal. Unknown files, corrupt manifests, redirected directories, unsupported hard links, revoked scope and a changed device remain refused. Exclusive creation cannot overwrite a file appearing during restoration. The atomic link/unlink boundary has a narrow recovery path: exactly two names for the same regular inode, one reserved manifest temporary name, and an authenticated payload identical to the journal; it removes only that temporary link and fsyncs the directory. This does not accept arbitrary external links.
- A removal tombstone commits before filesystem deletion and clears the retained content/DEK. Stale authenticated files cannot resurrect the transfer; enumeration skips tombstoned task IDs rather than treating them as an unknown attachment coordinator. Current cancellation remains in the journal even when every file is removed. This is same-device task transfer recovery, not permission to adopt another device's command receipts or a global filesystem transaction.
- The first focused test invocation had a missing closing brace; it was corrected before the behavioral RED run. Disabling runtime journal wiring then reproduced missing independent identity and lost cancellation recovery. Additional fault tests caught the first-ownership cache transition and retained key material in removal tombstones; both were fixed. PASS nine real SQLite/filesystem tests: missing file/directory plus original command replay across DB reopen, cancellation, device refusal/corrupt-file preservation, interrupted first ownership/cancel cache renames, deleted-file resurrection refusal, scope revocation, unknown/symlink path preservation and interrupted exclusive restoration. One link-interruption test initially assumed runtime construction scans files; it now triggers the actual manifest read before asserting recovery.
- PASS actual signed Fastify/PostgreSQL/native TaskCore integration deletes both the candidate package directory and its transfer directory immediately before the real publish request. The server expires that object; native retry reconstructs the encrypted manifest, reauthorizes the old object, retires it under new qualification, reconstructs/encrypts the candidate and confirms publication after the successful ACK is deliberately lost. One original candidate/validation and the reconciled sent outbox survive. Canonical Git import, task apply/rollback, owner ACL and logger secrecy remain covered. The first fault-injection run omitted createHash import and timed out in publishing; after fixing the fixture, a fresh run completed successfully. Provider/native selection remain fixtures, not installed two-client/TLS or power-loss acceptance.
- PASS existing manifest, 35 transfer, runtime/service/retirement, task workflow, native remote-publication and all 27 lifecycle companion tests. Final syntax/diff checks and exact dependency-link cleanup precede commit. C1/C2/C3/D remain unchecked. NEXT physical orphan cleanup and simultaneous full-client/global writer coordination, while preserving explicit recovery ownership for changed devices and already-lost legacy identities. Independent original-test helpers/dependencies, model conflict repair, private A/W/M/undo, Office fidelity, bounded streaming/lazy inventory and measured scale acceptance remain required. No deployment, installed release, rollout flag change or original dirty-checkout merge occurred.


2026-09-14 checkpoint 50 (bounded object expiry and provider cleanup worker):
- Previous goal turn was progress: a6dceea0 committed encrypted task-transfer journals. Inspection found durable object_cleanup_jobs but no executor or expired-object sweep. Added an object-row-locked sweep of at most 64 active expired objects. It distinguishes explicit expiration from unbound orphan expiration, moves them out of bindable state, removes wrapped keys and queues cleanup atomically. Active bound objects ignore orphan deadlines. The existing 16-minute delay remains to wait out the maximum issued upload credentials; no credentials are renewed for retired rows.
- Each deletion holds the object and job locks in their existing object-before-job order across a bounded provider call; concurrent workers skip locked objects. Only retired/deleted objects with due jobs are eligible, so a misqueued bound object is preserved. Provider failure keeps pending work with a fixed safe error code and exponential retry up to one hour. Provider success and deleted/completed SQL updates share the acknowledgement transaction. A crash/rollback after physical deletion repeats the idempotent provider delete instead of incorrectly assuming database completion. Job rows are retained as provenance.
- The private Qiniu adapter now signs the exact dedicated bucket/object key for the fixed HTTPS management host, includes UTC X-Qiniu-Date, rejects redirects, times out at five seconds and cancels response bodies. Only documented 200 and 612 confirm deletion/absence; authorization, generic 404, rate-limit and service failures retry without leaking provider exceptions or signatures. Implementation references [Qiniu delete](https://developer.qiniu.com/kodo/1257/delete) and [management authorization](https://developer.qiniu.com/kodo/1201/access-token). No public bucket fallback or renderer deletion API exists.
- Owner status/init recovery now recognizes expired/deleted orphan rows only when their retained cleanup job proves orphan-expired provenance, with no binding or explicit expiry policy. Current conversation/device/owner checks still run first; metadata does not restore keys, upload credentials or downloads. Explicit expiration, revocation and foreign owners remain denied. This prevents physical cleanup from breaking the earlier qualified upload-replacement recovery path.
- Added default-off COLLABORATION_OBJECT_CLEANUP_ENABLED and documented it in server/.env.example and the design note. Fastify onReady starts one non-overlapping ten-second loop, each processing at most eight deletion jobs after the sweep; onClose stops scheduling and drains current work. Fixed error codes expose retries/failures without provider data. Private-storage configuration is required independently of the retired objects' former KEKs. Migration 052 adds partial due-job and explicit-expiry indexes; it was applied in the final isolated PG fixture. No deployment or flag enablement occurred.
- RED provider test showed missing delete support; RED recovery test rejected retired/deleted orphans despite valid cleanup provenance. PASS focused signing/status/gating/lifecycle tests, including non-overlap, bounded deletion count and shutdown while retirement is pending. PASS object recovery/config/state machine/routes/client, policy and real Git/SQLite remote-publication companions.
- PASS actual signed Fastify/PostgreSQL/native TaskCore chain with the new cleanup fixture: delayed eligibility, key erasure, explicit-vs-orphan owner status, retry/backoff with sanitized error, two cleaners contending during provider deletion, live bound-object preservation, orphan recovery after deletion and foreign-owner denial. A second worker variant deliberately throws after provider success and SQL updates but before commit; real PG rolls back to expired/pending and the following retry completes. Fixture objects/keys for focused cleanup cases are seeded and provider calls are adapters; the existing native encrypted publication/download/apply/recovery chain remains real within that fixture scope. This is not a live Qiniu, full-client or power-loss test.
- Final syntax/diff checks and exact dependency-link cleanup precede commit. C1/C2/C3/D remain unchecked. NEXT cross-account local writer coordination and simultaneous full-client acceptance, then independently pinned original-test helpers/dependencies, unresolved model repair and independent private A/W/M/undo. Office fidelity, bounded streaming/lazy materialization and measured scale remain required. Legacy identities lost before journaling and device takeover still require explicit ownership recovery; no original dirty-checkout merge or installed release occurred.


### Checkpoint 51 — process-wide local application writer

- `local-writer.js` uses a permanent OS-user-level SQLite file independent of account, edition and Electron profile. A nonwaiting `BEGIN IMMEDIATE` holds the kernel write lock throughout each synchronous application/recovery operation. It stores no task data, never deletes the lock file, rejects path aliases/hardlinks and releases on exceptions/process death. There is no expiring lease that a stalled former owner can outlive. A single global lock deliberately serializes unrelated application targets too, covering parent/subdirectory aliases without a separate path registry.
- Production task workflow passes this coordinator to every `task-application` apply and rollback, including startup/local-only recovery. The broker requires it. Authorization remains before admission; plans/hashes and durable journals are checked after acquisition. A competing attempt returns `COLLAB_TASK_APPLICATION_BUSY` before its application journal or target writes. Existing per-account encrypted journals and immediate mutation checks remain authoritative for recovery.
- RED local-writer test failed on the missing module. PASS Node 22 and Electron Node 24 real process locks, reentrancy, exception release, SIGKILL release and unsafe paths. A separate real application fixture uses two account/profile SQLite journals and parent/subdirectory bindings to one file: A stalls after rename and before completion receipt; B apply and A recovery are busy; killing A releases the lock, B's stale preview fails, A recovers and B then applies. Tests use explicit temporary lock paths; no real user-home coordination file is created.
- RED Electron UI regression first failed because busy errors showed generic conflict guidance. PASS actual renderer retry with the exact application id, plan hash and deletion consent; English, Chinese and Arabic guidance is present. Existing ZIP/Git workflow and encrypted local recovery companions pass. PASS the complete signed Fastify/PostgreSQL/native TaskCore regression with isolated schemas and fixture storage/native selection, plus changed-module syntax and diff checks. Evidence: `/private/tmp/lily-51-{writer,task-writer,electron-writer,electron-task-writer,application,recovery,workflow,git-workflow,ui,http}.log`; RED logs are retained separately. These are not two installed-client or physical-provider acceptance.
- Scope remains bounded: this coordinates cooperating task-application brokers, not arbitrary editors or foreground engine subprocesses. No cross-filesystem transaction or automatic recovery retry timer is claimed; incomplete recovery remains available through the existing local recovery entry. C1/C2/C3/D remain unchecked. NEXT implement independent A/W/M application and contribution-specific undo through this coordinator, complete foreground writer admission and simultaneous full-client acceptance, then independently pinned original helpers/dependencies, model repair, Office fidelity, bounded streaming/lazy materialization and measured scale. No deployment, operator flag change, installed release or original dirty-checkout merge occurred.


### Checkpoint 52 — independent private A/W/M candidate on the native turn

- `task-local-candidate.js` reads authenticated A/M snapshot manifests and only their path union from real W, preserving local modifications, unrelated files and local deletions. Built-in Git `merge-file --stdout` resolves bounded UTF-8 text without writing a Git object; the existing bounded JSON adapter handles remaining supported JSON conflicts. Add/add, modify/delete, binary and unresolved text conflicts stay explicit. Root aliases, snapshot tampering, concurrent W changes and private staging overlap are refused. Current 64 MiB/file and 512 MiB candidate limits remain; this is not D streaming/Office acceptance. Git semantics checked against https://git-scm.com/docs/git-merge-file (stdout output and nonzero conflict status).
- The candidate's base manifest describes observed W, so applying it through the existing coordinator/journal retains private edits; rollback restores W rather than shared A. Tests perform actual apply, a stale-preview rejection and rollback. Source W is never changed during candidate preparation; no conflict markers become a successful file result.
- `local-materialization.js` stores encrypted candidate and last-synchronized-A records scoped to device/workspace/physical target. Only a confirmed first shared publication with initial H equal to the known baseline seeds A automatically; later unknown histories return `baseline_required`. Remote revision checks reject publications older than A. Preparing a candidate never advances A. Every attempt gets a persisted private directory identity before filesystem work and a fresh token; stale asynchronous attempts cannot replace their successor's record. Intact candidates reuse after database reopen; missing/tampered staging or changed W triggers a new candidate. Private attempt retention/cleanup remains part of the later bounded-storage work.
- Production `integration-worker.runIntent` invokes the native workflow preparation after confirmed remote publication, inside the original TaskCore turn and its owner guard. No new renderer-supplied paths or model-selected commits are accepted. English/Chinese/Arabic completion messages distinguish shared publication from private candidate preparation, local conflicts and missing A. Automatic private validation/application, startup reconciliation of completed shared intents, local task-card materialization projection and updating A from a write receipt are still NEXT, not completed by this checkpoint.
- RED candidate test failed on the absent module. PASS real Node 22 and Electron Node 24 Git merges, private changes/deletions, binary and add/add conflicts, source mutation during merge, encrypted SQLite reopen, missing candidate rebuilding, stale asynchronous preparation, unknown A and old-publication refusal. The test compares *all* shared Git objects before/after, including unreachable objects: no W blob is added. Existing application/writer/workflow, native turn recovery/status and integration-worker companions pass. Actual signed Fastify/PostgreSQL/native TaskCore acceptance confirms a private candidate appears automatically after remote publication while owner files and A remain unchanged; provider/native selection are fixtures. Final verification logs use `/private/tmp/lily-52-*`.
- C1/C2/C3/D remain unchecked. NEXT connect private candidate validation, foreground writer admission, automatic application with crash/receipt reconciliation and contribution-specific inverse changes. Continue independently pinned original-test helpers/dependencies and model repair; Office package fidelity, bounded streaming/lazy inventory, simultaneous full clients and measured scale remain required. No original dirty-checkout merge, operator rollout, deployment or installed release occurred.


### Checkpoint 53 — private candidate validation

- `local-candidate-validation.js` captures W′ in a distinct private validation Git repository and binds evidence to that real commit, candidate fingerprint/token, runtime and current pinned test policy. It reuses the syntax check and existing macOS/Node sandboxed test executor with an empty-parent private baseline. Shared candidate validation retains its two-parent default. No private repository instance is passed to shared publication or transfer.
- Trusted pinned original tests replace contributed/weakened tests only in the disposable execution copy. Missing policy remains `required`; actual failing project checks or changed candidate bytes remain `failed`. Evidence and the candidate's validation pointer are written together in encrypted SQLite after active-account, candidate-token and current-policy checks. Preparation/validation still does not advance A or write W.
- The original-session native workflow now validates a ready local candidate after remote publication. Its completion text separately describes private validation passed/failed/required and explicitly says files have not been applied; private validation failure cannot be hidden by shared publication success.
- RED test first failed on missing module. Restricted nested sandbox execution then failed; two escalation attempts were not executed because the approval service reported model capacity exhaustion. After scope inspection, the same authorized test retry succeeded. PASS real Node 22 and Electron Node 24 tests: private immutable commit, weakened-test refusal, passing/failing/missing-policy receipts, encryption and stale-candidate fencing. The separate `--guards-only` run explicitly skips pinned execution; the full test subsequently ran and passed. Existing pinned shared Node policy, A/W/M candidate/materialization and native turn regressions pass. Signed HTTP/PostgreSQL/native TaskCore tests exercise automatic local checks after confirmed remote publication; the added final assertion checks the private commit is absent from shared Git. Logs: `/private/tmp/lily-53-*`.
- C1/C2/C3/D remain unchecked. NEXT foreground-writer admission, automatic local application, matching validation-policy/manifest checks immediately before write, atomic A/local-receipt CAS and contribution-specific undo. Completed-shared-intent startup reconciliation and local card projection remain pending. Independently pinned helper/dependency coverage, model repair, Office fidelity, bounded streaming/lazy inventory, simultaneous full clients and measured scale remain required. No deployment, installed release, operator enablement or original dirty-checkout merge occurred.


### Checkpoint 54 — version restoration joins the local writer boundary

- Audit found another real workspace writer: `WorkspaceVersionService.restore` previously used only per-instance `_locks`/`_mutations`. The foreground start guard likewise only consults that local service; it is not cross-profile engine activity admission. Automatic W′ application must not be enabled from that check alone. Long-lived engine activity, terminal completion and crash/orphan ownership still need explicit coordination before safe automatic application.
- `local-writer.js` now exposes `runAsync`, holding the same nonwaiting kernel-backed SQLite lock across the awaited operation and releasing on success/failure. The synchronous task broker contract remains unchanged and still rejects async callbacks. Both APIs use the same acquisition/release implementation; no expiring lease or new persistent activity markers were introduced.
- Actual Git and fallback snapshot restoration, including recovery-point creation and rollback on failure, now run under that process-wide lock. Contention maps to the existing `WORKSPACE_VERSION_BUSY` error. Only operations writing the workspace acquire the new lock; save/automatic version snapshot operations retain their existing scheduling. Tests inject temporary coordination paths rather than touching the real home file.
- RED async lock test failed because `runAsync` was absent. PASS Node 22/Electron Node 24 async/sync exclusion, async failure release and existing process/crash/path guards. A real fallback snapshot restore pauses after restoring bytes: task application is blocked with no journal, then its obsolete preview is rejected after restoration releases the lock. The independent-process task writer test proves version restore is rejected while a child task application holds the lock. Existing full workspace-version-control (Git, local fallback and rollback faults), task application and writer regressions pass. Logs: `/private/tmp/lily-54-*`. Initial version tests lacked the borrowed Electron dependency and an injected fallback test path needed correction; final runs passed after resolving both fixture issues.
- C1/C2/C3/D remain unchecked. NEXT coordinate foreground engine activity across profiles with explicit completion/crash ownership, then automatically apply validated W′ with matching policy/manifest checks, persist the local receipt and CAS A, and implement contribution-specific undo. Completed-intent startup reconciliation, local card projection, original helper/dependency coverage, model repair, Office fidelity, bounded streaming/lazy inventory and measured/full-client acceptance remain. No deployment, installed release, rollout change or original dirty-checkout merge occurred.


### Checkpoint 55 — owned engine process-group exit confirmation

- Lifecycle audit found `OpencodeSharedServer` cleared `this.process` on engine exit before `terminate()` attempted cleanup, losing the handle to surviving tool processes. It now retains the owned child reference, initiates shutdown once and returns the same awaitable termination receipt to repeated/reentrant callers. A terminated instance cannot spawn a new engine beneath an old receipt; profile selection already creates a replacement instance.
- `terminateProcessGroup` uses existing TERM/KILL dispatch, then probes the owned detached POSIX process group until ESRCH confirms absence. Parent exit is insufficient. Permission errors and observation timeout yield an unconfirmed receipt; timeout does not cancel the requested hard-kill fallback. Confirmed absence cancels the fallback to avoid a later stale signal. Invalid/init/self PIDs are refused. Windows retains existing taskkill dispatch and explicitly returns `TREE_CONFIRMATION_UNAVAILABLE`, not successful proof.
- RED exit test failed because the helper did not exist. PASS real Node 22 and Electron Node 24 on macOS: an owned engine exits while its child ignores TERM and continues writing; confirmation remains pending until the group disappears, then file size remains stable. Actual `OpencodeSharedServer.ensureStarted` exit handling now retains and terminates that group despite `process === null`; repeated/reentrant teardown shares its receipt and stale restart is rejected. Existing kill-tree, orphan-reaper, shared-server and server-manager regressions pass. Logs: `/private/tmp/lily-55-*`.
- This is a bounded process-group receipt, not global writer admission. Deliberately detached tools, separately registered process jobs, Windows tree-completion proof, active-profile registration and the start/apply race still require coordination. Existing `resetSharedServer`/quit callers remain best-effort and are not claimed to await full shutdown. Do not enable automatic materialization using this receipt alone or kill active foreground work to obtain it.
- C1/C2/C3/D remain unchecked. NEXT connect foreground actor admission and exact quiescence to the local coordinator, then validated W′ application, local receipt/A CAS and contribution-specific undo. Preserve startup reconciliation/card projection, original helper/dependency validation, model repair, Office fidelity, bounded streaming/lazy inventory and measured/full-client acceptance. No installed client, unrelated process, production service, rollout flag or original dirty checkout was modified.


### Checkpoint 56 — conservative DOCX private candidate merge

- Added the packaged `merge_office.py` runtime helper. It uses python-docx for read-only format checks and lxml for bounded structural merging, while copying opaque ZIP part bytes without a python-docx save round trip. Differing paragraphs and table cells can merge at stable positions; same paragraph/cell edits, changed child structure and conflicting opaque parts remain conflicts. Relationship/content-type edits require resolution. Signed packages are refused. This first policy supports DOCX only, not Excel, PowerPoint or macro-enabled formats.
- ZIP checks reject duplicates/case/NFC aliases, traversal, symlink/encrypted entries and excess members/bytes. XML DTD/entities are refused with external resolution disabled. A private bounded subprocess writes an exclusive output, and the native adapter checks size/hash/policy before accepting it. Missing Python/dependencies, timeout or unsupported data returns `office_unsupported`; no text fallback is attempted for a contested DOCX. Production resolves the existing desktop bundled Python; tests explicitly use the available Codex Python runtime. No dependency was installed.
- The actual A/W/M private candidate path invokes the adapter, retains final W checks and never changes W or writes private bytes to shared Git. The merge policy now participates in durable candidate fingerprints, invalidating candidates prepared with the older policy. No automatic materialization or A advancement was enabled.
- RED Python test failed on the missing helper. PASS eight Python structural/security tests, real native private-candidate tests under Node 22 and Electron Node 24, existing text/JSON/application candidate regression and encrypted Git materialization/cache-policy regression. Initial materialization execution lacked jszip; rerun uses existing dependencies through NODE_PATH. Logs: `/private/tmp/lily-56-{red,python,candidate,electron,regression,materialization,electron-materialization}.log`. Fixtures verify unchanged non-main-document part bytes including a header and opaque custom/VBA-named streams; they do not prove real macro functionality, ZIP metadata/signature fidelity or rendered Office fidelity. The usual LibreOffice app path was absent; no rendered acceptance was performed.
- C1/C2/C3/D remain unchecked. NEXT complete Office shared-chain integration and fidelity/format coverage alongside foreground admission, validated local application/A CAS/undo, completed-intent startup/card projection, original helper/dependency validation, model repair, bounded streaming/lazy inventory and measured/full-client acceptance. No installed release, production service, rollout flag or original dirty checkout was modified.


### Checkpoint 57 — admitted local application and atomic A receipt

- Added the real private-candidate application path. It checks current encrypted validation evidence, policy, candidate token/fingerprint, task/delivery/target binding, W hashes, root identity and the expected A generation/revision. The existing per-file broker writes W′ under the supplied writer. A final whole-candidate hash check precedes one SQLite transaction containing the personal recovery journal's applied state, the candidate's applied receipt and A's generation/CAS advancement to shared M (never the private validation commit).
- An interrupted final receipt leaves A unchanged and the last durable per-file journal recoverable. Personal recovery records now distinguish materialization from legacy application. Incomplete materialization can use existing startup/manual rollback; applied materialization cannot use legacy whole-file rollback because that would leave A inconsistent. Contribution-specific undo remains required. After rollback, preparation builds a new attempt; after successful application/reopen, the same shared publication retains its receipt without overwriting later private edits. Candidate policy invalidation includes the new base-generation fields.
- Native `prepareIntegrationLocal` now runs validation → application when main-owned `localApplicationWriter` is supplied and emits an applied result in English/Chinese/Arabic. There is intentionally no broker-only default: production cross-profile foreground admission remains unimplemented. The signed HTTP/native fixture supplies a writer for its controlled, engine-free workspace and now proves the full remote publication → private validation → actual source write → A receipt path. Legacy publication/application tests then explicitly simulate subsequent owner edits back to their original fixture content; they do not silently undo or erase the materialization receipt.
- RED application regression failed on the missing module. PASS Node 22 and Electron Node 24 actual writes, receipt/A atomicity, idempotent replay, stale W/base/policy, missing validation, writer contention and receipt-fault rollback. PASS encrypted materialization restart/cache tests, real integration-turn labels, existing ZIP workflow and personal recovery regressions. PASS the complete signed Fastify/PostgreSQL/native TaskCore suite with fixture provider/selection/admission. Its first run reached native application successfully but failed an older fixture's baseline assumption; explicit fixture separation fixed the rerun. Evidence: `/private/tmp/lily-57-{red,apply,electron-apply,materialization,turn,workflow,recovery,http}.log`.
- C1/C2/C3/D remain open. NEXT supply the actual cross-profile foreground/job admission to the connected native writer path, reconcile completed intents and local status after restart, and implement contribution-specific undo. Original helper/dependency coverage, unresolved model repair, shared Office/fidelity/other formats, bounded streaming/lazy inventory and measured/full-client acceptance remain. Checkpoint 56 was committed as `568f7d27` after the prior approval-capacity failure cleared. No installed release, production rollout or original dirty-checkout merge occurred.


### Checkpoint 58 — foreground process-group registration

- Added a POSIX foreground writer using the same permanent OS-user SQLite admission lock as local application. Each detached launcher registers its own verified process-group ID in an empty, fsynced marker before executing a workspace command. The PID is in the filename, so a crash cannot leave a partially decoded identity. Admission only removes markers after ESRCH proves the group absent; permission/unknown errors remain busy. Marker ownership is independent of accounts, profiles and the spawning main process. Registration waits behind a held writer before executing commands.
- Actual OpenCode launch, legacy process jobs and durable worker launchers now register. Engine wrappers receive an owner IPC channel: owner death while the engine is running sends group TERM/KILL, while persistent jobs omit that channel. Normal command exit closes IPC so wrappers do not remain alive solely because of the channel. Whole-group confirmation from checkpoint 55 remains authoritative when an engine exits with surviving tools. Windows retains its existing launch path and the new foreground writer explicitly refuses unsupported coordination.
- The legacy job stop regression exposed single-PID termination leaving a real synthetic child running. Its exact test command/group was verified and cleaned. New legacy jobs capture a process identity while the wrapper waits for its command; stop checks that identity, signals the whole group and waits through forced termination. Old records without an identity retain their previous stop behavior. The deferred stdin handshake also protects immediately exiting commands from losing their identity before capture.
- RED foreground regression failed on the missing helper. PASS real Node 22 and Electron Node 24: surviving tools block application after leader exit, completed groups release admission, commands wait behind a writer, and killing an engine-owner process reaps its registered running group. Native shared-server exit tests, legacy/durable jobs, legacy TERM-ignoring group stop, media output, observations, generation fencing and shared-profile regression pass. Initial sandbox ps checks required escalation; an IPC lifecycle test then exposed the channel keeping an exited wrapper alive and passed after explicit disconnect handling. A transient approval-capacity rejection cleared on the same authorized retry. Logs: `/private/tmp/lily-58-*`.
- This registers the production launch paths but does not yet supply automatic native application admission. Warm idle engines still retain markers: NEXT request safe idle-profile shutdown without interrupting active views/work, add pending-local-write retries/restart reconciliation, and connect the foreground writer to the native application option. Deliberately detached unregistered commands, old-version unregistered processes/jobs, Windows completion proof and full installed-client acceptance remain outside these tests. C1/C2/C3/D stay open; contribution-specific undo, original helper/dependency coverage, model repair, Office/shared-format fidelity, bounded streaming/lazy inventory and scale acceptance remain. No installed release, production flag or original dirty checkout was changed.
